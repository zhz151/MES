# 本机与线上发布差异待办（短期跟踪）

> **性质**：记录「本机代码已改、线上 `zhz.js.cn` 尚未发布」的内容，供下次增量发布一次性带上。
> **维护规则**：每发布一次勾掉对应行、更新日期；清单全空时**删除本文件**，避免成为过期文档。
> 与部署体系关系：增量发布方法见 [`05_部署与运维.md`](05_部署与运维.md) 第四章；首装见 `14_部署执行单.md`。

## ✅ 已全部上线（截至 2026-09-08）

> 本文件当前**无「本机已改、暂缓上线」代码项**：批六~批九已随 **2026-09-08 `mes-deploy-20260908.zip`** 上线发布（`api\` 覆盖 + `web\` 覆盖 + `nssm restart MES.API`，云库启动自动 Migrate 应用批八两迁移 `20260907113212`/`20260907122038`），涵盖：
> - 批六 全站用户可见命名整理（系统名/计件标准/工单二级分组/订单列表折叠卡）；
> - 批七 订单两列表页列分组与默认显隐；
> - 批八 订单项次计价金额字段（计价单位/单价/总价）+ 打印双模式；
> - 批九 客户在产口径 + 变更日期默认隐藏/技术要求按钮 + 移动菜单二级分组展开修复。
> 早前 2026-09-07 `mes-deploy-20260907_full.zip`（09-06 三批 + 批四 + 批五）亦已上线。
> 各批次细节保留于下方 ✅ 节供追溯；**数据库层数据清理见文末「数据变更注记」，非代码发布项，勿与代码批次混淆**。
> ⚠️ **2026-09-09 更新（已打包待上传）**：09-08 发布后本机又新增改动（内容哈希核对 `publish/mes-deploy-20260908.zip`：静态仅 `web/css/app.css` 与本地不同，其余 index.html / js 均同；后端 MES.Services/Api dll、Blazor wasm、新增迁移 `20260908083206` 均在其后产生）。已登记为**批十~批十三**，见文末「维护追加区」，**2026-09-09 已整体打包 `mes-deploy-20260909_0349.zip`（未上传）**，服务器发布步骤见文末登记；如个别已另行单独上线，按维护规则勾除即可（`api\`/`web\` 覆盖幂等）。

### 历史：2026-09-05 发布（scanfix）已上线记录

> 本文件所列待发布项已随 **2026-09-05 `mes-deploy-20260905-scanfix.zip`** 全量发布全部上线（`web\` 覆盖 `C:\mes\web` 带上全部 WASM 与静态改动；本次夹带后端改动，`api\` 覆盖 + `nssm restart MES.API`）。**按维护规则清单已全空，本文件可删除。**

| 批次 | 内容 | 线上状态 |
|---|---|---|
| AppMenu 单源树菜单 + 移动端响应式布局批 | 桌面/手机菜单单源树、MobileLayout 改动、`index.html` 移动端检测等 | ✅ 已上线（已手动发布） |
| 打印富布局 35 列误伤修复 | 见下方已发布清单 | ✅ 已上线（2026-09-05 scanfix 发布带上） |
| 手机扫码无反馈修复（scanner.js 双保险+加固） | 见下方已发布清单 | ✅ 已上线（2026-09-05 scanfix 发布带上） |

## ✅ 已发布清单：打印富布局 35 列误伤修复

| 文件 | 改动 | 性质 |
|---|---|---|
| `MES.Blazor/wwwroot/js/print.js` | `openPdfFromApi(apiUrl, jsonBody, skipColumnCheck)` 加第三参，富布局请求跳过 35 列校验 | 静态文件 |
| `MES.Blazor/wwwroot/index.html` | `js/print.js?v=13` → `v=14`（强制浏览器拉新版，绕过旧缓存） | 静态文件（⚠️ 同文件还含已上线的移动端改动，重复覆盖幂等，无害） |
| `MES.Blazor/Pages/Batches/ProcessCardPrint.razor` | 工艺卡打印调用传第三参 `true` | WASM（须重新 publish 编译） |
| `MES.Blazor/Pages/Quality/Ncrs.razor.cs` | NCR「打印选中」去掉后端忽略的 columns、只发 ids + 传第三参 `true` | WASM（须重新 publish 编译） |

**问题背景**：工艺卡/质保 NCR 报告属富布局打印，但请求体携带 `Columns`（区块字段布局参数，工艺卡默认约 97 个），被 `print.js` 通用 35 列校验误拦，请求根本发不出去；后端 `ProcessCardPrintHelper` 富布局本不受 35 列限制。全仓 73 处 `openPdfFromApi` 审计：富布局误伤仅此 2 处（其余富布局单据不带 Columns，安全）。
**后端结论**：MES.Api **零改动**、无 EF 迁移、无数据变更。

## ✅ 已发布清单：手机扫码无反馈修复（scanner.js 双保险 + 加固）

| 文件 | 改动 | 性质 |
|---|---|---|
| `MES.Blazor/wwwroot/js/scanner.js` | 原生 BarcodeDetector 探测改 `getSupportedFormats()` 含 `qr_code`（原 `'BarcodeDetector' in window` 误判伪支持）；原生连续 1.5s 无结果自动降级 jsQR；jsQR 预加载 + 降采样到 480 + 帧率门限 100ms + 全程 try-catch 防中断 | 静态文件 |
| `MES.Blazor/wwwroot/index.html` | `js/scanner.js` → `js/scanner.js?v=1`（首引版本号，强制浏览器拉新版） | 静态文件 |
| `MES.Blazor/Pages/Equipment/EquipmentScan.razor.cs` | `DotNetObjectReference.Create(this)` 改存 `_dotNetRef` 字段 + OnInitialized 创建 + Dispose 释放（原 Create 不存字段 → GC 后扫码命中 JS 回调打空对象、页面无反应） | WASM（须重新 publish 编译） |

**问题背景**：夸克/微信内置等国产浏览器内核常「BarcodeDetector 有 API 无底座」（Android 原生实现依赖 Google Play 服务），`detect()` 恒空且被旧代码 try-catch 吞掉 → 卡死在原生死分支，jsQR 永远轮不到 → 摄像头画面在动但一直「扫描中」不识别。附 jsQR 老代码无 try-catch，单帧抛错即永久中断扫描循环。
**验证**：浏览器开 `https://zhz.js.cn/js/scanner.js?v=1`；夸克/微信内置进扫码页对准二维码应在约 1s 内识别（伪支持会自动降级 jsQR，识别延迟略增属正常）。

## 发布记录（本次已完成）

- **2026-09-05**：随 `mes-deploy-20260905-scanfix.zip` 全量发布，`web\` 覆盖 `C:\mes\web`、`api\` 覆盖 + `nssm restart MES.API`，两批前端待办 + 扫码审计后端改动一并上线。
- 验证：`https://zhz.js.cn/js/print.js` 内容已含 `skipColumnCheck`；工艺卡/质保 NCR 打印不再弹「列过多」；扫码报工页新 UI 正常。

> 清单已全空。按维护规则本文件应删除，如后续再有「本机已改、暂缓上线」内容可复用下方维护追加区重建。

## ✅ 去油酸洗排序筛选 + 批次执行进度/显示卫生批（2026-09-06 登记，2026-09-07 随 full.zip 上线）

> 两批均为「本机代码已改、线上 `zhz.js.cn` 未发布」。发布方式同 2026-09-05：`web\` 覆盖 `C:\mes\web`（带全部 WASM 与静态）、`api\` 覆盖 + `nssm restart MES.API`（本批含后端改动）。**无 EF 迁移**。

### 批一：去油酸洗列表 排序/筛选补漏 + 班次筛选中文化（本次）

| 文件 | 改动 | 性质 |
|---|---|---|
| `MES.Services/Batch/PicklingService.cs` | ① 入缸分页给「完工日期」列筛(Field=CompleteDate)手工接出缸子表 `Any(CompleteDate.Date in)`（原走 ApplyFilters 反射不到入缸实体→无效）；② 入缸 GetFilterContextsAsync 补 InDate/CompleteDate 日期候选、完工 GetOutRecordFilterContextsAsync 补 CompleteDate 候选（列头 date 漏斗原「该列无可用筛选项」）；③ **完工分页 GetOutRecordsPagedAsync 整段补 `ApplyFilters`**（此前完工页所有列头筛选后端全不生效）+ 补全 5 个缺失排序键（工段/制造规格/工厂牌号/备注/更新时间） | 后端 dll（覆盖 api + restart） |
| `MES.Blazor/Pages/Batches/PicklingInRecords.razor.cs` | 筛选上下文 switch 补 `"Shift" => DisplayHelper.GetShiftTypeText(v)`（班次中文：DayShift→白班/MiddleShift→中班/NightShift→夜班） | WASM |
| `MES.Blazor/Pages/Batches/PicklingOutRecords.razor.cs` | 同上班次筛选中文映射 | WASM |

### 批二：批次执行进度可视化 + 去油/酸洗卡片显示卫生（上会话遗留）

| 文件 | 改动 | 性质 |
|---|---|---|
| `MES.Blazor/Pages/Batches/BatchDetail.razor` / `.razor.css` | 批次执行进度可视化（去油/酸洗等工序卡片：入缸出缸日期/完工标记增强）；断切/检验/冷轧拔/油管断/委外显示「支数」、去油酸洗不显支数 | WASM |
| `MES.Core/DTOs/Batch/BatchTrackingVisualDto.cs` | 执行进度可视化 DTO 新字段（含出缸日/完工区等） | WASM（编译入 Blazor） |
| `MES.Services/Batch/ProductionRecordService.cs` | 5c 富化聚合增强（SectionVisualDto 按(工序组,工段)出缸入缸聚合；去油酸洗「仅入缸无完工」保持形态） | 后端 dll |
| `MES.Blazor/Pages/Batches/PicklingInRecords.razor.cs` | 完工操作人列只显纯姓名（去工号） | WASM |
| `MES.Blazor/Pages/Quality/*`（化学/翻边/压扁/晶粒/硬度/晶间/金相/点蚀/拉伸 9 页 `.razor.cs`） | 记录列表操作人列只显纯姓名 | WASM |
| `MES.Tests/Services/Batch/ProductionRecordServiceTests.cs` | 新富化/聚合单测补充 | 测试（仅本地，不上云） |
| `docs/frontend_pages.md`、批次接口/模块设计、商业差距分析等 | 文档同步 | — |

### 发布包（合并包，2026-09-06 打包；2026-09-07 已由 mes-deploy-20260907_full.zip 替代上线）

本机已用 `03-publish-local.ps1` 产出**合并包** **`E:\MES项目\MES\publish\mes-deploy-20260906_1945.zip`**（`api\` + `web\` 两级，Blazor BaseUrl 已置空，**无 EF 迁移**）——按当前工作区**全量发布**，内含 批一（去油酸洗排序筛选）+ 批二（批次执行进度/显示卫生）+ **批三（横屏提示条）** 及 09-06 首页手机看板等全部现存改动，**替代早前 `mes-deploy-20260906_0408.zip`**（0408 内容已被本包覆盖，上传只需本包，勿再单独传 0408）。服务器操作：把 zip 拷到服务器（RDP）解压覆盖后重启：

```powershell
# 服务器（管理员 PowerShell）
Expand-Archive -Path C:\mes\mes-deploy-20260906_1945.zip -DestinationPath C:\mes\_stage -Force
Copy-Item C:\mes\_stage\api\* C:\mes\api\ -Recurse -Force
Copy-Item C:\mes\_stage\web\* C:\mes\web\ -Recurse -Force
nssm restart MES.API
```
> 可选：覆盖前把旧产物归档到 `C:\mes\publish\20260906\` 便于回滚；`web\` 覆盖建议先清旧 `_framework` 避免陈旧 dll 残留（非必须）。
> 包内核对：`web\index.html` 已含 `listenLandscapeHintRefresh`、`web\css\app.css` 已含 `.rotate-hint-banner`（横屏提示条静态改动到位）。

## ✅ 批三 手机端「横屏提示条」（2026-09-06 登记，2026-09-07 随 full.zip 上线）

> 全站移动策略「除首页+扫码流外一律横屏」的第一环引导：竖屏进入宽表页顶部提示「需横屏查看（旋转手机自动切换）」，可关闭并本地持久；夸克/微信等锁竖屏浏览器显示特制文案（去系统开自动旋转）。**已并入 `mes-deploy-20260906_1945.zip` 合并包（替代 0408），上传只用该包即可。** 无 EF 迁移、后端 MES.Services/MES.Api 零改动。

| 文件 | 改动 | 性质 |
|---|---|---|
| `MES.Blazor/Shared/LandscapeHintRule.cs` | **新建**：静态规则（宽页=AppMenu 全部叶子−首页−两个扫码窄叶，排除登录/进出货/维修点选窄页与 /create·/edit 表单路径；路径归一化 NormalizePath） | WASM（重新 publish 编译） |
| `MES.Blazor/Shared/LandscapeHintBanner.razor` | **新建**：提示条组件（localStorage `landscape_hint_dismissed` 持久关闭、锁竖屏特制文案、IAsyncDisposable 退订） | WASM |
| `MES.Blazor/Shared/MobileLayout.razor` | IsMobile 级联前插 `<LandscapeHintBanner />` | WASM |
| `MES.Blazor/wwwroot/index.html` | 追加多监听器总线 `_landscapeHintRefs`/`listenLandscapeHintRefresh`/`stopLandscapeHintRefresh`（orientationchange+resize，不抢 ResponsiveLayout `_resizeRef` 单槽） | 静态文件（⚠️ 同文件含既有改动，重复覆盖幂等无害） |
| `MES.Blazor/wwwroot/css/app.css` | 追加 `.rotate-hint-banner`（复用 `.notice-card` 蓝左边框浅蓝底） | 静态文件 |
| `MES.Tests/Components/LandscapeHintRuleTests.cs` | **新建**：7 条单测（含全菜单叶子防漂移） | 测试（仅本地，不上云） |

**验证**：`dotnet build MES.Blazor` 0 错 0 警；`LandscapeHintRuleTests` 7/7、`AppMenuTests` 7/7。真机人工验收（横屏切换/锁竖屏文案/关闭持久）待手机侧。

## ✅ 批四 订单进度树（2026-09-07 登记，已随 mes-deploy-20260907_full.zip 上线）

> 订单管理列表每行新增「订单进度树」入口：订单号 → 主号 → 四阶段分支（原锁/生产/成检/入库）→ 重量叶 的只读进度树页面。**无 EF 迁移、无数据变更**（纯读聚合）。

| 文件 | 改动 | 性质 |
|---|---|---|
| `MES.Core/DTOs/Order/OrderProgressTreeDto.cs` | **新建**：层级树 DTO（订单号→主号→四阶段分支→叶子，主号含 ScheduleStage/IsCompleted/UrgencyLevel/预计完成等） | 后端 dll |
| `MES.Core/Interfaces/Order/IOrderProgressQueryService.cs` | **新建**：`GetTreeAsync(salesOrderNo)` | 后端 dll |
| `MES.Services/Order/OrderProgressQueryService.cs` | **新建**：聚合 WorkOrderExecutionSummary 快照 + 成检计划 + 实时成品检验/入库分支；完结主号仅成品入库分支 | 后端 dll |
| `MES.Api/Controllers/Order/OrderProgressController.cs` | **新建**：GET `api/order/progress?salesOrderNo=`（`[Authorize(Roles = OrderView)]`，无该订单工单时返回空 data） | 后端 dll |
| `MES.Api/Program.cs` | 注册 `IOrderProgressQueryService`（+1 行） | 后端 dll |
| `MES.Shared/Constants/ApiEndpoints.cs` | 加常量 `OrderProgress = "api/order/progress"` | 共享常量（api+web 均编译） |
| `MES.Blazor/Pages/Orders/OrderProgress.razor` / `.razor.cs` / `.razor.css` | **新建**：路由 `/orders/progress`；「执行关注=已完结」主号默认折叠；字体分级最小 15px；执行关注/紧急性/预计完成高亮胶囊；完结态主号灰字 | WASM |
| `MES.Blazor/Services/Order/OrderProgressService.cs` | **新建**：按 salesOrderNo 拉取进度树 | WASM |
| `MES.Blazor/Pages/Orders/Orders.razor` | 每行操作区加「订单进度树」入口（`NavigateToProgress(order.OrderNumber)` 带单号跳转） | WASM |
| `MES.Blazor/Program.cs` | 注册 `OrderProgressService`（+1 行） | WASM |
| `MES.Tests/Services/Order/OrderProgressQueryServiceTests.cs` | **新建**：进度树聚合单测 | 测试（仅本地，不上云） |

## ✅ 批五 原锁备注显示名更名 + EF 迁移（2026-09-07 登记，已随 mes-deploy-20260907_full.zip 上线）

> 原锁备注（RawMaterialLockRemarkKey）四类显示中文由「A质量补料/B执行返整/C执行计划/D完善计划」更名「质量补料/生产返整补足/执行用料计划/完善用料计划」（仅表意，`DisplayOrder` 不变）。⚠️ **本批含 EF 迁移**——云端发布除 `api\`+`web\` 覆盖与 `nssm restart MES.API` 外，**还需在服务器 apply 迁移 `20260906160752`**（详见下方云端操作）。

| 文件 | 改动 | 性质 |
|---|---|---|
| `MES.Data/Migrations/20260906160752_RenameRawMaterialLockRemarkDisplayNames.cs` (+Designer) | **新建迁移**：`UPDATE DictValueDefinitions.DisplayName` 4 行（质量补料/生产返整补足/执行用料计划/完善用料计划），`Down` 恢复 A/B/C/D 旧名；纯 SQL 数据 UPDATE，无表结构变更 | 迁移（云端库 apply） |
| `MES.Core/Constants/RawMaterialLockRemarkKeys.cs` | `KeyToChinese` 改新中文；`ChineseToKey` 补新中文并**保留旧前缀名兼容映射**（存量 `A质量补料` 等仍可归一） | 共享常量（api+web 均重编） |
| `MES.Data/Entities/WorkOrder/WorkOrderExecutionSummary.cs` | `RawMaterialLockRemark` 字段注释对齐新中文（纯注释） | 后端 dll |
| `MES.Core/DTOs/Scheduling/RawMaterialLockPendingSummaryDto.cs` | 截日桶行类别注释对齐新中文（纯注释） | 后端 dll |

> **云端库 apply 迁移**（服务器管理员 PowerShell，方法见 `05_部署与运维.md`，须 `--context AppDbContext` 打 `zhou/MESMN`）：
> ```powershell
> cd C:\mes\api   # 或服务器上放置含 MES.Data 的发布目录
> dotnet ef database update --context AppDbContext -m 20260906160752_RenameRawMaterialLockRemarkDisplayNames
> nssm restart MES.API
> ```
> 迁移仅改四行显示名，apply 前/后代码因 `ChineseToKey` 兼容映射均能正确显示，先后无碍；`api\`+`web\` 覆盖重启后（映射常量两端均编译）即可见新中文。

> **发布包**：批四/批五连同 09-06 三批（去油酸洗/批次进度/横屏）已重新合并打包为 **`E:\MES项目\MES\publish\mes-deploy-20260907_full.zip`**（`api\`+`web\` 两级，353 条目已验证含 MES.Api.dll / index.html / blazor.webassembly.js），**替代并覆盖 1945 内容，上传只需本包**（勿再传 1945）。服务器操作：改 `scripts/deploy-windows/09-apply-deploy.ps1` 顶部 `$Zip` 为该包名后管理员运行（stop→覆盖 api+web→start，API 启动自动 Migrate 应用 `20260906160752`）；或按 `05_部署与运维.md` §4.3 顺序更稳：先只覆盖 `api\` → `nssm restart MES.API`（触发迁移）→ 再覆盖 `web\`。云库 `MES` 上迁移仅推进 4 行显示名 UPDATE，无结构变更、可回滚（覆盖旧代码产物即可）。

> ✅ **2026-09-07 已按此上线**：`mes-deploy-20260907_full.zip` 已覆盖 `api\`+`web\`、服务已重启、云库迁移 `20260906160752` 已应用。

## ✅ 批六 全站用户可见命名整理（2026-09-07 登记，2026-09-08 随 mes-deploy-20260908.zip 上线）

> 纯前端改名批：① 顶部栏系统名「MES制造执行系统」→「MES制造系统」+ 汉堡图标换列表式 ViewList（桌面/手机/登录页/PWA 清单）；② 工资结算「生产计件类别/成检计件类别」→「生产计件标准/成检计件标准」（菜单+列表/编辑页标题）；③ 工单管理菜单二级分组「工单操作 · 工单查询」+ 叶子改名（工单首页→工单生成、用料计划总览→用料计划、定尺工单定尺数据→定尺工单定尺）并同步各页标题/返回/打印文案；④ 订单列表页 h5「订单管理」→「订单列表」，折叠卡改名「完成预估及延期风险」且**删除**卡内「订单接单·出库及现负荷汇总」5指标×12月小表（仅保留两张交期预估表）。
> **性质：全部 WASM + 静态文件，MES.Api / 后端 dll 零改动、无 EF 迁移、无数据变更**——只需按 `05_部署与运维.md` 用 `03-publish-local.ps1` 重新 publish 出 `web\` 覆盖 `C:\mes\web`（重新 publish 编译）即可，无需覆盖 `api\`、无需 restart API。若随下一批合并发布，照常 `api\`+`web\` 一起覆盖无碍（api 覆盖幂等）。

| 文件 | 改动 | 性质 |
|---|---|---|
| `MES.Blazor/Shared/AppMenu.cs` | ① 工资结算叶「生产计件类别/成检计件类别」→「生产计件标准/成检计件标准」；② 工单管理二级分组「工单操作[工单生成/用料计划/工单需求调整]·工单查询[工单执行状况/定尺工单定尺]」；叶子改名 工单首页→工单生成、用料计划总览→用料计划、定尺工单定尺数据→定尺工单定尺 | WASM（重新 publish 编译） |
| `MES.Blazor/Shared/MainLayout.razor` | 桌面顶栏汉堡图标 `Icons.Material.Filled.Menu`→`ViewList` + 系统名「MES制造执行系统」→「MES制造系统」 | WASM |
| `MES.Blazor/Shared/MobileLayout.razor` | 同上图标换 ViewList + 抽屉面板头系统名改名 | WASM |
| `MES.Blazor/Pages/Login.razor` | 登录页系统名改名（标题/footer 版权行） | WASM |
| `MES.Blazor/wwwroot/manifest.json` | PWA 清单 `name` 改名 | 静态文件 |
| `MES.Blazor/Pages/Payroll/PieceRateProductionCategories.razor` | h5 →「生产计件标准（基准价 × 维档系数）」 | WASM |
| `MES.Blazor/Pages/Payroll/FinalInspectionCategories.razor` | h5 →「成检计件标准（基准价 × 维档系数）」 | WASM |
| `MES.Blazor/Pages/Payroll/PieceRateProductionCategoryEdit.razor` | 编辑页标题 →「编辑/新增 生产计件标准」 | WASM |
| `MES.Blazor/Pages/Payroll/FinalInspectionCategoryEdit.razor` | 编辑页标题 →「编辑/新增 成检计件标准」 | WASM |
| `MES.Blazor/Pages/WorkOrders/WorkOrders.razor` | h5「工单管理」→「工单生成」 | WASM |
| `MES.Blazor/Pages/WorkOrders/MaterialPlanOverview.razor`(+`.cs`) | h5「用料计划总览」→「用料计划」；打印/导出标题同步（用料计划-工单原锁-错疑投料 等） | WASM |
| `MES.Blazor/Pages/WorkOrders/FixedLengthWorkOrderView.razor`(+`.cs`) | h5「定尺工单定尺数据」→「定尺工单定尺」；打印标题同步 | WASM |
| `MES.Blazor/Pages/WorkOrders/WorkOrderDetail.razor` | 返回按钮文案「工单首页」→「工单生成」（跳转不变） | WASM |
| `MES.Blazor/Pages/WorkOrders/WorkOrderMaterialPlan.razor` | 返回按钮「返回用料计划总览」→「返回用料计划」 | WASM |
| `MES.Blazor/Pages/Orders/Orders.razor`(+`.cs`) | h5「订单管理」→「订单列表」；折叠卡「订单接单-出库及现负荷汇总」→「完成预估及延期风险」，删除卡内接单·出库·现负荷 5指标×12月小表（含其加载/打印页代码）；保留两张交期预估表逐格联动筛选+各自打印。⚠️ 后端 `GetInOutSummaryAsync` 与前端 `OrderService.GetInOutSummaryAsync` **保留**（报表总览仍用），勿误删 | WASM |
| `MES.Tests/Components/AppMenuTests.cs` | 新增「工单管理二级分组（操作/查询）」断言防漂移 | 测试（仅本地，不上云） |
| `docs/frontend_pages.md` 等 | 文档同步（V48） | — |

**验证**：`dotnet build MES.Tests` 0 错 0 警；`AppMenuTests`（含新增断言）15/15 通过。界面生效待发布后浏览器核对（改名/分组/折叠卡仅剩两张预估表）。

## ✅ 批七 订单两列表页列分组/默认显隐调整（2026-09-07 登记，2026-09-08 随 mes-deploy-20260908.zip 上线）

> ① 订单列表页：原「① 基本信息 + ② 合同交付」两组**合并为单组「① 基本信息」**（订单确认→②、订单执行→③），默认仅显示 订单号/签订日期/业务员/客户名称/交期截止/订单总重量/含项次数，其余同组字段默认隐藏。② 订单成品(实时库存)：默认隐藏 工单号/最终客户/产品标准/工厂牌号/最小长度/最大长度/仓库批次/来源/来料单位/剩余米数/物料类型 11 列。
> **性质：全部 WASM，MES.Api / 后端 dll 零改动、无 EF 迁移**——仅需重新 publish 出 `web\` 覆盖 `C:\mes\web`。⚠️ 两页列偏好 localStorage 键已升 `v2`（`col_prefs_orders_v2` / `col_prefs_orders_pending_delivery_v2`），用户重进页面即按新默认加载（旧 v1 键数据自动弃用）。

| 文件 | 改动 | 性质 |
|---|---|---|
| `MES.Blazor/Pages/Orders/Orders.razor.cs` | `GetAllColumnDefs()` 合并「基本信息+合同交付」为单组（GroupKey 重排 ① 基本信息/② 订单确认/③ 订单执行），最终客户/交期起始/延期罚款设 `Visible=false`；加 `ColumnPrefsVersion="v2"`，Save/Load 用版本键 | WASM |
| `MES.Blazor/Pages/Orders/PendingDelivery.razor.cs` | 11 列 `Visible=false`；加 `ColumnPrefsVersion="v2"`，Save/Load 用版本键 | WASM |
| `docs/frontend_pages.md`、订单模块详细设计 | 文档同步（列分组 3 组/默认显隐/v2 键） | — |

---

## ✅ 批八 订单项次计价金额字段（计价单位/单价/总价）（2026-09-07 登记，2026-09-08 随 mes-deploy-20260908.zip 上线，云库两迁移已由启动自动 Migrate 应用）

> ① OrderItem 新增 3 字段：计价单位（PerKg 元/Kg / PerMeter 元/米 / PerPiece 元/支，C# enum + EF string nvarchar(20)）、单价 decimal(18,2)?、总价 decimal(18,2)?。总价=单价×取量（计价单位决定：元/Kg→合同重量、元/米→米数、元/支→支数），前端自动计算且**可手动覆盖**（覆盖后不自动刷新，点「恢复自动」回到自动）；后端仅存传入值。② 新建/编辑/查看三态「结算方式」移到理算重量之后，尾列顺序=结算方式→计价单位→单价→总价→备注→操作；新建页**新增备注输入框**；详情页备注列默认隐藏（项次列偏好键 v2）。③ 小数收敛（应用层前后端一致）：米数/合同重量/理算重量**直接四舍五入保留 1 位**——前端输入即 round1、自动总价按 round1 取量计价、**服务层写库入口 SetOrderItemFields 兜底 round1**、**数据工具导入 OrderItem 两列同样特判 round1**；DB 列精度与存量不动（米数 18,2、合同/理算 18,3）；单价/总价 2 位。④ 销售订单确认单打印双模式 + 存量收敛（2026-09-07 追加，见下表 SalesOrderPrintHelper/迁移行）：列表页「打印选中订单」与详情页「打印」改 MudMenu 下拉，含金额确认单 / 不含金额确认单（请求体 `OrderPrintBatchRequest.IncludeAmounts`，默认 true）；两模式「结算方式」均自第 4 位移至**理算重量之后**，含金额显示 计价单位/单价/总价 并汇总**订单总价**，不含金额隐藏三金额列仅保留结算方式；打印重量/米数收敛 1 位小数；数据工具 OrderItem 实体追加 3 列。
> **性质：非全 WASM**——本次含**后端 dll 变更 + 2 个 EF 迁移 + WASM**。真库 zhou/MESMN 已 apply：`20260907113212_AddOrderItemPricingUnitPriceColumns`（加 3 列 + 按工厂牌号 含316→30/2开头→35/其它→25、总价=ROUND(单价×合同重量,2) 存量模拟回填 677 行）与 `20260907122038_BackfillOrderItemAmountsRound1`（存量 米数/合同/理算 round1 + 总价按计价单位取量重算，已核 0 多精度残留）。上线仅需 publish `web\` 覆盖 `C:\mes\web`（后端 dll + WASM 均在本仓已编译，无需再执行 database update）。⚠️ 详情页项次列偏好键升 v2（`col_prefs_order_detail_items_v2`）。

| 文件 | 改动 | 性质 |
|---|---|---|
| `MES.Core/Enums/PricingUnit.cs` | 新增枚举 PricingUnit（PerKg/PerMeter/PerPiece） | 后端 dll |
| `MES.Core/Helpers/EnumHelper.cs` | Register PricingUnit 中英（元/Kg/元/米/元/支）+ _enumRemarks | 后端 dll |
| `MES.Blazor/Helpers/DisplayHelper.cs` | GetPriceUnitText 三重载 + FormatWeight1(1位)/FormatMoney2(2位) | WASM |
| `MES.Data/Entities/Order/OrderItem.cs`、`AppDbContext.Order.cs` | 实体 + EF 配置（PricingUnit string 转换 / UnitPrice·TotalPrice decimal(18,2)） | 后端 dll |
| `MES.Data/Migrations/20260907113212_AddOrderItemPricingUnitPriceColumns.cs` | 3 AddColumn + 存量模拟回填 SQL | EF 迁移（须 apply 真库） |
| `MES.Core/DTOs/Order/`（OrderItemDto/CreateOrderItemRequest/SaveAllOrderRequest） | 3 DTO 增加 PricingUnit/UnitPrice/TotalPrice | 后端 dll |
| `MES.Services/Order/OrderService.cs` | 2 投影 + SetOrderItemFields 扩展 + 3 调用点 | 后端 dll |
| `MES.Services/Printing/SalesOrderPrintHelper.cs` | 确认单打印**双模式**（includeAmounts）：两种模式「结算方式」均移至理算重量之后，含金额显示 计价单位/单价/总价 并合计订单总价，不含金额隐藏三金额列仅留结算方式；重量/米数打印 round1、金额 2 位；表驱动列渲染防错位。⚠️列宽收敛——含金额常量列合计 708pt（< A4 横向内容宽 ≈782pt），否则超宽致 `DocumentLayoutException`（打印选中订单报错/前端被全屏面板遮罩「卡死」），新增列时务必复查常量列总宽 | 后端 dll |
| `MES.Core/DTOs/Order/OrderPrintRequest.cs` | OrderPrintBatchRequest 增 `IncludeAmounts`（默认 true，兼容旧调用） | 后端 dll |
| `MES.Core/Interfaces/Order/IOrderService.cs`、`MES.Services/Order/OrderService.cs` | PrintOrderAsync/PrintOrderBatchAsync 增 `bool includeAmounts = true` 透传 | 后端 dll |
| `MES.Api/Controllers/Order/OrderController.cs` | print-file 端点透传 `request.IncludeAmounts` | 后端 dll |
| `MES.Data/Migrations/20260907122038_BackfillOrderItemAmountsRound1.cs` | 存量 米数/合同/理算 ROUND(…,1) + 总价=ROUND(单价×取量,2)（PerKg→合同/PerMeter→米数/PerPiece→支数，UnitPrice 空不重算） | EF 迁移（真库已 apply） |
| `MES.Blazor/Pages/Orders/Orders.razor`+`.razor.cs`、`OrderDetail.razor` | 「打印选中订单」/「打印」改 MudMenu（含金额确认单/不含金额确认单），PrintSelected/PrintOrder 增 includeAmounts 参数 | WASM |
| `MES.Services/DataExchange/DataExchangeRegistry.cs` | OrderItem 条目加 计价单位(枚举)/单价(元)/总价(元) | 后端 dll |
| `MES.Blazor/Pages/Orders/OrderCreate.razor` | 列重排+新增备注输入+金额列自动计算/手动覆盖+1位/2位收敛 | WASM |
| `MES.Blazor/Pages/Orders/OrderDetail.razor` | 同上 + 备注列默认隐藏 + 项次列偏好 v2 | WASM |
| `docs/数据库设计/订单上下文数据库设计.md`、订单模块详细设计、接口设计、数据工具接口设计、`docs/frontend_pages.md` | 文档同步 | — |

**验证**：`dotnet build MES.Api`+`MES.Blazor` 0 错 0 警；OrderServiceNumericTests 定向单测 10/10 通过；真库迁移后 sqlcmd 抽查（677 行回填、TotalPrice 无 NULL、单价∈{25/30/35}、0 行多精度残留、TotalPrice=round1 合同×单价）已过。界面生效待发布后浏览器核对，2026-09-08 上线后核对通过。

---

## ✅ 批九 客户在产口径 + 订单列显隐/按钮 + 移动菜单二级分组展开修复（2026-09-08 登记，已随 mes-deploy-20260908.zip 上线）

> ① 客户统计「待在产」两桶（整单未入库 / 扣除部分入库）**仅统计主号未完成（阶段≠1）订单**：已主号完成(stage1)的订单即使欠产/未入库也视为订单结束、不再计在产（查询口径调整，无数据变更；D26Z22M9001 之类已入库未发货完成单正确归「待发货(整单)」）。② 订单列表「② 订单确认」组「变更日期」列**默认隐藏**，orders 列偏好键 **v2→v3**（若批七先上线则用户键 v2→v3 平滑强制重置默认显隐）。③ 订单详情页「技术要求」由纯图标改为**图标+文字按钮**。④ **移动菜单点二级分组「不展开反收起」修复**：MobileMenuNode 两处递归组分支的 ChildContent 自含 `<MudExpansionPanels MultiExpansion="true">`（充当新级联源遮蔽 MobileLayout 外层单选容器）——此前所有层级 `MudExpansionPanel` 经级联注册进同一个外层单选容器，点开二级分组（理化检测/工单操作）时单选容器连带收起其父级根分组 → 整树卸载（安卓真机与 Mac Safari 半屏窗口 <960px 判进移动壳均复现）。桌面壳(MudNavGroup)机制不同不受影响。
> **性质：本批含后端 dll（①）+ WASM（②③④），MES.Api 零接口改动、无 EF 迁移、无数据变更**——覆盖 `api\` + `web\` + `nssm restart MES.API` 即可（api 覆盖幂等）。

| 文件 | 改动 | 性质 |
|---|---|---|
| `MES.Services/Order/CustomerService.cs` | `BuildStatsAsync`：WipNone/WipPartial 两桶包 `if (!isCompleted)`（仅阶段≠1 计入在产；同文件 ShippedDone/StockDone 依 isCompleted 分桶逻辑不变） | 后端 dll |
| `MES.Tests/Services/Order/CustomerServiceTests.cs` | 新增「整单完成欠产或未入库不计在产」用例（stage1 欠产+未入库单 → StockCompleted 归桶、WipNone/WipPartial 全 0） | 测试（仅本地，不上云） |
| `MES.Blazor/Pages/Orders/Orders.razor.cs` | 「变更日期」列 `Visible=false`（仍可列显隐切换找回）；`ColumnPrefsVersion` v2→v3 | WASM |
| `MES.Blazor/Pages/Orders/OrderDetail.razor` | 「技术要求」MudIconButton → MudButton 图标+文字 | WASM |
| `MES.Blazor/Shared/MobileMenuNode.razor` | 两处组分支（无 Policy / 带 Policy）ChildContent 自含 `<MudExpansionPanels MultiExpansion="true">` 隔离各层级 panel 级联容器 | WASM |
| `docs/接口设计/订单上下文接口设计.md`、`docs/frontend_pages.md` | 在产两桶口径注记（仅主号未完成订单计入） | — |

**验证**：`dotnet build MES.Api`+`MES.Blazor` 0 错 0 警；CustomerServiceTests 相关用例通过。在产口径可先用真库 sqlcmd 抽查 KH0299（改造前待在产(扣除部分入库) 1单/1kg/25元 → 改造后 0单；待发货(整单) 保持 3单/458kg/11855元）；移动菜单二级分组展开 2026-09-08 上线后真机/浏览器人工验收正常（用户确认）。

---

## 数据变更注记（数据库层清理，非代码发布；防漏必读）

> 本节记录「在真库 zhou/MESMN 或云库 MES 上直接执行的数据清理/修复」，与代码发布批次相互独立。**凡日后做此类数据变更动作，必须在本节补登**（含判据/影响行数/日期），防止未来把「清理后的少数数据」误判为丢失，或整库恢复（`08-restore-db.ps1`）后不知需按判据重清。
> 若从历史备份恢复真库或云库，可能重现下列已清理数据，须按判据再次清理。

### 2026-09-08：客户档案 CustomerProfile 清理「从未下过订单」单位（真库 + 云库均剩 21 条）
- **真库 zhou/MESMN**：早于本日已清理至 21 条（当时未登记，本条补登防漏）。
- **云库 MES（zhz.js.cn）**：2026-09-08 执行 DELETE 清理 268 条（289→21）。判据 = CustomerProfile 的 `(Salesman, EndCustomer)` 从未在任何 SalesOrder 的 `(Salesman, EndCustomer)` 命中（与客户管理统计口径一致；真库同判据验证删除后剩余恰 21，与真库一致）。用户拍板不备份直接删，执行成功 `Deleted=268 / Remaining=21`。
- **客户管理现 21 条为预期结果**，勿当数据丢失。若需复现/再清理判据：
  ```sql
  DELETE cp FROM CustomerProfile cp
  WHERE NOT EXISTS (SELECT 1 FROM SalesOrder so
     WHERE LTRIM(RTRIM(ISNULL(cp.Salesman,''))) = LTRIM(RTRIM(ISNULL(so.Salesman,'')))
       AND LTRIM(RTRIM(ISNULL(cp.EndCustomer,''))) = LTRIM(RTRIM(ISNULL(so.EndCustomer,''))));
  ```

### 既有历史：2026-09-06 批次出缸归位（真库）
> 真库 `zhou/MESMN` 执行 217 条出缸按(批次号,工段)组 1:1 归位（孤儿完工入缸 224→7），备份表 `PicklingInRecord_bak_20260906` / `PicklingOutRecord_bak_20260906` 保留（源自原「数据提醒」小节，已收编于此）。

## 维护追加区

（后续新增「本机已改、想暂缓上线」的内容在此补一行：文件 / 改动简述 / 静态 or WASM / 理由）

---

## ⏳ 批十~批十三 待发布（2026-09-09 登记，未打包）

> 判定依据：内容哈希 + 源码 mtime 晚于 `publish/mes-deploy-20260908.zip`（2026-09-08 00:39 生成）。**无 EF 迁移推进则按 `web\`(+`api\`) 覆盖 + restart 发布**；其中批十三含迁移 `20260908083206`，发布重启 API 启动自动 Migrate 会应用（真库若已手工 update 则自动跳过）。发布一次勾除一批。
>
> ✅ **2026-09-09 已打包（未上传）**：批十~十三整体已由 `03-publish-local.ps1` 打包为 **`publish/mes-deploy-20260909_0349.zip`**（353 条目 / 65.4M，含 `api\MES.Api.dll` `api\MES.Data.dll` `api\appsettings.json` + `web\index.html` `web\_framework` `manifest` `print.js` `app.css`），git 已先落盘快照 `09b9aa45`。**服务器发布步骤**：把 zip 拷至服务器 `C:\mes\` → 改 `09-apply-deploy.ps1` 顶部 `$Zip = "C:\mes\mes-deploy-20260909_0349.zip"` → 管理员运行（stop → 清 `_framework` → 覆盖 `api\`+`web\` → start）。云库 `MES` 启动自动 Migrate 应用 `20260908083206`（幂等 `IF NOT EXISTS` seed，真库 `zhou/MESMN` 已于 2026-09-08 含该迁移记录，无重复风险）。发布完成后按维护规则将下方批十~批十三勾 ✅/整段转为已发布并更新日期。

### ⏳ 批十 批次管理菜单拆分 + 生产执行核查页全系列 + 生产批次列表列显隐收敛 + 质量/批次 4 列表页默认显隐收敛 + 批次 5 处列表列显隐/列序二次收敛 + 质量 15 列表页默认列显隐收敛（frontend V59-V71 / 批次模块 V5.15-V5.26 / 质量管理模块 V8.8 §2.1/§2.6/§2.8-2.9/§2.11-2.20/§2.32-2.33）

> 「生产批次」+新增「生产执行核查」两入口（原批次首页错疑折叠卡整体迁出新页）；核查页错疑卡改名「错疑-生产批次执行」+默认折叠、列裁剪重组/过程检列/删搜索栏（页顶部模糊搜索+登记日期整行删除，PageState 去 Keyword/date）；**2026-09-09 续：生产批次列表列组重排 + 默认显隐收敛（7 组布局定稿）**——①组合并：原「产品要求」「质量要求」并入「工单信息」组、原「生产执行」并入「现执行状态」组、原「原始投料信息」12 字段并入「批次基本信息」组（自挂牌号后插入）；②「关联工单状态/流转判定/有效投料变更/成品切割跟踪」四核查组移为末尾 4 组整组默认隐藏（最终组序 批次基本信息→工单信息→现执行状态→四核查组，2026-09-09 二次调整现执行状态与工单信息对调、工单信息前置；默认仅显前三组）；**同日三调（字段级默认显隐收敛）**：批次基本信息仅显 生产编号/挂牌号/原料类型/来源牌号/来源规格/领料支数/领料重量/制造物品；工单信息仅显 订单号/主号/业务员/交货状态/工厂牌号/规格；现执行状态仅显 状态/截止执行日/当前工序/当前规格/当前工段/当前委外/下一工段（组内列序调：当前规格→当前工序后、当前设备→当前委外后、对应规格→下一工序后），同组余列均隐藏；列偏好键并入 `ColumnPrefsVersion="v2"`（`col_prefs_batches_v2`）→ **三调再升 `ColumnPrefsVersion="v3"`（`col_prefs_batches_v3`）** 强制新默认。**同日生产记录列表（/production-records）默认显隐收敛 + 列宽修复**：G1 执行信息 默认仅显 执行日期/生产编号/挂牌号/订单号/主号/工序名称/工段名称/工厂牌号/制造规格（工单号/执行序号 隐）；G2 产出数据 默认仅显 加工支数/加工重量/产类/平头数/断切倍数/预成切/长度状态/成品长度/符合工单长度/切后支数（设备名称/班次/操作人 隐）；G3 工艺参数/G4 追溯信息 整组隐；列偏好键并入 `ColumnPrefsVersion="v1"`（`col_prefs_production-records_v1`）强制新默认；**列宽根因**=生产记录表格带 `table-min-width` class + `--table-min-width:{总宽}px` Style（`.mud-table-root` min-width 锁全部可见列宽总和，此前 4 组全显约 27 列 >2900px 撑宽），生产批次无此设置故正常 → 已移除两处对齐生产批次，列宽由 `th` 宽提示自然撑开。验证 `dotnet build MES.Blazor` 0 警告 + ProductionRecords/Batches 定向单测 119 通过。**性质：WASM + 后端 dll（共享 `api/batch/list` 服务）**，无 EF 迁移、无数据变更。**同日续（4 列表页默认列显隐收敛，frontend V69）**：成检记录/过程检验/去油入缸/去油完工 4 张内联编辑列表页原**全列默认显示 + 表格带 `table-min-width` 强制最小宽**（列宽锁成可见列总和致过宽）——本次各页将次要/追溯/明细列改 `Visible=false` 默认隐藏（列选择器可开启）并移除 `table-min-width`+`--table-min-width` 两处，列宽由 th 提示自然撑开对齐生产批次，同时清理 `app.css` 已无引用的 `.compact-table.table-min-width` 规则。成检默认显 33 列（G5尺寸/G6压力/G7涡流超声波/G8辅助信息整组隐 + G1隐资格等级、G2隐生产类型/制造物品/制造状态/交货状态/最终用户/来料单位/长度状态、G3隐非定尺长度范围），过程检验默认显 24 列（G1隐执行序号、G4隐理论返整/入库/报废重+次品情况描述、G5辅助信息整组隐），入缸默认显 G1 核心列（隐执行序号/备注/数据来源/更新时间 + G2完工信息整组隐），完工默认隐备注/数据来源/更新时间；4 页列偏好键并入 `ColumnPrefsVersion="v1"`（`col_prefs_final-inspection_v1` / `col_prefs_process-inspection_v1` / `col_prefs_pickling-in-records_v1` / `col_prefs_pickling-out-records_v1`）强制新默认。验证 `dotnet build MES.Blazor` 0 警告 + FinalInspections/ProcessInspections/Pickling/ProductionRecords 定向单测 233 通过。**2026-09-09 再续（frontend V70 / 批次模块 V5.26）：批次管理 5 处列表默认列显隐/列序二次收敛**（隐藏列均可经列选择器开启）——① 去油/酸洗入缸：G1 增隐 工单号/设备名称/班次/操作人，**G2 完工信息 状态/完工日期 恢复默认显示、完工班次/完工操作人 默认隐藏**（v1 曾整组隐，本次按用户决策改回仅两操作信息隐），列偏好键 v1→v2；② 去油/酸洗完工：入缸信息 增隐 设备名称，列偏好键 v1→v2；③ 工段委外：委外信息 隐 要求收回日期/紧急，回收信息 隐 非正常回收(支)/非正常回收(重)/回收备注，列偏好键并入 v2；④ 委外回收：回收信息 隐 数据来源/更新时间，首并 `ColumnPrefsVersion="v1"`（此前第 2 参传 null）；⑤ 工艺卡流转卡打印：批次选择列表「当前规格」列移至「当前工序」后（对齐生产批次同组列序），列偏好键 v1→v2。验证 `dotnet build MES.Blazor` 0 错误 0 警告 + Pickling/SectionOutsource/OutsourceRecover/ProcessCardPrint/Batches 定向单测 125 通过。**性质：全部 WASM**，无 EF 迁移、无数据变更。**2026-09-09 再续（frontend V71 / 质量管理模块 V8.8）：质量管理 15 张列表页默认列显隐收敛**（隐藏列均可经列选择器开启）——① 过程检验 隐 工单号/设备名称/班次（列偏好键 v1→v2）；② 成检到料 隐 执行序/班次/数据来源/更新时间（首并 v1）；③ 成品检验 隐 设备名称/班次/工单号（更新时间已随 G8 隐；列偏好键 v1→v2）；④ 成检追踪 隐 工单号/最终用户/班次/更新日期（首并 v1）；⑤ 不合格报告 隐 更新日期（首并 v1）；⑥ 炉号登记 隐 更新日期（首并 v1）；⑦ 理化检测 9 列表页各隐 更新日期（9 页均首并 v1）。验证 `dotnet build MES.Blazor` 0 错误 0 警告 + 质量组件定向单测 6 通过。**性质：全部 WASM**，无 EF 迁移、无数据变更。

| 文件 | 改动 | 性质 |
|---|---|---|
| `MES.Blazor/Shared/AppMenu.cs` | 批次菜单拆分两入口（生产批次/生产执行核查） | WASM |
| `MES.Blazor/Pages/Batches/ProductionExecutionCheck.razor`+`.razor.cs`（新） | 生产执行核查只读页（错疑卡联动/列偏好 batchExecutionCheck_v6） | WASM |
| `MES.Blazor/Pages/Batches/Batches.razor`+`.razor.cs`、`BatchDetail.razor`、`BatchEdit.razor`、`BatchCreate.razor` 等 | 错疑折叠卡迁出/列偏好/可视化联动同步；**2026-09-09 Batches.razor.cs 列定义重排 7 组（批次基本信息[含原始投料自挂牌号后插入]/工单信息[含产品要求+质量要求]/现执行状态[含生产执行，与工单信息对调后置后]/关联工单状态/流转判定/有效投料变更/成品切割跟踪，后 4 核查组整组默认隐藏，GroupKey 连续 1-7）+ 三调字段级默认显隐（G1 仅显 8 字段含 5 原始投料、G2 仅显 6 字段、G3 仅显 7 字段且当前规格/当前设备/对应规格组内换位）+ ColumnPrefsVersion v2→v3（`col_prefs_batches_v2`→`col_prefs_batches_v3`）** | WASM |
| `MES.Services/Batch/BatchService.cs` | 共享列表通道（登记日期 To 端见批十一） | 后端 dll |
| `MES.Blazor/Pages/Batches/ProductionRecords.razor`+`.razor.cs` | **2026-09-09 列默认显隐收敛（G1 仅显 9/G2 仅显 10，G3 工艺参数/G4 追溯信息 整组隐）+ ColumnPrefsVersion null→v1（`col_prefs_production-records_v1`）+ 移除 `table-min-width`/`--table-min-width`（列宽对齐生产批次）** | WASM |
| `MES.Blazor/Pages/Quality/FinalInspections.razor`+`.razor.cs` | **2026-09-09 列默认显隐收敛（63列默认显 33：G1隐资格等级/G2隐 7 字段/G3隐非定尺长度范围，G5尺寸值·G6压力值·G7涡流超声波·G8辅助信息 整组隐）+ ColumnPrefsVersion null→v1（`col_prefs_final-inspection_v1`）+ 移除 `table-min-width`/`--table-min-width`**；**2026-09-09 再续（V71）：二调 隐 设备名称/班次（G1 检验执行）/工单号（G2 生产批次，更新日期已在 G8 整组隐）+ ColumnPrefsVersion v1→v2（`col_prefs_final-inspection_v2`）** | WASM |
| `MES.Blazor/Pages/Quality/ProcessInspections.razor`+`.razor.cs` | **2026-09-09 列默认显隐收敛（33列默认显 24：G1隐执行序号、G4隐理论返整/入库/报废重+次品情况描述、G5辅助信息整组隐）+ ColumnPrefsVersion null→v1（`col_prefs_process-inspection_v1`）+ 移除 `table-min-width`/`--table-min-width`**；**2026-09-09 再续（V71）：二调 隐 工单号（G1 生产批次）/设备名称、班次（G2 检验执行）+ ColumnPrefsVersion v1→v2（`col_prefs_process-inspection_v2`）** | WASM |
| `MES.Blazor/Pages/Batches/PicklingInRecords.razor`+`.razor.cs` | **2026-09-09 列默认显隐收敛 + 移除 `table-min-width`/`--table-min-width`；同日二调（V70）：G1 增隐 工单号/设备名称/班次/操作人（默认仅显 入缸日期/生产编号/挂牌号/订单号/主号/工序/工段/工厂牌号/制造规格/加工支数/加工重量/产类）+ G2 状态/完工日期 恢复默认显示、完工班次/完工操作人 隐（v1 整组隐改回）+ ColumnPrefsVersion v1→v2（`col_prefs_pickling-in-records_v2`）** | WASM |
| `MES.Blazor/Pages/Batches/PicklingOutRecords.razor`+`.razor.cs` | **2026-09-09 列默认显隐收敛 + 移除 `table-min-width`/`--table-min-width`；同日二调（V70）：入缸信息 增隐 设备名称 + ColumnPrefsVersion v1→v2（`col_prefs_pickling-out-records_v2`）** | WASM |
| `MES.Blazor/Pages/Batches/SectionOutsources.razor`+`.razor.cs` | **2026-09-09 列默认显隐收敛（V70）：委外信息 隐 要求收回日期/紧急，回收信息 隐 非正常回收(支)/非正常回收(重)/回收备注 + ColumnPrefsVersion v1→v2（`col_prefs_section-outsources_v2`）** | WASM |
| `MES.Blazor/Pages/Batches/OutsourceRecoveries.razor`+`.razor.cs` | **2026-09-09 列默认显隐收敛（V70 首并版本）：回收信息 隐 数据来源/更新时间 + 新增 `ColumnPrefsVersion="v1"` 并接 Save/Load 第 2 参（原传 null，`col_prefs_outsource-recoveries_v1`）** | WASM |
| `MES.Blazor/Pages/Batches/ProcessCardPrint.razor` | **2026-09-09 列序调整（V70）：批次选择列表「当前规格」列移至「当前工序」后（对齐生产批次同组列序）+ ColumnPrefsVersion v1→v2（`col_prefs_process_card_print_list_v2`）** | WASM |
| `MES.Blazor/Pages/Quality/MaterialReceiveChecks.razor`+`.razor.cs` | **2026-09-09（V71 首并版本）：隐 执行序/班次/数据来源/更新时间（G1 检验到料）+ 新增 `ColumnPrefsVersion="v1"` 并接 Save/Load 第 2 参（原传 null，`col_prefs_material-receive-checks_v1`）** | WASM |
| `MES.Blazor/Pages/Quality/QualityProcessTracking.razor`+`.razor.cs` | **2026-09-09（V71 首并版本）：隐 工单号/最终用户（G1）/班次（G2）/更新日期（G6）+ `ColumnPrefsVersion="v1"`（`col_prefs_quality-process-tracking_v1`）** | WASM |
| `MES.Blazor/Pages/Quality/Ncrs.razor`+`.razor.cs` | **2026-09-09（V71 首并版本）：更新日期 默认隐藏 + `ColumnPrefsVersion="v1"`（`col_prefs_ncrs_v1`）** | WASM |
| `MES.Blazor/Pages/Quality/FurnaceRegistrations.razor`+`.razor.cs` | **2026-09-09（V71 首并版本）：更新日期 默认隐藏 + `ColumnPrefsVersion="v1"`（`col_prefs_furnace-registration_v1`）** | WASM |
| `MES.Blazor/Pages/Quality/{ChemicalAnalyses,FlaringTests,FlatteningTests,GrainSizeTests,HardnessTests,IntergranularCorrosionTests,MetallographicTests,PittingCorrosionTests,TensileTests}.razor.cs` | **2026-09-09（V71，9 页首并版本）：理化检测 9 列表页各 更新日期 默认隐藏 + `ColumnPrefsVersion="v1"`（`col_prefs_{chemical-analysis,flaring-test,flattening-test,grain-size-test,hardness-test,intergranular-corrosion-test,metallographic-test,pitting-corrosion-test,tensile-test}_v1`）** | WASM |
| `MES.Blazor/wwwroot/css/app.css` | **2026-09-09 删除已无引用的 `.compact-table.table-min-width .mud-table-root` 规则及说明注释** | WASM 静态 |
| `MES.Tests/Components/Batch/ProductionExecutionCheckTests.cs`（新） | 页面无搜索栏等断言 | 测试（仅本地） |

### ⏳ 批十一 全仓日期范围搜索「至」端统一含当天（frontend V67 / 批次模块 V5.23）

> 27 处服务层 `XxxDateTo/EndDate/dateTo` 的 `≤ 至日 00:00` 一律改半开 `< Xxx.Value.AddDays(1)`（含「至」当天；datetime 列修复 off-by-one，date 列等价）。**性质：后端 dll**，无 EF 迁移、无数据变更。影响各列表页日期筛选：生产批次「登记日期」、质量各试验/成检/到料/化学/炉号/NCR/质量追踪、订单 签订/交期、工单 需求调整/执行、仓库 入库/出库历史、设备 报修。验证 `dotnet build MES.Api` 0 警告 + 定向单测 718 通过。

| 文件 | 改动 |
|---|---|
| `MES.Services/Batch/BatchService.cs`、`ProductionRecordService.cs` | CreatedTime/ExecDate To 端 |
| `MES.Services/Quality/*TestService.cs`、`FinalInspectionService.cs`、`MaterialReceiveCheckService.cs`、`NcrService.cs`、`QualityProcessTrackingService.cs`（15 文件） | InspectionDate/AnalysisDate/IncomingDate/ReceiveDate/ReportDate |
| `MES.Services/Order/OrderService.cs`、`OrderDemandAdjustmentService.cs`、`WorkOrder/WorkOrderService.cs` | SignDate/DeliveryStart/End/DeliveryDate |
| `MES.Services/Warehouse/InventoryService.cs`、`Equipment/RepairOrderService.cs` | InboundDate/OutboundDate/ReportTime |

### ⏳ 批十二 全站「文本居左·数值居中」表格样式 + 定尺联通强调（app.css 重构，2026-09-08）

> 静态内容哈希已证 `web/css/app.css` 与发布包不同（09-08 后追加：`.mud-table-cell.text-center` 数值居中兜底、`.flwo-emph-actual/.flwo-emph-compare` 定尺关键单元格强调）。**性质：静态 + WASM（各页数值类/auto-table 联动）**，重新 publish 覆盖 `web\` 即生效。

| 文件 | 改动 |
|---|---|
| `MES.Blazor/wwwroot/css/app.css` | 数值居中兜底类 + 定尺联通强调类 |
| `MES.Blazor/Pages/**`（全站表格页联动加 class，mtime 晚于包） | 数值/金额列居中 class 套用 |

### ⏳ 批十三 工单-定尺-用料投料核查上下文（读模型/页面/迁移）

> 工单上下文 09-08 后工作：用料计划拆分「用料投料核查」新页 + 实时关注/错疑卡联动、工单列表/需求调整/执行状况默认列显隐收敛、定尺联通视图读模型与**定尺切割偏差率种子迁移 `20260908083206`**（后端 dll mtime/迁移时间戳晚于包，未上线）。**性质：后端 dll + WASM + 1 个 EF 迁移（云库启动自动 Migrate apply）**；含配置/计划排程少量改动。

| 文件 | 改动 |
|---|---|
| `MES.Blazor/Pages/WorkOrders/MaterialInputConsistency.razor`+`.razor.cs`（新） | 用料投料核查新页 |
| `MES.Blazor/Pages/WorkOrders/*`（MaterialPlanOverview/OrderDemandAdjustment/WorkOrderExecution/FixedLengthWorkOrderView/WorkOrders 等） | 列显隐/拆分/联动 |
| `MES.Core/DTOs/WorkOrder/*`、`Interfaces/WorkOrder/*` | 错疑/定尺/联动 DTO 与接口 |
| `MES.Services/WorkOrder/FixedLengthWorkOrderService.cs`、`WorkOrderService.cs`、`WorkOrderExecutionService.cs`、`Scheduling/ProductionOverviewService.cs`、`Configuration/ConfigParameterService.cs` | 读模型/服务 |
| `MES.Api/Controllers/WorkOrder/WorkOrderExecutionController.cs`、`MES.Api/Program.cs` | Controller/DI |
| `MES.Data/Migrations/20260908083206_SeedFixedLengthCutDeviationRatio.cs`(+Designer)+ModelSnapshot、`Seed/DbInitializer.cs`、`MES.Core/Constants/CacheDefaults.cs` | EF 迁移 + seed + 常量 |
| `MES.Tests/**`（WorkOrders/MaterialPlanOverview/WorkOrderService/WorkOrderExecutionService/FixedLengthWorkOrderService 等） | 测试（仅本地） |
| `docs/frontend_pages.md`(V51-V58)、工单/仓库/计划排程模块设计、工单/计划排程接口设计 | 文档 |
