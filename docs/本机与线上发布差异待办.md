# 本机与线上发布差异待办（短期跟踪）

> **性质**：记录「本机代码已改、线上 `zhz.js.cn` 尚未发布」的内容，供下次增量发布一次性带上。
> **维护规则**：每发布一次勾掉对应行、更新日期；清单全空时**删除本文件**，避免成为过期文档。
> 与部署体系关系：增量发布方法见 [`05_部署与运维.md`](05_部署与运维.md) 第四章；首装见 `14_部署执行单.md`。

## 📌 当前状态（2026-09-15 整理）

> **线上最新发布**：`mes-deploy-20260914_0615.zip`（**批二十五 ~ 批二十九**，2026-09-14 已在云库服务器发布完毕，API 启动自动 Migrate 应用 **17** 条迁移 `20260910103357` ~ `20260912192419`；sqlcmd 复核 `1 1 1 1 17` 通过、首页 `home=200`）。上一版为 `mes-deploy-20260910_0200.zip`（批十 ~ 批二十四）。
> **已打包（待上传）**：`publish/mes-deploy-20260914_2333.zip`（**66.3 MB / 350 条目**，本机 2026-09-14 23:33 打出，含批三十 + 批三十一全量）；**零 EF 迁移**（最新 `20260912192419` 已随上一版 apply）；自检通过：`web/index.html` = `app.css?v=13` + `MES.Blazor.styles.css?v=2`、`web/appsettings.json` 无 BOM 且 `BaseUrl` 为空、`api/MES.Api.dll` + `web/_framework/blazor.webassembly.js` 齐备。
> - ⚠️⚠️ **上一包 `mes-deploy-20260914_2255.zip` 是坏包、已作废（曾上传并导致全站故障）**：登录后点任何菜单均显示「抱歉，此页面不存在」。根因＝行级批量脚本改 `.razor` 时**吞掉 95 个列表页的首字符**（`@page "/xxx"` → `page "/xxx"`），该行被 Razor 当**纯文本**渲染 → **编译 0 错 0 警、单测全过，但路由根本没注册** → 全站路由消失（`Index.razor` / `Login.razor` 未受损，故仍能登录、能看首页）。已修复（字节级在首字节前回补 `@`，编码/换行零改动），并**新增防回归测试** `MES.Tests/Components/RazorDirectiveIntegrityTests.cs`（全仓扫 `.razor` 行首裸指令；**今后任何 .razor 批量脚本跑完必须执行它**——编译器抓不到这类损坏）。
> - **本包（2333）验证**：wasm 内 UTF-16 字面量 `page "/orders"` 等 **坏包各 1 处 → 本包 0 处**（证明已转为 `RouteAttribute`）；`MES.Blazor.wasm` 体积 8,173,849 → **8,169,241 B**（文本渲染代码消失）；`@page` 总数 **177**（82 + 修复的 95）；`dotnet build -c Release` 0 错 0 警 + 定向单测 20 过。
> - ⚠️ **上传前先在服务器留回滚底**（`09-apply-deploy.ps1` 直接覆盖 `C:\mes\api` / `C:\mes\web`、不留底）：`Copy-Item C:\mes\api C:\mes\_backup\api-<时间> -Recurse` + `C:\mes\web` 同理。
> - 旧包 `publish/mes-deploy-20260914.zip`（06:16 = 已上线的批二十五~批二十九那版）保留作回滚底，暂不删。
> **未上线**：**批三十**（报表往来数据三卡「日期区间」+ 投料产出总况「完成日期范围」）与 **批三十一**（首页两张查询卡 + 3 端点放宽为仅登录 + 首页修正：删「订单负荷实时状况」卡 / 两卡补「清除」/ 常用入口桌面字号放大 1 号 + 手机真机磁贴拥挤修正 + 手机竖屏全站收敛「`.mh-shell`」与撤销自动弹「请横屏」+ **宽表手机端可用性收敛（粘性表头、不冻结任何列、滚动区放宽、工具栏换行）** + **V140 删除「手机横屏切电脑版」例外并连带删除 `LandscapeHintBanner`/`LandscapeHintRule` 机制**）—— 均本机已改、**已打包（2333）、待上传上线**，详见下一节。
> 📌 批二十九含两项改动：① 首页常用入口 9 → 11（补巡检/不合格反馈扫码）② 桌面端两卡改上下堆叠、各约半屏宽居中。
> ⚠️ **数据库层清理 / 结构变更见文末「数据变更注记」**，与代码发布批次相互独立，勿混为一谈。
> 📚 已上线批次（批一 ~ 批二十四）明细已按「维护规则」裁撤；需追溯时从 git 历史取回：`git log -p -- docs/本机与线上发布差异待办.md`
> 📌 页面 / 接口细节以 `docs/frontend_pages.md`（V85~**V140**）、`docs/模块设计/看板上下文详细设计.md`（~**V3.17**）、`docs/模块设计/质量管理模块详细设计.md`（V8.16~V8.31）、`docs/模块设计/订单模块详细设计.md`（V1.26~**V1.35**）、`docs/模块设计/物料模块详细设计.md`（~**V3.8**）、`docs/模块设计/批次模块详细设计.md`（~**V5.34**）、`docs/11_打印设计.md`（V2.18~**V2.19**）为准。

## ⏳ 批三十二 工程侧 P0 三项：健康检查端点 + 部署门禁 + 冒烟路由断言（2026-09-15 登记，**未打包、未上线**）

> **性质：后端 dll（`MES.Api`）+ 部署脚本（不随 zip 走，需单独拷到服务器 `C:\mes\deploy\`）**。
> **⚠️ 无 EF 迁移、无数据变更、无权限变更**（`/api/health` 匿名可访问）。
> **⚠️ `2333` 包不含本批**（该包 2026-09-14 23:33 打出，早于本批改动）→ **本批需重跑 `03-publish-local.ps1` 再打一次包**。
> **本批起因**：2026-09-14 全站故障（95 个 `.razor` 首行 `@page` 丢 `@`，**编译 0 错 0 警 + 3529 单测全过，却一个路由都没注册**，全站「抱歉，此页面不存在」）。原 4 项 HTTP 冒烟检查**在该故障中全部通过**（nginx SPA fallback 让 `/` 永远 200），暴露出「上线工程化」短板。

| 项 | 落点 | 说明 |
|---|---|---|
| **健康检查端点** | `MES.Api/Health/DatabaseHealthCheck.cs`（新）+ `MES.Api/Program.cs` | `AddHealthChecks` + DB 连通性探活 → `GET /api/health`；**刻意放在 `/api/` 下以复用 nginx 既有 `location /api/` 反向代理，无需改 nginx.conf、无需重载 nginx**；匿名可访问（部署脚本服务刚起、无 token 时要能探活），只回报状态与各项耗时，不暴露连接串 |
| **部署门禁** | `scripts/deploy-windows/09-apply-deploy.ps1`（重写） | 包体三件套（`MES.Api.dll` / `index.html` / `_framework\blazor.boot.json`）校验 + **wasm 路由门禁**（搜 UTF-16LE 裸指令标记 `page "/`；好包 0 命中、坏包 95 命中）→ **在动任何文件之前**中止；覆盖前备份 `api`/`web` 到 `C:\mes\_backup\<时间戳>\` 并写 `ROLLBACK.txt`；启后等端口 + 探活 `/api/health` |
| **冒烟路由断言** | `scripts/deploy-windows/06-verify.ps1`（重写） | 新增 `GET /api/health` 检查（期望 200）+ 下载 `_framework/MES.Blazor.wasm` 跑**同一道**路由门禁；每行的期望 HTTP 码改为按行声明（原为全局推断） |
| **README 同步** | `scripts/deploy-windows/README.md` | §9 期望输出补路由门禁与故障说明；「增量发布」章节改指向 `09-apply-deploy.ps1`；文件清单补 `08-restore-db.ps1` / `09-apply-deploy.ps1` |

> ⚠️ **脚本不随 zip 发布**：`09-apply-deploy.ps1` / `06-verify.ps1` / `README.md` 的改动需**手动拷到服务器 `C:\mes\deploy\`**（覆盖同名文件）才生效。
> ⚠️ **脚本必须保持纯 ASCII**：PS 5.1 按 ANSI 读无 BOM 的 `.ps1`，中文注释会吞掉行尾换行 → 紧随其后的那行**静默不执行**（已实测踩过）。本批两个脚本全文件 0 个非 ASCII 字节。
> **门禁为何用「二进制搜标记」而非「数路由」**：Blazor 路由编译期固化进程序集，源码级扫描只有在构建机上跑才有意义；部署脚本在服务器上只有编译产物，故直接在 wasm 里搜「不该出现的裸指令」最省事也最可靠（8 MB 约 1 秒）。
> **验证**：`dotnet build MES.Api` **0 错 0 警**（6 项目）；两脚本 `Parser::ParseFile` 解析通过；门禁逻辑用本机 `publish/blazor/wwwroot/_framework/MES.Blazor.wasm`（**8,169,241 B**）做**正负对照**实测通过（损坏标记 `page "/` → 0 命中；正对照 `/orders`、`/quality/ncr` → 命中；负对照垃圾串 → 0 命中）。
> **发布后核对**：`curl https://zhz.js.cn/api/health` → 200 + JSON；`06-verify.ps1` 全 OK 且路由门禁打印 `[OK ]`。
> 📌 另同步：`docs/12_商业MES差距分析.md` **V4.7 → V4.8**（修正「无统一缓存」/152 页面/质量模块覆盖 三处过期结论 + 新增 §五 工程侧差距章节）。

## ⏳ 批三十 报表往来数据三卡「日期区间」（客户/供应商/委外单位）+ 投料产出总况「完成日期范围」（2026-09-14 登记，**未打包、未上线**）

> **性质：后端 dll（`MES.Core` / `MES.Services` / `MES.Api`）+ WASM（`MES.Blazor`）+ 静态 css**（`app.css` 表头强调色）。
> **⚠️ 无 EF 迁移、无数据变更、无权限变更、无新增端点**（全部为既有 `list` 端点加可选 `[FromQuery] DateTime?` 参数 + 既有读聚合服务加窗口参数 + 前端 UI）→ **不需要** 迁移 SQL，`attachments\` 目录不涉及。
> **⚠️ 必须前后端同批打包**：`QueryParams` / 三个 `list` 端点 / `throughput-summary` 端点的 dll 与 WASM 必须同步，单独发布任一侧会出现「参数传了后端不认」或「后端改了前端不传」。
> **⚠️ 与批二十五 ~ 批二十九为同一工作树、建议一次性打包**（本次为纯追加，不改动前批次任何行为）。
> **⚠️ 用户可见行为变化（发布后核对项见文末）**：报表总览「客户往来数据」卡（Tab1 业务总况）、「供应商往来数据」卡（Tab4 物料执行）、「委外单位往来数据」卡（Tab5 生产执行）三卡工具栏均新增日期区间；「投料产出总况」两处入口的「完成月搜索」已替换为「完成日期起/止」。

### ① 客户往来数据：补「累计接单」列 + 表头分组强调色 + 接单区间 → 双区间（V123 / V124 / V125 累积）

- **V123（纯 WASM）**：报表总览客户卡补「**累计接单**」列（`TotalOrderCount/Weight/Amount`，与源列表页「客户管理」同源），单行 7 → **8 统计列**；「待发货(整单/非整单)」表头改**暖橙**（底 `#FFF3E0` / 字 `#E65100` / 底边 2px `#FFB74D`）、「待在产(整单未入库/扣除部分入库)」改**冷紫**（底 `#F3E5F5` / 字 `#6A1B9A` / 底边 2px `#CE93D8`）——报表卡为纯 HTML `<table>` 走**内联样式**（打印天然带上）；源列表页「客户管理」`/customers` 走 `GetTradeAccentCss(col.Key)` 拼 `th-accent-stock` / `th-accent-wip` 类，样式归口 `app.css`（**带 `!important` 且必须置于 `.col-g2` 之后**）。
- **V124（后端 + 前端）**：客户卡新增「接单日期区间」（单区间，`SignDateFrom/To`）——「本年接单」→「区间接单」（`SalesOrder.SignDate`）、「本年已发货」→「区间已发货」（`OutboundRecord.OutboundDate`）、累计接单前端渲染「—」（口径不同源）、存量 4 列不受影响；⚠️ `QueryParams.SignDateFrom/To` 与 `WorkOrderQueryParams` 原同名属性**同语义**，子类重复声明已删、上提至基类（消除 CS0108）。
- **V125（后端 + 前端）**：**拆为两个互相独立、可叠加的区间**——接单区间（→ `YearOrder`）+ 发货区间（`ShipDateFrom/To` → `ShippedDone`/`ShippedOther`）；`CustomerTradeActiveColumns()` 返回当前激活列集合（未启用区间 = null = 8 列全显），**未激活列（含「累计接单」）一律渲染灰色「—」**（`#9e9e9e`），表头仅在对应区间生效时改名；任一区间生效时**只保留激活列有值的客户行**；工具栏按用户指定顺序重排（搜索 → 接单组 → `MudDivider` → 发货组 → 显示行数 → 计数 →「清空全部区间」）。
- **⚠️ 行过滤下沉服务层**：`CustomerService.GetPagedAsync` 检测到区间即走 **`GetPagedInRangeModeAsync`**（先按 `ActiveStatsColumns`/`HasDataInActiveColumns` 过滤客户行再分页，`TotalCount` = **过滤后**条数，「共 N 条」与显示行同口径）；统计字段由 `ApplyStats` 从同一份桶回填（`AttachStatsAsync` 改为复用它）。
- **⚠️ `QueryParams` 属性（本批累计新增 10 个）**：`SignDateFrom/To`、`ShipDateFrom/To`（客户）、`SupplierOrderDateFrom/To`、`SupplierArrivalDateFrom/To`（供应商）、`VendorSendDateFrom/To`、`VendorRecoveryDateFrom/To`（委外）。
- **两处入口**：报表总览 `/reports/overview`「客户往来数据」卡 + 源列表页「客户管理」`/customers`（`MudGrid` 4/8 拆分、同款工具栏、`TradeColumnLabel` 动态表头、`ComputePageSums`/`RenderCell`/`GetCellDisplayText` 三处未激活列置「—」；⚠️ 源列表页统计列键名 **`YearOrdering`（带 -ing）**，报表卡为 `YearOrder`）。
- **文件**：`MES.Services/Order/CustomerService.cs`、`MES.Api/Controllers/Order/CustomerController.cs`、`MES.Core/Models/QueryParams.cs`(+`WorkOrderQueryParams`)、`MES.Blazor/Services/Order/CustomerService.cs`、`MES.Blazor/Pages/Reports/ReportOverview.razor(.cs)`、`MES.Blazor/Pages/Orders/Customers.razor(.cs)`、`MES.Blazor/wwwroot/css/app.css`、`MES.Tests/Services/Order/CustomerServiceTests.cs`。
- **验证**：`dotnet build MES.Api` + `dotnet build MES.Blazor` 0 错 0 警；`CustomerServiceTests` **42 过**（含发货区间 5 例 + 行过滤 3 例；⚠️ 受行过滤影响改写 5 处旧用例）。
- 文档：`订单模块详细设计.md` **V1.32 / V1.33**、`frontend_pages.md` **V123 / V124 / V125**。

### ② 投料产出总况：「完成月模糊搜索」→「完成日期范围」（V126）

- **动机**：原「完成月搜索」是前端本地按 `yyyy-MM` 模糊匹配，且后端恒返回近 12 个月 → **查不到 12 个月之外的历史区间**。
- **后端**：`IOrderThroughputQueryService.GetMonthlySummaryAsync(string? scope, DateTime? dateFrom = null, DateTime? dateTo = null)`；`GET api/order/throughput-summary` 新增 `[FromQuery] DateTime? dateFrom/dateTo`（`yyyy-MM-dd`）。任一端有值即**日期区间模式**：按「**完成月月首落入 [起, 止] 闭区间**」取数（⚠️ **该边界口径已于 2026-09-14 修正为「按订单真实完成日落入闭区间」——见 ⑥ / V130**），**不受 12 个月窗口限制**，且**整个所选区间跨订单聚合为唯一 1 行**（服务端单桶键 `RangeRowKey` 聚合后 `BuildRangeText` 写 `row.Month`）；两端皆空 → 沿用「最近 12 个月（含当月）」按完成月分行；单端为空 = 该端开放；起晚于止 = 空行。
- **前端两处入口同步**：`/orders` 卡片（`#order-throughput-table`）与 `/reports/overview`「业务总况」Tab 卡（`#report-throughput-table`）删「完成月搜索」，改 **「完成日期起 / 完成日期止」两个 `MudTextField T="string"`（`Placeholder="yyyy-MM-dd"`，**禁 `MudDatePicker`**，宽 140px）+ 应用 + 清除**（`Disabled` 绑 `!_throughputRangeMode`）；`_throughputRows` 简化为直取服务端 `Months`、首列直渲 `@row.Month`（**不再本地过滤、前端无 `ThroughputRangeText()`**）；空态文案二分支；**日期范围控件靠右紧凑成组**（`MudSpacer` + `d-flex align-center`，四项视为一个整体）；**区间模式下首列语义临时切换**「完成月」→「**选定范围**」，该唯一行显示区间文本（如 `2026-02-01 - 2026-09-14`，单端空显示「不限」）。
- **文件**：`MES.Core/Interfaces/Order/IOrderThroughputQueryService.cs`、`MES.Services/Order/OrderThroughputQueryService.cs`、`MES.Api/Controllers/Order/OrderThroughputController.cs`、`MES.Blazor/Services/Order/OrderService.cs`、`MES.Blazor/Pages/Orders/Orders.razor(.cs)`、`MES.Blazor/Pages/Reports/ReportOverview.razor(.cs)`、`MES.Tests/Services/Order/OrderThroughputQueryServiceTests.cs`。
- **验证**：`dotnet build MES.Api` + `dotnet build MES.Blazor` 0 错 0 警；`OrderThroughputQueryServiceTests` **18 过**（新增 5 例）。
- 文档：`订单模块详细设计.md` **V1.34** §5.14.3 + §6.1、`frontend_pages.md` **V126**。

### ③ 客户往来数据工具栏分组紧凑化（V127，纯 WASM）

- 原工具栏所有控件平铺于单一 `gap:8px` 容器 → 改为**外层 `gap:16px`（组间隔离）+ 组内 `gap:6px`（紧凑）**组合：①「搜索业务员/最终用户」②「接单日期起/止 + 应用 + 清除」③「发货日期起/止 + 应用 + 清除」④仅报表卡另有「显示行数 + 计数注释」；②③ 之间保留 `MudDivider` 竖分隔（`height:28px`，去掉原 `margin:0 2px`，间距由外层 gap 提供）；日期输入框宽 150 → **140px**（与「投料产出总况」一致）。
- ⚠️ `var customerRows = CustomerTradeShown();` 由原 `@{ }` 块**上提为 `@if` 块内裸声明**（代码上下文内不得再用 `@{`，否则 `RZ1010`）。
- **文件**：`MES.Blazor/Pages/Reports/ReportOverview.razor`、`MES.Blazor/Pages/Orders/Customers.razor`；文档 `frontend_pages.md` **V127**。

### ④ 供应商往来数据 + 委外单位往来数据：双区间（V128，报表卡 + 源列表页共四处）

- **区间形态 = 双区间（互相独立、可叠加）**——**供应商**：`出单区间`（`SupplierOrderDateFrom/To`，驱动「本年出单」→「**区间出单**」，取 `OrderDate`）+ `到货区间`（`SupplierArrivalDateFrom/To`，驱动「本年到货[扣除退货]」→「**区间到货**」、「本年退货」→「**区间退货**」，取入厂批 `InventoryBatch.InboundDate` / 退货 `OutboundRecord.OutboundDate`；两列同源同一窗口）。**委外单位**：`发出区间`（`VendorSendDateFrom/To`，驱动「本年委外」→「**区间委外**」，取 `SendOutDate`）+ `回收区间`（`VendorRecoveryDateFrom/To`，驱动「本年回收[扣除退回]」→「**区间回收**」、「本年退回」→「**区间退回**」，取 `RecoveryDate`）。**存量列不参与**（待收货 / 委外未回收）与**累计列不参与**（累计出单 / 累计委外）。
- **后端**：`SupplierService.BuildTradeStatsAsync(ctx, year, rows, orderFrom, orderTo, arrivalFrom, arrivalTo)` 拆两套独立窗口（局部函数 `InArrivalWindow`）与 `OutsourceVendorProfileService.BuildOutsourceStatsAsync(ctx, year, rows, sendFrom, sendTo, recoveryFrom, recoveryTo)`（局部函数 `InSendWindow` / `InRecoveryWindow`）——结束日按项目惯例 `< To.AddDays(1)` 闭区间含当天；两个 `list` 端点各加 4 个 `[FromQuery] DateTime?`；**区间模式下服务端一并行过滤**（`GetPagedInRangeModeAsync`，`TotalCount` = 过滤后条数）；Blazor `SupplierService` / `OutsourceVendorService` URL 拼 `orderDateFrom/orderDateTo/arrivalDateFrom/arrivalDateTo`、`sendDateFrom/sendDateTo/recoveryDateFrom/recoveryDateTo`（`yyyy-MM-dd`）。
- **报表卡**（`ReportOverview.razor(.cs)`）：两卡工具栏改 **V127 组合范式**（外层 `gap:16px` + 组内 `gap:6px`，日期框 140px，`MudDivider`）；`SupplierTradeCell`/`SupplierTradeSummary`/`OutsourceTradeCell`/`OutsourceTradeSummary` **由 static 改实例方法**；未激活列渲染灰色「—」；`SupplierTradeVisible()`/`OutsourceTradeVisible()` 区间生效时先剔除「激活列全为 0」的行；公共 `ParseCustomerTradeDate` **更名 `ParseTradeDate`**（客户 4 处调用同步）；⚠️ `var supplierRows/outsourceRows` 上提为 `@if` 块内裸声明（同 `RZ1010` 坑）。
- **源列表页两处同步**：「供应商管理」`/suppliers`（`Suppliers.razor(.cs)`）与「委外单位档案」`/outsource-vendors`（`OutsourceVendors.razor(.cs)`）：搜索栏 `MudGrid` 下方新增同款双区间工具栏 + 口径提示条；`TradeColumnLabel(col)` 接表头；往来信息**未激活列在分页合计（`ComputePageSums`）置「—」**；`ApplyRangeAsync()` 置 `_resetToFirstPage` 后 `ReloadServerData()`。
- ⚠️ **键名两套**：报表卡 `YearOrder`/`YearRecovered`/`Arrived`/`YearReturn`（不带 -ing），源列表页 `YearOrdering`（带 -ing）其余同名——与客户往来同款，勿混。
- **文件**：`MES.Services/Materials/SupplierService.cs`、`MES.Services/Batch/OutsourceVendorProfileService.cs`、`MES.Api/Controllers/Materials/SupplierController.cs`、`MES.Api/Controllers/Batch/OutsourceVendorProfileController.cs`、`MES.Core/Models/QueryParams.cs`、`MES.Blazor/Services/Materials/SupplierService.cs`、`MES.Blazor/Services/Batch/OutsourceVendorService.cs`、`MES.Blazor/Pages/Reports/ReportOverview.razor(.cs)`、`MES.Blazor/Pages/Materials/Suppliers.razor(.cs)`、`MES.Blazor/Pages/Batches/OutsourceVendors.razor(.cs)`。
- **验证**：`dotnet build MES.Api` + `dotnet build MES.Blazor` 0 错 0 警（`--no-incremental`）；定向单测 `SupplierServiceTests` + `OutsourceVendorProfileServiceTests` **46 过**。
- 文档：`物料模块详细设计.md` **V3.8** §4.10、`批次模块详细设计.md` **V5.34** §3.10、`frontend_pages.md` **V128**。

### ⑤ 报表总览「业务总况」Tab 卡序调整 + 「订单接单」卡标题行对齐（V129，纯 WASM）

- **卡序调整（用户 2026-09-14 指定）**：「业务总况」三张独立卡由「客户往来数据 → 订单接单·出库及现负荷汇总 → 投料产出总况」改为 **「订单接单·出库及现负荷汇总（含订单完成预估 / 延期交货订单预估 2 张子表）→ 投料产出总况 → 客户往来数据」**（「客户往来数据」移到「投料产出总况」下方）。仅 `ReportOverview.razor` 物理顺序调整，卡内容 / 折叠键 / 打印 id / 后端全不变。
- **「订单接单」卡标题行结构统一**：原 `div.d-flex.justify-space-between` + 内层 `div.d-flex.align-center style="gap:8px"` 使标题色条 / 标题文字比另两卡**右移 8px**（用户反馈「没有与其它 2 个左对齐」）→ 改为与另两卡同款：`div.d-flex.align-center.mb-2` + 图标 + 标题（`ml-1`）+ 说明（`text-hint ml-2`）+ 打印按钮包 `<div class="ml-auto">`。
- **「订单接单」表结构重构（方案 B，用户 2026-09-14 拍板）**：原 5 行 × 12 月矩阵把 3 个**当前时点存量**挤在**当月列**（成品库存(完工)/成品库存(未完工)/订单负荷量(实时)）→ ① 12 月矩阵**只留流量行**（接单量 / 出库量）；② 存量 3 行拆为表下独立小表**「当前负荷快照」**（指标 | 当前值，同卡内、同在打印 id `#report-inout-summary-table` 内 → 打印一次全含，无新 id / 无新打印按钮）；③ **「12月」后新增「汇总」列**（表头 `#e8f0fe`、单元格 `#fafcff`；口径 = **本年 12 个月合计**，接单量与出库量两行均给值；前端 `Sum()` 直算，新助手 `RenderYearTotalCell`）。**删除** `RenderCurrentOnly` / `_currentMonthIndex`（原设计语义错位 + 切年会把本年存量标进他年同月；⚠️ `MonthlyStock.razor.cs` 的同名字段属另一页，未动）。**DTO / 后端 / DB / 端点零改动**。
- **报表 5 项样式统一（用户 2026-09-14 指定）**：① **Tab2「现订单负荷总量」全表居中（即用户原话「现订单负荷总量，表头字段与数据值均居中显示」所指）**——该 Tab 页签名 = 「现订单负荷总量」，页体为 `MES.Blazor/Pages/Scheduling/WorkOrderLoadOverview.razor` 组件（`/plan-overview` 页复用同一组件，两处同步生效）：4 个数值列表头 `未编制计划(吨)` / `待落实量(吨)` / `待产量(吨)` / `预计天数` 由 `Class="text-right col-g100"` → `Class="text-center col-g100"`（原表头右对齐而数据格居中，左右不一致；且 `.auto-table th { text-align:left !important }` 会压掉 th 的 `text-right`，只有 `text-center` 有对应 th 规则见效），`类别` / `负荷节点` 的表头与数据格一并居中（`CellClass(row, "")` → `CellClass(row, "text-center")`，`summary-*` 分类底色 / `row-overall` 保留）。另 Tab1「订单接单」卡两表（流量矩阵 + 当前负荷快照）的「指标」列也一并居中（同批，使该卡整表居中）；② Tab4 供应商往来「**待收货**」表头加**暖橙**强调色（同客户卡「待发货」范式：`#fff3e0` / 字 `#E65100` / 底边 2px `#ffb74d` / 加粗）；③ Tab5 委外单位往来「**委外未回收**」表头加**冷紫**强调色（同客户卡「待在产」范式：`#f3e5f5` / 字 `#6A1B9A` / 底边 2px `#ce93d8`）——两处均走报表卡内联样式（打印天然带上），⚠️ **源列表页 `/suppliers`、`/outsource-vendors` 本次未同步**（如需同步另开一项）；④ Tab5「**段落流转分析**」由 `MudTable Class="auto-table"`（无格线）改为**带纵横格线的原生 HTML 表格**，样式对齐同 Tab 的「冷轧拔近日排程」（数值列居中 / `生产段落` 列居左加粗 / 页脚沿用 `col-footer-cell`、`col-footer-sum` 并补「合计」标签）；⑤ **数值列表头去右对齐**：`MudTh Class="text-right"` 3 处（Tab4 半成品待购、成品待购「待购量(kg)」、委外待穿孔「缺少量(kg)」）→ `Class="text-center"`（原先表头右对齐而数据格居中，且被 `app.css` 的 `.auto-table th { text-align:left !important }` 压成左对齐）。**纯 WASM：DTO / 后端 / DB / 端点零改动、无迁移、无权限变更**。
- **文件**：`MES.Blazor/Pages/Reports/ReportOverview.razor`、`ReportOverview.razor.cs`、`MES.Blazor/Pages/Scheduling/WorkOrderLoadOverview.razor`；文档 `frontend_pages.md` **V129**。
- **验证**：`dotnet build MES.Blazor --no-incremental` 0 错 0 警。

### ⑥ 投料产出总况「完成日期范围」口径修正（V130，后端 1 处 + 前端文案）

- **问题**：②（V126）引入的区间判定，服务端按「**完成月月首**」锚定（`new DateTime(doneAt.Year, doneAt.Month, 1)` 参与 [起, 止] 比较）→ **起日非月初则整个起始月被整月剔除**、**止日非月末则整个截止月被整月纳入**，用户按「完成日期」设定范围时数据对不上。
- **真库实证（全部口径，全库完成订单 17 单）**：区间 `2026-01-15 ~ 2026-02-20` 旧逻辑命中 **5 单**（1 月因月首 `01-01 < 01-15` 整月丢失）→ 按真实完成日应为 **9 单**。
- **修正（用户 2026-09-14 拍板方案 A 最小改动）**：`MES.Services/Order/OrderThroughputQueryService.cs` 区间判定改为 `doneAt.Value.Date` 直比闭区间（**起止当日均含**）；整区间仍聚合为 1 行、默认 12 个月窗口行为零变化。`IOrderThroughputQueryService` / `OrderThroughputController` 的 XML 注释口径同步。
- **脚注按模式切换**：两处入口（`Orders.razor` `#order-throughput-table`、`ReportOverview.razor` `#report-throughput-table`）脚注改由新增 `ThroughputFootnote()` 输出——**区间模式显式声明「按订单真实完成日落入区间过滤、整区间聚合为 1 行」+ 各列为命中订单的全生命周期合计（与完成日不相关）——生产投料 = 各批次工艺卡领料重之和、入库列 = 各批次入库量之和，区间之外发生的投料/入库量也会计入本行**（`ProductionBatch` 无投料日期字段，逐列按事件日期切分不可行，故不改口径、只补说明）；报表卡头说明改为「按完成日落入所选区间，整区间聚合为 1 行」。
- **文件**：`MES.Services/Order/OrderThroughputQueryService.cs`、`MES.Core/Interfaces/Order/IOrderThroughputQueryService.cs`、`MES.Api/Controllers/Order/OrderThroughputController.cs`、`MES.Blazor/Pages/Orders/Orders.razor(.cs)`、`MES.Blazor/Pages/Reports/ReportOverview.razor(.cs)`、`MES.Tests/Services/Order/OrderThroughputQueryServiceTests.cs`。
- **验证**：`dotnet build MES.Api` + `dotnet build MES.Blazor` 0 错 0 警；`OrderThroughputQueryServiceTests` **19 过**（新增 1 例「边界按真实完成日直比含起止当日」；改写 1 例原「月首不入区间的订单被排除」为「按真实完成日纳入」）。
- **发布后核对**：`/orders` 与 `/reports/overview` 的「投料产出总况」填「完成日期起/止」后，行数应与 `WorkOrderExecutionSummary` 中完成日落在区间内的订单数一致（**不再受月初/月末对齐影响**）；脚注应出现「按订单真实完成日落入该区间过滤」与「全生命周期合计」提示。
- 文档：`订单模块详细设计.md` **V1.35** §5.14.3 + §6.1、`frontend_pages.md` **V130**。

### ⑦ 供应商/委外源列表页表头强调色 + 静态资源版本串纪律修复（V131，纯 WASM + index.html）

- **① 强调色同步源列表页**（承接 ④ V128 与 ⑤ V129 遗留的「源列表页本次未同步」）——`/suppliers` 的「**待收货**」列加暖橙、`/outsource-vendors` 的「**委外未回收**」列加冷紫，与报表总览 Tab4/Tab5 两卡对齐。实现照抄 V123 客户管理范式：两页 `.razor.cs` 各新增 `GetTradeAccentCss(col.Key)`（`"Pending"` → ` th-accent-stock` / ` th-accent-wip`），`.razor` 表头 `_headerClass` 拼接；样式归口 app.css 既有 `th.th-accent-*`（`!important`，须在 `.col-g2` 之后）。
- **② 静态资源版本串纪律修复（部署级缺陷）**——`index.html` 的 `css/app.css?v=2` 自 2026-09-04（`ede83097`）起**未再抬串**，而 app.css 其后至少改 3 次（含把「执行进度」卡片 20 个 `.detail-*` 规则由 scoped `BatchDetail.razor.css` **上提至 app.css**：HEAD 版 app.css 含 `detail-` **0** 处 / 工作树 **20** 处）；而 `nginx-mes.conf.template` 对 `location /css/` 下发 `Cache-Control: public, max-age=31536000, immutable` → **浏览器取过一次 `app.css?v=2` 后一年内不重取** → 新上提样式全部不下发，表现为**「原来设定的显示样式没有了」**（`MES.Blazor.styles.css` 亦无版本串，同机制会命中完全依赖 scoped CSS 的订单进度树页）。
- **修复**：`css/app.css?v=2` → **`?v=3`**、`MES.Blazor.styles.css` → **`?v=1`**，并在该两行上方补版本串纪律注释。
- **文件**：`MES.Blazor/wwwroot/index.html`、`MES.Blazor/wwwroot/css/app.css`（注释同步）、`MES.Blazor/Pages/Materials/Suppliers.razor(.cs)`、`MES.Blazor/Pages/Batches/OutsourceVendors.razor(.cs)`。
- **验证**：`dotnet build MES.Blazor` 0 错 0 警。纯显示层，无后端 / 无 EF 迁移 / 无权限变更 / 无列偏好 bump。
- **发布后核对**：① `/suppliers`「待收货」表头暖橙、`/outsource-vendors`「委外未回收」表头冷紫，与报表总览 Tab4/Tab5 同色；② 浏览器 F12 → Network 应看到 **`app.css?v=3`**（不再是 `?v=2`）；③ 「批次执行进度」卡片（批次详情页 / 批次计划 / 成检计划 / NCR 建单页弹窗）的进度条、统计条、工段卡底色恢复正常。
- **⚠️ 执行纪律**：`app.css` / `MES.Blazor.styles.css` 内容一改**必须同一提交内抬串**，否则本次部署的样式在已访问过站点的浏览器上不生效（老用户需 Ctrl+F5 才能验证，属假阴性）。
- 文档：`frontend_pages.md` **V131**。

### ⑧ 发布注意事项与实施顺序

- ⚠️ **无 EF 迁移**（`dotnet ef` 不参与）→ 打包后 API 启动**不会**产生新迁移记录；发布后核对 `__EFMigrationsHistory` 条数应仍为 **17**（不新增）。
- ⚠️ **未新增端点、未改权限**：三张往来卡沿用各自既有 `list` 端点、投料产出沿用既有 `throughput-summary`，均为**新增可选查询参数**（不传 = 完全旧行为）→ 老前端配新后端、或新前端配旧后端的**降级表现分别为「区间不生效」**，不报错。
- ⚠️ **列偏好无版本 bump**（`/customers`、`/suppliers`、`/outsource-vendors` 列定义未增删；V123 只在报表卡补列）→ 老用户列显隐不受影响。
- **建议打包**：与批二十五 ~ 批二十九**同批一次性打包**（同一工作树，WASM 整体替换）；打包脚本与流程见 `05_部署与运维.md` 第四章（⚠️ `03-publish-local.ps1` 须用已修复版：纯 ASCII + `appsettings.json` 无 BOM 写入 + 自检断言 BaseUrl 已改写）。
- **发布后核对**：① `/reports/overview` Tab1 卡片自上而下应为 **订单接单·出库及现负荷汇总 → 投料产出总况 → 客户往来数据**，且三卡标题行（折叠图标 / 标题色条 / 说明 / 打印按钮）**左边缘在同一条竖线上**（「订单接单」卡已修掉 8px 偏移）；客户卡工具栏应为「搜索 / 接单日期组 / 竖分隔 / 发货日期组 / 显示行数 / 计数」；填入接单区间→「区间接单」有数、未激活列（含累计接单）变灰「—」、客户行被过滤；② Tab4 供应商卡、Tab5 委外卡同理（出单/到货、发出/回收）；③ `/customers`、`/suppliers`、`/outsource-vendors` 三列表页应出现同款日期区间工具栏且分页合计列随激活状态变「—」；④ `/orders` 与 `/reports/overview` 的「投料产出总况」应显示「完成日期起/止」，填区间后首列变「选定范围」且**仅 1 行**、清空后回落近 12 个月逐月。

## ⏳ 批三十一 首页两张查询卡（订单进度查询 / 生产批次进度查询）+ 3 个数据源端点权限放宽为「仅登录」+ 首页修正（删「订单负荷实时状况」卡 / 两卡补「清除」/ 常用入口字号与手机端磁贴修正）+ 手机竖屏全站收敛「`.mh-shell`」（2026-09-14 登记，**未打包、未上线**）

> **性质：后端 dll（`MES.Api` 三处 `[Authorize]` 放宽）+ WASM（`MES.Blazor` 新增 2 卡 + 抽 1 组件）+ 静态 css**（`app.css` 新增 4 条规则，另见 ⑧ 的字号/钩子/兜底调整与 8 条死 CSS 删除）。
> **⚠️ 无 EF 迁移、无数据变更、无新增端点**（全部复用既有端点，仅放宽授权）→ **不需要** 迁移 SQL，`attachments\` 目录不涉及。
> **⚠️ 必须前后端同批打包**：WASM 里 `Index.razor` 调用的两个新卡依赖同批 dll 的授权放宽；若只发 WASM 不发 API，无订单/批次权限的用户一查即 403（页面静默失效，无报错）。
> **⚠️ 与批三十为同一工作树、建议一次性打包**（本次为纯追加，不改动前批次任何行为）。

### ① 需求与口径（用户拍板 2026-09-14）

- **首页新增两张查询卡**：① 「订单进度查询」（输入订单号 → 就地渲染**订单进度树**）；② 「生产批次进度查询」（输入生产编号 → 就地渲染**批次执行进度**卡片）。**结果就地渲染，不是链接跳转**（区别于原「订单进度」菜单页 / 批次详情页）。
- **⚠️ 关键约束：首页对所有登录用户开放** → 两卡数据源端点必须**不带业务角色档**，否则无订单/批次权限的用户一查就 403。
- 交互：卡内 `MudTextField`（`Label="订单号"` / `Label="生产编号"`）+「查询」按钮，**回车即查**（`OnKeyDown`）；结果区 `.home-query-result` 限高 420px 纵向滚动（手机端不限高，随页面滚动）；查不到给 `MudAlert` 业务提示（「未找到该订单的工单数据」/「未找到该生产编号的批次」）且不渲染结果。

### ② 权限放宽（3 个端点 → 仅需登录）

| 端点 | 原权限 | 现权限 | 说明 |
|------|--------|--------|------|
| `GET api/order/progress` | OrderView 档 | **仅需登录**（`[Authorize]`） | 首页「订单进度查询」卡数据源 |
| `GET api/batch/by-batch-no/{batchNo}` | BatchView 档 | **仅需登录**（`[Authorize]`） | 首页「生产批次进度查询」卡「生产编号 → 批次 Id」跳板 |
| `GET api/batch/{id}/tracking` | BatchView 档 | **仅需登录**（`[Authorize]`） | 首页「生产批次进度查询」卡执行进度数据源 |

- **⚠️ 副作用（有意接受）**：知道订单号 / 生产编号的**任意登录用户**均可查到该订单进度树、该批次详情与执行进度（均只读、无写库、无操作日志）。
- **⚠️ 守住不顺手放宽**：按 Id 取批次详情 `GET api/batch/{id}`（`BatchController.GetById`）**仍保持 BatchView 档**（页面入口属批次管理页职责）；`/orders/progress` 页面仍保持 `OrderView` 档（菜单门控仍由「订单管理」分组承担）。
- **护栏测试**：`MES.Tests/Controllers/HomeQueryEndpointAuthorizationTests.cs`（反射断言上述 3 端点方法级 `[Authorize]` 无 `Roles`，且 `GetById` 仍带角色档）——谁改回角色档即红灯。

### ③ 组件抽取（纯重构，行为不变）

- **新增 `MES.Blazor/Shared/OrderProgressTree.razor(.cs/.razor.css)`**：把订单进度树本体自 `/orders/progress` 页抽出为共享组件，供页面与首页卡两处复用。
  - 参数：`Tree`（`OrderProgressTreeDto?`）+ `Compact`（`bool`）。
  - **⚠️ scoped CSS 不能跨组件**：原 `Pages/Orders/OrderProgress.razor.css`（64 条 `.op-*` 规则）**整文件 `git mv`** 为 `Shared/OrderProgressTree.razor.css`（内容零改动，仅改首行注释）；页面级打印样式留在 `OrderProgress.razor` 的 `<style>` 块内（普通类选择器如 `.op-main-node` 不带 scope 属性，仍能命中）。
  - **`Compact=true`**（首页紧凑态）：**所有主号默认折叠**（首页半屏宽，避免刷屏），点主号可展开；`/orders/progress` 页 `Compact=false`（仅**已完结**主号默认折叠，与改造前一致）。
  - **⚠️ 折叠态初始化用引用守卫**：`_collapsedInitializedFor` 记录已初始化的 `Tree` 引用，仅在 `Tree` 换对象时（`OnParametersSet`）重置折叠集，避免父组件重渲染把用户手动展开的节点又折回去。
- **`Pages/Orders/OrderProgress.razor(.cs)` 改为复用**：`.cs` 只保留取数与页头动作（`OnInitializedAsync` 读 `salesOrderNo` 查询参数、`Back`、`OnPrintAsync`），树渲染交给组件。

### ④ 新增两张首页查询卡

- `MES.Blazor/Shared/OrderProgressQueryCard.razor(.cs)`：`OrderProgressService.GetAsync` → `<OrderProgressTree Tree="_tree" Compact="true" />`。
- `MES.Blazor/Shared/BatchProgressQueryCard.razor(.cs)`：`BatchService.GetByBatchNoAsync` 换出 Id → `<BatchProgressCard BatchId="_batchId" />`。
- 两卡均走 `[CascadingParameter(Name="IsMobile")]`：手机端壳（MobileLayout）`mh-card pa-3` / `Elevation=0` / `Typo.subtitle1`，桌面端 `home-stack-card` / `Elevation=1` / `Typo.h6`。
- `Pages/Index.razor` **桌面与手机两分支**均在订单负荷卡之后追加两卡（两分支勿各维护）。
- `wwwroot/css/app.css` 新增 `.home-query-bar` / `.home-query-result` 及 `.mh-container` 下的手机端覆盖 4 条规则。

### ⑤ ⚠️ 静态资源版本串（发布后必核）

- `app.css` 内容已改 → `index.html` 抬串 **`app.css?v=3` → `v=4`**；`MES.Blazor.styles.css`（scoped CSS 打包产物，随组件增删而变）抬串 **`v=1` → `v=2`**。
- **⚠️ 执行纪律**：`app.css` / `MES.Blazor.styles.css` 内容一改**必须同一提交内抬串**，否则本次样式在已访问过站点的浏览器上不生效（nginx `location /css/` 为 `max-age=31536000 immutable`，老用户需 Ctrl+F5，属假阴性）。

### ⑥ 测试与验证

- **新增测试**：`HomeQueryEndpointAuthorizationTests.cs`（4 例授权护栏）+ `MES.Tests/Components/HomeQueryCardsTests.cs`（3 例卡契约：就地渲染 `.op-root`、Compact 全折叠与点击展开、查不到给提示且不渲染进度卡）。
- **验证**：`dotnet build MES.sln --no-incremental` → 9 项目 0 错 0 警；定向单测（`HomeQueryCardsTests` / `HomeQueryEndpointAuthorizationTests` 等）99 过。
- **发布后核对**：① 首页（桌面 + 手机）应出现「订单进度查询」「生产批次进度查询」两卡；② 用**无订单/批次权限的普通账号**登录，输入已知订单号 / 生产编号应能正常查出结果（不 403）；③ 首页订单进度树**默认全部主号折叠**，点主号可展开；④ 浏览器 F12 → Network 应看到 **`app.css?v=4`**、**`MES.Blazor.styles.css?v=2`**；⑤ `/orders/progress` 页行为不变（仅已完结主号默认折叠）。
- 文档：`frontend_pages.md` **V132 → V133**、`接口设计/订单上下文接口设计.md`（§4.9 权限列）、`接口设计/批次上下文接口设计.md`（§4.3 两行 + 说明）、`模块设计/看板上下文详细设计.md`（**V3.16**）。

### ⑦ 首页修正（2026-09-14 同日，用户反馈）

- **① 删除首页「订单负荷实时状况」卡片（用户要求）**：`Pages/Index.razor` **桌面分支**（`MudTable` 三阶段表 + 合计 Footer）与**手机分支**（`.mh-stage-card` 阶段色条卡）**两处整块移除**；`Pages/Index.razor.cs` 瘦身为**仅剩 `[CascadingParameter(Name="IsMobile")]`**（删 `WorkOrderStageRow` / `BuildCrossTable` / `Tons` / `_rows` / 4 个 footer 字段 / `OnInitializedAsync` / `@inject WorkOrderExecutionService`）→ **首页自本次起无任何常驻取数**。
- **⚠️ 只下线首页入口、不清理链路**：后端 `GET api/workorder-execution/dashboard-summary` 端点、`IWorkOrderExecutionService.GetDashboardSummaryAsync`（`MES.Services`）、前端 `MES.Blazor.Services.WorkOrderExecutionService.GetDashboardSummaryAsync` **全部保留**（`MES.Api` / `MES.Services` 本次零改动）。如需连同后端一并删除请另行确认。
- **② 两卡补「清除」按钮 = 取消查询（用户反馈「不知道怎么取消」）**：`Shared/OrderProgressQueryCard` / `Shared/BatchProgressQueryCard` 的查询按钮右侧各加 `Variant.Outlined` / `Color.Default` / `StartIcon=Icons.Material.Filled.Clear` 的「清除」按钮（`Disabled="_loading || !CanClear"`，`CanClear = _searched || 输入非空`）；点击清空输入框 + 结果区 + `_searched`，**回到未查询空态**（批次卡另清 `_resolvedBatchNo` / `_batchId`）。**纯前端复位，不触后端、不影响 `/orders/progress` 与批次详情页**。
- **③ 样式与版本串**：`app.css` 新增 `.home-query-input`（`flex:1 1 auto; min-width:0`）与 `.home-query-actions`（手机端 `justify-content:flex-end`）；`app.css` 抬串 **`?v=4` → `?v=5`**（`MES.Blazor.styles.css` 本次无 `.razor.css` 改动，保持 `?v=2`）。
- **④ 验证**：`dotnet build MES.sln --no-incremental` **9 项目 0 错 0 警**；定向单测 **9 过**（`HomeQueryCardsTests` 3 → **5** 例，新增两卡「点清除复位为空态」，订单卡另断言「清除后输入框已空 → 再点查询不发起请求」）。
- **发布后核对（补充）**：① 首页**不应**再出现「订单负荷实时状况」卡（桌面 + 手机）；② 桌面首页自上而下应为 **常用入口 → 订单进度查询 → 生产批次进度查询** 三卡；③ 两卡各应有「查询」「清除」两个按钮，点「清除」后输入框与结果区同时清空；④ F12 → Network 应看到 **`app.css?v=5`**。
- 文档：`frontend_pages.md` **V133**、`模块设计/看板上下文详细设计.md` **V3.16**。

### ⑧ 首页常用入口字号放大 + 手机端（真机）磁贴拥挤修正（2026-09-14 同日，用户反馈）

> **性质：纯 WASM（`app.css` + `index.html`）**，未触碰任何 `.razor` / `.razor.cs` / 后端 → **无单测影响、无 API 影响、无迁移**。

- **① 桌面端磁贴字号放大 1 号（用户原话「字体请放大 1 号」，经确认仅桌面端）**：`app.css` `.shortcut-tile { font-size: 14px }` → **`16px`**；手机端字号不跟着放大（由 `.mh-card .shortcut-tile` 的 `12px` 覆盖）。
- **② ⚠️ 手机端「与 F12 模拟不一致 / 比较拥挤」根因 = 手机端样式钩子挂在 `.mh-container` 后代选择器上**：原手机端规则写作 `.mh-container .shortcut-grid` / `.mh-container .shortcut-tile`（及 `.mh-container .home-query-*`），依赖 `.mh-container` 在真机 DOM 命中；F12 设备模拟命中（三列），**真机未命中** → 这批规则整体失效、回落桌面值（`flex: 1 1 160px` → 窄屏挤成 2 列）。
  - **修复**：样式钩子由祖先 `.mh-container` **改挂卡自身 `.mh-card`**（MobileLayout 渲染的每张卡都带 `mh-card`，命中稳定），涉及 `.mh-card .shortcut-grid` / `.shortcut-tile` / `.shortcut-tile span` / `.home-query-bar` / `.home-query-actions` / `.home-query-result` **六条**；`.mh-container { padding-top:0; padding-bottom:16px }` 保留不动。
- **③ 手机磁贴改「上下结构」**：`.mh-card .shortcut-tile { flex: 1 1 28%; flex-direction: column; gap: 2px; padding: 8px 2px; font-size: 12px; line-height: 1.2 }` + `.mh-card .shortcut-tile span { text-align: center; word-break: keep-all }` —— **icon 在上、文字在下**、三列并排（`flex-basis: 28%`），长名（如「不合格反馈扫码」6 字）竖排不再把窄格挤成多行；`.mh-card .shortcut-grid { gap: 6px }` 收窄格间距。
- **④ 窄屏「半屏卡」兜底**：新增 `@media (max-width: 959px) { .home-stack-card { width: 100% } }` —— V3.15 起 `.home-stack-card` 桌面端半屏宽居中；若**真机被 JS 判进桌面布局**，卡片只占半屏、右侧留白 → 窄屏一律撑满整宽。
- **⑤ 删除 8 条死 CSS**（批三十一 ⑦ 删除「订单负荷实时状况」卡后遗留、全仓 grep 零引用）：`.stage-pill` / `.stat-count` / `.stat-weight` / `.stat-unit` / `.stat-sep` / `.stat-muted` / `.stat-caption` / `.mh-stage-card`。
- **⚠️ ⑥ 静态资源版本串**：`app.css` 再次变更 → `index.html` **`css/app.css?v=5` → `?v=6`**（`MES.Blazor.styles.css` 无 `.razor.css` 改动，保持 `?v=2`）。**本次尤其不可省** —— ② 的修复全靠新 CSS，真机若已缓存旧串则**修复完全不生效、症状与修复前一模一样**。
- **验证**：`dotnet build MES.Blazor --no-incremental` **3 项目 0 错 0 警**。
- **发布后核对（补充）**：① **桌面**首页常用入口磁贴文字明显大于改前；② **手机真机**（非 F12 模拟、建议无痕或先清缓存）常用入口应为 **每行 3 个磁贴、icon 在上文字在下、不拥挤**，且两张查询卡为整宽而非半屏；
- 文档：`frontend_pages.md` **V134**（追加 V134 段）、`模块设计/看板上下文详细设计.md` **V3.17**。

### ⑨ 手机竖屏全站收敛「全站手机壳 `.mh-shell`」+ 撤销自动弹「请横屏」提示条（2026-09-14 同日，用户反馈「除首页外其它页面竖屏太杂乱」）

> **性质：纯 WASM（`MobileLayout.razor` + `app.css` + `index.html`）**，**未改任何页面本体**（全仓 160+ 页零改动）、未动后端 / API / 数据库、无单测行为变化。

- **背景与拍板**：全仓 160+ 页面在手机上渲染的都是同一套桌面布局（**只有首页有 `IsMobile` 手机分支**），竖屏表现为「页头按钮挤成一团 / 筛选工具栏控件平铺溢出 / 宽表只能左右拖 / 内容贴边」。用户拍板 **「竖屏也要可用」**（放宽 2026-09-06「除首页 + 扫码流外一律横屏查看」），并选择：全部页面统一收敛、宽表用「粘性首列 + 表头」、提示条**不再自动弹出**。
- **① 钩子**：`MobileLayout.razor` 内真实渲染 `<div class="mh-shell">`（包住 `<CascadingValue IsMobile>` 与 `@ChildContent`）作为**全站手机壳唯一钩子**。⚠️ **禁止**改成「祖先容器 + 后代选择器」——V134 真机样式失效的根因正是钩子挂 `.mh-container` 未命中。桌面 `MainLayout` 不加此类 → 桌面零影响。
- **② 收敛四项**（`app.css` 新增「全站手机壳」段）：**a.** 页头 `.mud-card-header-actions` 独占一行换行左对齐（覆盖 MudBlazor 默认 `flex:0 0 auto` + `-8px` 负边距）；**b.** `.mud-grid > .mud-grid-item { flex-basis:100% !important; max-width:100% !important }` → 筛选工具栏 / 表单字段逐行全宽（⚠️ `MudGrid` 是 flex 容器不是 CSS grid，列宽来自 `.mud-grid-item-{断点}-{n}` 的百分比）；`.d-flex` 允许换行；**c.** 容器左右 `12px`、卡片内边距收紧；**d.** 宽表粘性表头 + 粘性首列。
- **③ 宽表要点**：`.mud-table-container` 加 `overflow-y:auto` + `max-height: max(260px, calc(100vh - 220px))`（表头 sticky 必须有界高度）；**只让表头最后一行 sticky**（两行分组表头避免重叠）；首列 `position:sticky; left:0` + 不透明背景 + 右侧分隔线，z-index 表头 5 / 首列 4 / 交叉格 6；`border-collapse` 改 `separate` + `border-spacing:0`（`collapse` 下粘性单元格底边框不绘制、表头底线消失；MudBlazor 自身 `StickyHeader` 亦用 separate）。⚠️ 全仓 `RowClass=` **0 处**（无行级底色），首列白底不会盖行色；**将来新增带底色行需给该行首格单独指定 background**。四条表格规则包在 `@media screen` 内，不影响打印。
- **④ 撤销自动弹「请横屏」**：`MobileLayout.razor` 移除 `<LandscapeHintBanner />` 调用（原位留注释说明「恢复只需把这一行加回」）；组件与 `LandscapeHintRule.cs` **保留不删**，`LandscapeHintRuleTests` 8 例照旧；`ResponsiveLayout` 的**横屏 ≥700 自动切桌面宽表保持有效**。
- **⚠️ ⑤ 静态资源版本串**：`index.html` **`css/app.css?v=6` → `?v=7`**（`MES.Blazor.styles.css` 保持 `?v=2`）。不抬串则真机拿不到本次全部竖屏收敛。
- **验证**：`dotnet build MES.Blazor --no-incremental` **3 项目 0 错 0 警** + 定向单测 **22 过**（`LandscapeHintRuleTests` / `AppMenuTests` / `HomeQueryCardsTests`）。⚠️ 本次因 `MES.Api` 进程 + Visual Studio 锁定 `MES.Api\bin`，用 `dotnet test --no-build` 跑既有测试程序集（测试改动仅 XML 注释）。
- **发布后核对（补充）**：① 手机真机（非 F12 模拟、建议无痕）任一列表页：页头按钮应**另起一行**、筛选控件**逐行全宽**、不再被挤出屏幕；② 表格**纵向滚动时表头固定**、**左右拖动时首列固定**；③ **不再出现**「请横屏」提示条；④ 横屏（≥700）仍自动切桌面版宽表。
- 文档：`frontend_pages.md` **V135**（追加 V135 段 + §6 索引表补「全站手机壳」行、改写「移动横屏提示条」行）。
- **⑩ 宽表手机端可用性收敛（2026-09-14 追加，同一工作树）**：手机竖屏看「现订单负荷总量」报表与**仓库各列表**「根本无法查看」→ **保留全部列、不隐藏，接受大量左右移动**（用户明确否决「默认收起日期桶」「逐行卡片化」两方案），只解决「左右拖动时迷路」。**a.** 滚动区高度 `calc(100vh - 220px)` → **`calc(100vh - 140px)`** + 补 `100dvh` 行；**b.** `overscroll-behavior: contain`（横滑到边界不带动页面/返回手势）；**c.** ⚠️ **首列是批量勾选框时粘住第 2 列**（`th/td:first-child:has(.mud-checkbox) + th/td` 设 `position:sticky; left:40px`）——仓库库存查询等页首列是 40px 复选框，粘复选框对「认行」毫无帮助；`:has()` 旧 WebView 不支持则整条静默忽略、回落只粘首列；**d.** `.list-toolbar` 窄屏换行（56 页共用）。**⚠️ 先厘清**：`table-layout` MudBlazor 6.19 未声明、app.css `.mud-table{table-layout:fixed}` 作用于外层 div 是**死规则**，`<table>` 实为 `table-layout:auto` → **横向滚动本来就有效、列不会被压缩**，问题只在「拖动时列名/行标识滚出屏幕」。分组栏与表格的横向同步**已由 `table-nav.js: initGroupHeaders`（`transform: translateX(-scrollLeft)`）实现**，本次未改。**⚠️ 静态资源版本串**：`index.html` **`css/app.css?v=7` → `?v=8`**。**验证**：`dotnet build MES.Blazor` **3 项目 0 错 0 警**（纯 CSS/HTML）。**发布后核对**：手机真机任一宽表列表（仓库库存查询 / 现订单负荷总量）——左右拖动时**表头与首列（勾选框列时为第 2 列）始终可见**。
- 文档（⑩）：`frontend_pages.md` **V136**（追加 V136 段 + §6 索引表「全站手机壳」行补宽表细项）。

### ⑪ ⚠️ 手机端列表「每条记录竖向显示」真根因修正 —— 各列表页 `<MudTable>` 加 `Breakpoint="Breakpoint.None"`（2026-09-14 同日，用户反馈）

> **性质：纯 WASM（`.razor` 页面逐页加参数 + `app.css` 注释 + `index.html`）**，**未动后端 / API / 数据库 / 迁移、无单测影响**。

- **用户原话**：「举个例子，仓库的列表数据，在手机端，现在的每条记录都是纵向显示的。我的意思还是直接横向吧，就是向右移动的范围会比较大。」
- **⚠️⚠️ 真根因（推翻 ⑩ 的判断）**：`MudTable` 的 `Breakpoint` **默认 = `Breakpoint.Xs`**（MudBlazor 6.19 `MudTableBase.cs`，xml 文档亦写「the default behavior is breaking on Xs」）→ 手机竖屏（< 600px）外层 div 带 `mud-xs-table` 类，MudBlazor.min.css 的 `@media(max-width:600px)` 把**表头整条 `display:none`**、每格改 `display:flex` 并用 `:before{content:attr(data-label)}` 补列名 → **一条记录被拆成 N 行「列名 — 值」竖排**（仓库库存 20 列 = 每条记录 20 行）。**故手机端原本就没有横向滚动，是 MudBlazor 自带移动布局在接管**；⑩ 那批 `.mh-shell .mud-table-head …` 粘性规则在真机因「无 thead 可粘」**全部落空**（>600px 不堆叠，故只在真机竖屏暴露）。
- **修法**：各列表页 `<MudTable>` 显式加 **`Breakpoint="Breakpoint.None"`**（先例 `MiscWorkMonthly.razor`）。任何断点都不切移动布局；>600px 本就不堆叠 → **桌面 / 横屏零影响**；关闭后走 `table-layout:auto` + `.mud-table-cell{white-space:nowrap!important}` → 表宽 = Σ 列 min-content（1200~2400px）> 手机容器 → `.mud-table-container{overflow-x:auto!important}` 横向滚动自然生效（§6.29 原意）。`MudGlobal` 无表格配置项 → 只能逐页加参数。
- **范围（127 处 / 82 文件）**：**a.** 主列表页（含 `ServerData=` 或 `<MudTablePager>`，72 文件）整文件插入 **69 处**；**b.** 只读展示型附属表（详情页明细 / 报表 Tab 内数据表，按块级扫描跳过含输入控件的块）**58 处**。**有意保留竖排的两类**：`Class="edit-table"` 编辑录入表（手机端「列名—值」反而利于录入）、`WarehouseOutbound.razor` 出库操作表。⚠️ 其中 **3 处落在未跟踪（`??`）文件**（`InspectionPatrolViewDialog.razor` / `InspectionPatrols.razor` / `NonconformingFeedbacks.razor`）→ `git diff` 只显示 124 处，差额即此。
- **配套**：`app.css` 第 4 组「宽表」注释块补本次真根因（并修正原「逐页零改动」表述）；**⚠️ 静态资源版本串**：`index.html` **`css/app.css?v=8` → `?v=9`**（`MES.Blazor.styles.css` 无 `.razor.css` 改动，保持 `?v=2`）。
- **验证**：`dotnet build MES.Blazor` **3 项目 0 错 0 警**。（批改脚本曾使 `InspectionPatrolViewDialog.razor` 首行 `@*` 的 `@` 丢失 → 已修复；**批量改 `.razor` 后必须 build 复核首字符未丢**。）
- **发布后核对**：手机真机（非 F12 模拟、建议无痕或先清缓存）打开**仓库库存查询** / **现订单负荷总量** / **NCR 列表** → 应为**真正的横向宽表 + 表头**（不再是「列名 — 值」竖排）、可左右拖动且表头 + 首列（勾选框列时为第 2 列）始终可见；**编辑录入页**（如批次创建、报工录入）应仍为竖排「列名—值」，属预期。
- 文档（⑪）：`frontend_pages.md` **V137**（追加 V137 段）。

### ⑫ 手机端表格「不冻结任何列」——删除粘性首列 / 勾选框场景粘第 2 列（2026-09-14 同日，用户反馈）

> **性质：纯 WASM（`app.css` + `index.html`）**，**未动任何 `.razor` / `.razor.cs` / 后端 / API / 数据库 / 迁移**，无单测影响。

- **用户原话**：「移动时没必要冻住字段。例如：仓库的列表中，现在是仓库的批次号冻住的。其它的所有列表，在手机端也是如此，没必要冻住任何一列或几列。」
- **删除的规则**（`app.css`「全站手机壳」第 4 组，`@media screen` 内）：**a.** `.mh-shell .mud-table-head tr:last-child th:first-child, .mh-shell .mud-table-body td:first-child { position:sticky; left:0; … }`（V135 的**粘性首列**）；**b.** `.mh-shell .mud-table-head tr:last-child th:first-child:has(.mud-checkbox) + th, .mh-shell .mud-table-body td:first-child:has(.mud-checkbox) + td { position:sticky; left:40px; … }`（V136 的**勾选框场景粘第 2 列**）；**c.** 配套 z-index 三条（交叉格 6 / 勾选第 2 列 5 / 首列数据格 4）。原位留注释记明删除原因。
- **保留**：**粘性表头**（纵向滚动时知道看的是哪一列，与「左右移动不冻字段」不冲突）+ `border-collapse:separate` + `.mud-table-container` 的 `overflow-x/y:auto` / `max-height` / `overscroll-behavior` + `.list-toolbar` 换行。
- **未动的两处**：`edit-table` 编辑录入表、`WarehouseOutbound.razor` 出库操作表（保持手机端竖排）；**考勤表页面级 `.sticky-left` / `.sticky-right`**（岗位列 + 月份合计列冻结）**本次保留** —— 属录入网格非列表，如需一并去掉请另行提出。
- **⚠️ 静态资源版本串**：`index.html` **`css/app.css?v=9` → `?v=10`**（`MES.Blazor.styles.css` 保持 `?v=2`）。不抬串则真机拿不到本次删除（旧 `?v=9` 仍带列冻结）。
- **验证**：`dotnet build MES.Blazor --no-incremental` **3 项目 0 错 0 警**（纯 CSS / HTML）。
- **发布后核对**：手机真机（非 F12 模拟、建议无痕或先清缓存）打开 仓库库存查询 / 现订单负荷总量 / NCR 列表 → 左右拖动时**列随内容一起移动、无任何列被钉住**；纵向滚动时**表头仍固定**。
- 文档（⑫）：`frontend_pages.md` **V138**（追加 V138 段）。

### ⑬ 工资结算 4 张密集网格页手机端一并「不冻结任何列」（2026-09-14 同日，用户答复「一并去掉」）

> **性质：纯 WASM（`app.css` + `index.html`）**，**未动任何 `.razor` / `.razor.cs` / 后端 / API / 数据库 / 迁移**，无单测影响。

- **来由**：⑫ 汇报时说明「考勤表页面级 `.sticky-left`/`.sticky-right`（岗位列 + 月份合计列冻结）本次未动，如需一并去掉请说」→ 用户答复**「一并去掉」**。
- **涉及 4 页**（同一套类）：`Payroll/Attendance.razor`（考勤表）、`Payroll/AllowanceMonthly.razor`（津贴与处罚月表）、`Payroll/MonthlyWages.razor`（月工资表）、`Payroll/MonthlySummary.razor`（月工资津贴汇总）。冻结列 = 左「工号/姓名/岗位类别/岗位」+ 右「出勤天数/总小时（本月合计）」。
- **做法（⚠️ 只在小屏取消、桌面保留）**：`app.css`「全站手机壳」段新增**第 6 组**：`.mh-shell .attendance-grid .sticky-left, .mh-shell .attendance-grid .sticky-right { position: static; left: auto; right: auto; z-index: auto }`。特异性 (0,3,0) 压过原 (0,2,0)；`thead th.sticky-left/right`(0,3,1) 只声明 `z-index`，`static` 后不适用；**表头纵向 sticky 不受影响、仍保留**。桌面 `MainLayout` 不带 `.mh-shell` → 桌面行为不变（桌面 1920px 下约 1900px 宽仍会横向滚动，冻结仍有价值）。
- **⚠️ 追加（同日）：`MonthlySummary`（月工资津贴汇总）手机端放开横向滚动**——核查时发现该页与其它 3 页表现不一致：它额外带 `.monthly-summary-grid`，把基类 `.attendance-grid { min-width:max-content }` 覆盖成 `width:100%; min-width:0; table-layout:fixed`（桌面设计目标＝免横滚），**手机 360px 下会把 18 列等比压到屏宽（每列约 20px），既看不清也不会横滚**。用户拍板**「手机端放开横滚」**→ 手机壳内补 `.mh-shell .attendance-grid.monthly-summary-grid { width:auto; min-width:max-content; table-layout:auto }`（(0,3,0) 压过 (0,2,0)）；桌面无 `.mh-shell` → 维持原状。
- **⚠️ 静态资源版本串**：`index.html` **`css/app.css?v=10` → `?v=11`**（首版）→ **`?v=12`**（本追加；两项同属 V139、同提交）。
- **验证**：`dotnet build MES.Blazor --no-incremental` **3 项目 0 错 0 警**（纯 CSS / HTML）。
- **发布后核对**：手机真机（建议无痕或先清缓存）打开 考勤表 / 月工资表 / 津贴与处罚月表 / 月工资津贴汇总 → 左右拖动时**无任何列被钉住**；纵向滚动时**表头仍固定**；**月工资津贴汇总页可左右拖动看到全部 14 个金额列（不再被压成 20px/列）**。桌面端这 4 页维持原样（列仍冻结，且汇总页桌面仍不横滚）。
- 文档（⑬）：`frontend_pages.md` **V139**（追加 V139 段；V138 段 ④ 补注「已由 V139 一并去掉」）。

### ⑭ 删除「手机横屏切电脑版」例外 + 删除「请横屏」提示条机制 —— 手机/触屏设备一律走手机壳（2026-09-14 同日，用户拍板）

> **性质：WASM（`ResponsiveLayout.razor` / `MobileLayout.razor` / `app.css` / `index.html`）+ 删除 2 个组件文件与 1 个测试类**；**未动后端 / API / 数据库 / 迁移**。

- **来由**：用户问「我们原来设置的，手机横向查看时，是完全按『电脑』样式的；现在是否已经没必要了，可以全按手机版了」。分析确认：该例外的原始理由（宽表竖屏不可读）已被 V135–V139 四批改动推翻（`.mh-shell` 手机壳 + 各列表页 `Breakpoint="Breakpoint.None"` 恢复真横滚 + 粘性表头 + 不冻结任何列），且例外本身有 3 项代价 → 用户拍板**「干脆全删例外」**，并同意横屏筛选网格放宽为每行 2 列、提示条机制一并删除。
- **改动一：删例外**（`MES.Blazor/Shared/ResponsiveLayout.razor`）—— 原判定表达式尾部 `&&!(matchMedia('(orientation: landscape)').matches && window.innerWidth>=700)`（2026-09-05 拍板：手机横屏且 ≥700px 改走桌面 `MainLayout`）**整条删除**。现在手机/触屏设备**不再按横竖屏区分**，一律 `MobileLayout`。
  - **收益**：手机横屏不再丢 `.mh-shell` 收敛规则（页头换行 / MudGrid 堆叠 / 宽表粘性表头与限高滚动区）；扫码流窄页（`/mobile-report`、`/equipment-scan`、`/mobile-quality/*`）不再被误切桌面版；**旋转屏幕不再换壳 → 不再重建页面子树，已查询列表 / 未提交表单状态得以保留**。
  - **⚠️ 已知代价（用户知情接受）**：判据含 UA / `pointer:coarse` / `maxTouchPoints` → **触屏笔记本与大平板横屏也走移动布局**（大屏被「手机化」）。**纯鼠标桌面机不受影响**（宽≥960 且无触屏特征 → 仍 `MainLayout`）。若日后要救触屏笔记本，加一条「`innerWidth>=1200` 且非物理窄屏 → 桌面」的兜底即可（需注意排在 `max-device-width<=767` 之后，防夸克「电脑模式」钻空子）。
- **改动二：横屏筛选网格每行 2 列**（`app.css` 手机壳段第 2 组之后）—— `@media (orientation: landscape) { .mh-shell .mud-grid > .mud-grid-item:has(.mud-input-control) { flex-basis:50% !important; max-width:50% !important } }`。⚠️ **只匹配含输入控件的项**——全仓 454 个 `<MudItem>` 中 426 个是 `xs="12"` 的内容块（表格/卡片/进度树），一律 50% 会把内容压成半宽。不支持 `:has()` 的旧 WebView 整条丢弃、静默回落逐行全宽。
- **改动三：删提示条机制**（死代码清理）—— 删 `MES.Blazor/Shared/LandscapeHintBanner.razor`、`MES.Blazor/Shared/LandscapeHintRule.cs`、`MES.Tests/Components/LandscapeHintRuleTests.cs`（8 例，含 2 条「菜单新增窄叶漏接」防漂移断言）；`MobileLayout.razor` 原「保留不删、恢复只需加回一行」注释改为「已连同单测一并删除」。⚠️ 该机制自 V135 起已不渲染，规则只被自己的单测引用。
- **⚠️ 静态资源版本串**：`index.html` **`css/app.css?v=12` → `?v=13`**（`MES.Blazor.styles.css` 保持 `?v=2`）。
- **验证**：`dotnet build MES.Tests --no-incremental` **8 项目 0 错 0 警** + 定向单测 **14 过**（`AppMenuTests` / `HomeQueryCardsTests`）。
- **发布后核对**：手机真机竖屏⇄横屏来回切换 → 始终是手机壳观感（页头按钮换行、卡片满宽），**不再切出桌面侧栏**；横屏下筛选区为**每行 2 个输入框**、表格/卡片仍整宽；**旋转屏幕后已查询的列表与填写的表单不丢**；扫码页（报工/巡检/反馈/设备）横屏**仍是大按钮点选流**、不再变成桌面版。
- 文档（⑭）：`frontend_pages.md` **V140**（新增 V140 段，V139 降为历史变更）。

## ⏳ 批二十八 订单模块（进度树「投料 + 产出」两维度重构 + 进度树就地打印 + 订单列表页「投料产出总况」卡片）（2026-09-14 登记，**已于 2026-09-14 随包 `mes-deploy-20260914_0615.zip` 部署云库**）

> **性质：后端 dll（`MES.Core` / `MES.Services` / `MES.Api`）+ WASM（`MES.Blazor`）+ 1 个新增 API 端点**（`GET api/order/throughput-summary`）。
> **⚠️ 无 EF 迁移、无数据变更**（纯读聚合改造与新增只读查询）→ **不需要** 迁移 SQL，`attachments\` 目录不涉及。
> **⚠️ 与批二十七 / 批二十九同属一个工作树、必须一次性打包**：本批不含质量模块改动，但**前后端同批才有意义**（本批 ①②③ 的 WASM 与 dll 必须同步；质量页 WASM 依赖批二十七端点与迁移）→ 单独发布本批会出现「前端版本与后端不匹配」，**不可单独发布**。
> **⚠️ 覆盖关系**：本文档「批二十七 ⑤ 首页看板 + 订单进度树」中「已完结主号分支扩充为 **6 支**（生产投料/过程检/成品检/余料入库/备料入库/成品入库）」的描述**已被本批 ① 覆盖**（现为 **4 专属支 + 订单成品入库**，两个检验分支已删除），发布核对时以本批为准。

### ① 订单进度树「已完结主号」分支改「投料 + 产出」两维度（前端 V116 登记）

> 用户 2026-09-14 拍板：完结主号由原「6 支」改为**两维度**——序1 投料按工艺卡实开、序 2~5 产出全部按仓库实收。

- **序1 生产投料[生产类型]** 重量改取**生产批次工艺卡** Σ `ProductionBatch.InputWeight`（原取 `WorkOrderExecutionSummary.InputWeight` 快照口径**废弃**）：批次**仅排除**「返整(`Rework`) / 委外生产(`Subcontract`) / 对外加工(`ExternalProcessing`)」三类，**不再限定制造物品**（原 `ManufacturingItem ∈ {OrderFinished, SpecialDeliveryStatus}` 限制取消）；批次自带订单号 + 主号，**零 join**。标题后缀 = 该主号投料批次的 `ProductionType` 去重串（如「生产投料[荒管生产+在制生产+外购]」）。
- **序3 新增「次品入库」** = 次品库 `DEFECT` 6 类物料类型（次品圆棒 / 次品荒管 / 次品半成品 / 次品成品 / 次品在制 / 报废品）经 `InventoryBatch.ProductionBatchNo` 反查主号聚合；**每叶可附同叶退货出库量**（同仓库 `OutboundRecord.OutboundType == ReturnOut`），DTO 新增 `MainProgressLeafDto.ReturnWeightKg`，前端红色「退货 X kg」并列在入库量右侧。
- **序2 / 序4 更名**：「余料入库 / 备料入库」→「**在制品入库 / 备料成品**」（口径不变，序2 = WIP `Surplus`、序4 = FG `Finished`）。
- **序5 更名**：「成品入库」→「**订单成品入库**」。
- **删除**原「过程检 / 成品检」两支（`ProcessInspectionDefect` / `FinalInspectionDefect` DTO 字段 + `BuildProcessInspectionBranch` / `BuildFinalInspectionDefectBranch` / `BuildDefectBranch` 服务方法一并删除）——两维口径下检验不合格产出已由次品库实收体现，快照口径重复。
- **「临界成品(`CriticalFinished`)」不纳入产出**（来源为外购成品，本质属投料）。
- **非完结主号 4 支保持原样**（原料锁定 / 生产执行[待产] / 成品检验 / 订单成品入库），仅末支标题同步更名。
- **文件**：`MES.Services/Order/OrderProgressQueryService.cs`（±240 行）、`MES.Core/DTOs/Order/OrderProgressTreeDto.cs`、`MES.Blazor/Pages/Orders/OrderProgress.razor(.cs/.css)`。
- 文档：`订单模块详细设计.md` **V1.26** §5.17、`frontend_pages.md` **V116**。

### ② 进度树两项比率高亮 + 订单级「总投料产出」聚合行（**纯前端派生**，V117 登记）

- 主号灰条汇总行与订单级汇总行的「投料产出率」「产出成品比」改 `.op-sum-hl`（红 `#c62828` + 粗体）；`CompletionSummaryText`（原返回单串）重构为 **`BuildCompletionSummary` 返回分段列表** `List<OpSummarySegment>`（`record OpSummarySegment(string Text, bool Highlight = false)`），razor 逐段 `<span>` 渲染（段间 `；`、末尾 `。`），汇总行容器 `display:flex; flex-wrap:wrap`（避免 Razor 换行空白落进文本）。
- **订单级汇总行**：`MainNos` 非空且**全部 `IsCompleted`** 时，在订单号块渲染 `总投料产出：生产投料500kg；订单成品入库400kg；余次备产出入库50kg；投料产出率90.0%；产出成品比88.9%。` = **各主号三项净量直接求和**（复用 `Nets(main)`；跨主号聚合故投料项恒「生产投料」不带生产类型后缀）；零值整行省略、分母 ≤0 的比率项不渲染。
- 文件：`MES.Blazor/Pages/Orders/OrderProgress.razor(.cs/.css)`；文档 `订单模块详细设计.md` **V1.27** §5.17.5、`frontend_pages.md` **V117**。

### ③ 进度树「就地打印」（所见即所得，保留折叠状态 + 打印日期页脚）（**纯前端**，V118 登记）

- 卡头新增「打印」按钮（`OnPrintAsync` → `Task.Yield()` 后 `JS.InvokeVoidAsync("window.print")`），「返回订单列表」按钮同加 `op-print-hide` 使二者打印时隐藏；**按页面当前折叠/展开状态原样输出**（折叠区由 `@if (!IsCollapsed(...))` 条件渲染、本就不在 DOM，无需额外逻辑）。
- 页面末尾新增**非 scoped** `<style>@@media print`：`@@page { size: landscape; margin: 8mm }`、`body { -webkit-print-color-adjust: exact }`、隐藏 `.mud-appbar` / `.mud-drawer` / `.op-print-hide`、`.mud-layout` 与 `.mud-main-content` 解除 flex + 滚动并置 `margin-left / padding-top: 0`（否则多页只印首屏、左侧留抽屉宽度）、`.op-main-node { break-inside: avoid }`。
- **打印页脚** `.op-print-footer`（屏幕隐藏、仅 `@@media print` 显示）输出 `打印日期：yyyy-MM-dd`；`_printDate` 在 `OnPrintAsync` 中刷新为 `DateTime.Today`，`StateHasChanged()` + `await Task.Yield()` 后再调 `window.print`（保证日期先落到 DOM）。
- ⚠️ **样式必须写在页面内 `<style>` 而非 `.razor.css`**：`.mud-appbar` / `.mud-drawer` / `.mud-main-content` / `.mud-layout` 属**其它组件**的元素，Blazor scoped CSS 追加 `[b-xxx]` 属性后匹配不到。**不新增 JS 桥接函数**（`print.js` / `index.html` 版本号不动）。
- 文件：`MES.Blazor/Pages/Orders/OrderProgress.razor(.cs/.css)`；文档 `订单模块详细设计.md` **V1.28** §5.17.4、`11_打印设计.md` **V2.19** §2.2（就地打印约定）、`frontend_pages.md` **V118**。

### ④ 订单列表页「投料产出总况」折叠卡片（**新增后端查询服务 + 端点 + WASM**，V119 登记）

- **入口**：`/orders` 卡头新增「投料产出总况」按钮（置于「完成预估及延期风险」之后、「新建订单」之前），**默认折叠、首次展开懒加载**，卡片渲染于交期预估卡片之后。
- **行 = 订单完成月**（订单级档位归并序 == 1「主号完成」，完成日取该订单各主号 `WarehousingEndDate` 最大值 → 与订单列表「执行关注=主号完成」绿 Chip 同源）；**固定近 12 个月（含当月）**，无完成订单的月不产生行。
- **10 列**：`完成月 / 订单数 / 生产投料 / 订单成品入库 / 余库料入库 / 次品入库 / 备料成品 / 投料产出率 / 产出成品比 / 退货`；重量 kg **四舍五入取整**（**不用**本页既有 `DisplayHelper.FormatDecimalAsInt` 的截断口径）、比率 0~1 一位小数（分母 ≤0 → `null` → 前端显示 `—`）。
- **口径下拉 6 档**（`ProductionScopeKeys`：全部 / 纯生产 / 单一生产类型 4 档；空值 / 未知值归一 `All`；**禁用可清除叉**），返整 / 委外生产 / 对外加工三类一律排除。
- **订单成品双路径**：「全部」口径走 `InventoryBatch.SalesOrderNo` 直取 + `OrderFinished`（**只交付态**，与进度树同源）；「纯生产 / 单一类型」口径走 `ProductionBatchNo` 反查 + `MaterialType ∈ (OrderFinished, SpecialDeliveryStatus)`（**含非交付态**）——U 型管常规生产只到非交付态、交付态由外购委外批次产出，否则该口径整列为空；交付态部分被生产类型过滤自然排除，故不重复计入。
- **订单数口径**：非「该月完成订单总数」，而是**该口径下真实相关订单数** —— 该完成月中**在本生产类型范围内有生产批次**的订单数（判定 = 订单命中投料查询结果集键集；产出/退货经同一批次反查必为其子集）；某月在该口径下无相关订单（如该月完成订单全部只有外购批次）则**整行隐藏**。真库核验：全部口径下三档完成月均为「完成订单数 == 有本口径投料批次的订单数」（6/5/6），故现有「全部」档显示值不变。
- **退货**（次品库 `ReturnOut`，经生产批号反查归属）**已分别从「生产投料」与「次品入库」扣减，同时单列供核对**。
- **模糊搜索**：按完成月（`yyyy-MM`）前端本地过滤。
- **打印**：标题行右侧「打印」按钮（数据未加载时不渲染）走 `getTableHtml` + `printRawHtml`（Mode A 前端渲染，与同页交期预估小表同模式）；**所见即所得** = 当前口径 + 当前完成月搜索结果，打印标题 `投料产出总况（{口径中文}）`、页脚日期由 `print.js` `openPrintWindow` 统一输出（默认 landscape）；**不新增 JS 桥接函数**（`print.js` / `index.html` 版本号不动）。
- **文件**：**新增** `MES.Core/Constants/ProductionScopeKeys.cs`、`MES.Core/DTOs/Order/OrderThroughputSummaryDto.cs`、`MES.Core/Interfaces/Order/IOrderThroughputQueryService.cs`、`MES.Services/Order/OrderThroughputQueryService.cs`、`MES.Api/Controllers/Order/OrderThroughputController.cs`、`MES.Tests/Services/Order/OrderThroughputQueryServiceTests.cs`（13 例）；**修改** `MES.Api/Program.cs`（DI）、`MES.Shared/Constants/ApiEndpoints.cs`（`OrderThroughputSummary = "api/order/throughput-summary"`）、`MES.Blazor/Services/Order/OrderService.cs`、`MES.Blazor/Pages/Orders/Orders.razor(.cs)`。
- 权限：**OrderViewer / OrderEditor / OrderFull / Admin**（`Roles.Policies.OrderView`）。
- 文档：`订单模块详细设计.md` **V1.29** §5.14.3 + §6.1、`frontend_pages.md` **V119**。

### ⑤ 发布注意事项与验证

- ⚠️ **本批无 EF 迁移**，但**必须与批二十七（17 个迁移）同批发布**（见批头依赖说明）。
- ⚠️ **列偏好无版本 bump**（`/orders` 主表列定义未动，新卡片不参与列偏好）→ 老用户列表列显隐不受影响。
- ⚠️ **新卡片首屏为「近 12 个月」窗口**：真库当前可出表的完成月集中在 2026-01 ~ 03 三个月（其余月无完成订单 → 不显示行）属**预期**；另「余库料入库 / 次品入库 / 备料成品 / 退货」四列在真库**近空**（库存批次 `ProductionBatchNo` 与生产批次表对不上：WIP 5 行引用 2025-12 老批号、DEFECT 3 行订单号/批号全空、FG `Finished` 706 行仅 6 行有可用批号）→ 显示 `0` 属**数据链路未补全**，非本批 bug。
- **验证（本机）**：`dotnet build MES.Api` / `dotnet build MES.Blazor` **0 错 0 警**；订单模块定向单测 **615 通过 / 0 失败**（含本批新增 **13** 例：完成月归并、档位归并序排除、12 个月窗口、退货双向扣减、双路径交付态/非交付态、余库料与备料反查、口径切换、口径归一、比率 `null`、无完成订单早返回、**订单数=本口径相关订单数、该口径无相关订单月整行隐藏**）。**④ 打印为纯前端复用既有 JS 桥接（`getTableHtml` / `printRawHtml`），不新增单测**。
- **发布后核对**：`/orders/progress` 完结主号应显示「生产投料[类型] / 在制品入库 / 次品入库 / 备料成品 / 订单成品入库」5 支 + 汇总行灰条 + 打印按钮（含页脚日期）；`/orders` 卡头应出现「投料产出总况」按钮，展开后为 10 列表格、口径下拉可切 6 档、完成月搜索可过滤、标题行「打印」按钮可出带口径标题与打印日期页脚的横版表格。

## ⏳ 批二十九 看板（首页「常用入口」补齐扫码组两项 9 → 11 + 桌面两卡改上下堆叠各约半屏宽居中）（2026-09-14 登记，**已于 2026-09-14 随包 `mes-deploy-20260914_0615.zip` 部署云库**）

> **性质：纯 WASM（`MES.Blazor`）** —— **无接口、无 DTO、无 EF 迁移、无数据变更**；但**必须与批二十七 / 批二十八 同批发布**（同一工作树，WASM 整体替换）。

- **① 入口清单**：`MES.Blazor/Shared/AppShortcuts.cs` 在「报工扫码」之后新增 **「巡检扫码」**（`/mobile-quality/patrol`，`Icons.Material.Filled.Checklist` + `#00695C`）与 **「不合格反馈扫码」**（`/mobile-quality/feedback`，`Icons.Material.Filled.ReportProblem` + `#AD1457`），扫码组三项相邻排列（清单本身由桌面与手机两分支共用）。
- **⚠️ 两项 `Policy` 均为 `null`（仅需登录）**：与侧栏「扫码管理」组整组不挂档一致（该组 6 项中仅「工位管理 / 员工管理」挂 `ScanView`）。**不可**图省事挂 `ScanView`——否则侧栏可见而首页不可见，且 `AppShortcutsTests` 直接失败。
- **② 桌面端两卡改上下堆叠 + 各约半屏宽居中**：`Index.razor` 桌面分支去掉 `MudGrid`/`MudItem`（原 `xs=12 md=6` / `md=6` 左右等宽两栏），改为 `.home-stack`（`display:flex; flex-direction:column; gap:16px`）纵向堆叠、两卡 `.home-stack-card`（**`width:50%; margin:0 auto`**）；卡片 `Style="height:100%"` 移除。**手机端未受影响**（本就是全宽纵向堆叠，`mh-*` 体系）。
- **文件**：**修改** `MES.Blazor/Shared/AppShortcuts.cs`、`MES.Tests/Components/AppShortcutsTests.cs`（原断言「除报工扫码外均带策略」→ `常用入口_仅扫码组三项无角色策略`，三项 `BeEquivalentTo`）、`MES.Blazor/Pages/Index.razor`（桌面分支布局）、`MES.Blazor/wwwroot/css/app.css`（`.home-stack` / `.home-stack-card`）。
- **文档**：`看板上下文详细设计.md` **V3.15**（§2 布局框图 9 → 11 磁贴 + 上下堆叠、§常用入口表条目数/组成/策略/图标色）、`frontend_pages.md` **V121**。
- **发布后核对**：以任一账号登录首页，「常用入口」区应出现 11 个磁贴（非扫码档账号也能看到三项扫码入口，与侧栏「扫码管理」一致）、点「巡检扫码」「不合格反馈扫码」应分别进入 `/mobile-quality/patrol` / `/mobile-quality/feedback`；**桌面端**两卡应为**上下排列**且各占约半屏宽、水平居中（不再左右并排），手机端仍为全宽上下两卡。

## ⏳ 批二十七 质量模块大改（巡检 / 不合格反馈 / 照片附件 / 单据式打印 / NCR 待处理两组重构 + 忽略改状态档）+ 首页看板与订单进度树（2026-09-11~13 登记，**已于 2026-09-14 随包 `mes-deploy-20260914_0615.zip` 部署云库**）

> **性质：后端 dll + WASM + 静态 js/css + 17 个 EF 迁移**（云库随新 API 启动自动 Migrate；⚠️ 迁移数量多，**dll / WASM / 迁移必须同批发布**）。

> **打包登记（2026-09-14 06:15，开发机 `03-publish-local.ps1`）**：包 `publish/mes-deploy-20260914_0615.zip`（**66.2 MB / 350 条目**）。已核实包内 `api\MES.Api.dll`、`api\MES.Data.dll`、`web\index.html`、`web\css\app.css`、`web\_framework\MES.Blazor.wasm` 齐备；`web\appsettings.json` = `{"ApiSettings":{"BaseUrl":""}}`（同源，**无 UTF-8 BOM**，由脚本自检断言 `head=123,13,10` + `BaseUrl is empty`）。**本包为批二十五 ~ 批二十九全量超集**，旧包 `mes-deploy-20260910_1839.zip`（批二十六）作废。
> **服务器侧执行记录（2026-09-14，用户 RDP 执行，AI 无通道）**：① 包已拷至服务器 `C:\mes\`；② **云库已手动备份** `C:\MSSQL\Backup\manual_before_deploy_20260914.bak`（16825 页，成功）；③ `09-apply-deploy.ps1`（`$Zip = C:\mes\mes-deploy-20260914_0615.zip`）已运行 → `MES.API` STOP/START 成功、`Get-Service` = **Running**；④ 启动日志 `Application started` / `Hosting environment: Production` / `Content root path: C:\mes\api` → **迁移已随启动应用**（`__EFMigrationsHistory` 新增 **17** 条，**已 sqlcmd 复核通过 `1 1 1 1 17`** = 表已删/表已在/列已在/列已在/迁移数，首页 `home=200`，`C:\mes\api\MES.Api.dll` 时间戳与包内一致 → **部署完成**）；⑤ `06-verify.ps1` 首页项因脚本自身 bug 失败（见下条），其余三项 **`manifest 200` / `login 200` / `hangfire 401` 全 OK**。⚠️ 启动日志另有一次 `Failed to bind to address http://127.0.0.1:7000: address already in use` 崩溃（见下条 09 修复），nssm 自动重启后正常。
> ⚠️ **另修复两个部署脚本（2026-09-14 服务器实测暴露）**：① `06-verify.ps1` —— 变量名 `$home` 与 PowerShell **只读自动变量 `$HOME`** 大小写不敏感同名 → 赋值抛 `VariableNotWritable`，首页检查恒拿到 `C:\Users\Administrator` 而判 FAIL；改 `$homeCode`，顺带 `$args`（同为自动变量）改 `$curlArgs`。② `09-apply-deploy.ps1` —— `nssm stop` **立即返回** + 仅 `Start-Sleep 2` → 旧进程未释放 7000 就启新进程，Kestrel 绑定失败后整进程崩溃（本次靠 nssm 自动重启救回，属**静默风险**）；改为循环等待「服务 Stopped **且** 7000 不再 Listen」（上限 30 s）否则 `throw` 中止部署；两脚本注释同步改英文（此前 09 含中文，同属编码坑）。**两文件须下次部署前同步到 `C:\mes\deploy\`，本次部署无需重跑。**
> ⚠️ **本次连带修复 `03-publish-local.ps1`（发布脚本）**：① 改写 `appsettings.json` 由 `Set-Content -Encoding UTF8`（PS 5.1 会写入 UTF-8 BOM，Blazor 侧读配置会失败）改为 `[System.IO.File]::WriteAllText(..., UTF8Encoding($false))` 无 BOM 写入；② 新增两条自检 —— 无 BOM 断言 + `BaseUrl` 确已改写为空断言（后者能抓到「改写被静默跳过」）；③ **脚本恢复纯 ASCII**（PS 5.1 以 ANSI 读取无 BOM 的 `.ps1`，中文注释会错位解码并**吞掉行尾换行**，导致紧随其后的 `WriteAllText` 行被并进注释而**静默不执行** —— 本次即因此产生过一个 `BaseUrl=http://localhost:7000` 的错误包，已重打包修正）。
> **打包状态**：**已打包并于 2026-09-14 在云库服务器部署完成**（`publish/mes-deploy-20260914_0615.zip`，含批二十五 ~ 批二十九全量）——本节各批状态由「未打包未上线」转为**已上线**。
> **⚠️ 落盘目录**：`attachments/`（服务器 `api\attachments\`）——巡检 / 不合格反馈 / 过程检验 / 成品检验 四类照片均落此，覆盖 `api\` 时**必须保留该目录**。
> **⚠️ 打包须连带「批二十八」**（2026-09-14 订单模块改动，无迁移）——**两批必须一次性打包**；另见批二十八 ① 对本文档 ⑤「订单进度树 6 支」描述的覆盖。

### ① 新增两个质量子模块（巡检 / 不合格反馈）

| 文件 | 改动 |
|---|---|
| `MES.Data/Entities/Quality/` | 新增 `InspectionPatrol` / `InspectionPatrolItem` / `InspectionPatrolAttachment` / `NonconformingFeedback` / `NonconformingFeedbackAttachment` 五实体；`AppDbContext.Quality.cs` 配置与索引 |
| `MES.Core/DTOs/Quality/` | 新增 `InspectionPatrolDto.cs` / `NonconformingFeedbackDto.cs`；`MES.Core/Constants/` 新增 `InspectionPatrolPhotoTypes.cs`（每类 ≤4 张，V8.27 由 3 上调）/ `QualityPhotoLimits.cs`（单条 ≤4 张 = `PerRecord`，V8.27 由 3 上调）；`MES.Core/Enums/NcrPendingSourceType.cs`（新枚举，**刻意不复用会污染工位报工的 `ReportTemplateType`**） |
| `MES.Core/Interfaces/Quality/` + `MES.Services/Quality/` | 新增 `IInspectionPatrolService` / `INonconformingFeedbackService` / `IScanQualityService` 及三实现 |
| `MES.Api/Controllers/Quality/` | 新增 `InspectionPatrolController.cs` / `NonconformingFeedbackController.cs` / `ScanQualityController.cs`（`api/scan-quality/*` **仅登录、不挂质量档**） |
| 附件基础设施 | 新增 `MES.Core/Interfaces/Infrastructure/IAttachmentStorage.cs` + `MES.Services/Infrastructure/AttachmentStorage.cs`（落盘 `Attachment:RootPath`，存储名 `GUID+扩展名`，`ResolveSafePath` 防目录遍历）+ `MES.Blazor/wwwroot/js/attachment.js`（客户端 canvas 压缩，最长边 1600px / 质量 0.8） |
| `MES.Blazor/Pages/Quality/` | 新增 `InspectionPatrols.razor(.cs)` / `InspectionPatrolForm.razor(.cs)` / `InspectionPatrolViewDialog.razor` / `NonconformingFeedbacks.razor(.cs)` / `NonconformingFeedbackForm.razor(.cs)` / `NonconformingFeedbackViewDialog.razor` |
| `MES.Blazor/Pages/Tools/ScanQuality.razor(.cs)` | 新增 `/mobile-quality/{Kind}`（`patrol` / `feedback`）：只扫批次码、操作人 = 登录账号服务端覆写；巡检闭环定位（待整改 = `NeedRectification && !IsClosed`，`RectifyAsync` 只写验证结果/整改人/是否闭环） |
| 共用组件（新） | `MES.Blazor/Shared/ScanPhotoPicker.razor(.cs)` + `ScanPhotoItem.cs`、`InspectionPhotoDialog.razor(.cs)` |
| `MES.Blazor/Shared/AppMenu.cs` | 质量管理组新增「**巡检**」（首项，在过程检验之前）与「**不合格反馈**」（在不合格报告之前）；扫码管理组新增「巡检扫码」「不合格反馈扫码」 |
| `MES.Blazor/Shared/LandscapeHintRule.cs` | 两条扫码窄叶加入 `NarrowMenuLeafHrefs`（否则竖屏误弹「请横屏」）+ 防漂移测试 |
| 打印 | 新增 `MES.Services/Printing/InspectionPatrolPrintHelper.cs` / `NonconformingFeedbackPrintHelper.cs` |
| 常量 / DI | `MES.Shared/Constants/ApiEndpoints.cs`、`MES.Core/Constants/CacheKeys.cs`、`MES.Api/Program.cs`、`MES.Blazor/Program.cs` |

> **补记（2026-09-12）不合格反馈单「来源类型」三分支**：`NonconformingFeedback` 新增 `SourceType`（枚举 `NonconformingFeedbackSourceType`：**生产工段 / 过程检验 / 成品检验**，存英文 Key）+ `InspectionItem`（**仅成品检验档用**）；成品检验档**不填工段**（`SectionName`/`SequenceNumber` 已放开可空），「成检类型（预检/终检）」由工序 + 批次是否含「附加成检」**推导**（非用户选项，只读展示）。服务层出口统一 `NormalizeLocation` 归一化（成品检验→清工段、置检验项目；其余→清检验项目、保留工段），跨分支残留字段必被清空。前端：`NonconformingFeedbackForm` / `ScanQuality`（**巡检档无此项**）/ `NonconformingFeedbacks` 列表（+「来源类型」「检验项目」两列，列偏好 **v2→v3**）/ `NonconformingFeedbackViewDialog`。见迁移 `20260912052001` 与「数据变更注记」。

### ② 不合格品去向 / NCR 处置方式 4 档 → 5 档（全链路）

- **过程检验**不合格处理 **3 档 → 4 档**（返整 / 入在制库 / 入次品库 / 退货）；**成品检验** **4 档 → 5 档**（返整 / 入在制库 / 入次品库 / 退货 / 可入备库）；组名「不合格处理」→「**不合格品去向**」。
- **NCR `DisposalMethod`** 删 `WarehouseEntry`，新增 `InProcessWarehouse` / `FinishedWarehouse`；`Scrap` 中文「报废」→「**入次品库**」（`MES.Core/Enums/DisposalMethod.cs` + `EnumHelper.cs`）。
- **列偏好版本 bump**：过程检 v2→v3、成品检 v2→**v4**；扫码报工成品检不合格输入 4→5 格。
- **读模型 / 汇总联动**：`QualityProcessTracking`、`WorkOrderExecutionSummary` 快照、订单进度树、成检追踪、成检计划、`DataExchangeRegistry`（导入导出表头）、报表总览 NCR 卡片与月度汇总。
- 涉及 `MES.Services/Quality/*`、`WorkOrder/WorkOrderExecutionService.cs`、`Order/OrderProgressQueryService.cs`、`Scheduling/*`、`DataExchange/DataExchangeRegistry.cs`、`MES.Blazor/Pages/Quality/*`、`Pages/Tools/ScanExecute.razor`、`Pages/Reports/ReportOverview.razor(.cs)`；常量 `MES.Core/Constants/WarehouseCodes.cs`（新增，RAW/FG/DEFECT/WIP 仓库码唯一出口）。

### ③ 检验照片 + 单据式打印（照片页版式 V2.14；照片上限 V8.27 上调）

- 过程检验 / 成品检验各新增附件子表 + 「照片(N)」列（点击开共用 `InspectionPhotoDialog`，补拍 / 查看 / 删除，**每条 ≤4 张** `QualityPhotoLimits.PerRecord`，V8.27 由 3 上调）；扫码报工页两类检验表单**同页直拍**（提交后按记录 Id 逐张补写，单张失败隔离不阻断）。
- 新增「打印选中单据」：`POST api/{process-inspection|final-inspection}/print-selected-doc-file`（A4 竖版**每条一页** + 照片，**单次 ≤20 条**）。
- NCR「打印选中报告」**自动带出关联不合格反馈单**的问题照片（`Ncr.NonconformingFeedbackId` → `IAttachmentStorage.ReadAsync`）。
- **照片页版式 V2.14（2026-09-12 二次拍板）**：字段页恒 1 页 + **照片统一另起第 2 页**（页首归属行「编号: 前缀-{Id:D4}」）；**V2.14 起照片单列铺排、每页 2 张**，单张定高 **290pt**（原「每行 2 张 / 200pt」；巡检两区合并为连续表格，由 120pt 一并提到 290pt）；过程检验 / 成品检验字段区启用**紧凑排版档**；两组照片皆空则**整页省略** → 页数 = **无照片 1 页 / 有照片 = 1 页字段页 + ⌈N/2⌉ 页照片**。
- 文件：`MES.Services/Printing/{ProcessInspection,FinalInspection,InspectionPatrol,NonconformingFeedback,Ncr}PrintHelper.cs`；`MES.Blazor/Pages/Quality/{ProcessInspections,FinalInspections}.razor(.cs)`；回归保护 `MES.Tests/Services/Printing/*`（手工 PNG + PDF 字节扫描断言页数，18 例）。
- **照片张数上限 V8.27 上调**：`QualityPhotoLimits.PerRecord` 3 → **4**（过程检验 / 成品检验 / 不合格反馈每条记录）、`QualityPhotoLimits.PerType` 3 → **4**（巡检单每类，两类合计单个巡检单最多 8 张）；前端 `ScanPhotoPicker.MaxCount` 默认值改 `QualityPhotoLimits.PerRecord`、`ScanQuality` 问题照片改由 `PerRecord` 供给（原误用巡检常量）；**纯约束调整、无迁移**。
- 文档：`docs/11_打印设计.md` **V2.15**（§3.4）、`质量管理模块详细设计.md` **V8.27**、`质量管理上下文数据库设计.md` **V8.14**、`质量管理上下文接口设计.md` **V8.22**、`08_扫码执行设计.md` **V4.9**、`docs/frontend_pages.md` **V107**。

### ④ NCR「待处理批次」两组重构 + 让步放行三字段 + 超阈值遗漏「忽略」（**前后端 + 迁移**）

- **待处理拆两组**：「待处理批次」卡片与「不合格品实时待处理」表**各分两节两表**——「**正常提交**」（来源=不合格反馈，**无条件列出**，`Bucket=NormalSubmitted`）/「**超阈值遗漏**」（来源=过程检验 / 成品检验，`Bucket=OverageMissing`），各自条数 chip。
- **超阈值遗漏判据（组级一条）**：组内不合格**合计 = 让步放行支 + 各流向支**，**合计 > `NcrThreshold.Count`（绝对支数）且 合计÷组内总数 > `NcrThreshold.Percent`（占比）**（**均严格大于**，旧 `>=` 已改）→ 产出**一条**被动记录；**让步放行计入分子但永不单独成行**。组聚合键 = 过程检验「批次 + 工序」/ 成品检验「批次 + **成检类型** + 检验项目」。
- **组定位键（~~三处~~ 两处一致；09-13 起）** `GroupKey = "{SourceType}|{ProductionBatchId}|{ProcessName}|{InspectionType}|{InspectionItem}"`（列表行 `GroupKey` / `Ncr.SourceGroupKey`；第三处「忽略台账唯一索引」随表删除）。
- **流向（方案 B/A）**：被动行**可展开明细**（返整 / 入在制库 / 可入备库 / 入次品库 / 退货 各 N 支）；「**次品流向**」列**只读**取组内支数最多流向（并列按 **返整 > 入在制库 > 可入备库 > 入次品库 > 退货** 定序），主动行（不合格反馈）为 NULL。
- **G1 问题反馈组新增 3 列**（被动比主动恰多 4 字段）：`… 次品支数 / 次品重量 / 问题描述 / **次品流向** / 让步支数 / 让步重量 / 让步说明`；`ConcessionRemark` 组内**去重拼接**（`string.Join("；", Distinct)`）。
- **忽略（仅超阈值遗漏组；⚠️ 09-13 已改造，勿按旧设计理解）**：**原**设计为独立台账 `NcrPendingDismissal`（存合计快照 + 自动复活 + 「显示已忽略」开关 + 「恢复」端点）——**该设计已作废**。**现行**：忽略 = 在建单页 G1 之后点「忽略」按钮，按**正常建单流程**登记一张 **`Ncr.Status = Ignored`** 的不合格报告；因 `Ncr.SourceGroupKey` 排重**不看状态**，该组**永久**不再列出（**无自动复活**）；无台账表、无 dismiss/restore 端点、无「显示已忽略」开关（如需撤销，直接编辑/删除那张忽略档 NCR）。
- **「反馈部门」口径 = 位置**：过程检验 / 不合格反馈 → **工段**；成品检验 → **成检项目**。
- **阈值配置**：`NcrThreshold` 由 8 项（按流向）收敛为 **2 项** `Count`（默认 5）/ `Percent`（默认 0.10）；`DbInitializer` 与迁移 SQL 同步。
- **文件**：`MES.Core/Enums/NcrPendingBucket.cs`（新）+ `NcrStatus.cs`（09-13 加 `Ignored` 档）、`MES.Data/Entities/Quality/Ncr.cs`（+4 列）+ `AppDbContext.Quality.cs`（09-13 删 `NcrPendingDismissal` 配置）；`MES.Core/DTOs/Quality/NcrDto.cs` / `NcrPendingCheckDto.cs`；`MES.Services/Quality/NcrService.cs`（`GetPendingChecksAsync` 重写；09-13 删 `DismissPendingAsync` / `RestorePendingAsync`、`CreateAsync` 支持 `Status=Ignored`）；`MES.Api/Controllers/Quality/NcrController.cs`（09-13 删 2 端点）；`MES.Blazor/Pages/Quality/Ncrs.razor(.cs)`（列偏好 **`col_prefs_ncrs_v2`**，v1→v2；09-13 删已忽略开关/表/恢复，`GetStatusColor` 补 Ignored）/ `NcrForm.razor(.cs)`（选择器按两组分节；09-13 G1 行序重排 + 忽略按钮 + 待处理卡默认折叠）；`MES.Services/Printing/NcrPrintHelper.cs`、`DataExchange/DataExchangeRegistry.cs`（Ncr +4 列、36→40 列、「流向」→「次品流向」）。
- 文档：`质量管理模块详细设计.md` **V8.31**、`质量管理上下文接口设计.md` **V8.26**、`质量管理上下文数据库设计.md` **V8.18**、`配置上下文详细设计.md`（NcrThreshold 6→2）、`11_打印设计.md` **V2.18**、`frontend_pages.md` **V111**、`接口设计/数据工具接口设计.md` **V7.20**。

### ⑤ 首页看板 + 订单进度树 + 批次进度弹窗（WASM 为主）

> ℹ️ 「生产编号 → 生产批次详情」链接（4 页）**已随批二十六打包**（见下方组），本组不重复登记。

- **首页**：删「质量检验」卡；卡片更名 + 数值样式统一 + 阶段配色；「常用入口」收敛为 **9 项并磁贴化**（新增 `MES.Blazor/Shared/AppShortcuts.cs`，专用 `AppShortcut` 类型带 Icon/Accent），**手机端渲染同一份清单**；桌面两卡改**等宽 `md=6`**；单数口径改按 `SalesOrderNo` 去重（**含后端**：`WorkOrderExecutionDashboardItem` 改按 `ScheduleStage` 一行 + `UrgentOrderCount`/`UrgentWeight`，`WorkOrderExecutionService` 同步）。
- **订单进度树**：「执行关注=已完结」主号分支扩充为 **6 支**（生产投料/过程检/成品检/余料入库/备料入库/成品入库）、「生产投料」标题加生产类型后缀 → `MES.Services/Order/OrderProgressQueryService.cs`、`MES.Core/DTOs/Order/OrderProgressTreeDto.cs`、`MES.Blazor/Pages/Orders/OrderProgress.razor(.cs/.css)`。
  > ⚠️ **已被批二十八 ① 覆盖**（09-14 改为「投料 + 产出」两维度：4 专属支 + 订单成品入库，两个检验分支已删除）——本行仅记录 09-11~13 当时状态，**发布核对以批二十八为准**。
- **批次执行进度**：区块三卡片抽为 `MES.Blazor/Shared/BatchProgressCard.razor`，批次详情页内联引用、批次计划 / 成检计划经新增 `Shared/BatchProgressDialog.razor` 弹窗引用（`MaxWidth.Large`，**不跳转批次详情页**）；⚠️ 卡片 `.detail-*` 类由 `BatchDetail.razor.css` **上提 `wwwroot/css/app.css`**（Blazor scoped CSS 不能跨组件）。
- 其他：`ReportOverview`、`RawMaterialLockPlanAndExecution`、`WorkOrderLoadOverview`、`BatchPlans`、`FinalInspectionPlan`、`MaterialPlanOverview`、`ProductionOverviewService`、`BatchService`、`ProductionRecordService`、`BatchDetail.razor(.css)`、`app.css`、`index.html`。

### ⑥ EF 迁移清单（本批新增 **17** 个，`20260910103357` ~ `20260912192419`）

| 迁移 | 内容 | 数据影响 |
|---|---|---|
| `20260910103357_RenameQualityTestFurnaceNoToBatchNo` | 8 张试验表列 `FurnaceNo` → `BatchNo`（列 + 索引改名） | 零行变更（纯改名）；见「数据变更注记」2026-09-10 |
| `20260911055011_AddQualityDefectReturnFields` | 过程检 +`DefectReturnQuantity`/`TheoreticalReturnWeight`；成品检 +`DefectReturnQuantity`/`DefectReturnWeight` | 纯加列，存量 NULL |
| `20260911055600_AddWorkOrderExecutionDefectReturnWeight` | `WorkOrderExecutionSummary` +`ProcessInspectionReturnWeight` / `FinalInspectionReturnWeight` | 纯加列，存量 NULL |
| `20260911055929_SeedDisposalMethodReturnAndNcrReturnThresholds` | `EnumDisplayDefinitions` 加 `DisposalMethod.Return`（退货，序 4）；`ConfigParameters` 加 `NcrThreshold.ReturnCount=5` / `ReturnPercent=0.05` | 幂等种子 |
| `20260911060250_AddQualityProcessTrackingDefectReturnQuantity` | `QualityProcessTracking` +`DefectReturnQuantity`（NOT NULL DEFAULT 0） | 纯加列 |
| `20260911064754_AdjustNcrDisposalMethodTiers` | NCR 处置方式切 5 档 + `Ncr` 防御性回写 | 见「数据变更注记」 |
| `20260911071723_AddFinalInspectionInProcessWarehouseTier` | 成检第 5 档「入在制库」3 列 | 见「数据变更注记」 |
| `20260911083807_AddNonconformingFeedback` | 新建 `NonconformingFeedback` / `NonconformingFeedbackAttachment` | 新建，0 行 |
| `20260911121230_AddInspectionPatrol` | 新建 `InspectionPatrol` / `InspectionPatrolItem` / `InspectionPatrolAttachment` | 新建，0 行 |
| `20260911130718_RenameInspectionPatrolEquipmentCodeToName` | `InspectionPatrol.EquipmentCode` → `EquipmentName` | 零行变更（纯改名） |
| `20260911144334_AddInspectionPatrolRectificationOperator` | `InspectionPatrol` +`RectificationOperator` nvarchar(200) | 纯加列 |
| `20260911152837_RemoveFeedbackIsHandledAndLinkNcrToFeedback` | 删 `NonconformingFeedback.IsHandled` + `Ncr` 加 `NonconformingFeedbackId`（FK SET NULL） | ⚠️ 见「数据变更注记」（删列不可逆） |
| `20260911171626_AddInspectionAttachments` | 新建 `ProcessInspectionAttachment` / `FinalInspectionAttachment` | 见「数据变更注记」 |
| `20260911210734_AddNcrFlowDirectionAndDisposalDictionary` | `Ncr` 处置方式改 8 档字典（列扩容 30）+ 新增「流向」列 `FlowDirection`；`DictValueDefinitions` 加 `NcrDisposalKey` 8 行；`EnumDisplayDefinitions` EnumKey 改名 | 见「数据变更注记」 |
| `20260912052001_AddNonconformingFeedbackSourceType` | 不合格反馈单 +`SourceType` nvarchar(30) NOT NULL 默认 `ProductionSection` + `InspectionItem` nvarchar(30) 可空；`SectionName` / `SequenceNumber` 放开为**可空**（成品检验档无工段） | ⚠️ 存量 `SourceType` 由列默认值回填 `ProductionSection`，见「数据变更注记」 |
| `20260912071847_AddNcrPendingDismissalAndConcession` | 新建 `NcrPendingDismissal`（含 `UK_NcrPendingDismissal_Group` 唯一索引）；`Ncr` +4 列（`ConcessionQuantity` / `ConcessionWeight` / `ConcessionRemark` / `SourceGroupKey`）；`NcrThreshold` 删 8 项旧流向阈值 → 幂等插入 `Count=5` / `Percent=0.10` | 纯加列 / 建表（存量 NULL、0 行）+ 配置口径收敛，见「数据变更注记」 |
| `20260912192419_DropNcrPendingDismissalAndSeedNcrIgnoredStatus` | **删表** `NcrPendingDismissal`（含唯一索引）；`EnumDisplayDefinitions` 幂等插入 `NcrStatus/Ignored = 忽略`（序 4）；`Ncr.Status` **值域**扩为 4 档（`Ignored` 新增，**列结构不变**） | ⚠️ 删表（该表为「人工决策留痕」非业务数据；真库该表 **0 行**）、见「数据变更注记」09-13 |

### ⑦ 控制台告警与 DotNetObjectReference 僵尸引用修复（**纯 WASM + 静态 js**）

> 来源：线上 F12 控制台三条信息复核。① ② 为告警（其中 ② 暴露一处死代码），**③ 为真 bug**（与 Edge 打印崩溃无关，是独立问题，Release 构建下同样发生）。

| 文件 | 改动 |
|---|---|
| `MES.Blazor/wwwroot/index.html` | ① `<head>` 补 `<meta name="mobile-web-app-capable" content="yes" />`（Chrome/Edge 已弃用 `apple-` 前缀名，两条并存才对）；② 删 `<script src="js/pwa-install.js"></script>`，把其中**唯一有实际作用的一行** `beforeinstallprompt → e.preventDefault()` 内联进内联 `<script>`（**刻意保留**，见下）；③ 改写横屏提示条监听总线 |
| `MES.Blazor/wwwroot/js/pwa-install.js` | **删除文件**：全仓 0 调用方的死代码（`triggerPwaInstall` / `getPwaInstallPrompt` / `window.pwaInstallPrompt` 三个全局无人调用）。原实现只 `preventDefault()` 压制浏览器原生安装横幅、却从不调 `prompt()`，属**半截功能** |
| `MES.Blazor/Shared/LandscapeHintBanner.razor` | `stopLandscapeHintRefresh` 调用去掉 `_dotNetRef` 参数（原因同下） |

> **② 的取舍（用户拍板：不要安装横幅）**：MES 不做 PWA 安装引导 → **保留** `preventDefault()` 压制浏览器原生「安装 MES 应用」横幅。
> 代价：DevTools 会保留一条 **info 级**提示 `Banner not shown: beforeinstallpromptevent.preventDefault() called` —— 那是「你压制了横幅却没调 prompt()」的提醒，**属预期、非错误**；只要压制横幅就无法避免（除非删 `manifest.json` 的 `<link>`，副作用更大）。**故本条不再计为待修项**。

**③ 根因（真 bug，已在发布产物 `blazor.webassembly.js` 源码坐实）**：JSInterop 反序列化 reviver 对 **.NET 引用**是 `new DotNetObject(id)` —— **每次跨边界传递都新建 JS 包装对象**；只有 **JS 引用**才走 `getWrappedObject()` 缓存复用。故 `index.html` 原 `_landscapeHintRefs.indexOf(dotnetRef)` **恒为 `-1`**：

- `stop` 永远删不掉 → 组件销毁 `_dotNetRef.Dispose()` 后数组残留僵尸引用 → 之后每次 `resize`/`orientationchange` 回调已释放 id → `System.ArgumentException: There is no tracked object with id 'N'`（Uncaught in promise）；`N` = 该会话第 N 个创建的 DotNetObjectReference。
- `listen` 的 `indexOf` 去重同样失效 → 数组**只增不减**（内存泄漏，每次 resize 把全部僵尸各报一次）。

**修复**：改为**整体清空**（同一时刻只存在一个提示条——仅 `MobileLayout` 渲染、`ResponsiveLayout` 整壳切换）+ `invokeMethodAsync(...).catch()` 兜底 dispose/resize 竞态。
**触发场景**：手机**竖屏→横屏**时 `ResponsiveLayout` 判定 `innerWidth≥700` 整壳切 `MainLayout` → banner 随之销毁；而触发该切换的正是同一个 resize/orientationchange 事件。

**⚠️ 发布注意**：`web\js\pwa-install.js` 在线上会作为**孤儿文件残留**（覆盖发布不删文件），已无引用不会加载，可手动删除。
**验证**：`dotnet build MES.Blazor` **0 错 0 警**；定向 `LandscapeHintRuleTests` + `AppMenuTests` **17/17** 通过。界面生效待发布后核对：手机竖屏⇄横屏来回切换，控制台不应再出现 `no tracked object`；`apple-mobile-web-app-capable is deprecated` 应消失；`Banner not shown` **保留**（info 级、有意压制横幅，见上②）。

### ⑧ 发布注意事项

- ⚠️ **dll + WASM + 迁移必须同批**：`RenameInspectionPatrolEquipmentCodeToName`（改名）与 `RemoveFeedbackIsHandledAndLinkNcrToFeedback`（删列）一旦库先改而 dll 滞后，旧 dll 生成 SQL 会报「列名无效」。
- ⚠️ **列偏好版本普遍 bump**（过程检 v3 / 成品检 v4 / 不合格反馈 **v3** / **不合格报告 v2**（`col_prefs_ncrs_v2`）/ 多页 v1~v4）：老用户列显隐会被强制重置为新默认（**预期行为**）。
- ⚠️ **`attachments\` 目录须保留**（见批头提示）；覆盖 `web\` 前建议先 `nssm stop MES.API`，覆盖完 `nssm start MES.API` 让迁移自动应用。
- ⚠️ **DataExchange 硬度 / 晶粒度导入模板表头**已由「炉批号」→「生产编号」（属批二十六内容，随本批一并提示用户重新下载模板）；**不合格报告（Ncr）模板**亦须重下（+4 列、36→40，「流向」更名「次品流向」）。
- ⚠️ **发布后须复核 NCR 待处理列表**：新阈值口径 `Count=5` / `Percent=0.10`（**严格大于**）可能与旧 8 项口径产生**不同的待处理组集合**（旧口径按流向分组分别判阈，新口径按组内合计判定）→ 属**预期口径变更**，非数据异常；如需调整，改配置表 `NcrThreshold` 两项即可（无需发版）。
- ⚠️ **NCR「忽略」语义已变（09-13）**：库内**不存在**旧的忽略台账数据（`NcrPendingDismissal` 建表至删表间真库 0 行），故删表**无状态丢失**；但若**线上库曾有人点过「忽略」**，那批组会重新出现在待处理列表 → 按新流程重新「忽略」即可（**勿当数据丢失**）。
- **验证**：`dotnet build MES.Api` 0 错 0 警；打印版式定向 `Services.Printing` 18/18；各批定向单测记录见 `docs/frontend_pages.md` 对应 V 条目。

## 📦 已打包待上传（批二十五 + 批二十六，`mes-deploy-20260910_1839.zip`）

> 本组为 **2026-09-10 18:39 已打包、本机未见上传回执** 的内容；若已上传服务器，整组可直接删除。

## ⏳ 批二十六 「生产编号 → 生产批次详情」补链 + 8 理化试验模块字段正名 `FurnaceNo`→`BatchNo` + 批次进度弹窗宽度收敛（2026-09-10 登记，本机已改、已打包待上传）

> **打包登记（2026-09-10 18:39）**：随 **`publish/mes-deploy-20260910_1839.zip`** 打包（**api 141 + web 209 = 350 条，65.5M**；产物已核实含 `MES.Api.dll` / `index.html` / `blazor.webassembly.js`，**无 `service-worker*` / `bootstrap.min.css` 残留**，`MES.Data.dll` 已含新迁移）。**本包为超集——已含批二十五全部内容，`mes-deploy-20260910_0310.zip` 作废，直接上传本包即可。**
> 服务器操作：解压覆盖 `C:\mes\api` + `C:\mes\web` → `nssm restart MES.API`（前端静态覆盖无需重启 nginx）。
> ⚠️ **本批含 EF 迁移 `20260910103357_RenameQualityTestFurnaceNoToBatchNo`**（8× `RenameColumn` + 8× `RenameIndex`，**纯改名、零行数据变更**）——**属 `05_部署与运维.md` §4.2「数据库只加不减（禁止 RenameColumn）」的一次例外**：当前仍为试验期（云库 `MES` 为真库副本）、且 **dll 与 WASM 同批发布**所以列名语义两端一致，用户已确认执行。

> **性质：后端 dll + WASM + 1 个 EF 迁移（云库随新 API 启动自动 Migrate 应用；亦可用手动 SQL，见下）**。

### ① 生产编号 → 生产批次详情 补链（批三十三，纯前端 + NCR 后端小改）

新增 4 处直接链接（`/batches/{id}`）：**去油酸洗入缸** `/pickling-in-records`（BatchView，无需降级）、**成品检验** `/quality/final-inspection`（QualityView ⊄ BatchView → 按角色降级）、**不合格报告** `/quality/ncrs`（无页级策略 → 按角色降级）、**报表总览·NCR 待处理表** `/reports/overview`（ReportView ⊆ BatchView，无需降级）。
⚠️ `Policies.BatchView` 为逗号分隔多角色串，`ClaimsPrincipal.IsInRole` 只认单角色名 → 判定一律 `Split(',').Any(user.IsInRole)`。缺口 4 组（A 纯前端 / B 需后端补 Id / C 8 试验页 / D 语义待定）**仅登记台账不实现**，选型原则见 `docs/frontend_pages.md` §7。

| 文件 | 改动 |
|---|---|
| `MES.Core/DTOs/Quality/NcrDto.cs`、`NcrPendingCheckDto.cs` | +`int ProductionBatchId`（只读、服务端回填） |
| `MES.Services/Quality/NcrService.cs` | +`GetBatchIdMapAsync` + 两个 `FillProductionBatchIdsAsync` 重载；`GetAllAsync`/`GetByIdAsync`/`GetPendingChecksAsync` 查询后按 `BatchNo` 反查 `ProductionBatch` 回填（⚠️ 内存字典 `StringComparer.OrdinalIgnoreCase`） |
| `MES.Blazor/Pages/Batches/PicklingInRecords.razor.cs` | +`OpenBatchDetail(int)`；`BatchNo` 列改 `MudLink` |
| `MES.Blazor/Pages/Quality/FinalInspections.razor(.cs)` | +`@inject AuthenticationStateProvider` + `_canViewBatch` 降级 + `OpenBatchDetail` + `BatchNo` 分支 `MudLink` |
| `MES.Blazor/Pages/Quality/Ncrs.razor(.cs)`、`MES.Blazor/Pages/Reports/ReportOverview.razor(.cs)` | `BatchNo` 单元格改 `MudLink`（Id=0 或无权限降级纯文本） |
| `MES.Tests/Services/Quality/NcrServiceTests.cs` | +批次回填用例（定向 73 绿） |

### ② 8 个理化试验模块字段正名 `FurnaceNo` → `BatchNo`

扩口/压扁/晶粒度/硬度/晶间腐蚀/金相/点腐蚀/室温拉伸 八页列标签「生产编号」本就正确，`FurnaceNo` 仅历史命名遗留 → **全链路正名**。**`ChemicalAnalysis.FurnaceNo`（真炉号）与 `QualityProcessTracking.FurnaceNo`（= `pb.SourceHeatNo`）不动。**

| 文件 | 改动 |
|---|---|
| `MES.Data/Entities/Quality/{Flaring,Flattening,GrainSize,Hardness,IntergranularCorrosion,Metallographic,PittingCorrosion,Tensile}Test.cs` | 属性 `FurnaceNo` → `BatchNo`（8 文件） |
| `MES.Data/AppDbContext.Quality.cs` | 8 处 `Property(e => e.BatchNo)` + 8 个索引名 `IX_{表}_BatchNo` |
| `MES.Core/DTOs/Quality/{同上}TestDto.cs` | DTO + Create/Update Request 字段改名（8 文件） |
| `MES.Services/Quality/{同上}TestService.cs`、`CertificateService.cs` | 查询/投影/排序/关键字改名；质量证明书自动填充取数条件同步（25 处） |
| `MES.Blazor/Pages/Quality/{同上}Tests.razor.cs`、`{同上}TestCreate.razor` | 列定义 `Key="BatchNo"`、**`SortKey` 同步 `batchno`**（服务端排序按属性名反射，不改则静默回退 `CreatedTime`）、编辑/校验/映射（16 文件） |
| `MES.Services/DataExchange/DataExchangeRegistry.cs` | 8 条字段映射改名；**硬度/晶粒度表头「炉批号」→「生产编号」** |
| `MES.Tests/Services/Quality/{同上}TestServiceTests.cs` | 单测改名（8 文件，定向 113 绿） |
| `MES.Data/Migrations/20260910103357_RenameQualityTestFurnaceNoToBatchNo.cs`(+Designer)+`AppDbContextModelSnapshot.cs` | **新建迁移**：8× `RenameColumn` + 8× `RenameIndex`（纯改名、零行变更） |

> ⚠️ **连带影响**：`DataExchange` 硬度/晶粒度 Excel 导入模板表头已由「炉批号」改为「生产编号」，**用户旧模板须重新下载**（其余 6 类原表头已是「生产编号」无变化）。

### ③ 批次执行进度弹窗宽度收敛（纯 WASM）

批次计划 `/scheduling/batch-plans` 与成检计划 `/scheduling/final-inspection-plan` 的「生产编号」弹窗：`DialogOptions` 由 `MaxWidth.ExtraLarge + FullWidth`（MudBlazor 6.19 实为 `max-width:1920px` 叠 `width:calc(100%-64px)`，几乎满屏、右侧大片留白）改为 **`MaxWidth.Large`（1280px）+ 不开 `FullWidth`**，按内容自适应收缩。

| 文件 | 改动 |
|---|---|
| `MES.Blazor/Pages/Scheduling/BatchPlans.razor.cs`、`FinalInspectionPlan.razor.cs` | `OpenBatchProgressAsync` 的 `DialogOptions` 改 `MaxWidth.Large`、去 `FullWidth` |

### 云端库 apply 迁移（两种方式任选）

**方式 A（推荐，§4.3 顺序铁律）**：覆盖 `api\` 后首次启动新 API，**自动 Migrate 应用 `20260910103357`** → 确认日志无异常 → 再覆盖 `web\`。

**方式 B（手动 SQL）**：已生成幂等脚本 `publish/sql/20260910103357_RenameQualityTestFurnaceNoToBatchNo.sql`（16 条 `sp_rename`，每条含 `IF NOT EXISTS(__EFMigrationsHistory)` 守卫 + 末尾写历史表）——

```powershell
# 服务器管理员 PowerShell，云库 MES
sqlcmd -S localhost\SQLEXPRESS -d MES -E -C -i .\20260910103357_RenameQualityTestFurnaceNoToBatchNo.sql
```

重新生成命令（开发机）：`dotnet ef migrations script 20260909112729_AddSectionOutsourcePricing 20260910103357_RenameQualityTestFurnaceNoToBatchNo --context AppDbContext --project MES.Data --startup-project MES.Api --idempotent -o <文件>`

> ⚠️ **不可只发布 `web\` 而不同批发布 `api\`**：库列名已改而旧 dll 仍按 `FurnaceNo` 生成 SQL → 全部试验页报「列名无效」。
> ✅ 真库 `zhou/MESMN` 已 apply 并经 sqlcmd 复核（8 表 `BatchNo` 全在 / `FurnaceNo` 残留 0；8 新索引全在 / 旧索引残留 0），登记见文末「数据变更注记」2026-09-10。

**验证**：`dotnet build MES.Api` + `dotnet build MES.Blazor --no-incremental` 均 0 错 0 警；定向单测 8 个 TestService **113/113** 通过；真库迁移后 sqlcmd 复核通过。界面生效待发布后浏览器核对（8 试验页列名/排序、两计划页弹窗宽度）。

---

## ⏳ 批二十五 用户管理角色分配补「工资结算」档位 + 全仓 14→15 主菜单口径 + 死代码清理（2026-09-10 登记，**已并入批二十六包 `mes-deploy-20260910_1839.zip`**）

> **打包登记（2026-09-10）**：随 **`mes-deploy-20260910_0310.zip`** 打包（api+web 共 350 条，65.5M），**待上传服务器**解压 `C:\mes` 覆盖 + `nssm restart MES.API`；本次同时夹带同提交（1fb52d0f）的死代码清理：删 `service-worker.js`/`service-worker.published.js`（未注册 PWA 残留）+ csproj `<ServiceWorkerAssetsManifest>` + 发布脚本/nginx/docs 提及同步、删零引用 `css/bootstrap/bootstrap.min.css`、37 行未用 `@inject`（36 razor）、删前端零调用 `PieceRateFinalInspectionCategoryService.MatchPriceAsync`。产物已核实**不含 service-worker\* / bootstrap 残留**（首次打包残留已清 `publish/blazor` 目录重打）；**无 EF 迁移、无数据变更**。

> 来源：docs 复核（R7）发现**用户管理「角色分配」弹窗缺「工资结算」档位**——`UserRoleDisplayHelper.MenuTiers` 仅 14 主菜单（缺 Salary），Salary 三档（SalaryViewer/Editor/Full）**无法经界面为存量用户分配**（仅能由 Admin 隐式全权进入，非 Admin 用户即使想要工资结算权限也授不了，进不了工资结算页）。修复：MenuTiers 补 `Salary/工资结算`（Scan 与 Configuration 之间，与 Roles.Menus/GetAllRoles 顺序一致）；**新建用户默认仍 = 无**（DefaultViewerMenus 保持 9 业务域，工资结算按系统菜单显式授权，与 03_安全 §3.3「其余系统菜单（…工资结算…）= None」一致）。**性质：前端 WASM + 后端 dll（Roles.cs 仅注释改动）；无 EF 迁移、无数据变更**。

| 文件 | 改动 | 性质 |
|---|---|---|
| `MES.Blazor/Helpers/UserRoleDisplayHelper.cs` | 类注释与 MenuTiers 注释 14→15 主菜单；`MenuTiers` 增 `new("Salary", "工资结算")`（Scan 后、Configuration 前）；`DefaultViewerMenus` 不变（9 业务域，Salary 默认无） | WASM（重新 publish 编译） |
| `MES.Blazor/Shared/UserEditDialog.razor` | 顶部注释 + 分组标签「角色分配（14 主菜单 × 3 档）→（15 主菜单 × 3 档）」 | WASM |
| `MES.Shared/Constants/Roles.cs` | `Menus` 类 XML 摘要「14 主菜单」→「15 主菜单」（仅注释；Menus 常量/GetAllRoles 本就含 Salary） | WASM+后端 dll（注释，无行为差异） |
| `MES.Tests/Core/UserRoleDisplayHelperTests.cs` | `MenuTiers_共14个主菜单` → `共15个主菜单_含工资结算`（断言 Salary 显示名「工资结算」、不在 DefaultViewerMenus、顺序在 Scan 与 Configuration 之间）+ 系统菜单注释 + `GetRoleDisplayName("SalaryViewer")="工资结算-查"` 断言 | 测试（仅本地，不上云） |

验证 `dotnet build MES.Blazor` 0 错 0 警；定向 `UserRoleDisplayHelperTests` 10/10 通过。界面生效待发布后浏览器核对：用户编辑弹窗角色列表应出现「工资结算」下拉档位（默认「无」，可手动授 Salary 三档）。

---

## 数据变更注记（数据库层清理，非代码发布；防漏必读）

> 本节记录「在真库 zhou/MESMN 或云库 MES 上直接执行的数据清理/修复」，与代码发布批次相互独立。**凡日后做此类数据变更动作，必须在本节补登**（含判据/影响行数/日期），防止未来把「清理后的少数数据」误判为丢失，或整库恢复（`08-restore-db.ps1`）后不知需按判据重清。
> 若从历史备份恢复真库或云库，可能重现下列已清理数据，须按判据再次清理。

### 2026-09-09：供应商档案 SupplierProfile 清理「无任何采购/委外往来」单位（真库 zhou/MESMN 与云库 MES 均已执行 121→10）
- **内容**：真库 zhou/MESMN 供应商档案删除 111 行（121→10）。判据 = 该档案行**既无任何单据以 SupplierId 引用、也按「供应商名+物料分类」从未命中任何采购单(PurchaseOrder)或委外单(SubcontractOrder)的名+分类**（与供应商列表 ② 往来信息归行口径一致，归行键=PO.SupplierName+PO.MaterialCategory、WW.SupplierName+WW.OutMaterialCategory）。两子类：106 行名称从未出现在任何单的纯死档案 + 5 行同名供应商另有其它分类在采购、但其本分类行无单（江苏银拓/远晟/正驰、浙江骏达/永成创岳 的 OrderFinished 空档案），用户拍板 111 行全删。全库 SupplierProfile 的 FK 引用方仅 PurchaseOrder.SupplierId / SubcontractOrder.SupplierId，判据已排除 → 无 FK 阻断。
- **影响行数（复查）**：Deleted=111 / Remaining=10（其中 8 行 SupplierId+(名+分类) 双命中、1 行仅 SupplierId、1 行仅名+分类命中；建档 MaterialCategory 无空值，采购单仅 RoughTube 184 + CriticalFinished 33，OrderFinished/RoundBar/SemiFinished 分类从无采购单）。
- **云库 MES（zhz.js.cn，服务器本机 `localhost\SQLEXPRESS`）执行（2026-09-10）**：预览同判据 `pre_total=121 | will_delete=111 | remaining_after=10`，orphan_po_supplier=0 / orphan_ww_supplier=0；执行 `Deleted=111 / Remaining=10`，两孤儿复核均 0。执行前已备份 `C:\MSSQL\Backup\manual_before_supplier_clean_20260910.bak`（**Express 不支持 `BACKUP ... WITH COMPRESSION`，用 `WITH INIT`**；首版带 COMPRESSION 备份失败、同批 DELETE 因 1046 编译错整体未执行无副作用，修正后成功）。判据 SQL 无字符串字面量，无中文编码问题。
- **供应商管理现 10 行为预期结果**，勿当数据丢失（同旨 2026-09-08 客户清理剩 21）。若整库恢复（`08-restore-db.ps1`）重现 121 行，须按判据重清：
  ```sql
  DELETE sp FROM SupplierProfile sp
  WHERE NOT EXISTS (SELECT 1 FROM PurchaseOrder po WHERE po.SupplierId = sp.Id)
    AND NOT EXISTS (SELECT 1 FROM SubcontractOrder ww WHERE ww.SupplierId = sp.Id)
    AND NOT EXISTS (SELECT 1 FROM PurchaseOrder po WHERE po.SupplierName = sp.SupplierName AND po.MaterialCategory = sp.MaterialCategory)
    AND NOT EXISTS (SELECT 1 FROM SubcontractOrder ww WHERE ww.SupplierName = sp.SupplierName AND ww.OutMaterialCategory = sp.MaterialCategory);
  ```

### 2026-09-09：采购/委外计价回填（真库 zhou/MESMN 经 EF 迁移 `20260909060707_AddPurchaseAndSubcontractPricing` apply，非手动 SQL）
- **内容**：`PurchaseOrder`/`SubcontractReturnItem` 补 `PricingUnit` 列并幂等回填存量（**判据 = 单价 `IS NULL`**，不覆盖手输价）：采购计价单位=PerKg、荒管族(RoughTube/DefectRoughTube)=18 元/kg、成品族(Finished/OrderFinished/CriticalFinished/DefectFinished/SpecialDeliveryStatus)=26 元/kg、`TotalAmount=ROUND(Weight×UnitPrice,2)`；委外明细=PerKg、`ProcessUnitPrice=1.2`、`ProcessTotalAmount=ROUND(RequiredWeight×1.2,2)`。默认价常量与 `MES.Core/Helpers/MaterialPricingDefaults.cs` 人工一致。
- **影响行数（回填后复查）**：PurchaseOrder 计 223 行单价/总价全部填齐（回填后 `UnitPrice IS NULL`=0）；SubcontractReturnItem 计 2 行加工单价/金额全部填齐（回填后 `ProcessUnitPrice IS NULL`=0）。抽查 TotalAmount=Weight×18/26 全对。
- **⚠️ 迁移 ID `20260909060707` 已记入真库 `__EFMigrationsHistory`**；云库 `MES` 随下次部署 API 启动自动 Migrate 自动重放（幂等，判据单价 IS NULL）。若整库恢复旧备份，此迁移缺失时会自动重放回填，勿当异常；此 223/2 行全填价属预期终态。

### 2026-09-09：委外单位档案 OutsourceVendorProfile 建档（真库 zhou/MESMN 经 EF 迁移 `20260909094550_AddOutsourceVendorProfile` apply，非手动 SQL）
- **内容**：新表 `OutsourceVendorProfile`（委外单位 × 委外工段档案）建表 + 双唯一索引后，从存量 `SectionOutsource` 去重回填：`SELECT DISTINCT OutsourceVendor(VendorName), SectionName, IsInternal→IsWorkshop`，共 **12 行（WV0001~WV0012，其中本厂车间 IsWorkshop=4 行）**。**属新增档案数据（迁移回填），非清理**；真库 `__EFMigrationsHistory` 已记 `20260909094550`。
- **影响行数（回填后复查）**：Total=12 / DistinctRowKeys=12（无重复行键），与 SectionOutsource 239 行去重后的 12 个(单位×工段)组合一致。
- **⚠️ 云库 MES**：随下次部署 API 启动自动 Migrate 自动建表 + 回填（源 = 云库自己的 SectionOutsource 存量，去重数量与真库不同属正常）。若整库恢复旧备份，此迁移缺失时会自动重放建表回填，勿当异常。

### 2026-09-09：工段委外计价回填（真库 zhou/MESMN 经 EF 迁移 `20260909112729_AddSectionOutsourcePricing` apply，非手动 SQL）
- **内容**：`SectionOutsource` 补 `PricingUnit`/`UnitPrice`/`TotalAmount` 三列并幂等回填（**判据 = `UnitPrice IS NULL`**，不覆盖手输价）：厂外行（IsInternal=0）`PricingUnit='PerKg'`、`UnitPrice=CASE SectionName WHEN 'ColdRollDraw' THEN 1.4 ELSE 0.8 END`、`TotalAmount=CASE WHEN SendWeight>0 THEN ROUND(CAST(SendWeight AS decimal(18,3))×UnitPrice,2) ELSE NULL END`；厂内行（IsInternal=1，本厂车间档案 IsWorkshop）三列留空。默认价常量与 `MES.Core/Helpers/MaterialPricingDefaults.cs` 人工一致。
- **影响行数（回填后复查）**：SectionOutsource 总 239 行 —— 厂内 14 行三列全 NULL（`internal_no_price_null=0`）；厂外 225 行全填齐（`external_null_price=0`：ColdRollDraw 224×1.4 + 其余工段 1×0.8，`PricingUnit` 全 PerKg，`TotalAmount=ROUND(SendWeight×价,2)` 全匹配 `mismatch=0`）。
- **⚠️ 迁移 ID `20260909112729` 已记入真库 `__EFMigrationsHistory`**；云库 `MES` 随下次部署 API 启动自动 Migrate 自动重放（幂等，判据单价 IS NULL，源=云库自己的 SectionOutsource 存量，行数可与真库不同属正常）。若整库恢复旧备份，此迁移缺失时会自动重放回填，勿当异常。

### 2026-09-10：8 个理化试验表字段正名 `FurnaceNo` → `BatchNo`（真库 zhou/MESMN 经 EF 迁移 `20260910103357_RenameQualityTestFurnaceNoToBatchNo` apply，非手动 SQL）
- **内容**：8 张试验表（`HardnessTest` / `GrainSizeTest` / `FlaringTest` / `FlatteningTest` / `IntergranularCorrosionTest` / `MetallographicTest` / `PittingCorrosionTest` / `TensileTest`）列 `FurnaceNo` → `BatchNo`（语义本就是「生产编号」，历史命名遗留，用户 2026-09-10 拍板正名），索引 `IX_{表}_FurnaceNo` → `IX_{表}_BatchNo`。**纯 `RenameColumn`/`RenameIndex`，不搬数据、零行变更**。
- **影响行数（复查）**：8 表 `BatchNo` 列全部存在、`FurnaceNo` 残留列 **0**；8 个新索引全部存在、旧索引残留 **0**（`ChemicalAnalysis` / `QualityProcessTracking` 保留的 `FurnaceNo` 索引不计）。
- **⚠️ 不动**：`ChemicalAnalysis.FurnaceNo`（真炉号）、`QualityProcessTracking.FurnaceNo`（= `pb.SourceHeatNo` 来源炉号）。
- **⚠️ 迁移 ID `20260910103357` 已记入真库 `__EFMigrationsHistory`**；**云库 `MES` 随下次部署 API 启动自动 Migrate 自动重放**（幂等，SQL Server `sp_rename` 对已改名对象会报错，但 EF 迁移仅执行一次，不会重放两次）。**⚠️ 配套：后端 dll + WASM 必须与新迁移同批发布**——旧 dll 仍按 `FurnaceNo` 生成 SQL，会在库已改名后报「列名无效」。
- **⚠️ 连带影响**：`DataExchange` 硬度/晶粒度 Excel 导入模板表头由「炉批号」→「生产编号」，**用户旧模板须重新下载**。

### 2026-09-11：成品检「不合格品去向」4 档 → 5 档（真库 zhou/MESMN 经 EF 迁移 `20260911071723_AddFinalInspectionInProcessWarehouseTier` apply）
- **内容（纯加列，无数据回写/无删除）**：① `FinalInspection` 新增 `DefectInProcessWarehouseQuantity`（int NULL）/ `DefectInProcessWarehouseWeight`（int NULL）——新增第 5 档「入在制库」（前端列序 返整/入在制库/入次品库/退货/可入备库）；② `QualityProcessTracking` 读模型新增 `DefectInProcessWarehouseQuantity`（int NOT NULL DEFAULT 0）；③ `WorkOrderExecutionSummary` 快照新增 `FinalInspectionInProcessWarehouseWeight`（int NULL）。过程检验继续 4 档（在制件无备库概念）不变。
- **影响行数（复查）**：真库本次为**加列**，存量行新列值 = NULL / 0，**未修改任何既有列**；`FinalInspection` 存量行因新档为 0，`QualifiedQuantity` 等派生口径与改前一致。
- **⚠️ 读模型刷新**：`QualityProcessTracking` 的 `DefectInProcessWarehouseQuantity` 存量行落 0 属预期；如需按最新口径重算 `QualifiedQuantity`（五档次品），走既有「成检追踪刷新」入口全量重算即可，非必需（新档为 0 时结果不变）。
- **⚠️ 迁移 ID `20260911071723` 已记入真库 `__EFMigrationsHistory`**；云库 `MES` 随下次部署 API 启动自动 Migrate 自动重放（`AddColumn` 幂等由 EF 迁移仅执行一次保证）。**⚠️ 配套：后端 dll + WASM 必须与新迁移同批发布**——旧 dll 无该属性映射、旧 WASM 无该列/输入格。

### 2026-09-11：NCR 处置方式 4 档 → 5 档（真库 zhou/MESMN 经 EF 迁移 `20260911064754_AdjustNcrDisposalMethodTiers` apply）
- **内容**：`DisposalMethod` 枚举删 `WarehouseEntry`（入库）、新增 `InProcessWarehouse`（入在制库，来源过程检验）/ `FinishedWarehouse`（可入备库，来源成品检验）；`Scrap` 枚举名不变、中文显示「报废」→「入次品库」。迁移内容 = `EnumDisplayDefinitions` 幂等切 5 档（删 WarehouseEntry 行、插 InProcessWarehouse/FinishedWarehouse、`Scrap` 显示名与 DisplayOrder 重排 1~5）+ `Ncr` 表**防御性回写** `DisposalMethod='WarehouseEntry'` → `'InProcessWarehouse'`。
- **影响行数（复查）**：真库 `Ncr` 表存量 **5 行（Rework 2 / Scrap 3）**，`WarehouseEntry` 行 **0 → 回写 0 行**；`EnumDisplayDefinitions` 处置方式行 5 行（Rework/InProcessWarehouse/FinishedWarehouse/Scrap/Return，DisplayOrder 1~5）。属**枚举显示配置调整 + 防御性回写**，非清理。
- **⚠️ 回写判据**：`Ncr` 表**无来源类型字段**（不存 `SourceType`/`ProcessName`/`MaterialName`），存量 `WarehouseEntry` 行**无法判别过程检/成品检来源**，故统一回写为 `InProcessWarehouse`。本机真库无此类行、实际无影响；**云库若有此类存量行（旧包期间从过程检/成品检待处理卡片建过「入库」档 NCR）将同样统一落为「入在制库」**，如需精确区分须人工按批次+检验项目回溯改档（迁移不自动判定）。
- **⚠️ 迁移 ID `20260911064754` 已记入真库 `__EFMigrationsHistory`**；云库 `MES` 随下次部署 API 启动自动 Migrate 自动重放（幂等，`IF NOT EXISTS` 包裹插入）。**⚠️ 配套：后端 dll + WASM 必须与新迁移同批发布**——旧 dll 的 `EnumHelper` 仍按 4 档输出「入库」，与新库显示配置不一致（读的是 DB 配置行，缺失值会回退枚举名）。

### 2026-09-11：不合格反馈删「是否处理」列 + Ncr 加 `NonconformingFeedbackId` 外键（真库 zhou/MESMN 经 EF 迁移 `20260911152837_RemoveFeedbackIsHandledAndLinkNcrToFeedback` apply）
- **内容**：① `NonconformingFeedback` 表**删除 `IsHandled` 列**（连同索引 `IX_NonconformingFeedback_IsHandled`），并 `Ncr` 表**新增可空列 `NonconformingFeedbackId`** + 索引 `IX_Ncr_NonconformingFeedbackId` + 外键 `FK_Ncr_NonconformingFeedback_NonconformingFeedbackId`（`ON DELETE SET NULL`）。语义：不合格反馈「已处理」= 是否已生成 Ncr。
- **⚠️ 数据影响：存量 `IsHandled` 标记全部丢失（列删除，不可逆）** —— 真库该列存量行数已不复存在、无备份价值（原标记仅为手工勾选的弱状态，且未与 Ncr 关联），用户 2026-09-11 拍板直接删列。**若整库恢复旧备份重现 `IsHandled` 列，按「删列即可」处理，无需回填**（新表结构不含该列，恢复后须重新 apply 本迁移或手工 `ALTER TABLE NonconformingFeedback DROP COLUMN IsHandled`）。
- **⚠️ `Ncr.NonconformingFeedbackId` 存量行（本次迁移前已建的 5 行）全部为 NULL 属预期**：迁移不回填历史关联（旧数据无反馈来源概念）；如需把历史反馈与 NCR 手工关联，按批次+检验项目人工判定后 `UPDATE Ncr SET NonconformingFeedbackId=... WHERE Id=...`，非必需。
- **⚠️ 迁移 ID `20260911152837` 已记入真库 `__EFMigrationsHistory`**；云库 `MES` 随下次部署 API 启动自动 Migrate 自动重放（`DropColumn`/`AddColumn` 由 EF 迁移仅执行一次保证）。**⚠️ 配套：后端 dll + WASM 必须与新迁移同批发布**——旧 dll 仍按 `IsHandled` 生成 SQL，会在列已删后报「列名无效」。

### 2026-09-11：真库临时测试数据（排查「不合格反馈保存无反馈」问题，已自清）
- **内容**：为定位「编辑不合格反馈 → 删照片 → 点保存无反馈」问题，在真库 `zhou/MESMN` 经 API 做了三组端到端实测：`PUT api/nonconforming-feedback/1`（页面同款 payload）、`POST api/nonconforming-feedback/1/attachments`（上传测试 png，生成 `NonconformingFeedbackAttachment.Id=4`）、`DELETE api/nonconforming-feedback/1/attachments/4`（删除，行与磁盘文件均清）。**属临时测试数据，测试后已全部删除**。
- **影响行数（收尾后复查）**：表内仅存 `Id=3` 一行（原存量，19:25:03）；**`IDENT_CURRENT('NonconformingFeedbackAttachment')` = 3，若引用最新 identity 值注意 4 已被跳过**（测试插入后删除所致，无孤儿行）。
- **⚠️ 本机残留孤儿文件**：`MES.Api/bin/Debug/net8.0/attachments/699620c40573490a90645a454a4e687e.jpg`（2026-09-11 20:41:47，来自用户真机操作时「先写盘、DB 行未落库」的缺陷路径）——**本机 bin 目录，不影响真库/云库**，随 `dotnet clean`/重发自然消失。真库/云库 `attachments` 目录无此文件。
- **性质：非清理、非结构变更**，无判据需重放；登记目的仅为「identity 跳号 + 本机孤儿文件」留痕，**勿当数据丢失**。

### 2026-09-12：过程检验/成品检验记录照片附件两表建档（真库 zhou/MESMN 经 EF 迁移 `20260911171626_AddInspectionAttachments` apply）
- **内容（纯建表，无数据回写/无删除）**：新增 `ProcessInspectionAttachment` / `FinalInspectionAttachment` 两表（字段：`InspectionId` / `FileName` / `StoredName` / `ContentType` / `SizeBytes` / `SortOrder`），各带 `IX_*_InspectionId` 索引与 `FK_*`（指向 `ProcessInspection` / `FinalInspection`，`ON DELETE CASCADE`）。照片张数上限单条 3 张（常量唯一出口 `MES.Core/Constants/QualityPhotoLimits.PerRecord`）。
- **影响行数（复查）**：两表本次为**新建**，存量 0 行；**未修改任何既有表/列**。
- **性质：结构变更（新增）**，无判据需重放；若整库恢复旧备份，两表缺失时本迁移会自动重放建表（`CREATE TABLE` 由 EF 迁移仅执行一次保证幂等）。
- **⚠️ 迁移 ID `20260911171626` 已记入真库 `__EFMigrationsHistory`**；云库 `MES` 随下次部署 API 启动自动 Migrate 自动重放。**⚠️ 配套：后端 dll + WASM 必须与新迁移同批发布**——旧 dll 无该实体映射，旧 WASM 无照片列/照片弹窗/扫码拍照入口。
- **⚠️ 磁盘附件**：落盘目录 `attachments/`（服务器为 `api\attachments\`），随发布覆盖 `api\` 时**须保留该目录**（同不合格反馈/巡检照片既有约定）。

### 2026-09-12：NCR 处置方式 5 档枚举 → 8 档字典 + 拆出「流向」列（真库 zhou/MESMN 经 EF 迁移 `20260911210734_AddNcrFlowDirectionAndDisposalDictionary` apply）
- **背景（用户拍板）**：原 `DisposalMethod` 5 档枚举语义实为「流向」（物料实际去向，由过程检/成品检记录带出），**不可扩展**；「不合格处置」须字典化（可随时加档），两者是不同概念不可混淆。**流向**保留枚举 5 档（只读带出，不字典化——有实际记录时本质不存在额外流动性）；**处置方式**改为 8 档可扩展字典（`DictKey=NcrDisposalKey`）。
- **内容（结构 + 字典种子，无存量回写/无删除）**：① `Ncr.DisposalMethod` 列**保留原列**、类型扩容 `nvarchar(20)`→`nvarchar(30)`，语义由「流向」改为「处置方式」（8 档字典 Key）；② `Ncr` 表**新增可空列 `FlowDirection`** `nvarchar(30)` + 索引 `IX_Ncr_FlowDirection`（枚举 5 档）；③ `DictValueDefinitions` **幂等插入** `NcrDisposalKey` 8 行（让步放行/返工/返整(新卡流转)/入在制库(可改制)/可入备库(尺寸偏差)/入次品库(修正改制)/入次品库(报废)/退货）；④ `EnumDisplayDefinitions` **EnumKey `DisposalMethod` → `FlowDirection`** 改名（5 行，配置行随枚举同步改名，避免成为指向不存在枚举的死数据、且 `FlowDirection` 能拿到配置表覆盖）。
- **影响行数（复查）**：真库 `Ncr` 表存量 **5 行（Rework 2 / Scrap 3）**，`DisposalMethod` 列**原值零迁移**（原枚举名 Rework/Scrap 等 5 名恰好是 `NcrDisposalKey` 中对应 5 个 Key，语义即 8 档中的「返整(新卡流转)」/「入次品库(报废)」等）；`FlowDirection` 新列**存量全 NULL**（历史行无来源检验记录可回溯）；`DictValueDefinitions` 新增 8 行；`EnumDisplayDefinitions` 5 行改名。**属结构变更 + 字典种子 + 配置改名，非清理**。
- **⚠️ 语义映射**：原流向 5 值 → 处置方式 8 档中的同 5 类（返整→返整(新卡流转) / 入在制库→入在制库(可改制) / 可入备库→可入备库(尺寸偏差) / 入次品库→入次品库(报废) / 退货→退货）；其余 3 档（让步放行 / 返工 / 入次品库(修正改制)）为新增，其中**让步放行 + 返工由生产及检验工段在正式操作前预先判定**（不产生流向）。月度汇总表行维度 = **处置方式**（非流向）。
- **⚠️ 迁移 ID `20260911210734` 已记入真库 `__EFMigrationsHistory`**；云库 `MES` 随下次部署 API 启动自动 Migrate 自动重放（`AlterColumn`/`AddColumn`/索引由 EF 迁移仅执行一次保证；两段字典与配置 SQL 自带幂等守卫）。**⚠️ 配套：后端 dll + WASM 必须与新迁移同批发布**——旧 dll 的 `EnumHelper` 仍按 `DisposalMethod` 输出 5 档枚举显示、旧 WASM 无「流向」列与 8 档处置方式下拉；库先改而 dll 滞后时，`Ncr.DisposalMethod` 写入旧枚举名仍可读（同为字符串列），但新 8 档字典 Key 无法识别。

### 2026-09-12：不合格反馈单新增「来源类型」`SourceType` + `InspectionItem`，工段/序号放开可空（真库 zhou/MESMN 经 EF 迁移 `20260912052001_AddNonconformingFeedbackSourceType` apply）

- **背景（用户拍板）**：原不合格反馈单只有「工序 + 工段」一种位置形态；来源可能是**生产工段上报**、**过程检验发现**或**成品检验发现**，后者位置信息应为「工序 + 检验项目」而非工段 → 新增来源类型维度，位置字段按来源分档（**方案 B：独立字段承载，不把检验项目编码进 `SectionName`**）。
- **内容（结构变更 + 1 行默认值回填）**：① **新增 `SourceType`** `nvarchar(30)` **NOT NULL**，列默认值 `ProductionSection`（存量回填「生产工段」）；② **新增 `InspectionItem`** `nvarchar(30)` 可空（枚举 `InspectionItem`，**仅成品检验档使用**）；③ `SectionName` `nvarchar(50)` 与 `SequenceNumber` `int` 由 NOT NULL **放开为可空**（成品检验档无工段、无序号）。
- **影响行数（复查）**：真库 `NonconformingFeedback` 存量 **1 行**，`SourceType` 由列默认值一次性回填 `ProductionSection`（旧数据只有生产工段/过程检验两种录入场景，且该行由生产环节上报）；`InspectionItem` 存量全 NULL（新列）；**不删行、不改既有业务值**。
- **⚠️ 迁移 ID `20260912052001` 已记入真库 `__EFMigrationsHistory`**；云库 `MES` 随下次部署 API 启动自动 Migrate 自动重放（`AddColumn` 默认值 + `AlterColumn` 由 EF 迁移仅执行一次保证）。**⚠️ 配套：后端 dll + WASM 必须与新迁移同批发布**——旧 dll 写 `NonconformingFeedback` 不带 `SourceType`（NOT NULL 有默认值故不报错，但落库恒为 `ProductionSection`）、旧 WASM 无来源类型下拉与「检验项目」列。**⚠️ 若整库恢复旧备份，`SourceType` 回填判据 = 该行 `SourceType IS NULL` → 置 `ProductionSection`**（无需按业务重判，历史行只有生产工段场景）。

### 2026-09-13：NCR 忽略改由状态档承载 —— 删表 `NcrPendingDismissal` + `EnumDisplayDefinitions` 补 `NcrStatus/Ignored`（真库 zhou/MESMN 经 EF 迁移 `20260912192419_DropNcrPendingDismissalAndSeedNcrIgnoredStatus` apply）

- **背景（用户拍板「加状态档 + 删忽略台账」）**：09-12 引入的「忽略台账 + 合计快照 + 自动复活 + 恢复」机制被判为过度设计——忽略本质就是「这个组不开完整 NCR」，用**一张状态=忽略的 NCR** 表达即可，既少一张表、少两端点，也天然复用 `Ncr.SourceGroupKey` 排重（**排重不看状态**）。
- **内容（结构变更 + 幂等种子，无业务存量回写）**：
  - ① **`DROP TABLE [NcrPendingDismissal]`**（含唯一索引 `UK_NcrPendingDismissal_Group`）。
  - ② **`EnumDisplayDefinitions` 幂等插入** `EnumKey=N'NcrStatus'` / `Value=N'Ignored'` / `DisplayName=N'忽略'` / `DisplayOrder=4`（`IF NOT EXISTS` 守卫；`DbInitializer` 的 `!Any()` 种子对存量库不生效，故补数据迁移）。
  - ③ **`Ncr.Status` 值域 3 → 4 档**：新增 `Ignored`；**列结构/长度不变**（`nvarchar(15)` + `HasConversion<string>`，最长值 `Processing` 10 字符），**无 DDL**。
- **影响行数（复查）**：`NcrPendingDismissal` 真库 **0 行**（建表 09-12 至删表 09-13 间无线上写入）→ **删表无状态丢失**；`EnumDisplayDefinitions` **+1 行**；`Ncr` 表**零行变更**（仅值域扩展）。
- **⚠️ 撤销忽略的现行做法**：直接编辑/删除那张 `NcrStatus.Ignored` 的 NCR（`Ncr.SourceGroupKey` 随之释放，该组重新出现在待处理列表）——**无 restore 端点**。
- **⚠️ 迁移 ID `20260912192419` 已记入真库 `__EFMigrationsHistory`**；云库 `MES` 随下次部署 API 启动自动 Migrate 自动重放。**⚠️ 配套：后端 dll + WASM 必须与新迁移同批发布**——旧 dll 仍读 `NcrPendingDismissal` 并暴露 dismiss/restore 端点（表已删 → 运行时报错）；旧 WASM 仍渲染「显示已忽略」开关与已忽略表。
- **⚠️ 若线上曾点过「忽略」**（真库为 0 行，但线上库可能有），那批组会重新出现在待处理列表 → 按新流程重新「忽略」即可，**勿当数据丢失**。

### 2026-09-12：NCR 待处理重构 —— 新增忽略台账表 `NcrPendingDismissal` + `Ncr` 让步放行 3 列 / 组键 + 阈值参数 8 项收敛为 2 项（真库 zhou/MESMN 经 EF 迁移 `20260912071847_AddNcrPendingDismissalAndConcession` apply；**⚠️ 其中 `NcrPendingDismissal` 表已于 2026-09-13 删除，见上条**）

- **背景（用户拍板「全按默认」）**：NCR「待处理批次」由单一列表改为**两组**——「正常提交」（来源=不合格反馈，无条件列出）与「**超阈值遗漏**」（来源=过程检验/成品检验，**组级一条**：组内不合格合计 = 让步放行支 + 各流向支，**合计 > Count 且 合计÷组内总数 > Percent**，均**严格大于**）。超阈值遗漏组支持**人工忽略**（可不建单），并**数据变多时自动复活**。
- **内容（结构变更 + 幂等种子 + 参数清理，无业务存量回写 / 无业务删除）**：
  - ① **新建 `NcrPendingDismissal` 表**（`SourceType` nvarchar(30) / `ProductionBatchId` int / `InspectionType` nvarchar(30) 默认空串 / `InspectionItem` nvarchar(30) 默认空串 / `ProcessName` nvarchar(50) 默认空串 / `DismissedQuantity` int（**忽略时的合计快照**）/ `Remark` nvarchar(200) 可空 + BaseEntity 四列），含**唯一索引 `UK_NcrPendingDismissal_Group`**（`SourceType, ProductionBatchId, InspectionType, InspectionItem, ProcessName` = 组定位键，与列表行 `GroupKey`、`Ncr.SourceGroupKey` 逐段一致）。
  - ② **`Ncr` 表新增 4 列**：`ConcessionQuantity` int 可空（让步放行支数）、`ConcessionWeight` int 可空（让步放行重量）、`ConcessionRemark` nvarchar(500) 可空（让步说明，**组内去重拼接**）、`SourceGroupKey` nvarchar(120) 可空（**来源待处理组键**，系统回写，供同组避免重复建单）。
  - ③ **阈值参数 `NcrThreshold` 由 8 项收敛为 2 项**：SQL **先删** `ParamKey NOT IN (Count, Percent)` 的 8 个旧流向项（ReworkCount/ReworkPercent/WarehouseCount/WarehousePercent/ScrapCount/ScrapPercent/ReturnCount/ReturnPercent），再**幂等插入** `Count=5`（绝对支数，严格大于）与 `Percent=0.10`（占比，严格大于，0.1=10%）；两段 INSERT 均 `IF NOT EXISTS` 守卫。
- **影响行数（复查）**：真库 `Ncr` 表存量行**新增 4 列全 NULL**（历史行无来源记录可回溯）；`NcrPendingDismissal` 为**新建表**、存量 0 行；`ConfigParameters` 中 `NcrThreshold` 分类**删 8 行 / 补插至 2 行**。**属结构变更 + 配置口径收敛，非业务数据清理**。
- **⚠️ 阈值口径变更（无需重放判据）**：旧 8 项流向阈值**已不再被任何代码读取**（服务层改为两项组级判据），删除它们**不影响任何业务数据**；**若整库恢复旧备份**，重放本迁移 SQL 即可重建（条件删除 + 幂等插入），**无需按业务重判**。
- **⚠️ 忽略台账是「人工决策留痕」而非业务数据**：整库恢复后该表为空 → 此前「已忽略」状态丢失、被忽略的超阈值组会重新出现在待处理列表（可接受，重新忽略即可），**勿当数据丢失**。**⚠️ 该表已于 2026-09-13 删除（见下条 09-13 注记）——本条仅存档原设计。**
- **⚠️ 迁移 ID `20260912071847` 已记入真库 `__EFMigrationsHistory`**；云库 `MES` 随下次部署 API 启动自动 Migrate 自动重放。**⚠️ 配套：后端 dll + WASM 必须与新迁移同批发布**——旧 dll 无 `NcrPendingDismissal` 映射、读旧 8 项阈值、不产出 `Bucket`/`GroupKey`/让步放行字段；旧 WASM 无两组待处理卡片、无让步放行三列、无忽略/恢复按钮。**另：数据工具 Ncr 导入模板表头已变更（列数 36→40、「流向」更名「次品流向」），旧模板须重新下载。**

### 2026-09-11：巡检 / 不合格反馈子模块建表 + 次品退货字段补列（真库 zhou/MESMN 经 EF 迁移 apply，非手动 SQL；**本条为 2026-09-12 集中补登防漏**）

> 前期 4 条已单独登记（见上：`20260911064754` / `20260911071723` / `20260911152837` / `20260911171626`），以下 **8 个迁移此前未登记，本次一并补登**——均为**纯结构变更（建表 / 加列 / 改名 / 幂等种子）**，无数据清理、无存量回写，故无判据需重放。

| 迁移 ID | 内容 | 数据影响 |
|---|---|---|
| `20260911055011_AddQualityDefectReturnFields` | `ProcessInspection` +`DefectReturnQuantity`/`TheoreticalReturnWeight`；`FinalInspection` +`DefectReturnQuantity`/`DefectReturnWeight` | 纯加列，存量 NULL |
| `20260911055600_AddWorkOrderExecutionDefectReturnWeight` | `WorkOrderExecutionSummary` +`ProcessInspectionReturnWeight` / `FinalInspectionReturnWeight` | 纯加列，存量 NULL（快照字段，需重算才回填） |
| `20260911055929_SeedDisposalMethodReturnAndNcrReturnThresholds` | `EnumDisplayDefinitions` 加 `DisposalMethod.Return`（退货，DisplayOrder 4）；`ConfigParameters` 加 `NcrThreshold.ReturnCount=5` / `ReturnPercent=0.05` | 幂等种子（`IF NOT EXISTS` 包裹），各 1~2 行 |
| `20260911060250_AddQualityProcessTrackingDefectReturnQuantity` | `QualityProcessTracking` +`DefectReturnQuantity`（int NOT NULL DEFAULT 0） | 纯加列；存量行落 0，如需按新口径重算走「成检追踪刷新」入口 |
| `20260911083807_AddNonconformingFeedback` | **新建** `NonconformingFeedback` / `NonconformingFeedbackAttachment` 两表（含索引与 FK 级联） | 新建表，存量 0 行 |
| `20260911121230_AddInspectionPatrol` | **新建** `InspectionPatrol` / `InspectionPatrolItem` / `InspectionPatrolAttachment` 三表 | 新建表，存量 0 行 |
| `20260911130718_RenameInspectionPatrolEquipmentCodeToName` | `InspectionPatrol.EquipmentCode` → `EquipmentName`（纯 `RenameColumn`） | **零行变更**（建表迁移 `20260911121230` 原名 `EquipmentCode`，本迁移仅改名不搬数据） |
| `20260911144334_AddInspectionPatrolRectificationOperator` | `InspectionPatrol` +`RectificationOperator`（nvarchar(200)，整改人） | 纯加列，存量 NULL |

- **⚠️ 迁移 ID 均已记入真库 `__EFMigrationsHistory`**；云库 `MES` 随下次部署 API 启动自动 Migrate 自动重放（`CreateTable`/`AddColumn` 幂等由 EF 迁移仅执行一次保证；两个种子 SQL 自带 `IF NOT EXISTS` 守卫）。
- **⚠️ 配套铁律**：**后端 dll + WASM 必须与本批迁移同批发布**（旧 dll 无新实体映射、旧 WASM 无巡检/不合格反馈页面与照片入口）。
- **⚠️ 整库恢复（`08-restore-db.ps1`）后**：以上建表/加列会随新 API 启动自动重放，勿当异常；**唯一需人工注意**的是上述已登记的四条中 `IsHandled` 删列与 `Ncr` 5 档回写（见对应条目）。
- **⚠️ 磁盘附件目录**：巡检 / 不合格反馈 / 过程检验 / 成品检验四类照片统一落 `attachments/`，覆盖 `api\` 时须保留。

### 2026-09-08：客户档案 CustomerProfile 清理「从未下过订单」单位（真库 + 云库均剩 21 条）
- **真库 zhou/MESMN**：早于本日已清理至 21 条（当时未登记，本条补登防漏）。
- **云库 MES（zhz.js.cn）**：2026-09-08 执行 DELETE 清理 268 条（289→21）。判据 = CustomerProfile 的 `(Salesman, EndCustomer)` 从未在任何 SalesOrder 的 `(Salesman, EndCustomer)` 命中（与客户管理统计口径一致；真库同判据验证删除后剩余恰 21，与真库一致）。用户拍板不备份直接删，执行成功 `Deleted=268 / Remaining=21`。
- **客户管理现 21 条为预期结果**，勿当数据丢失。若需复现/再清理判据：
  ```sql
  DELETE cp FROM CustomerProfile cp
  WHERE NOT EXISTS (SELECT 1 FROM SalesOrder so
     WHERE LTRIM(RTRIM(ISNULL(cp.Salesman,''))) = LTRIM(RTRIM(ISNULL(so.Salesman,'')))
       AND LTRIM(RTRIM(ISNULL(cp.EndCustomer,''))) = LTRIM(RTRIM(ISNULL(so.EndCustomer,''))));
  ```

### 既有历史：2026-09-06 批次出缸归位（真库）
> 真库 `zhou/MESMN` 执行 217 条出缸按(批次号,工段)组 1:1 归位（孤儿完工入缸 224→7），备份表 `PicklingInRecord_bak_20260906` / `PicklingOutRecord_bak_20260906` 保留（源自原「数据提醒」小节，已收编于此）。

## 环境问题注记（与代码发布无关；问题消失后删除本节）

> 本节记录「非项目缺陷、纯运行环境导致」的问题，目的是**避免重复排查**。与上方发布批次、上方「数据变更注记」均相互独立；问题消失（如浏览器修复）后**直接删除本节**。

### 2026-09-13：Edge 153 打印预览崩溃 —— 本机 Ctrl+P 报 `media_size` TypeError，**非项目缺陷**

- **现象**：本机 Edge 打开 Blazor 页面按 Ctrl+P 报 `TypeError: Cannot read properties of undefined (reading 'media_size')`，打印预览异常；线上 `zhz.js.cn` 一切正常。
- **根因**：**Edge 于 2026-09-13 02:26:51 自动由 `152.0.4191.66` 升级至 `153.0.4234.32`**。Edge 打印预览本身的 WebUI（`edge://print/`）在 `updateSettingsValues(e)` 中只对 `e.media_size` 做了空值保护、**未保护参数 `e` 自身**，在「Edge 153 × Debug 构建的 Blazor WASM 页面」这一组合下抛错。**与项目代码无关**——打印链路 `print.js` 等 5 个脚本 + MudBlazor CSS，本机 = 线上 = 发布包，**逐字节 md5 一致**。
- **实测矩阵**（同一台电脑、同一页面、同一台打印机）：

  | 浏览器 | 页面构建 | 服务 | 结果 |
  |---|---|---|---|
  | Edge **152** | **Debug** | 本机 DevServer `:5001` | **不崩** |
  | Edge 153 | **Debug** | 本机 DevServer `:5001` | **崩** |
  | Edge 153 | Release | 本机 DevServer `:5001` | 不崩 |
  | Edge 153 | Release publish | 本地静态服务 `:8099` | 不崩 |
  | Edge 153 | Release publish | nginx（线上） | 不崩 |
  | Chrome | Debug | 本机 DevServer `:5001` | 不崩 |

  唯一变量 = **Edge 主版本**。复现路径：用 `…\Microsoft\Edge\Application\152.0.4191.66\msedge.exe --user-data-dir=<临时目录> --remote-debugging-port=9333` 打开 `https://localhost:5001/` 按 Ctrl+P（2026-09-13 实测正常）。
- **处置**：**切勿为此修改 `print.js`** —— 那是给浏览器自身缺陷打补丁，Edge 修复后会变成项目里的死代码。本机需用 Edge 打印时改用 **Chrome** 或 **`dotnet run -c Release`**；**线上不受影响，无需任何动作**；`edgeupdate` 服务为 Automatic，微软修复版发布后自动生效。
- **影响面**：仅「本机开发 + Edge + Debug 构建」这一种组合，**线上零影响**。

## 维护追加区

（后续新增「本机已改、想暂缓上线」的内容在此补一行：文件 / 改动简述 / 静态 or WASM / 理由）
