# 本机与线上发布差异待办（短期跟踪）

> **性质**：记录「本机代码已改、线上 `zhz.js.cn` 尚未发布」的内容，供下次增量发布一次性带上。
> **维护规则**：每发布一次勾掉对应行、更新日期；清单全空时**删除本文件**，避免成为过期文档。
> 与部署体系关系：增量发布方法见 [`05_部署与运维.md`](05_部署与运维.md) 第四章；首装见 `14_部署执行单.md`。

## ✅ 已全部上线（截至 2026-09-08）

> 本文件当前**无「本机已改、暂缓上线」代码项**：批六~批九已随 **2026-09-08 `mes-deploy-20260908.zip`** 上线发布（`api\` 覆盖 + `web\` 覆盖 + `nssm restart MES.API`，云库启动自动 Migrate 应用批八两迁移 `20260907113212`/`20260907122038`），涵盖：
> - 批六 全站用户可见命名整理（系统名/计件标准/工单二级分组/订单列表折叠卡）；
> - 批七 订单两列表页列分组与默认显隐；
> - 批八 订单项次计价金额字段（计价单位/单价/总价）+ 打印双模式；
> - 批九 客户在产口径 + 变更日期默认隐藏/技术要求按钮 + 移动菜单二级分组展开修复。
> 早前 2026-09-07 `mes-deploy-20260907_full.zip`（09-06 三批 + 批四 + 批五）亦已上线。
> 各批次细节保留于下方 ✅ 节供追溯；**数据库层数据清理见文末「数据变更注记」，非代码发布项，勿与代码批次混淆**。
> ⚠️ **2026-09-10 更新（批十~二十四已整体重打包待上传，最新包=0200）**：09-08 发布后本机又新增改动，已登记为**批十~批二十四**，见文末「维护追加区」。**2026-09-10 已用 `03-publish-local.ps1` 全量重打包 `publish\mes-deploy-20260910_0200.zip`（68,738,896B，**含 09-10 追加的「报表系统→报表总览」拍平**，为最终待上传包；早前 `mes-deploy-20260910.zip`(01:36，无拍平) 与 `mes-deploy-20260909.zip` 均被其取代，勿上传）**——内含批十~批十三（替代更早 0349 包，内容已被覆盖）及批十四~批二十四全部改动（菜单整序：订单在库成品改名/报表总览拍平置首页下/数据工具移至参数表后）。线上云库重启 API 将自动 Migrate 补齐 3 个迁移：`20260909060707_AddPurchaseAndSubcontractPricing`/`20260909094550_AddOutsourceVendorProfile`/`20260909112729_AddSectionOutsourcePricing`。**数据清理注记「云库供应商清理（121→10）」已于 2026-09-10 在云库执行完毕（见文末注记），本包不含数据变更**。服务器发布步骤：zip 拷到 `C:\mes\` 后执行（管理员 PowerShell，09-apply-deploy 同款原语）：`nssm stop MES.API` → `Expand-Archive` 至 `_stage` → 清 `web\_framework` → 覆盖 `api\`、`web\` → `nssm start MES.API`（启动自动 Migrate）；旧产物可先归档 `C:\mes\publish\<日期>\` 留底。发布完成按维护规则勾除批十~二十四。

### 历史：2026-09-05 发布（scanfix）已上线记录

> 本文件所列待发布项已随 **2026-09-05 `mes-deploy-20260905-scanfix.zip`** 全量发布全部上线（`web\` 覆盖 `C:\mes\web` 带上全部 WASM 与静态改动；本次夹带后端改动，`api\` 覆盖 + `nssm restart MES.API`）。**按维护规则清单已全空，本文件可删除。**

| 批次 | 内容 | 线上状态 |
|---|---|---|
| AppMenu 单源树菜单 + 移动端响应式布局批 | 桌面/手机菜单单源树、MobileLayout 改动、`index.html` 移动端检测等 | ✅ 已上线（已手动发布） |
| 打印富布局 35 列误伤修复 | 见下方已发布清单 | ✅ 已上线（2026-09-05 scanfix 发布带上） |
| 手机扫码无反馈修复（scanner.js 双保险+加固） | 见下方已发布清单 | ✅ 已上线（2026-09-05 scanfix 发布带上） |

## ✅ 已发布清单：打印富布局 35 列误伤修复

| 文件 | 改动 | 性质 |
|---|---|---|
| `MES.Blazor/wwwroot/js/print.js` | `openPdfFromApi(apiUrl, jsonBody, skipColumnCheck)` 加第三参，富布局请求跳过 35 列校验 | 静态文件 |
| `MES.Blazor/wwwroot/index.html` | `js/print.js?v=13` → `v=14`（强制浏览器拉新版，绕过旧缓存） | 静态文件（⚠️ 同文件还含已上线的移动端改动，重复覆盖幂等，无害） |
| `MES.Blazor/Pages/Batches/ProcessCardPrint.razor` | 工艺卡打印调用传第三参 `true` | WASM（须重新 publish 编译） |
| `MES.Blazor/Pages/Quality/Ncrs.razor.cs` | NCR「打印选中」去掉后端忽略的 columns、只发 ids + 传第三参 `true` | WASM（须重新 publish 编译） |

**问题背景**：工艺卡/质保 NCR 报告属富布局打印，但请求体携带 `Columns`（区块字段布局参数，工艺卡默认约 97 个），被 `print.js` 通用 35 列校验误拦，请求根本发不出去；后端 `ProcessCardPrintHelper` 富布局本不受 35 列限制。全仓 73 处 `openPdfFromApi` 审计：富布局误伤仅此 2 处（其余富布局单据不带 Columns，安全）。
**后端结论**：MES.Api **零改动**、无 EF 迁移、无数据变更。

## ✅ 已发布清单：手机扫码无反馈修复（scanner.js 双保险 + 加固）

| 文件 | 改动 | 性质 |
|---|---|---|
| `MES.Blazor/wwwroot/js/scanner.js` | 原生 BarcodeDetector 探测改 `getSupportedFormats()` 含 `qr_code`（原 `'BarcodeDetector' in window` 误判伪支持）；原生连续 1.5s 无结果自动降级 jsQR；jsQR 预加载 + 降采样到 480 + 帧率门限 100ms + 全程 try-catch 防中断 | 静态文件 |
| `MES.Blazor/wwwroot/index.html` | `js/scanner.js` → `js/scanner.js?v=1`（首引版本号，强制浏览器拉新版） | 静态文件 |
| `MES.Blazor/Pages/Equipment/EquipmentScan.razor.cs` | `DotNetObjectReference.Create(this)` 改存 `_dotNetRef` 字段 + OnInitialized 创建 + Dispose 释放（原 Create 不存字段 → GC 后扫码命中 JS 回调打空对象、页面无反应） | WASM（须重新 publish 编译） |

**问题背景**：夸克/微信内置等国产浏览器内核常「BarcodeDetector 有 API 无底座」（Android 原生实现依赖 Google Play 服务），`detect()` 恒空且被旧代码 try-catch 吞掉 → 卡死在原生死分支，jsQR 永远轮不到 → 摄像头画面在动但一直「扫描中」不识别。附 jsQR 老代码无 try-catch，单帧抛错即永久中断扫描循环。
**验证**：浏览器开 `https://zhz.js.cn/js/scanner.js?v=1`；夸克/微信内置进扫码页对准二维码应在约 1s 内识别（伪支持会自动降级 jsQR，识别延迟略增属正常）。

## 发布记录（本次已完成）

- **2026-09-05**：随 `mes-deploy-20260905-scanfix.zip` 全量发布，`web\` 覆盖 `C:\mes\web`、`api\` 覆盖 + `nssm restart MES.API`，两批前端待办 + 扫码审计后端改动一并上线。
- 验证：`https://zhz.js.cn/js/print.js` 内容已含 `skipColumnCheck`；工艺卡/质保 NCR 打印不再弹「列过多」；扫码报工页新 UI 正常。

> 清单已全空。按维护规则本文件应删除，如后续再有「本机已改、暂缓上线」内容可复用下方维护追加区重建。

## ✅ 去油酸洗排序筛选 + 批次执行进度/显示卫生批（2026-09-06 登记，2026-09-07 随 full.zip 上线）

> 两批均为「本机代码已改、线上 `zhz.js.cn` 未发布」。发布方式同 2026-09-05：`web\` 覆盖 `C:\mes\web`（带全部 WASM 与静态）、`api\` 覆盖 + `nssm restart MES.API`（本批含后端改动）。**无 EF 迁移**。

### 批一：去油酸洗列表 排序/筛选补漏 + 班次筛选中文化（本次）

| 文件 | 改动 | 性质 |
|---|---|---|
| `MES.Services/Batch/PicklingService.cs` | ① 入缸分页给「完工日期」列筛(Field=CompleteDate)手工接出缸子表 `Any(CompleteDate.Date in)`（原走 ApplyFilters 反射不到入缸实体→无效）；② 入缸 GetFilterContextsAsync 补 InDate/CompleteDate 日期候选、完工 GetOutRecordFilterContextsAsync 补 CompleteDate 候选（列头 date 漏斗原「该列无可用筛选项」）；③ **完工分页 GetOutRecordsPagedAsync 整段补 `ApplyFilters`**（此前完工页所有列头筛选后端全不生效）+ 补全 5 个缺失排序键（工段/制造规格/工厂牌号/备注/更新时间） | 后端 dll（覆盖 api + restart） |
| `MES.Blazor/Pages/Batches/PicklingInRecords.razor.cs` | 筛选上下文 switch 补 `"Shift" => DisplayHelper.GetShiftTypeText(v)`（班次中文：DayShift→白班/MiddleShift→中班/NightShift→夜班） | WASM |
| `MES.Blazor/Pages/Batches/PicklingOutRecords.razor.cs` | 同上班次筛选中文映射 | WASM |

### 批二：批次执行进度可视化 + 去油/酸洗卡片显示卫生（上会话遗留）

| 文件 | 改动 | 性质 |
|---|---|---|
| `MES.Blazor/Pages/Batches/BatchDetail.razor` / `.razor.css` | 批次执行进度可视化（去油/酸洗等工序卡片：入缸出缸日期/完工标记增强）；断切/检验/冷轧拔/油管断/委外显示「支数」、去油酸洗不显支数 | WASM |
| `MES.Core/DTOs/Batch/BatchTrackingVisualDto.cs` | 执行进度可视化 DTO 新字段（含出缸日/完工区等） | WASM（编译入 Blazor） |
| `MES.Services/Batch/ProductionRecordService.cs` | 5c 富化聚合增强（SectionVisualDto 按(工序组,工段)出缸入缸聚合；去油酸洗「仅入缸无完工」保持形态） | 后端 dll |
| `MES.Blazor/Pages/Batches/PicklingInRecords.razor.cs` | 完工操作人列只显纯姓名（去工号） | WASM |
| `MES.Blazor/Pages/Quality/*`（化学/翻边/压扁/晶粒/硬度/晶间/金相/点蚀/拉伸 9 页 `.razor.cs`） | 记录列表操作人列只显纯姓名 | WASM |
| `MES.Tests/Services/Batch/ProductionRecordServiceTests.cs` | 新富化/聚合单测补充 | 测试（仅本地，不上云） |
| `docs/frontend_pages.md`、批次接口/模块设计、商业差距分析等 | 文档同步 | — |

### 发布包（合并包，2026-09-06 打包；2026-09-07 已由 mes-deploy-20260907_full.zip 替代上线）

本机已用 `03-publish-local.ps1` 产出**合并包** **`E:\MES项目\MES\publish\mes-deploy-20260906_1945.zip`**（`api\` + `web\` 两级，Blazor BaseUrl 已置空，**无 EF 迁移**）——按当前工作区**全量发布**，内含 批一（去油酸洗排序筛选）+ 批二（批次执行进度/显示卫生）+ **批三（横屏提示条）** 及 09-06 首页手机看板等全部现存改动，**替代早前 `mes-deploy-20260906_0408.zip`**（0408 内容已被本包覆盖，上传只需本包，勿再单独传 0408）。服务器操作：把 zip 拷到服务器（RDP）解压覆盖后重启：

```powershell
# 服务器（管理员 PowerShell）
Expand-Archive -Path C:\mes\mes-deploy-20260906_1945.zip -DestinationPath C:\mes\_stage -Force
Copy-Item C:\mes\_stage\api\* C:\mes\api\ -Recurse -Force
Copy-Item C:\mes\_stage\web\* C:\mes\web\ -Recurse -Force
nssm restart MES.API
```
> 可选：覆盖前把旧产物归档到 `C:\mes\publish\20260906\` 便于回滚；`web\` 覆盖建议先清旧 `_framework` 避免陈旧 dll 残留（非必须）。
> 包内核对：`web\index.html` 已含 `listenLandscapeHintRefresh`、`web\css\app.css` 已含 `.rotate-hint-banner`（横屏提示条静态改动到位）。

## ✅ 批三 手机端「横屏提示条」（2026-09-06 登记，2026-09-07 随 full.zip 上线）

> 全站移动策略「除首页+扫码流外一律横屏」的第一环引导：竖屏进入宽表页顶部提示「需横屏查看（旋转手机自动切换）」，可关闭并本地持久；夸克/微信等锁竖屏浏览器显示特制文案（去系统开自动旋转）。**已并入 `mes-deploy-20260906_1945.zip` 合并包（替代 0408），上传只用该包即可。** 无 EF 迁移、后端 MES.Services/MES.Api 零改动。

| 文件 | 改动 | 性质 |
|---|---|---|
| `MES.Blazor/Shared/LandscapeHintRule.cs` | **新建**：静态规则（宽页=AppMenu 全部叶子−首页−两个扫码窄叶，排除登录/进出货/维修点选窄页与 /create·/edit 表单路径；路径归一化 NormalizePath） | WASM（重新 publish 编译） |
| `MES.Blazor/Shared/LandscapeHintBanner.razor` | **新建**：提示条组件（localStorage `landscape_hint_dismissed` 持久关闭、锁竖屏特制文案、IAsyncDisposable 退订） | WASM |
| `MES.Blazor/Shared/MobileLayout.razor` | IsMobile 级联前插 `<LandscapeHintBanner />` | WASM |
| `MES.Blazor/wwwroot/index.html` | 追加多监听器总线 `_landscapeHintRefs`/`listenLandscapeHintRefresh`/`stopLandscapeHintRefresh`（orientationchange+resize，不抢 ResponsiveLayout `_resizeRef` 单槽） | 静态文件（⚠️ 同文件含既有改动，重复覆盖幂等无害） |
| `MES.Blazor/wwwroot/css/app.css` | 追加 `.rotate-hint-banner`（复用 `.notice-card` 蓝左边框浅蓝底） | 静态文件 |
| `MES.Tests/Components/LandscapeHintRuleTests.cs` | **新建**：7 条单测（含全菜单叶子防漂移） | 测试（仅本地，不上云） |

**验证**：`dotnet build MES.Blazor` 0 错 0 警；`LandscapeHintRuleTests` 7/7、`AppMenuTests` 7/7。真机人工验收（横屏切换/锁竖屏文案/关闭持久）待手机侧。

## ✅ 批四 订单进度树（2026-09-07 登记，已随 mes-deploy-20260907_full.zip 上线）

> 订单管理列表每行新增「订单进度树」入口：订单号 → 主号 → 四阶段分支（原锁/生产/成检/入库）→ 重量叶 的只读进度树页面。**无 EF 迁移、无数据变更**（纯读聚合）。

| 文件 | 改动 | 性质 |
|---|---|---|
| `MES.Core/DTOs/Order/OrderProgressTreeDto.cs` | **新建**：层级树 DTO（订单号→主号→四阶段分支→叶子，主号含 ScheduleStage/IsCompleted/UrgencyLevel/预计完成等） | 后端 dll |
| `MES.Core/Interfaces/Order/IOrderProgressQueryService.cs` | **新建**：`GetTreeAsync(salesOrderNo)` | 后端 dll |
| `MES.Services/Order/OrderProgressQueryService.cs` | **新建**：聚合 WorkOrderExecutionSummary 快照 + 成检计划 + 实时成品检验/入库分支；完结主号仅成品入库分支 | 后端 dll |
| `MES.Api/Controllers/Order/OrderProgressController.cs` | **新建**：GET `api/order/progress?salesOrderNo=`（`[Authorize(Roles = OrderView)]`，无该订单工单时返回空 data） | 后端 dll |
| `MES.Api/Program.cs` | 注册 `IOrderProgressQueryService`（+1 行） | 后端 dll |
| `MES.Shared/Constants/ApiEndpoints.cs` | 加常量 `OrderProgress = "api/order/progress"` | 共享常量（api+web 均编译） |
| `MES.Blazor/Pages/Orders/OrderProgress.razor` / `.razor.cs` / `.razor.css` | **新建**：路由 `/orders/progress`；「执行关注=已完结」主号默认折叠；字体分级最小 15px；执行关注/紧急性/预计完成高亮胶囊；完结态主号灰字 | WASM |
| `MES.Blazor/Services/Order/OrderProgressService.cs` | **新建**：按 salesOrderNo 拉取进度树 | WASM |
| `MES.Blazor/Pages/Orders/Orders.razor` | 每行操作区加「订单进度树」入口（`NavigateToProgress(order.OrderNumber)` 带单号跳转） | WASM |
| `MES.Blazor/Program.cs` | 注册 `OrderProgressService`（+1 行） | WASM |
| `MES.Tests/Services/Order/OrderProgressQueryServiceTests.cs` | **新建**：进度树聚合单测 | 测试（仅本地，不上云） |

## ✅ 批五 原锁备注显示名更名 + EF 迁移（2026-09-07 登记，已随 mes-deploy-20260907_full.zip 上线）

> 原锁备注（RawMaterialLockRemarkKey）四类显示中文由「A质量补料/B执行返整/C执行计划/D完善计划」更名「质量补料/生产返整补足/执行用料计划/完善用料计划」（仅表意，`DisplayOrder` 不变）。⚠️ **本批含 EF 迁移**——云端发布除 `api\`+`web\` 覆盖与 `nssm restart MES.API` 外，**还需在服务器 apply 迁移 `20260906160752`**（详见下方云端操作）。

| 文件 | 改动 | 性质 |
|---|---|---|
| `MES.Data/Migrations/20260906160752_RenameRawMaterialLockRemarkDisplayNames.cs` (+Designer) | **新建迁移**：`UPDATE DictValueDefinitions.DisplayName` 4 行（质量补料/生产返整补足/执行用料计划/完善用料计划），`Down` 恢复 A/B/C/D 旧名；纯 SQL 数据 UPDATE，无表结构变更 | 迁移（云端库 apply） |
| `MES.Core/Constants/RawMaterialLockRemarkKeys.cs` | `KeyToChinese` 改新中文；`ChineseToKey` 补新中文并**保留旧前缀名兼容映射**（存量 `A质量补料` 等仍可归一） | 共享常量（api+web 均重编） |
| `MES.Data/Entities/WorkOrder/WorkOrderExecutionSummary.cs` | `RawMaterialLockRemark` 字段注释对齐新中文（纯注释） | 后端 dll |
| `MES.Core/DTOs/Scheduling/RawMaterialLockPendingSummaryDto.cs` | 截日桶行类别注释对齐新中文（纯注释） | 后端 dll |

> **云端库 apply 迁移**（服务器管理员 PowerShell，方法见 `05_部署与运维.md`，须 `--context AppDbContext` 打 `zhou/MESMN`）：
> ```powershell
> cd C:\mes\api   # 或服务器上放置含 MES.Data 的发布目录
> dotnet ef database update --context AppDbContext -m 20260906160752_RenameRawMaterialLockRemarkDisplayNames
> nssm restart MES.API
> ```
> 迁移仅改四行显示名，apply 前/后代码因 `ChineseToKey` 兼容映射均能正确显示，先后无碍；`api\`+`web\` 覆盖重启后（映射常量两端均编译）即可见新中文。

> **发布包**：批四/批五连同 09-06 三批（去油酸洗/批次进度/横屏）已重新合并打包为 **`E:\MES项目\MES\publish\mes-deploy-20260907_full.zip`**（`api\`+`web\` 两级，353 条目已验证含 MES.Api.dll / index.html / blazor.webassembly.js），**替代并覆盖 1945 内容，上传只需本包**（勿再传 1945）。服务器操作：改 `scripts/deploy-windows/09-apply-deploy.ps1` 顶部 `$Zip` 为该包名后管理员运行（stop→覆盖 api+web→start，API 启动自动 Migrate 应用 `20260906160752`）；或按 `05_部署与运维.md` §4.3 顺序更稳：先只覆盖 `api\` → `nssm restart MES.API`（触发迁移）→ 再覆盖 `web\`。云库 `MES` 上迁移仅推进 4 行显示名 UPDATE，无结构变更、可回滚（覆盖旧代码产物即可）。

> ✅ **2026-09-07 已按此上线**：`mes-deploy-20260907_full.zip` 已覆盖 `api\`+`web\`、服务已重启、云库迁移 `20260906160752` 已应用。

## ✅ 批六 全站用户可见命名整理（2026-09-07 登记，2026-09-08 随 mes-deploy-20260908.zip 上线）

> 纯前端改名批：① 顶部栏系统名「MES制造执行系统」→「MES制造系统」+ 汉堡图标换列表式 ViewList（桌面/手机/登录页/PWA 清单）；② 工资结算「生产计件类别/成检计件类别」→「生产计件标准/成检计件标准」（菜单+列表/编辑页标题）；③ 工单管理菜单二级分组「工单操作 · 工单查询」+ 叶子改名（工单首页→工单生成、用料计划总览→用料计划、定尺工单定尺数据→定尺工单定尺）并同步各页标题/返回/打印文案；④ 订单列表页 h5「订单管理」→「订单列表」，折叠卡改名「完成预估及延期风险」且**删除**卡内「订单接单·出库及现负荷汇总」5指标×12月小表（仅保留两张交期预估表）。
> **性质：全部 WASM + 静态文件，MES.Api / 后端 dll 零改动、无 EF 迁移、无数据变更**——只需按 `05_部署与运维.md` 用 `03-publish-local.ps1` 重新 publish 出 `web\` 覆盖 `C:\mes\web`（重新 publish 编译）即可，无需覆盖 `api\`、无需 restart API。若随下一批合并发布，照常 `api\`+`web\` 一起覆盖无碍（api 覆盖幂等）。

| 文件 | 改动 | 性质 |
|---|---|---|
| `MES.Blazor/Shared/AppMenu.cs` | ① 工资结算叶「生产计件类别/成检计件类别」→「生产计件标准/成检计件标准」；② 工单管理二级分组「工单操作[工单生成/用料计划/工单需求调整]·工单查询[工单执行状况/定尺工单定尺]」；叶子改名 工单首页→工单生成、用料计划总览→用料计划、定尺工单定尺数据→定尺工单定尺 | WASM（重新 publish 编译） |
| `MES.Blazor/Shared/MainLayout.razor` | 桌面顶栏汉堡图标 `Icons.Material.Filled.Menu`→`ViewList` + 系统名「MES制造执行系统」→「MES制造系统」 | WASM |
| `MES.Blazor/Shared/MobileLayout.razor` | 同上图标换 ViewList + 抽屉面板头系统名改名 | WASM |
| `MES.Blazor/Pages/Login.razor` | 登录页系统名改名（标题/footer 版权行） | WASM |
| `MES.Blazor/wwwroot/manifest.json` | PWA 清单 `name` 改名 | 静态文件 |
| `MES.Blazor/Pages/Payroll/PieceRateProductionCategories.razor` | h5 →「生产计件标准（基准价 × 维档系数）」 | WASM |
| `MES.Blazor/Pages/Payroll/FinalInspectionCategories.razor` | h5 →「成检计件标准（基准价 × 维档系数）」 | WASM |
| `MES.Blazor/Pages/Payroll/PieceRateProductionCategoryEdit.razor` | 编辑页标题 →「编辑/新增 生产计件标准」 | WASM |
| `MES.Blazor/Pages/Payroll/FinalInspectionCategoryEdit.razor` | 编辑页标题 →「编辑/新增 成检计件标准」 | WASM |
| `MES.Blazor/Pages/WorkOrders/WorkOrders.razor` | h5「工单管理」→「工单生成」 | WASM |
| `MES.Blazor/Pages/WorkOrders/MaterialPlanOverview.razor`(+`.cs`) | h5「用料计划总览」→「用料计划」；打印/导出标题同步（用料计划-工单原锁-错疑投料 等） | WASM |
| `MES.Blazor/Pages/WorkOrders/FixedLengthWorkOrderView.razor`(+`.cs`) | h5「定尺工单定尺数据」→「定尺工单定尺」；打印标题同步 | WASM |
| `MES.Blazor/Pages/WorkOrders/WorkOrderDetail.razor` | 返回按钮文案「工单首页」→「工单生成」（跳转不变） | WASM |
| `MES.Blazor/Pages/WorkOrders/WorkOrderMaterialPlan.razor` | 返回按钮「返回用料计划总览」→「返回用料计划」 | WASM |
| `MES.Blazor/Pages/Orders/Orders.razor`(+`.cs`) | h5「订单管理」→「订单列表」；折叠卡「订单接单-出库及现负荷汇总」→「完成预估及延期风险」，删除卡内接单·出库·现负荷 5指标×12月小表（含其加载/打印页代码）；保留两张交期预估表逐格联动筛选+各自打印。⚠️ 后端 `GetInOutSummaryAsync` 与前端 `OrderService.GetInOutSummaryAsync` **保留**（报表总览仍用），勿误删 | WASM |
| `MES.Tests/Components/AppMenuTests.cs` | 新增「工单管理二级分组（操作/查询）」断言防漂移 | 测试（仅本地，不上云） |
| `docs/frontend_pages.md` 等 | 文档同步（V48） | — |

**验证**：`dotnet build MES.Tests` 0 错 0 警；`AppMenuTests`（含新增断言）15/15 通过。界面生效待发布后浏览器核对（改名/分组/折叠卡仅剩两张预估表）。

## ✅ 批七 订单两列表页列分组/默认显隐调整（2026-09-07 登记，2026-09-08 随 mes-deploy-20260908.zip 上线）

> ① 订单列表页：原「① 基本信息 + ② 合同交付」两组**合并为单组「① 基本信息」**（订单确认→②、订单执行→③），默认仅显示 订单号/签订日期/业务员/客户名称/交期截止/订单总重量/含项次数，其余同组字段默认隐藏。② 订单成品(实时库存)：默认隐藏 工单号/最终客户/产品标准/工厂牌号/最小长度/最大长度/仓库批次/来源/来料单位/剩余米数/物料类型 11 列。
> **性质：全部 WASM，MES.Api / 后端 dll 零改动、无 EF 迁移**——仅需重新 publish 出 `web\` 覆盖 `C:\mes\web`。⚠️ 两页列偏好 localStorage 键已升 `v2`（`col_prefs_orders_v2` / `col_prefs_orders_pending_delivery_v2`），用户重进页面即按新默认加载（旧 v1 键数据自动弃用）。

| 文件 | 改动 | 性质 |
|---|---|---|
| `MES.Blazor/Pages/Orders/Orders.razor.cs` | `GetAllColumnDefs()` 合并「基本信息+合同交付」为单组（GroupKey 重排 ① 基本信息/② 订单确认/③ 订单执行），最终客户/交期起始/延期罚款设 `Visible=false`；加 `ColumnPrefsVersion="v2"`，Save/Load 用版本键 | WASM |
| `MES.Blazor/Pages/Orders/PendingDelivery.razor.cs` | 11 列 `Visible=false`；加 `ColumnPrefsVersion="v2"`，Save/Load 用版本键 | WASM |
| `docs/frontend_pages.md`、订单模块详细设计 | 文档同步（列分组 3 组/默认显隐/v2 键） | — |

---

## ✅ 批八 订单项次计价金额字段（计价单位/单价/总价）（2026-09-07 登记，2026-09-08 随 mes-deploy-20260908.zip 上线，云库两迁移已由启动自动 Migrate 应用）

> ① OrderItem 新增 3 字段：计价单位（PerKg 元/Kg / PerMeter 元/米 / PerPiece 元/支，C# enum + EF string nvarchar(20)）、单价 decimal(18,2)?、总价 decimal(18,2)?。总价=单价×取量（计价单位决定：元/Kg→合同重量、元/米→米数、元/支→支数），前端自动计算且**可手动覆盖**（覆盖后不自动刷新，点「恢复自动」回到自动）；后端仅存传入值。② 新建/编辑/查看三态「结算方式」移到理算重量之后，尾列顺序=结算方式→计价单位→单价→总价→备注→操作；新建页**新增备注输入框**；详情页备注列默认隐藏（项次列偏好键 v2）。③ 小数收敛（应用层前后端一致）：米数/合同重量/理算重量**直接四舍五入保留 1 位**——前端输入即 round1、自动总价按 round1 取量计价、**服务层写库入口 SetOrderItemFields 兜底 round1**、**数据工具导入 OrderItem 两列同样特判 round1**；DB 列精度与存量不动（米数 18,2、合同/理算 18,3）；单价/总价 2 位。④ 销售订单确认单打印双模式 + 存量收敛（2026-09-07 追加，见下表 SalesOrderPrintHelper/迁移行）：列表页「打印选中订单」与详情页「打印」改 MudMenu 下拉，含金额确认单 / 不含金额确认单（请求体 `OrderPrintBatchRequest.IncludeAmounts`，默认 true）；两模式「结算方式」均自第 4 位移至**理算重量之后**，含金额显示 计价单位/单价/总价 并汇总**订单总价**，不含金额隐藏三金额列仅保留结算方式；打印重量/米数收敛 1 位小数；数据工具 OrderItem 实体追加 3 列。
> **性质：非全 WASM**——本次含**后端 dll 变更 + 2 个 EF 迁移 + WASM**。真库 zhou/MESMN 已 apply：`20260907113212_AddOrderItemPricingUnitPriceColumns`（加 3 列 + 按工厂牌号 含316→30/2开头→35/其它→25、总价=ROUND(单价×合同重量,2) 存量模拟回填 677 行）与 `20260907122038_BackfillOrderItemAmountsRound1`（存量 米数/合同/理算 round1 + 总价按计价单位取量重算，已核 0 多精度残留）。上线仅需 publish `web\` 覆盖 `C:\mes\web`（后端 dll + WASM 均在本仓已编译，无需再执行 database update）。⚠️ 详情页项次列偏好键升 v2（`col_prefs_order_detail_items_v2`）。

| 文件 | 改动 | 性质 |
|---|---|---|
| `MES.Core/Enums/PricingUnit.cs` | 新增枚举 PricingUnit（PerKg/PerMeter/PerPiece） | 后端 dll |
| `MES.Core/Helpers/EnumHelper.cs` | Register PricingUnit 中英（元/Kg/元/米/元/支）+ _enumRemarks | 后端 dll |
| `MES.Blazor/Helpers/DisplayHelper.cs` | GetPriceUnitText 三重载 + FormatWeight1(1位)/FormatMoney2(2位) | WASM |
| `MES.Data/Entities/Order/OrderItem.cs`、`AppDbContext.Order.cs` | 实体 + EF 配置（PricingUnit string 转换 / UnitPrice·TotalPrice decimal(18,2)） | 后端 dll |
| `MES.Data/Migrations/20260907113212_AddOrderItemPricingUnitPriceColumns.cs` | 3 AddColumn + 存量模拟回填 SQL | EF 迁移（须 apply 真库） |
| `MES.Core/DTOs/Order/`（OrderItemDto/CreateOrderItemRequest/SaveAllOrderRequest） | 3 DTO 增加 PricingUnit/UnitPrice/TotalPrice | 后端 dll |
| `MES.Services/Order/OrderService.cs` | 2 投影 + SetOrderItemFields 扩展 + 3 调用点 | 后端 dll |
| `MES.Services/Printing/SalesOrderPrintHelper.cs` | 确认单打印**双模式**（includeAmounts）：两种模式「结算方式」均移至理算重量之后，含金额显示 计价单位/单价/总价 并合计订单总价，不含金额隐藏三金额列仅留结算方式；重量/米数打印 round1、金额 2 位；表驱动列渲染防错位。⚠️列宽收敛——含金额常量列合计 708pt（< A4 横向内容宽 ≈782pt），否则超宽致 `DocumentLayoutException`（打印选中订单报错/前端被全屏面板遮罩「卡死」），新增列时务必复查常量列总宽 | 后端 dll |
| `MES.Core/DTOs/Order/OrderPrintRequest.cs` | OrderPrintBatchRequest 增 `IncludeAmounts`（默认 true，兼容旧调用） | 后端 dll |
| `MES.Core/Interfaces/Order/IOrderService.cs`、`MES.Services/Order/OrderService.cs` | PrintOrderAsync/PrintOrderBatchAsync 增 `bool includeAmounts = true` 透传 | 后端 dll |
| `MES.Api/Controllers/Order/OrderController.cs` | print-file 端点透传 `request.IncludeAmounts` | 后端 dll |
| `MES.Data/Migrations/20260907122038_BackfillOrderItemAmountsRound1.cs` | 存量 米数/合同/理算 ROUND(…,1) + 总价=ROUND(单价×取量,2)（PerKg→合同/PerMeter→米数/PerPiece→支数，UnitPrice 空不重算） | EF 迁移（真库已 apply） |
| `MES.Blazor/Pages/Orders/Orders.razor`+`.razor.cs`、`OrderDetail.razor` | 「打印选中订单」/「打印」改 MudMenu（含金额确认单/不含金额确认单），PrintSelected/PrintOrder 增 includeAmounts 参数 | WASM |
| `MES.Services/DataExchange/DataExchangeRegistry.cs` | OrderItem 条目加 计价单位(枚举)/单价(元)/总价(元) | 后端 dll |
| `MES.Blazor/Pages/Orders/OrderCreate.razor` | 列重排+新增备注输入+金额列自动计算/手动覆盖+1位/2位收敛 | WASM |
| `MES.Blazor/Pages/Orders/OrderDetail.razor` | 同上 + 备注列默认隐藏 + 项次列偏好 v2 | WASM |
| `docs/数据库设计/订单上下文数据库设计.md`、订单模块详细设计、接口设计、数据工具接口设计、`docs/frontend_pages.md` | 文档同步 | — |

**验证**：`dotnet build MES.Api`+`MES.Blazor` 0 错 0 警；OrderServiceNumericTests 定向单测 10/10 通过；真库迁移后 sqlcmd 抽查（677 行回填、TotalPrice 无 NULL、单价∈{25/30/35}、0 行多精度残留、TotalPrice=round1 合同×单价）已过。界面生效待发布后浏览器核对，2026-09-08 上线后核对通过。

---

## ✅ 批九 客户在产口径 + 订单列显隐/按钮 + 移动菜单二级分组展开修复（2026-09-08 登记，已随 mes-deploy-20260908.zip 上线）

> ① 客户统计「待在产」两桶（整单未入库 / 扣除部分入库）**仅统计主号未完成（阶段≠1）订单**：已主号完成(stage1)的订单即使欠产/未入库也视为订单结束、不再计在产（查询口径调整，无数据变更；D26Z22M9001 之类已入库未发货完成单正确归「待发货(整单)」）。② 订单列表「② 订单确认」组「变更日期」列**默认隐藏**，orders 列偏好键 **v2→v3**（若批七先上线则用户键 v2→v3 平滑强制重置默认显隐）。③ 订单详情页「技术要求」由纯图标改为**图标+文字按钮**。④ **移动菜单点二级分组「不展开反收起」修复**：MobileMenuNode 两处递归组分支的 ChildContent 自含 `<MudExpansionPanels MultiExpansion="true">`（充当新级联源遮蔽 MobileLayout 外层单选容器）——此前所有层级 `MudExpansionPanel` 经级联注册进同一个外层单选容器，点开二级分组（理化检测/工单操作）时单选容器连带收起其父级根分组 → 整树卸载（安卓真机与 Mac Safari 半屏窗口 <960px 判进移动壳均复现）。桌面壳(MudNavGroup)机制不同不受影响。
> **性质：本批含后端 dll（①）+ WASM（②③④），MES.Api 零接口改动、无 EF 迁移、无数据变更**——覆盖 `api\` + `web\` + `nssm restart MES.API` 即可（api 覆盖幂等）。

| 文件 | 改动 | 性质 |
|---|---|---|
| `MES.Services/Order/CustomerService.cs` | `BuildStatsAsync`：WipNone/WipPartial 两桶包 `if (!isCompleted)`（仅阶段≠1 计入在产；同文件 ShippedDone/StockDone 依 isCompleted 分桶逻辑不变） | 后端 dll |
| `MES.Tests/Services/Order/CustomerServiceTests.cs` | 新增「整单完成欠产或未入库不计在产」用例（stage1 欠产+未入库单 → StockCompleted 归桶、WipNone/WipPartial 全 0） | 测试（仅本地，不上云） |
| `MES.Blazor/Pages/Orders/Orders.razor.cs` | 「变更日期」列 `Visible=false`（仍可列显隐切换找回）；`ColumnPrefsVersion` v2→v3 | WASM |
| `MES.Blazor/Pages/Orders/OrderDetail.razor` | 「技术要求」MudIconButton → MudButton 图标+文字 | WASM |
| `MES.Blazor/Shared/MobileMenuNode.razor` | 两处组分支（无 Policy / 带 Policy）ChildContent 自含 `<MudExpansionPanels MultiExpansion="true">` 隔离各层级 panel 级联容器 | WASM |
| `docs/接口设计/订单上下文接口设计.md`、`docs/frontend_pages.md` | 在产两桶口径注记（仅主号未完成订单计入） | — |

**验证**：`dotnet build MES.Api`+`MES.Blazor` 0 错 0 警；CustomerServiceTests 相关用例通过。在产口径可先用真库 sqlcmd 抽查 KH0299（改造前待在产(扣除部分入库) 1单/1kg/25元 → 改造后 0单；待发货(整单) 保持 3单/458kg/11855元）；移动菜单二级分组展开 2026-09-08 上线后真机/浏览器人工验收正常（用户确认）。

---

## 数据变更注记（数据库层清理，非代码发布；防漏必读）

> 本节记录「在真库 zhou/MESMN 或云库 MES 上直接执行的数据清理/修复」，与代码发布批次相互独立。**凡日后做此类数据变更动作，必须在本节补登**（含判据/影响行数/日期），防止未来把「清理后的少数数据」误判为丢失，或整库恢复（`08-restore-db.ps1`）后不知需按判据重清。
> 若从历史备份恢复真库或云库，可能重现下列已清理数据，须按判据再次清理。

### 2026-09-09：供应商档案 SupplierProfile 清理「无任何采购/委外往来」单位（真库 zhou/MESMN 与云库 MES 均已执行 121→10）
- **内容**：真库 zhou/MESMN 供应商档案删除 111 行（121→10）。判据 = 该档案行**既无任何单据以 SupplierId 引用、也按「供应商名+物料分类」从未命中任何采购单(PurchaseOrder)或委外单(SubcontractOrder)的名+分类**（与供应商列表 ② 往来信息归行口径一致，归行键=PO.SupplierName+PO.MaterialCategory、WW.SupplierName+WW.OutMaterialCategory）。两子类：106 行名称从未出现在任何单的纯死档案 + 5 行同名供应商另有其它分类在采购、但其本分类行无单（江苏银拓/远晟/正驰、浙江骏达/永成创岳 的 OrderFinished 空档案），用户拍板 111 行全删。全库 SupplierProfile 的 FK 引用方仅 PurchaseOrder.SupplierId / SubcontractOrder.SupplierId，判据已排除 → 无 FK 阻断。
- **影响行数（复查）**：Deleted=111 / Remaining=10（其中 8 行 SupplierId+(名+分类) 双命中、1 行仅 SupplierId、1 行仅名+分类命中；建档 MaterialCategory 无空值，采购单仅 RoughTube 184 + CriticalFinished 33，OrderFinished/RoundBar/SemiFinished 分类从无采购单）。
- **云库 MES（zhz.js.cn，服务器本机 `localhost\SQLEXPRESS`）执行（2026-09-10）**：预览同判据 `pre_total=121 | will_delete=111 | remaining_after=10`，orphan_po_supplier=0 / orphan_ww_supplier=0；执行 `Deleted=111 / Remaining=10`，两孤儿复核均 0。执行前已备份 `C:\MSSQL\Backup\manual_before_supplier_clean_20260910.bak`（**Express 不支持 `BACKUP ... WITH COMPRESSION`，用 `WITH INIT`**；首版带 COMPRESSION 备份失败、同批 DELETE 因 1046 编译错整体未执行无副作用，修正后成功）。判据 SQL 无字符串字面量，无中文编码问题。
- **供应商管理现 10 行为预期结果**，勿当数据丢失（同旨 2026-09-08 客户清理剩 21）。若整库恢复（`08-restore-db.ps1`）重现 121 行，须按判据重清：
  ```sql
  DELETE sp FROM SupplierProfile sp
  WHERE NOT EXISTS (SELECT 1 FROM PurchaseOrder po WHERE po.SupplierId = sp.Id)
    AND NOT EXISTS (SELECT 1 FROM SubcontractOrder ww WHERE ww.SupplierId = sp.Id)
    AND NOT EXISTS (SELECT 1 FROM PurchaseOrder po WHERE po.SupplierName = sp.SupplierName AND po.MaterialCategory = sp.MaterialCategory)
    AND NOT EXISTS (SELECT 1 FROM SubcontractOrder ww WHERE ww.SupplierName = sp.SupplierName AND ww.OutMaterialCategory = sp.MaterialCategory);
  ```

### 2026-09-09：采购/委外计价回填（真库 zhou/MESMN 经 EF 迁移 `20260909060707_AddPurchaseAndSubcontractPricing` apply，非手动 SQL）
- **内容**：`PurchaseOrder`/`SubcontractReturnItem` 补 `PricingUnit` 列并幂等回填存量（**判据 = 单价 `IS NULL`**，不覆盖手输价）：采购计价单位=PerKg、荒管族(RoughTube/DefectRoughTube)=18 元/kg、成品族(Finished/OrderFinished/CriticalFinished/DefectFinished/SpecialDeliveryStatus)=26 元/kg、`TotalAmount=ROUND(Weight×UnitPrice,2)`；委外明细=PerKg、`ProcessUnitPrice=1.2`、`ProcessTotalAmount=ROUND(RequiredWeight×1.2,2)`。默认价常量与 `MES.Core/Helpers/MaterialPricingDefaults.cs` 人工一致。
- **影响行数（回填后复查）**：PurchaseOrder 计 223 行单价/总价全部填齐（回填后 `UnitPrice IS NULL`=0）；SubcontractReturnItem 计 2 行加工单价/金额全部填齐（回填后 `ProcessUnitPrice IS NULL`=0）。抽查 TotalAmount=Weight×18/26 全对。
- **⚠️ 迁移 ID `20260909060707` 已记入真库 `__EFMigrationsHistory`**；云库 `MES` 随下次部署 API 启动自动 Migrate 自动重放（幂等，判据单价 IS NULL）。若整库恢复旧备份，此迁移缺失时会自动重放回填，勿当异常；此 223/2 行全填价属预期终态。

### 2026-09-09：委外单位档案 OutsourceVendorProfile 建档（真库 zhou/MESMN 经 EF 迁移 `20260909094550_AddOutsourceVendorProfile` apply，非手动 SQL）
- **内容**：新表 `OutsourceVendorProfile`（委外单位 × 委外工段档案）建表 + 双唯一索引后，从存量 `SectionOutsource` 去重回填：`SELECT DISTINCT OutsourceVendor(VendorName), SectionName, IsInternal→IsWorkshop`，共 **12 行（WV0001~WV0012，其中本厂车间 IsWorkshop=4 行）**。**属新增档案数据（迁移回填），非清理**；真库 `__EFMigrationsHistory` 已记 `20260909094550`。
- **影响行数（回填后复查）**：Total=12 / DistinctRowKeys=12（无重复行键），与 SectionOutsource 239 行去重后的 12 个(单位×工段)组合一致。
- **⚠️ 云库 MES**：随下次部署 API 启动自动 Migrate 自动建表 + 回填（源 = 云库自己的 SectionOutsource 存量，去重数量与真库不同属正常）。若整库恢复旧备份，此迁移缺失时会自动重放建表回填，勿当异常。

### 2026-09-09：工段委外计价回填（真库 zhou/MESMN 经 EF 迁移 `20260909112729_AddSectionOutsourcePricing` apply，非手动 SQL）
- **内容**：`SectionOutsource` 补 `PricingUnit`/`UnitPrice`/`TotalAmount` 三列并幂等回填（**判据 = `UnitPrice IS NULL`**，不覆盖手输价）：厂外行（IsInternal=0）`PricingUnit='PerKg'`、`UnitPrice=CASE SectionName WHEN 'ColdRollDraw' THEN 1.4 ELSE 0.8 END`、`TotalAmount=CASE WHEN SendWeight>0 THEN ROUND(CAST(SendWeight AS decimal(18,3))×UnitPrice,2) ELSE NULL END`；厂内行（IsInternal=1，本厂车间档案 IsWorkshop）三列留空。默认价常量与 `MES.Core/Helpers/MaterialPricingDefaults.cs` 人工一致。
- **影响行数（回填后复查）**：SectionOutsource 总 239 行 —— 厂内 14 行三列全 NULL（`internal_no_price_null=0`）；厂外 225 行全填齐（`external_null_price=0`：ColdRollDraw 224×1.4 + 其余工段 1×0.8，`PricingUnit` 全 PerKg，`TotalAmount=ROUND(SendWeight×价,2)` 全匹配 `mismatch=0`）。
- **⚠️ 迁移 ID `20260909112729` 已记入真库 `__EFMigrationsHistory`**；云库 `MES` 随下次部署 API 启动自动 Migrate 自动重放（幂等，判据单价 IS NULL，源=云库自己的 SectionOutsource 存量，行数可与真库不同属正常）。若整库恢复旧备份，此迁移缺失时会自动重放回填，勿当异常。

### 2026-09-08：客户档案 CustomerProfile 清理「从未下过订单」单位（真库 + 云库均剩 21 条）
- **真库 zhou/MESMN**：早于本日已清理至 21 条（当时未登记，本条补登防漏）。
- **云库 MES（zhz.js.cn）**：2026-09-08 执行 DELETE 清理 268 条（289→21）。判据 = CustomerProfile 的 `(Salesman, EndCustomer)` 从未在任何 SalesOrder 的 `(Salesman, EndCustomer)` 命中（与客户管理统计口径一致；真库同判据验证删除后剩余恰 21，与真库一致）。用户拍板不备份直接删，执行成功 `Deleted=268 / Remaining=21`。
- **客户管理现 21 条为预期结果**，勿当数据丢失。若需复现/再清理判据：
  ```sql
  DELETE cp FROM CustomerProfile cp
  WHERE NOT EXISTS (SELECT 1 FROM SalesOrder so
     WHERE LTRIM(RTRIM(ISNULL(cp.Salesman,''))) = LTRIM(RTRIM(ISNULL(so.Salesman,'')))
       AND LTRIM(RTRIM(ISNULL(cp.EndCustomer,''))) = LTRIM(RTRIM(ISNULL(so.EndCustomer,''))));
  ```

### 既有历史：2026-09-06 批次出缸归位（真库）
> 真库 `zhou/MESMN` 执行 217 条出缸按(批次号,工段)组 1:1 归位（孤儿完工入缸 224→7），备份表 `PicklingInRecord_bak_20260906` / `PicklingOutRecord_bak_20260906` 保留（源自原「数据提醒」小节，已收编于此）。

## 维护追加区

（后续新增「本机已改、想暂缓上线」的内容在此补一行：文件 / 改动简述 / 静态 or WASM / 理由）

---

## ⏳ 批十~批十六 待发布（2026-09-09 登记，未打包）

> 判定依据：内容哈希 + 源码 mtime 晚于 `publish/mes-deploy-20260908.zip`（2026-09-08 00:39 生成）。**无 EF 迁移推进则按 `web\`(+`api\`) 覆盖 + restart 发布**；其中批十三含迁移 `20260908083206`，发布重启 API 启动自动 Migrate 会应用（真库若已手工 update 则自动跳过）。发布一次勾除一批。
>
> ✅ **2026-09-09 已打包（未上传）**：批十~十三整体已由 `03-publish-local.ps1` 打包为 **`publish/mes-deploy-20260909_0349.zip`**（353 条目 / 65.4M，含 `api\MES.Api.dll` `api\MES.Data.dll` `api\appsettings.json` + `web\index.html` `web\_framework` `manifest` `print.js` `app.css`），git 已先落盘快照 `09b9aa45`。**服务器发布步骤**：把 zip 拷至服务器 `C:\mes\` → 改 `09-apply-deploy.ps1` 顶部 `$Zip = "C:\mes\mes-deploy-20260909_0349.zip"` → 管理员运行（stop → 清 `_framework` → 覆盖 `api\`+`web\` → start）。云库 `MES` 启动自动 Migrate 应用 `20260908083206`（幂等 `IF NOT EXISTS` seed，真库 `zhou/MESMN` 已于 2026-09-08 含该迁移记录，无重复风险）。发布完成后按维护规则将下方批十~批十三勾 ✅/整段转为已发布并更新日期。

### ⏳ 批十 批次管理菜单拆分 + 生产执行核查页全系列 + 生产批次列表列显隐收敛 + 质量/批次 4 列表页默认显隐收敛 + 批次 5 处列表列显隐/列序二次收敛 + 质量 15 列表页默认列显隐收敛（frontend V59-V71 / 批次模块 V5.15-V5.26 / 质量管理模块 V8.8 §2.1/§2.6/§2.8-2.9/§2.11-2.20/§2.32-2.33）

> 「生产批次」+新增「生产执行核查」两入口（原批次首页错疑折叠卡整体迁出新页）；核查页错疑卡改名「错疑-生产批次执行」+默认折叠、列裁剪重组/过程检列/删搜索栏（页顶部模糊搜索+登记日期整行删除，PageState 去 Keyword/date）；**2026-09-09 续：生产批次列表列组重排 + 默认显隐收敛（7 组布局定稿）**——①组合并：原「产品要求」「质量要求」并入「工单信息」组、原「生产执行」并入「现执行状态」组、原「原始投料信息」12 字段并入「批次基本信息」组（自挂牌号后插入）；②「关联工单状态/流转判定/有效投料变更/成品切割跟踪」四核查组移为末尾 4 组整组默认隐藏（最终组序 批次基本信息→工单信息→现执行状态→四核查组，2026-09-09 二次调整现执行状态与工单信息对调、工单信息前置；默认仅显前三组）；**同日三调（字段级默认显隐收敛）**：批次基本信息仅显 生产编号/挂牌号/原料类型/来源牌号/来源规格/领料支数/领料重量/制造物品；工单信息仅显 订单号/主号/业务员/交货状态/工厂牌号/规格；现执行状态仅显 状态/截止执行日/当前工序/当前规格/当前工段/当前委外/下一工段（组内列序调：当前规格→当前工序后、当前设备→当前委外后、对应规格→下一工序后），同组余列均隐藏；列偏好键并入 `ColumnPrefsVersion="v2"`（`col_prefs_batches_v2`）→ **三调再升 `ColumnPrefsVersion="v3"`（`col_prefs_batches_v3`）** 强制新默认。**同日生产记录列表（/production-records）默认显隐收敛 + 列宽修复**：G1 执行信息 默认仅显 执行日期/生产编号/挂牌号/订单号/主号/工序名称/工段名称/工厂牌号/制造规格（工单号/执行序号 隐）；G2 产出数据 默认仅显 加工支数/加工重量/产类/平头数/断切倍数/预成切/长度状态/成品长度/符合工单长度/切后支数（设备名称/班次/操作人 隐）；G3 工艺参数/G4 追溯信息 整组隐；列偏好键并入 `ColumnPrefsVersion="v1"`（`col_prefs_production-records_v1`）强制新默认；**列宽根因**=生产记录表格带 `table-min-width` class + `--table-min-width:{总宽}px` Style（`.mud-table-root` min-width 锁全部可见列宽总和，此前 4 组全显约 27 列 >2900px 撑宽），生产批次无此设置故正常 → 已移除两处对齐生产批次，列宽由 `th` 宽提示自然撑开。验证 `dotnet build MES.Blazor` 0 警告 + ProductionRecords/Batches 定向单测 119 通过。**性质：WASM + 后端 dll（共享 `api/batch/list` 服务）**，无 EF 迁移、无数据变更。**同日续（4 列表页默认列显隐收敛，frontend V69）**：成检记录/过程检验/去油入缸/去油完工 4 张内联编辑列表页原**全列默认显示 + 表格带 `table-min-width` 强制最小宽**（列宽锁成可见列总和致过宽）——本次各页将次要/追溯/明细列改 `Visible=false` 默认隐藏（列选择器可开启）并移除 `table-min-width`+`--table-min-width` 两处，列宽由 th 提示自然撑开对齐生产批次，同时清理 `app.css` 已无引用的 `.compact-table.table-min-width` 规则。成检默认显 33 列（G5尺寸/G6压力/G7涡流超声波/G8辅助信息整组隐 + G1隐资格等级、G2隐生产类型/制造物品/制造状态/交货状态/最终用户/来料单位/长度状态、G3隐非定尺长度范围），过程检验默认显 24 列（G1隐执行序号、G4隐理论返整/入库/报废重+次品情况描述、G5辅助信息整组隐），入缸默认显 G1 核心列（隐执行序号/备注/数据来源/更新时间 + G2完工信息整组隐），完工默认隐备注/数据来源/更新时间；4 页列偏好键并入 `ColumnPrefsVersion="v1"`（`col_prefs_final-inspection_v1` / `col_prefs_process-inspection_v1` / `col_prefs_pickling-in-records_v1` / `col_prefs_pickling-out-records_v1`）强制新默认。验证 `dotnet build MES.Blazor` 0 警告 + FinalInspections/ProcessInspections/Pickling/ProductionRecords 定向单测 233 通过。**2026-09-09 再续（frontend V70 / 批次模块 V5.26）：批次管理 5 处列表默认列显隐/列序二次收敛**（隐藏列均可经列选择器开启）——① 去油/酸洗入缸：G1 增隐 工单号/设备名称/班次/操作人，**G2 完工信息 状态/完工日期 恢复默认显示、完工班次/完工操作人 默认隐藏**（v1 曾整组隐，本次按用户决策改回仅两操作信息隐），列偏好键 v1→v2；② 去油/酸洗完工：入缸信息 增隐 设备名称，列偏好键 v1→v2；③ 工段委外：委外信息 隐 要求收回日期/紧急，回收信息 隐 非正常回收(支)/非正常回收(重)/回收备注，列偏好键并入 v2；④ 委外回收：回收信息 隐 数据来源/更新时间，首并 `ColumnPrefsVersion="v1"`（此前第 2 参传 null）；⑤ 工艺卡流转卡打印：批次选择列表「当前规格」列移至「当前工序」后（对齐生产批次同组列序），列偏好键 v1→v2。验证 `dotnet build MES.Blazor` 0 错误 0 警告 + Pickling/SectionOutsource/OutsourceRecover/ProcessCardPrint/Batches 定向单测 125 通过。**性质：全部 WASM**，无 EF 迁移、无数据变更。**2026-09-09 再续（frontend V71 / 质量管理模块 V8.8）：质量管理 15 张列表页默认列显隐收敛**（隐藏列均可经列选择器开启）——① 过程检验 隐 工单号/设备名称/班次（列偏好键 v1→v2）；② 成检到料 隐 执行序/班次/数据来源/更新时间（首并 v1）；③ 成品检验 隐 设备名称/班次/工单号（更新时间已随 G8 隐；列偏好键 v1→v2）；④ 成检追踪 隐 工单号/最终用户/班次/更新日期（首并 v1）；⑤ 不合格报告 隐 更新日期（首并 v1）；⑥ 炉号登记 隐 更新日期（首并 v1）；⑦ 理化检测 9 列表页各隐 更新日期（9 页均首并 v1）。验证 `dotnet build MES.Blazor` 0 错误 0 警告 + 质量组件定向单测 6 通过。**性质：全部 WASM**，无 EF 迁移、无数据变更。

| 文件 | 改动 | 性质 |
|---|---|---|
| `MES.Blazor/Shared/AppMenu.cs` | 批次菜单拆分两入口（生产批次/生产执行核查） | WASM |
| `MES.Blazor/Pages/Batches/ProductionExecutionCheck.razor`+`.razor.cs`（新） | 生产执行核查只读页（错疑卡联动/列偏好 batchExecutionCheck_v6） | WASM |
| `MES.Blazor/Pages/Batches/Batches.razor`+`.razor.cs`、`BatchDetail.razor`、`BatchEdit.razor`、`BatchCreate.razor` 等 | 错疑折叠卡迁出/列偏好/可视化联动同步；**2026-09-09 Batches.razor.cs 列定义重排 7 组（批次基本信息[含原始投料自挂牌号后插入]/工单信息[含产品要求+质量要求]/现执行状态[含生产执行，与工单信息对调后置后]/关联工单状态/流转判定/有效投料变更/成品切割跟踪，后 4 核查组整组默认隐藏，GroupKey 连续 1-7）+ 三调字段级默认显隐（G1 仅显 8 字段含 5 原始投料、G2 仅显 6 字段、G3 仅显 7 字段且当前规格/当前设备/对应规格组内换位）+ ColumnPrefsVersion v2→v3（`col_prefs_batches_v2`→`col_prefs_batches_v3`）** | WASM |
| `MES.Services/Batch/BatchService.cs` | 共享列表通道（登记日期 To 端见批十一） | 后端 dll |
| `MES.Blazor/Pages/Batches/ProductionRecords.razor`+`.razor.cs` | **2026-09-09 列默认显隐收敛（G1 仅显 9/G2 仅显 10，G3 工艺参数/G4 追溯信息 整组隐）+ ColumnPrefsVersion null→v1（`col_prefs_production-records_v1`）+ 移除 `table-min-width`/`--table-min-width`（列宽对齐生产批次）** | WASM |
| `MES.Blazor/Pages/Quality/FinalInspections.razor`+`.razor.cs` | **2026-09-09 列默认显隐收敛（63列默认显 33：G1隐资格等级/G2隐 7 字段/G3隐非定尺长度范围，G5尺寸值·G6压力值·G7涡流超声波·G8辅助信息 整组隐）+ ColumnPrefsVersion null→v1（`col_prefs_final-inspection_v1`）+ 移除 `table-min-width`/`--table-min-width`**；**2026-09-09 再续（V71）：二调 隐 设备名称/班次（G1 检验执行）/工单号（G2 生产批次，更新日期已在 G8 整组隐）+ ColumnPrefsVersion v1→v2（`col_prefs_final-inspection_v2`）** | WASM |
| `MES.Blazor/Pages/Quality/ProcessInspections.razor`+`.razor.cs` | **2026-09-09 列默认显隐收敛（33列默认显 24：G1隐执行序号、G4隐理论返整/入库/报废重+次品情况描述、G5辅助信息整组隐）+ ColumnPrefsVersion null→v1（`col_prefs_process-inspection_v1`）+ 移除 `table-min-width`/`--table-min-width`**；**2026-09-09 再续（V71）：二调 隐 工单号（G1 生产批次）/设备名称、班次（G2 检验执行）+ ColumnPrefsVersion v1→v2（`col_prefs_process-inspection_v2`）** | WASM |
| `MES.Blazor/Pages/Batches/PicklingInRecords.razor`+`.razor.cs` | **2026-09-09 列默认显隐收敛 + 移除 `table-min-width`/`--table-min-width`；同日二调（V70）：G1 增隐 工单号/设备名称/班次/操作人（默认仅显 入缸日期/生产编号/挂牌号/订单号/主号/工序/工段/工厂牌号/制造规格/加工支数/加工重量/产类）+ G2 状态/完工日期 恢复默认显示、完工班次/完工操作人 隐（v1 整组隐改回）+ ColumnPrefsVersion v1→v2（`col_prefs_pickling-in-records_v2`）** | WASM |
| `MES.Blazor/Pages/Batches/PicklingOutRecords.razor`+`.razor.cs` | **2026-09-09 列默认显隐收敛 + 移除 `table-min-width`/`--table-min-width`；同日二调（V70）：入缸信息 增隐 设备名称 + ColumnPrefsVersion v1→v2（`col_prefs_pickling-out-records_v2`）** | WASM |
| `MES.Blazor/Pages/Batches/SectionOutsources.razor`+`.razor.cs` | **2026-09-09 列默认显隐收敛（V70）：委外信息 隐 要求收回日期/紧急，回收信息 隐 非正常回收(支)/非正常回收(重)/回收备注 + ColumnPrefsVersion v1→v2（`col_prefs_section-outsources_v2`）** | WASM |
| `MES.Blazor/Pages/Batches/OutsourceRecoveries.razor`+`.razor.cs` | **2026-09-09 列默认显隐收敛（V70 首并版本）：回收信息 隐 数据来源/更新时间 + 新增 `ColumnPrefsVersion="v1"` 并接 Save/Load 第 2 参（原传 null，`col_prefs_outsource-recoveries_v1`）** | WASM |
| `MES.Blazor/Pages/Batches/ProcessCardPrint.razor` | **2026-09-09 列序调整（V70）：批次选择列表「当前规格」列移至「当前工序」后（对齐生产批次同组列序）+ ColumnPrefsVersion v1→v2（`col_prefs_process_card_print_list_v2`）** | WASM |
| `MES.Blazor/Pages/Quality/MaterialReceiveChecks.razor`+`.razor.cs` | **2026-09-09（V71 首并版本）：隐 执行序/班次/数据来源/更新时间（G1 检验到料）+ 新增 `ColumnPrefsVersion="v1"` 并接 Save/Load 第 2 参（原传 null，`col_prefs_material-receive-checks_v1`）** | WASM |
| `MES.Blazor/Pages/Quality/QualityProcessTracking.razor`+`.razor.cs` | **2026-09-09（V71 首并版本）：隐 工单号/最终用户（G1）/班次（G2）/更新日期（G6）+ `ColumnPrefsVersion="v1"`（`col_prefs_quality-process-tracking_v1`）** | WASM |
| `MES.Blazor/Pages/Quality/Ncrs.razor`+`.razor.cs` | **2026-09-09（V71 首并版本）：更新日期 默认隐藏 + `ColumnPrefsVersion="v1"`（`col_prefs_ncrs_v1`）** | WASM |
| `MES.Blazor/Pages/Quality/FurnaceRegistrations.razor`+`.razor.cs` | **2026-09-09（V71 首并版本）：更新日期 默认隐藏 + `ColumnPrefsVersion="v1"`（`col_prefs_furnace-registration_v1`）** | WASM |
| `MES.Blazor/Pages/Quality/{ChemicalAnalyses,FlaringTests,FlatteningTests,GrainSizeTests,HardnessTests,IntergranularCorrosionTests,MetallographicTests,PittingCorrosionTests,TensileTests}.razor.cs` | **2026-09-09（V71，9 页首并版本）：理化检测 9 列表页各 更新日期 默认隐藏 + `ColumnPrefsVersion="v1"`（`col_prefs_{chemical-analysis,flaring-test,flattening-test,grain-size-test,hardness-test,intergranular-corrosion-test,metallographic-test,pitting-corrosion-test,tensile-test}_v1`）** | WASM |
| `MES.Blazor/wwwroot/css/app.css` | **2026-09-09 删除已无引用的 `.compact-table.table-min-width .mud-table-root` 规则及说明注释** | WASM 静态 |
| `MES.Tests/Components/Batch/ProductionExecutionCheckTests.cs`（新） | 页面无搜索栏等断言 | 测试（仅本地） |

### ⏳ 批十一 全仓日期范围搜索「至」端统一含当天（frontend V67 / 批次模块 V5.23）

> 27 处服务层 `XxxDateTo/EndDate/dateTo` 的 `≤ 至日 00:00` 一律改半开 `< Xxx.Value.AddDays(1)`（含「至」当天；datetime 列修复 off-by-one，date 列等价）。**性质：后端 dll**，无 EF 迁移、无数据变更。影响各列表页日期筛选：生产批次「登记日期」、质量各试验/成检/到料/化学/炉号/NCR/质量追踪、订单 签订/交期、工单 需求调整/执行、仓库 入库/出库历史、设备 报修。验证 `dotnet build MES.Api` 0 警告 + 定向单测 718 通过。

| 文件 | 改动 |
|---|---|
| `MES.Services/Batch/BatchService.cs`、`ProductionRecordService.cs` | CreatedTime/ExecDate To 端 |
| `MES.Services/Quality/*TestService.cs`、`FinalInspectionService.cs`、`MaterialReceiveCheckService.cs`、`NcrService.cs`、`QualityProcessTrackingService.cs`（15 文件） | InspectionDate/AnalysisDate/IncomingDate/ReceiveDate/ReportDate |
| `MES.Services/Order/OrderService.cs`、`OrderDemandAdjustmentService.cs`、`WorkOrder/WorkOrderService.cs` | SignDate/DeliveryStart/End/DeliveryDate |
| `MES.Services/Warehouse/InventoryService.cs`、`Equipment/RepairOrderService.cs` | InboundDate/OutboundDate/ReportTime |

### ⏳ 批十二 全站「文本居左·数值居中」表格样式 + 定尺联通强调（app.css 重构，2026-09-08）

> 静态内容哈希已证 `web/css/app.css` 与发布包不同（09-08 后追加：`.mud-table-cell.text-center` 数值居中兜底、`.flwo-emph-actual/.flwo-emph-compare` 定尺关键单元格强调）。**性质：静态 + WASM（各页数值类/auto-table 联动）**，重新 publish 覆盖 `web\` 即生效。

| 文件 | 改动 |
|---|---|
| `MES.Blazor/wwwroot/css/app.css` | 数值居中兜底类 + 定尺联通强调类 |
| `MES.Blazor/Pages/**`（全站表格页联动加 class，mtime 晚于包） | 数值/金额列居中 class 套用 |

### ⏳ 批十三 工单-定尺-用料投料核查上下文（读模型/页面/迁移）

> 工单上下文 09-08 后工作：用料计划拆分「用料投料核查」新页 + 实时关注/错疑卡联动、工单列表/需求调整/执行状况默认列显隐收敛、定尺联通视图读模型与**定尺切割偏差率种子迁移 `20260908083206`**（后端 dll mtime/迁移时间戳晚于包，未上线）。**性质：后端 dll + WASM + 1 个 EF 迁移（云库启动自动 Migrate apply）**；含配置/计划排程少量改动。

| 文件 | 改动 |
|---|---|
| `MES.Blazor/Pages/WorkOrders/MaterialInputConsistency.razor`+`.razor.cs`（新） | 用料投料核查新页 |
| `MES.Blazor/Pages/WorkOrders/*`（MaterialPlanOverview/OrderDemandAdjustment/WorkOrderExecution/FixedLengthWorkOrderView/WorkOrders 等） | 列显隐/拆分/联动 |
| `MES.Core/DTOs/WorkOrder/*`、`Interfaces/WorkOrder/*` | 错疑/定尺/联动 DTO 与接口 |
| `MES.Services/WorkOrder/FixedLengthWorkOrderService.cs`、`WorkOrderService.cs`、`WorkOrderExecutionService.cs`、`Scheduling/ProductionOverviewService.cs`、`Configuration/ConfigParameterService.cs` | 读模型/服务 |
| `MES.Api/Controllers/WorkOrder/WorkOrderExecutionController.cs`、`MES.Api/Program.cs` | Controller/DI |
| `MES.Data/Migrations/20260908083206_SeedFixedLengthCutDeviationRatio.cs`(+Designer)+ModelSnapshot、`Seed/DbInitializer.cs`、`MES.Core/Constants/CacheDefaults.cs` | EF 迁移 + seed + 常量 |
| `MES.Tests/**`（WorkOrders/MaterialPlanOverview/WorkOrderService/WorkOrderExecutionService/FixedLengthWorkOrderService 等） | 测试（仅本地） |
| `docs/frontend_pages.md`(V51-V58)、工单/仓库/计划排程模块设计、工单/计划排程接口设计 | 文档 |

### ⏳ 批十四 物料计价落地：采购订单 + 圆棒穿孔(委外) 计价单位/单价/总价（2026-09-09 登记，未打包）

> 用户口径：复用 `PricingUnit` 枚举默认 PerKg（元/kg）；采购回填/新建默认 荒管族→18 元/kg、其余成品族→26 元/kg，委外穿孔→1.2 元/kg；`MaterialPricingDefaults`（`MES.Core/Helpers/`）为唯一事实源，EF 迁移回填 SQL 常量人工同步。历史行已随迁移幂等回填（真库已 apply，见文末「数据变更注记」2026-09-09）。**性质：后端 dll + WASM + 1 个 EF 迁移（云库启动自动 Migrate apply）**，无额外数据清理。不打版，随下次 `03-publish-local.ps1` 发布，发布一次勾除。

| 文件 | 改动 |
|---|---|
| `MES.Core/Helpers/MaterialPricingDefaults.cs`（新） | 计价默认唯一事实源：DefaultPricingUnit=PerKg/荒管18/成品26/穿孔1.2/IsRoughTubeFamily/IsFinishedFamily/ComputeTotal 三分支 |
| `MES.Data/Entities/Materials/PurchaseOrder.cs`、`SubcontractReturnItem.cs`、`MES.Data/AppDbContext.Material.cs` | 实体加 `PricingUnit`（HasConversion\<string\> MaxLength 20，镜像 OrderItem） |
| `MES.Core/DTOs/Materials/PurchaseOrderDto.cs`、`SubcontractOrderDto.cs`、`SubcontractReturnItemListDto.cs` | DTO/请求加 PricingUnit；委外主表 DTO 加 `TotalAmount`（=Σ明细） |
| `MES.Services/Materials/PurchaseOrderService.cs` | 建/批建/更新按计价单位 ComputeTotal（元/kg 按 Weight 不再按支数）；空单价按分类默认 18/26；列表投影 + ToDto 补 PricingUnit；**ToPrintDict 打印投影补 PricingUnit/UnitPrice/TotalAmount 三 key（修复「打印选中」PDF 价格列空白）**；`PrintOrderCore` 打印护栏：渲染前校验「请求打印列 ⊆ ToPrintDict 已注册列」，缺列抛业务异常而非静默空白（防价格类同型复发） |
| `MES.Services/Materials/SupplierService.cs`、`MES.Services/StandardRegister/GradeMappingService.cs` | **删手写 ToPrintDict 白名单改 TablePrintHelper DTO 反射**（该模式全仓仅 3 处、此处 2 处纯冗余），仅最小 resolver 保口径：IsActive→启用/停用、Density→F4、SpecialMaterial→特殊/常规 |
| `MES.Services/Materials/SubcontractOrderService.cs` | 明细计价单位默认 PerKg、ProcessType=Piercing 空单价自动带 1.2、金额手填优先否则自动=单价×取量；委外主表 TotalAmount=Σ明细 |
| `MES.Services/DataExchange/DataExchangeRegistry.cs` | 采购/委外导入列注册加「计价单位→PricingUnit」（导入经 EF 直写不自动补默认价，须按模板显式填） |
| `MES.Blazor/Pages/Materials/PurchaseOrders.razor.cs`、`PurchaseOrderCreate.razor`、`PurchaseOrderDetail.razor` | 列表加 计价单位/单价/总价 3 列 + 页脚合计；新建自动带出默认、总价自动算；详情编辑态单价可改/总价只读；列表价格 3 列默认隐藏（列显隐勾选可显，ColumnPrefsVersion bump v2） |
| `MES.Blazor/Pages/Materials/SubcontractOrders.razor.cs`、`SubcontractReturnItems.razor.cs` | 委外单列表加「委外金额(元)」列；子项查询列表加 计价单位/加工单价/加工金额 3 列；**子项查询价格 3 列默认隐藏**（列显隐勾选可显，ColumnPrefsVersion bump v2） |
| `MES.Blazor/Pages/Materials/SubcontractOrderCreate.razor`、`SubcontractOrderDetail.razor` | 子表加 计价单位(Select 默认 PerKg)；ProcessType=Piercing 单价自动带 1.2；金额空自动=单价×需求重量、非空手输优先；详情子表 DetailPrefsKey bump v1 |
| `MES.Data/Migrations/20260909060707_AddPurchaseAndSubcontractPricing.cs`(+Designer)+ModelSnapshot | 加列 + 幂等回填（判据单价 IS NULL）；真库已 apply、云库随下次部署自动 Migrate |
| `MES.Tests/Core/Helpers/MaterialPricingDefaultsTests.cs`（新）、`Services/Materials/PurchaseOrderServiceTests.cs`、`SubcontractOrderServiceTests.cs` | 计价默认/总价口径/服务测试；打印回归：采购页面默认可见列全集 → PDF 成功 + 传未注册列 → 抛护栏异常、供应商/牌号映射列全集 → PDF 成功（定向 110 全绿） |

### ⏳ 批十五 供应商管理列表仿客户管理加 ② 往来信息 统计列组（2026-09-09 登记，未打包）

> 用户口径：以「供应商名称 + 物料分类」为基准归到档案行（列表每行 = 一档案行），采购 PO + 委外主表 WW **合并**进同 5 列：**累计出单/本年出单（N单·吨·万元 三要素）/已到货(净=毛到货−累计退货，吨)/待收货(吨)/本年累计退货(吨)**；委外基准=主表 OutWeight(发)/InWeight(收)（与 RecalcSubcontractStatus 同源），委外金额=Σ 子项 ProcessTotalAmount。页结构仿客户管理两分组 + 页脚当页合计 + 打印 Mode A（统计列为派生字段，Mode B DTO 反射取不到）。**性质：后端 dll + WASM**，无 EF 迁移、无数据变更。不打版，随下次 `03-publish-local.ps1` 发布，发布一次勾除。

> ⚠️ **2026-09-09 更正（按真实数据形态）**：初版以 `SupplierId` 归桶不准。真库核验：217 采购单中 **105 单的 `PO.MaterialCategory` ≠ 其 SupplierId 指向档案的分类**——SupplierId 只指同名供应商多档案之一（如宏达 SU0003/SU0070/… 跨 3-4 分类），单头分类各自独立 → 按 SupplierId 会把多分类混进单行。**归桶键改为 `PO.SupplierName+PO.MaterialCategory`（委外 `WW.SupplierName+WW.OutMaterialCategory`）精确分流到页内同名同分类档案行（OrdinalIgnoreCase）；单头分类在本页无对应档案行 → 不属本页任何行不计数**。委外子金额 group 限定到本页委外单 Id 集（避免全表扫）。既有 2 用例补单头 SupplierName/档案行分类后语义不变，另 +1 跨分类分流回归用例。

> ⚠️ **2026-09-09 二度更正（口径定案，AskUserQuestion 拍板）**：
> - 「已到货」改**真·本年到货**：`ArrivedWeight` = **本年仓库入厂批毛**（`InventoryBatch.InboundSource∈{Purchase,Subcontract}` 且 `InboundDate.Year==今年`，经 `SourceOrderNo` 归单到档案行）**− 本年退货**（退货出库年==今年，与「本年退货」列同源），负数截 0——**不再是累计净到货快照**，跨年退货不扣本年到货。列名 →「**本年到货[扣除退货]**」（Width 170→200）。
> - 「本年累计退货」更名「**本年退货**」（Width 170 保持）。
> - 「待收货」**保持跨年欠交快照不变**（非年份口径）；到货/待收/退货三列只显吨、**不加金额**（到货金额线性折算单价是精度假象，拍板不做；**后推翻——见三度更正**）。
> - 统计归桶：单次 `InventoryBatch` 扫描同时出 ① 退货归单 ② 本年到货毛归单（共享 batchNoToOrderNo/yearArrivalByOrder）；采购/委外两侧循环 `ArrivedWeight += yearArrivalByOrder[单号]`，收尾统一 `Arrived − YearReturn` 净额化。
> - 前端列显隐 `ColumnPrefsVersion="v2"→"v3"`（`col_prefs_suppliers_v3`），**默认隐藏 编码/联系人/联系电话/地址** 4 列（列显隐可打开）。

> ⚠️ **2026-09-09 三度更正（到货/待收补金额列，用户拍板）**：确认「金额用途=参考」，推翻二度「不加金额」。金额**按单号聚合、非笼统全局单价**（批十四默认模拟价 18/26 不用于折吨）——每张采购单用其 `TotalAmount`（整单货款）、委外单用 Σ子项 `ProcessTotalAmount`（加工费），乘各自单级重量份额认领到状态桶：**到货货款 = 该单金额×(该单到货净重/应到重)、待收货款 = 该单金额×(欠交重/应到重)**，与客户「成交金额阶梯认领」同构，将来逐单录真实金额公式不变。**范围：仅「本年到货[扣除退货]」「待收货」两列补金额（吨+万 组合文本），「本年退货」仍只显吨**；到货金额同扣本年退货货款（净，负数截0）；委外到货/待收金额=加工费×收回/欠交份额（分母=发出 OutWeight，委外属加工费体制非买料货款）。DTO +`ArrivedAmount`/`PendingAmount`；列宽 Arrived 200→220、Pending 170→200。

| 文件 | 改动 |
|---|---|
| `MES.Core/DTOs/Materials/SupplierProfileDto.cs` | **+11 统计字段**（Total/Year×Count·Weight·Amount、ArrivedWeight **本年到货净**(本年入厂批毛−本年退货，负数截0)、**ArrivedAmount 到货货款净**、PendingWeight 待收货、**PendingAmount 待收货款**、YearReturnWeight 本年退货） |
| `MES.Services/Materials/SupplierService.cs` | 桶聚合 `BuildTradeStatsAsync`（**按「供应商名+单头分类」归档案行，非 SupplierId**；以页内档案行建 (名,分类)→Id 索引，采购+委外一次拉取再分流；**单次 InventoryBatch 扫描同时出退货归单（ReturnOut.ReturnSourceBatchNo→批.BatchNo→批.SourceOrderNo(CG/WW)→订单(名,分类)落档案行，Chunk(1000)）与本年到货毛归单（InboundSource∈{Purchase,Subcontract} 且 InboundDate.Year==今年按 SourceOrderNo 累 InitialWeight）**；退货按 OutboundDate.Year 切累计/本年；**到货货款/本年退货货款/待收货款按单级认领 = 该单金额(采购 TotalAmount / 委外 Σ子项) × 对应净重/应到重**（桶循环加毛与退货货款、收尾 `Arrived=Max(0,毛−本年退货)` 重量与货款同步净额化））+ `AttachTradeStatsAsync` 回填 GetPagedAsync（11 字段）；`PrintSupplierListAsync`（Mode A，同客户 `PrintCustomerListAsync`） |
| `MES.Api/Controllers/Materials/SupplierController.cs` | +`POST print-list-file`（MaterialView，请求体 `OrderPrintListRequest`） |
| `MES.Blazor/Pages/Materials/Suppliers.razor`+`.razor.cs` | ① 基本信息 / ② 往来信息 两分组（col-g1/g2 + col-group-start 分隔 + 分组标题栏对齐 initGroupHeaders）；5 统计只读列（SortKey/FilterType null、不可编辑/排序/筛选；列标签：**本年到货[扣除退货]**/待收货/**本年退货**）+ `RenderStatText/BuildStatText`（吨/万 F1、全 0 →「—」）+ 页脚合计 `ComputePageSums`（反射 `_statFieldMap`：有单数或金额 → 组合文本吨/万，纯重量列才只显吨；到货/待收两列经 map 接 `ArrivedAmount`/`PendingAmount`）+ 打印改 Mode A（`print-list-file`）+ `ColumnPrefsVersion="v3"`（`col_prefs_suppliers_v3`，**默认隐藏 编码/联系人/联系电话/地址 → 默认 9 列可见**，强制老用户按新默认载入；列宽 Arrived 220/Pending 200） |
| `MES.Tests/Services/Materials/SupplierServiceTests.cs` | +3 用例：采购(部分/完成/强完)+委外子金额合并累计与本年（TotalCount=4/TotalWeight=20000/YearReturn=0/**Arrived=12000 = 本年入厂毛 8000+2000+委外收回 2000，去年批次不计**、Pending=3000、**ArrivedAmount≈24666.67/PendingAmount≈6333.33 按单认领**、d2 完成单仍计到货货款 3000）；退货链净扣（InboundSource=Purchase/**Arrived=4400 = 本年入厂毛 5000 − 本年退货 600，去年退货 400 不扣本年到货**、**ArrivedAmount=8800 = 毛货款 10000 − 本年退货货款 1200**、YearReturn=600 仅本年、Pending=1000、**PendingAmount=2000**）；**同名供应商跨分类分流回归**（+3 本年入厂批按单头分类分流，Finished/RoundBar 各 Arrived=6000/**ArrivedAmount=12000、PendingAmount=8000** 不混算）；组件 SuppliersTests 4 例不受列显隐/列名变化影响 |

### ⏳ 批十六 委外单位档案模块（批次管理·工段委外单位主档，建档/录入候选源）（2026-09-09 登记，未打包）

> 用户口径：批次管理「工段委外」的外协单位，仿「供应商管理」建独立档案 `OutsourceVendorProfile`，唯一差别是把「物料分类」维度换成「**委外工段**」，行键 = (委外单位名 × 委外工段)；纳入厂内车间（一二三四五厂）同档案打 `IsWorkshop` 标记（= 委外单 IsInternal 语义，仅冷轧拔开放，勾选自动锁工段 `ColdRollDraw`）；编码 `WV`+4 位流水自动生成；工段委外录入两处 MudAutocomplete 与扫码报工共用候选源 `SearchVendorsAsync` 改查**启用档案去重单位名**（仍保留 CoerceValue 自由输入兼容临时新单位/历史文本）；历史 239 行委外单文本不动，四元组重复检查/导入导出复合键/按单位汇总均不回卷。**性质：后端 dll + WASM + 1 个 EF 迁移（`20260909094550_AddOutsourceVendorProfile`，含存量去重回填 12 行，真库已 apply，见文末「数据变更注记」2026-09-09）**。菜单挂批次管理组（叶子「委外单位管理」`/outsource-vendors`，随组 BatchMenu，移动端单源自动带出）。不打版，随下次 `03-publish-local.ps1` 发布，发布一次勾除。

| 文件 | 改动 |
|---|---|
| `MES.Data/Entities/Batch/OutsourceVendorProfile.cs`、`MES.Data/AppDbContext.cs`、`AppDbContext.Batch.cs`（新） | 实体 + DbSet + Fluent：表 `OutsourceVendorProfile`，UK(VendorCode)=`UK_OutsourceVendor_Code`、UK(VendorName,SectionName)=`UK_OutsourceVendor_Name_Section`；SectionName 存英文 Key（`SectionKeys`）；IsWorkshop 默认 false、IsActive 默认 true |
| `MES.Data/Migrations/20260909094550_AddOutsourceVendorProfile.cs`(+Designer)+`AppDbContextModelSnapshot.cs`（新） | 建表 + 双唯一索引 + 存量回填：`SELECT DISTINCT OutsourceVendor, SectionName, IsInternal` FROM `SectionOutsource` → WV0001~WV0012，IsInternal→IsWorkshop（真库已 apply 12 行） |
| `MES.Core/DTOs/Batch/OutsourceVendorProfileDto.cs`、`MES.Core/Interfaces/Batch/IOutsourceVendorProfileService.cs`（新）；`MES.Shared/Constants/ApiEndpoints.cs`、`MES.Core/Constants/CacheKeys.cs` | DTO 三件套 + 接口（GetPaged/GetById/GetActive/Create/CreateBatch/Update/Delete/GetFilterContexts）+ 常量 `api/outsource-vendor`、过滤上下文缓存键 |
| `MES.Services/Batch/OutsourceVendorProfileService.cs`（新） | WV 编码自增（单建 `CodeGenerator`、批建按 max 流水预推）、行键唯一(名×工段，OrdinalIgnoreCase)冲突抛 `BusinessException`、批内重复校验、CRUD、`GetFilterContextsAsync`（IMemoryCache，`CacheKeys.OutsourceVendorProfileFilterContexts`） |
| `MES.Services/Batch/SectionOutsourceService.cs` | `SearchVendorsAsync` 候选改查启用档案（IsActive）去重单位名，其余消费方不变 |
| `MES.Api/Controllers/Batch/OutsourceVendorProfileController.cs`（新）；`MES.Api/Program.cs` | `[Route("api/outsource-vendor")]`：list/get/{id}/active/filter-contexts=`BatchView`，POST/batch/PUT=`BatchEdit`，DELETE=`BatchDelete`，无打印端点；DI 注册 |
| `MES.Blazor/Services/Batch/OutsourceVendorService.cs`（新）；`MES.Blazor/Program.cs` | 前端 Service + DI 注册（批次上下文组） |
| `MES.Blazor/Pages/Batches/OutsourceVendors.razor`+`.razor.cs`（新） | 列表 `/outsource-vendors`（BatchView）：8 列（VendorCode 默认隐藏、SectionName 中文下拉/筛选项、IsWorkshop 本厂/外协、IsActive 启用/停用 chip）列显隐/排序/筛选/搜索 + 内联编辑（`IsWorkshop` 勾选自动锁 `SectionName=ColdRollDraw` 且禁改）+ 删除 ConfirmDialog；列偏好 key `outsource-vendors` v1、PageState 同 key、方向键导航 |
| `MES.Blazor/Pages/Batches/OutsourceVendorCreate.razor`（新） | 新建 `/outsource-vendors/create`（BatchEdit）：多行批量表格（行号/单位名*/委外工段* 下拉/本厂车间 勾选锁冷轧拔/联系人/电话/启用/备注/删行），校验非空后 `CreateBatchAsync` |
| `MES.Blazor/Shared/AppMenu.cs` | 批次管理组「工段委外」后加叶子「**委外单位管理**」`/outsource-vendors` |
| `MES.Tests/Services/Batch/OutsourceVendorProfileServiceTests.cs`（新）、`MES.Tests/Components/AppMenuTests.cs` | +14 用例（WV0001 首档/连续自增/同单位同工段冲突/同单位不同工段放行/空名拒绝/批建 3 行自增/批内重复/与库内重复/改名撞车/正常修改/删除/关键字搜索/筛选上下文/**SearchVendorsAsync 仅启用档案去重**）+ 批次组「委外单位管理」叶子断言（URL 唯一、全树单实例）；定向 23 绿，Api/Blazor build 0 错 0 警 |

### ⏳ 批十七 工段委外加计价三字段 + 委外单位改档案驱动下拉（2026-09-09 登记，未打包）

> 用户口径（AskUserQuestion 两轮拍板）：① 计价单位共享 `PricingUnit` 枚举下拉、新建默认 PerKg（元/Kg）；② 总价 `MaterialPricingDefaults.ComputeTotal` 自动算但**手填优先**（服务端仅在 TotalAmount 空时自动算）；③ **删 SectionOutsource 手动 IsInternal 开关**，改由委外单位档案驱动——下拉选「本厂车间(IsWorkshop)」档案 → 行自动 IsInternal=true、Status=Virtual、三价格字段禁用留空（厂内仅冷轧拔开放）；④ 委外单位 PC 新建+列表内联编辑**仅列 active 档案、按行委外工段过滤，禁手输造新**（CoerceValue=false，档外先建档）；⑤ **扫码报工工段委外页一并改造**（去厂内开关+仅档案下拉，价格服务端按工段默认自动落库，手改留待办）；⑥ Create/Batch/Update 服务端全局校验——委外单位必命中档案且启用，档外一律拒绝先建档；**仅当委外单位实际被改才强制命中档案**，历史档外单位原值不改则不拦。默认单价唯一事实源：`ColdRollDraw`→1.4 元/Kg、其余工段→0.8 元/Kg（厂内行无价）。**本批撤销批十六「SearchVendorsAsync 仍保留 CoerceValue 自由输入」方案**（服务端方法/API 端点/前端方法删除，改走 `GET api/outsource-vendor/active` 全量档案）。**性质：后端 dll + WASM + 1 个 EF 迁移（`20260909112729_AddSectionOutsourcePricing` 真库已 apply 幂等回填 239 行，见文末「数据变更注记」2026-09-09）**。不打版，随下次 `03-publish-local.ps1` 发布，发布一次勾除。

| 文件 | 改动 |
|---|---|
| `MES.Data/Entities/Batch/SectionOutsource.cs`、`MES.Data/AppDbContext.Batch.cs` | 加 `PricingUnit?`（枚举字符串 MaxLength 20）、`UnitPrice?` decimal(18,4)、`TotalAmount?` decimal(18,2)（同采购/委外样板精度） |
| `MES.Data/Migrations/20260909112729_AddSectionOutsourcePricing.cs`(+Designer)+`AppDbContextModelSnapshot.cs` | 加列 + 幂等回填（判据 UnitPrice IS NULL）：厂外(IsInternal=0) → PricingUnit='PerKg'、UnitPrice=CASE SectionName WHEN 'ColdRollDraw' THEN 1.4 ELSE 0.8 END、TotalAmount=ROUND(SendWeight×UnitPrice,2) 仅 SendWeight>0；厂内保持三列 NULL；真库已 apply 239 行（225 厂外：224 ColdRollDraw×1.4 + 1 其它×0.8，14 厂内全空，TotalAmount 0 不匹配） |
| `MES.Core/Helpers/MaterialPricingDefaults.cs` | 加 `ColdRollDrawSectionUnitPrice=1.4m`、`OtherSectionUnitPrice=0.8m`、`DefaultSectionOutsourceUnitPrice(sectionKey)`（SQL 迁移常量人工同步，头部警示注释更新） |
| `MES.Core/DTOs/Batch/SectionOutsourceDtos.cs` | `SectionOutsourceDto`/`CreateSectionOutsourceRequest`/`UpdateSectionOutsourceRequest` 加 3 可空字段 |
| `MES.Services/Batch/SectionOutsourceService.cs` | Create/BatchCreate/Update 委外单位**档案驱动**（命中 (单位×工段) 档案 → IsInternal=IsWorkshop，忽略请求 IsInternal；厂内仅冷轧拔校验；未建档/已停用抛 BusinessException；**仅 vendor 实际变更才强制命中档案**，历史档外原值不改不拦）；计价默认+手填优先（厂内三价置 null；厂外 unit 空→PerKg、unitPrice 空→工段默认、total 空→ComputeTotal）；**删 `SearchVendorsAsync`**；GetPaged 投影/排序、PrintBatchAsync 补三键 |
| `MES.Api/Controllers/Batch/SectionOutsourceController.cs`、`MES.Shared/Constants/ApiEndpoints.cs`、`MES.Blazor/Services/Batch/OutsourceVendorService.cs` | 删 vendor-search 端点/前端旧方法；OutsourceVendorService 加 `GetActiveAsync`（`GET api/outsource-vendor/active` active 全量，前端按行工段过滤） |
| `MES.Blazor/Pages/Batches/SectionOutsourceCreate.razor` | 委外单位 MudAutocomplete 改档案 DTO（active 按行工段过滤、CoerceValue=false）；**删 IsInternal MudSwitch**（选 IsWorkshop 档案自动厂内只读提示）；**加三价单元格**（计价单位下拉默认 PerKg/单价/总价，厂外默认带出+总价自动算可手改，厂内禁用清空；列宽 ~110/110/130） |
| `MES.Blazor/Pages/Batches/SectionOutsources.razor`+`.razor.cs` | 委外信息组加 计价单位/单价/总价 三列内联编辑（厂内 Disabled）；委外单位编辑改档案 autocomplete（按 item.SectionName 过滤 active，档外历史值不改可保存）；IsInternal 列只读派生；`ColumnPrefsVersion` v2→v3（`col_prefs_section-outsources_v3`）；SaveEdit 映射新字段触发服务端重判定；**同日二调（追加）：计价三列默认隐藏**（列显隐勾选可显，`ColumnPrefsVersion` v3→v4，`col_prefs_section-outsources_v4`） |
| `MES.Blazor/Pages/Tools/ScanExecute.razor` | 扫码工段委外同步改造：委外单位 MudAutocomplete 改档案 DTO（按扫码工段过滤 active，含本厂车间档案）、`CoerceValue=false`；删 IsInternal MudSwitch（IsWorkshop 档案自动厂内只读提示）；不新增扫码价格输入（价格服务端按工段默认自动落库，手改留待办） |
| `MES.Tests/Services/Batch/SectionOutsourceServiceTests.cs`、`OutsourceVendorProfileServiceTests.cs`、`Components/Batch/SectionOutsourcesTests.cs` | seed 建对应 OutsourceVendorProfile 适配既有厂内/厂外用例（厂内一/二车间→IsWorkshop 档案，删请求 IsInternal 参数）；新增计价用例（冷轧拔默认 1.4/总价、Pickle 0.8、厂内三价 null、手填优先、档外/停用拒、Update 改档外拒、vendor 不改放行）；删 SearchVendorsAsync 用例；组件注册 OutsourceVendorService + 补新价列断言；**定向 69 绿**，Api/Blazor build 0 错 0 警 |

### ⏳ 批十八 委外单位管理列表仿供应商建 ② 往来信息 统计列组（2026-09-09 登记，未打包）

> 用户口径（AskUserQuestion 拍板 5 列·含金额 + 一并加打印选中）：委外单位档案列表（`/outsource-vendors`，批十六）仿「供应商管理」② 往来信息分组，加 **5 统计列 = 累计委外 / 本年委外（单数+发出吨+金额万，金额=发出单 `TotalAmount` 按发出日期分本年）/ 本年回收（吨，按回收日期取正常回收 `RecoveryWeight`）/ 在委外未回收（吨，净值）/ 本年退回（吨，非正常退回 `UnprocessedWeight`）**；回收/退回/在委外三列**只显吨**（`OutsourceRecovery` 无单价/金额，数据现实）。归桶键 = `(OutsourceVendor, SectionName)` **OrdinalIgnoreCase**；**排除厂内 `IsInternal`**（无价、Status=Virtual、永不回收，同 monthly-summary/读模型约定；IsWorkshop 档案行即厂内 → 累计 0 显「—」）。新单已强制命中档案，历史档外文本不属本页任何行不计。委外单据状态口径（服务端已维护可直接用）：`Recovered`=(正常+非正常回收重)≥发出重×0.99（config `WarehouseThreshold/OutsourceRecoveryRatio`），否则 `PendingRecovery`；`Virtual`=厂内。**性质：后端 dll + WASM**，无 EF 迁移、无数据变更，随下次 `03-publish-local.ps1` 发布一次勾除。

| 文件 | 改动 |
|---|---|
| `MES.Core/DTOs/Batch/OutsourceVendorProfileDto.cs` | ② 统计段 +9 字段：TotalOrderCount/TotalWeight/TotalAmount（累计全期）、YearOrderCount/YearWeight/YearAmount（本年，发出日期 SendOutDate.Year）、YearRecoveredWeight（本年回收 RecoveryDate.Year==今年 ΣRecoveryWeight）、PendingWeight（在委外净欠=当前 Status==PendingRecovery 行 Σ Max(0, SendWeight−Σ(Recovery+Unprocessed)全量)）、YearReturnWeight（本年退回 RecoveryDate.Year==今年 ΣUnprocessedWeight） |
| `MES.Core/Interfaces/Batch/IOutsourceVendorProfileService.cs` | +`PrintOutsourceVendorListAsync`（Mode A 列表打印，`PrintColumnDef` 入参） |
| `MES.Services/Batch/OutsourceVendorProfileService.cs` | `GetPagedAsync` 投影后调 `AttachTradeStatsAsync` 回填 9 字段；私有桶 `OutsourceVendorStatsBucket` + `BuildOutsourceStatsAsync`（按页内 (名×工段)→Id OrdinalIgnoreCase 分流，仅 `!IsInternal` 且单位名 in 页内名集 拉 `SectionOutsource` → Ids → `OutsourceRecoveries` Chunk(1000) 一次 group 出按年份 ΣRecovery/Unprocessed + 全量，内存分流累加、负数截 0）；`PrintOutsourceVendorListAsync`（`TablePrintHelper.GeneratePdf` autoWidth/alignCenter/headerMaxLines:0，同 `SupplierService.PrintSupplierListAsync`） |
| `MES.Api/Controllers/Batch/OutsourceVendorProfileController.cs` | +`POST print-list-file`（`BatchView`，请求体 `OrderPrintListRequest`，返 `委外单位列表.pdf`） |
| `MES.Blazor/Services/Batch/OutsourceVendorService.cs` | +`PrintListFileAsync` 无独立方法——前端走 `openPdfFromApi`（ApiEndpoints 常量已含 `api/outsource-vendor`） |
| `MES.Blazor/Pages/Batches/OutsourceVendors.razor`+`.razor.cs` | ① 基本信息 / ② 往来信息 两分组（col-g1/g2 + col-group-start 分隔 + 分组标题栏 initGroupHeaders，容器 id → `outsource-vendors-list-table`）；① 8 列加 GroupKey=1（SectionName 中文下拉/筛、IsWorkshop 本厂/外协、IsActive 启用/停用）；② 5 只读统计列（SortKey/FilterType null、不可编辑/排序/筛选；Width 累计/本年 200、本年回收 160、在委外 140、本年退回 140）+ `RenderStatText/BuildStatText`（单/吨/万 F1、全 0 →「—」）+ 页脚合计 `ComputePageSums`（反射 `_statFieldMap`：累计/本年 单+吨+万，回收/在委外/退回 仅吨）+ 全选列 + 工具栏「打印选中 (N)」Mode A（`GetCellDisplayText`：② 走 RenderStatText，① IsWorkshop→本厂车间/外协单位、IsActive→启用/停用、SectionName→中文）+ `ColumnPrefsVersion` v1→v2（`col_prefs_outsource-vendors_v2`，**默认可见 单位名/委外工段/本厂外协/状态 + 5 统计列，隐藏 编码/联系人/电话/备注**，强制老用户按新默认载入） |
| `MES.Tests/Services/Batch/OutsourceVendorProfileServiceTests.cs` | +5 用例：年份切分与在外净欠负数截0（去年发出回收不计本年/今年回收+退回、超回收行截 0 → 累计 3 单 3500/本年 2 单 2500/本年回收 1400/退回 300/在外 1000）；厂内 IsInternal=1 行不计（本厂车间档案全 0）；同名跨工段按档案行分流不混算（ColdRollDraw 2 单 3000 vs OuterPolish 1 单 3000）；档外文本（无档案行键）不计；Mode A 打印字典行含统计文本 → 返回非空 PDF；既有 14 例不回归（统计仅 GetPagedAsync 回填）；**定向 18 绿**，Api/Blazor build 0 错 0 警 |

**2026-09-09 追加细化（用户 5 点 UX 反馈，随本批同包）：**
1. **本年回收[扣除退回] / 委外未回收 补金额（按重量份额分摊）**：`OutsourceRecovery` 无单价/金额 → 金额从发出单 `TotalAmount` 按份额分摊（同供应商 ② 到货/待收货款口径，非笼统全局单价）。新增 DTO 字段 `YearRecoveredAmount`（= Σ 发出单 TotalAmount × 本年正常回收重 Min(份额,发出重)/发出重，退回不产生金额）、`PendingAmount`（仅 `Status==PendingRecovery` 且净欠>0：Σ TotalAmount × 净欠重/发出重）；均仅当 `SendWeight>0 && TotalAmount>0 && 份额>0` 才累加。
2. **回收仅正常 `RecoveryWeight`**：非正常 `UnprocessedWeight` 属「退回」本厂（退回不产生金额）——字段级口径不变，退回行已在 (1) 中排除出金额。
3. **「本厂/外协」字段改「本厂」**：字段名/表头改「本厂」，本厂标「是」、外协留空（非编辑格只显 是/空，编辑态 MudSwitch，布尔筛选 是/外协；`GetWorkshopText` 替代原「外协单位」文案；新建页表头同改）。
4. **列改名**：`本年回收` → **`本年回收[扣除退回]`**、`在委外未回收` → **`委外未回收`**（用户 AskUserQuestion 拍板）。宽度微调（回收 160→200、在委外 140→180、退回→160）。
5. **② 往来信息组数据一律靠左**：委外单位页 + 供应商页（`Suppliers.razor/.cs` 删 `_centerColumnKeys/IsNumericColumn` text-center）两页统计组数据格与页脚均左对齐。
6. 仅标签/文案/宽度/CSS 变，默认可见列/顺序不变 → **ColumnPrefsVersion 不 bump**（仍 v2）。测试补 `YearRecoveredAmount/PendingAmount` 断言 + 新增「退回不产生金额、未回收整单按份额」用例 → **定向 19 绿**，Api/Blazor build 0 错 0 警。

### ⏳ 批十九 物料进出存报表 inout 月格改彩带入x/出y + 末尾拆「进出汇总/实时库存」两列（2026-09-09 登记，未打包）

> 用户口径（AskUserQuestion 拍板）：**去月结存** + **绿入/红出**——① 物料进出存报表（`/warehouse/monthly-stock`，4 报表切换之 inout）**每月格去掉月末结存 [结]**、改**彩带「入x/出y」**（入=浅绿底深绿字 `#1e7e34/#e6f4ea`、出=浅红底深红字 `#c62828/#fdecea`，**内联样式**保证 getTableHtml 抓 DOM 打印与屏显一致，0 侧省略、两侧皆 0 留空）；② **末尾单列拆两列** = 倒数第 2 列「**进出汇总**」（该行全年 入/出 = TotalIn/TotalOut，同彩带格式）+ 末列「**实时库存**」（原「实时数据」更名，**只显示当前库存量** ClosingWeight 单值 t）；③ 入库/出库/库存报表单值展示不变（库存报表每月「结」+末尾「实时结存」=ClosingWeight 亦不变）。**性质：纯 WASM 前端**（无 EF 迁移、无数据变更），随下次 `03-publish-local.ps1` 发布一次勾除。

| 文件 | 改动 |
|---|---|
| `MES.Blazor/Pages/Reports/MonthlyStock.razor`+`.razor.cs` | inout thead 拆「进出汇总」「实时库存」两 th（原单「实时数据」），tbody 月格改 `MonthCell(m,r)`（未来月留空、inout 走彩带）、末尾 inout 渲染 BuildFlowMarkup(TotalIn/TotalOut) + FormatWeight(ClosingWeight) 两 td；新增 `BuildFlowMarkup`（彩带入x/出y 内联样式）/`MonthCell`/`FooterCellText`，删除 `FormatMonthCell/FormatTotalCell/FormatInOutClosing`；底部注记同步 inout 新格式 |
| 文档 | 仓库模块详设 V7.18→V7.19（§4.10 三处+API 表行）、仓库上下文接口设计 V3.27→V3.28（§2.15.6 展示约定+端点行）、frontend V75→V76（顶部注记+§2 报表行）；后端 DTO/服务/测试零改动 |

验证 `dotnet build MES.Blazor` 0 错 0 警。

### ⏳ 批二十 报表业务总况 + 订单列表页 3 小表加「金额」三色展示（2026-09-09 登记，未打包）

> 用户口径（AskUserQuestion 两轮拍板：金额折算=「复用客户结算分治口径(推荐)」、适用范围=「两处一并改(推荐)」）：金额一律以**万元**、**去小数取整**，订单金额源=订单项次 `TotalPrice`（元）；出库/库存/负荷无订单侧金额 → 复用客户往来统计「结算分治」口径按订单折算（过磅实称不封顶 / 理算+过磅-负封顶合同：发货→库存阶梯认领、超产不计价），已抽取共享折算器防两条代码路径漂移。三小表改后形态：
> - ①「订单接单·出库及现负荷汇总」（业务总况，按年 12 月）：**接单量/出库量两行** `x吨/y万`、**成品库存(完工/未完工)/订单负荷量三行** `x吨/y万`（仅当前月格取值，跨年余月显 `-`）；
> - ②「订单(整单)完成预估」+ ③「风险-已延期订单(整单)」（业务总况 + 订单列表页 `Orders.razor` 同款两张折叠小表）：单元格 `z单/x吨/y万` 三色区分（蓝单 `#1565C0`/绿吨 `#2E7D32`/橙万 `#E65100`），**原「延期罚款」急中急子集红标 `[*a/b]` 不再显示**（UrgentCount/UrgentWeight 字段删除）。
> - 金额口径：接单额=本年签订排除取消订单按签订月；出库额=订单 shipMoney×本年出库份额按出库月重量分摊；库存额按执行关注=主号完成(1)与否分档（全部年份）；负荷额=未完工订单项次总价合计−未完工库存额。整数值取整 `AwayFromZero`；全 0 显 `-`，单列有值 0 分量仍显 `0吨/0万`。桶金额=桶内整单项次总价合计（未计价订单计 0）。**性质：后端 dll + WASM**，无 EF 迁移、无数据变更，随下次 `03-publish-local.ps1` 发布一次勾除。

| 文件 | 改动 |
|---|---|
| `MES.Core/Helpers/SettlementMoneyCalculator.cs`（新） | 结算分治金额折算共享器：`SplitOrder(inboundKg,outboundKg,stockKg,weighPoolW/M,fixedPoolW/M)→(Ship/Stock/Wip Money)`——过磅池实称不封顶、固定池发货→库存阶梯认领封顶合同池重；客户往来统计与报表业务总况共用，防口径漂移 |
| `MES.Core/DTOs/Order/OrderInOutSummaryDto.cs` | +5 金额字段（元）：`OrderAmountByMonth`/`OutboundAmountByMonth`（decimal[12] 按月）、`FinishedStockCompletedAmount`/`FinishedStockUncompletedAmount`/`TurnoverAmount` |
| `MES.Core/DTOs/Order/OrderDeliveryEstimateDto.cs` | `OrderDeliveryBucketDto` 删 `UrgentCount/UrgentWeight`、+`Amount`（桶内整单项次总价合计，元） |
| `MES.Services/Order/CustomerService.cs` | BuildStats 两池折算改调 `SettlementMoneyCalculator.SplitOrder`（删原私有 CalcPoolMoney），客户往来金额口径同源不漂移 |
| `MES.Services/Order/OrderService.cs` | `GetOrderInOutSummaryAsync` 订单 select 补 OrderId/OrderNumber/FinishedInbound·Outbound·StockWeight + `LoadPoolsByOrderAsync`（OrderItem 按 SalesOrderId 聚过磅/固定池 重量×金额，与客户统计同池口径）→ 按上节口径折算接单额/库存额/负荷额；出库额=成品批次(`SalesOrderNo` 关联订单号)本年 SalesOut 按月分摊 shipMoney；`GetDeliveryEstimateAsync` select 补 OrderId、按整单项次总价合计落桶 Amount，删延期罚款 Urgent 归集 |
| `MES.Blazor/Helpers/OrderOverviewFormatter.cs`（新） | 整数值取整 `AwayFromZero` 彩色 MarkupString 渲染器（内联样式保证打印 getTableHtml 与屏显一致）：`RenderInOutCell(kg,元)→x吨/y万`、`RenderEstimateCell(单,吨,元)→z单/x吨/y万`；单/吨/万三色 + 灰「/」分隔 |
| `MES.Blazor/Pages/Reports/ReportOverview.razor`+`.razor.cs` | ① 接单量/出库量行格改 `OrderOverviewFormatter.RenderInOutCell`、成品库存(完工/未完工)/订单负荷量行改 `RenderCurrentOnly`（仅当前月取值，余显 `-`，月份角标联动）；② ③ 两小表格改 `RenderEstimateCell`；行左标签「重量」→「重量/金额」/「单数/重量」→「单数/重量/金额」+ 图例注记；删旧 `FormatInOutWeight/FormatDeliveryBucket`（含延期罚款红标） |
| `MES.Blazor/Pages/Orders/Orders.razor`+`.razor.cs` | 完成预估/延期风险两折叠小表格改 `OrderOverviewFormatter.RenderEstimateCell`（`FormatDeliveryBucket` 转调），行标签「单数/重量」→「单数/重量/金额」+ 注记；**延期罚款不再单列显示** |
| `MES.Tests/Services/Order/OrderServiceTests.cs` | 去 UrgentCount/UrgentWeight 断言；+3 用例：交期预估桶金额=项次总价合计（含未排产不入桶）；接单额/库存额/负荷额按结算池折算（完工 5000/未完工库存 3000/负荷 10000，重量口径回归）；出库额按月分摊（发货 3500 元 2月2500/8月1000）；`SeedOrderItem/SeedSummaryFull` 帮手；定向 229（Order+Customer）绿、Api/Blazor build 0 错 0 警 |
| 文档 | 订单上下文接口设计（§业务总况/交期预估 DTO 字段 + 金额口径）、订单模块详细设计、frontend_pages（业务总况与订单列表小表单元格描述） |

### ⏳ 批二十一 报表总览三 Tab 加「往来数据」表 + 源列表页 ② 列样式统一（2026-09-09 登记，未打包）

> 用户口径（AskUserQuestion 四轮拍板：内嵌表形态=**整表一次性加载+身份列排序**、标题区分=**标题着色+左侧色条**、0 值显示=**0 成分省略全 0 显「—」**、客户卡位置=**业务总况内最前**）：报表总览 业务总况/物料执行/生产执行 三个 Tab 各加一张「往来数据」表，**字段与数据值与对应源列表页完全同源**（直接调源上下文列表端点 `GetPagedAsync` PageSize=5000 整表加载，② 统计随服务端回填），并同步三个源列表页 ② 统计列样式——**单数蓝/吨绿/万橙三色、吨与万取整无小数、0 成分省略全 0 显「—」**（此前吨/万为小数显示）。共享格式化器抽取到 `OrderOverviewFormatter`，三源列表页 + 报表卡同一数值口径防漂移。
> - **业务总况 Tab 顶部「客户往来数据」**（同源订单上下文「客户管理」）：业务员/最终用户（**客户端点击排序 + 关键字搜索**）+ 本年接单 / 本年已发货(整单) / 本年已发货(非整单) / 待发货(整单) / 待发货(非整单) / 待在产(整单未入库) / 待在产(扣除部分入库) 七统计列（全部 z单/x吨/y万）。
> - **物料执行 Tab 末尾「供应商往来数据」**（同源「供应商管理」）：供应商名称/物料分类/备注（名称/分类点击排序 + 名称/分类/备注 搜索）+ 累计出单 / 本年出单（z单/x吨/y万）/ 本年到货[扣除退货] / 待收货（x吨/y万）/ 本年退货（仅吨）。
> - **生产执行 Tab 末尾「委外单位往来数据」**（同源「委外单位管理」档案，**排除本厂 IsWorkshop 行**）：委外单位名/委外工段（单位/工段点击排序 + 搜索，工段名转中文）+ 累计委外 / 本年委外（z单/x吨/y万）/ 本年回收[扣除退回] / 委外未回收（x吨/y万）/ 本年退回（仅吨）。
> - **标题区分（标题着色 + ::before 左侧 4px 色条）**：物料执行 荒管类(1 蓝)/成品类(2 绿)/圆钢类(3 紫) 既区分，供应商往来=**第 4 色 橙**；生产执行 冷轧拔近日排程(1)/段落流转(2)/生产量(3)/委外(4) 既区分，委外单位往来数据=**第 5 色 青**。
> - **性质：纯 WASM 前端**（无 EF 迁移、无数据变更、**后端零改动**——三张表复用既有 list 端点与已回填统计字段），随下次 `03-publish-local.ps1` 发布一次勾除。

| 文件 | 改动 |
|---|---|
| `MES.Blazor/Helpers/OrderOverviewFormatter.cs` | +`RenderTradeMarkup`（富文本）/`RenderTradeText`（纯文本，供 tooltip/合计）——往来统计格共享格式化：z单/x吨/y万 **内联三色**取整（AwayFromZero）、**0 成分省略**、全 0 显「—」、`withCount=false` 忽略单数；吨 kg→吨、金额元→万元 |
| `MES.Blazor/Pages/Orders/Customers.razor.cs` | ② 统计格改走 `OrderOverviewFormatter`（`RenderStatMarkup/RenderStatText/ResolveStat`，8 列），悬停 title=纯文本、全 0「—」、取整无小数 |
| `MES.Blazor/Pages/Materials/Suppliers.razor.cs` | ② 统计格同改走 formatter（TotalOrdering/YearOrdering 含单、Arrived/Pending x吨/y万、YearReturn 仅吨） |
| `MES.Blazor/Pages/Batches/OutsourceVendors.razor.cs` | ② 统计格同改走 formatter（TotalOrdering/YearOrdering 含单、YearRecovered/Pending x吨/y万、YearReturn 仅吨） |
| `MES.Blazor/Pages/Reports/ReportOverview.razor.cs` | 注入 CustomerService/SupplierService/OutsourceVendorService；LoadTab1/4/5 懒加载收尾调 Load{Customer,Supplier,Outsource}TradeAsync（`GetPagedAsync` PageSize=5000，**委外行过滤 IsWorkshop 本厂**，失败不阻断 Tab）；每表 行集+关键字+排序状态 + Visible 过滤/排序（业务员/最终用户、供应商名/物料分类、委外单位名/工段中文，`StringComparer.OrdinalIgnoreCase`）+ `*TradeCell` 静态映射 DTO 统计字段（MarkupString） |
| `MES.Blazor/Pages/Reports/ReportOverview.razor` | 三 Tab 各插「往来数据」卡（`summary-card`，标题 `<span class="report-tt report-tt-N">`，220px MudTextField Immediate 搜索，caption 口径注记，打印按钮 Disabled=空集，`#report-{customer,supplier,outsource}-trade` 容器 `max-height:50vh` 内滚动）；表体身份列 th `@onclick` 点击排序 + ▲/▼ 箭头，统计列只读三色格 |
| `MES.Blazor/wwwroot/css/app.css` | +`.report-tt`（inline-flex + `::before` 4px 圆角色条 `background:currentColor`）+ `.report-tt-1~5` 五色（蓝 #1976D2 / 绿 #43A047 / 紫 #8E24AA / 橙 #FB8C00 / 青 #00897B） |
| `MES.Tests/Components/Helpers/OrderOverviewFormatterTests.cs`（新） | +10 用例：全成分中文组合取整/整吨整万/仅单省略吨万/仅吨(withCount=false)/仅万/全 0「—」文本侧 + 三色含灰分隔/全 0 纯破折号/仅重量不出单万/仅金额不出单吨 富文本侧 |

验证 `dotnet build MES.Blazor` + `dotnet build MES.Api` 0 错 0 警；定向 `OrderOverviewFormatterTests` 10 全绿。

### ⏳ 批二十二 报表往来卡交互增强 + 客户列表②靠左 + 质量管理标题分色（2026-09-10 登记，未打包）

> 用户五点口径（含 AskUserQuestion 三轮拍板：**行数形态=行数下拉无翻页(默认 10/可 10·20·50·全部)**、**汇总口径=当前显示的 N 条(合计按显示行)**、**排序指示=浅蓝背景+加粗(去 ▲/▼ 箭头文字)**）：对批二十一新增的三张「往来数据」卡做交互收敛——**默认折叠、展开默认显 10 行、行数下拉切换、下方合计按当前显示行汇总**、表头排序去掉箭头文字改浅蓝背景+加粗；另客户管理列表 ② 组改**靠左**；质量管理 Tab 五张小表标题仿「生产执行」加**着色+色条**区分。
> - **（一）客户管理列表 ② 靠左**：`Customers.razor(.cs)` 删除②数据/页脚 `text-center`（`_centerColumnKeys`/`IsNumericColumn` 移除），全列靠左，同供应商/委外单位 V74 口径。
> - **（二）~（四）三张往来卡同改**（`report:customer-trade`/`report:supplier-trade`/`report:outsource-trade`）：头部加折叠图标（默认折叠键已入 `DefaultCollapsedCards`，localStorage 记忆）；展开后工具栏=220px 关键字搜索 + **显示行数 MudSelect(10/20/50/0=全部)** + `显示 N / 共 M 条｜合计按显示行计` 注记；表格身份列 th 保留点击排序但**去 ▲/▼**，改 `.report-th-sorted` 浅蓝背景+加粗；tbody 只渲当前显示行；**新增 `<tfoot>` 合计行**（客户身份列 colspan=2、供应商 colspan=3、委外 colspan=2，背景 #fffde7 加粗，每统计列按 `*TradeSummary` 汇总当前显示行——与 `*TradeValues` 同 withCount 成员规则）。
> - **（五）质量管理 Tab 标题分色**：不合格品实时待处理/月度汇总→`report-tt-1`(蓝)、待检批支重汇总→`report-tt-2`(绿)、近日/月度成检量数据→`report-tt-3`(紫)（同源逻辑上下文同色；仅改标题文字包 `<span class="report-tt report-tt-N">`，不动卡片结构）。
> - **性质：纯 WASM 前端**（无 EF 迁移、无数据变更、后端零改动），随批十~批二十一之后下次 `03-publish-local.ps1` 发布一次勾除。

| 文件 | 改动 |
|---|---|
| `MES.Blazor/Pages/Orders/Customers.razor` / `.razor.cs` | ② 数据格/页脚去 `text-center` 靠左（删 `_centerColumnKeys`/`IsNumericColumn`） |
| `MES.Blazor/Pages/Reports/ReportOverview.razor.cs` | 三张往来卡状态 +`*TradeTake`(int 默认 10)/`*TradeShown()`(过滤排序后 Take，0=全部)；`SortArrow` 方法删改 `*TradeSortedClass`(被排序列加 `report-th-sorted`)；新增 `*TradeValues`(静态列取值 tuple)+`*TradeSummary`(IEnumerable 当前显示行汇总 MarkupString)；三卡折叠键入 `DefaultCollapsedCards` |
| `MES.Blazor/Pages/Reports/ReportOverview.razor` | 三张往来卡改折叠卡（标题旁折叠图标 + `@if(!IsCardCollapsed(...))`）+ 行数下拉 + `显示 N/共 M` 注记 + 身份列 `<th class="report-th-sort@(*SortedClass)">` 去箭头 + tbody 显 `*TradeShown()` + `<tfoot>` 合计行；质量管理 Tab 五标题包 `report-tt-N` |
| `MES.Blazor/wwwroot/css/app.css` | +`.report-th-sort`(排序 th 基础：左对齐/浅灰底/边框/pointer) + `.report-th-sorted`(浅蓝 #e3f2fd 底 + 边框 #90caf9 + font-weight:700) |

验证 `dotnet build MES.Blazor`（含 MES.Api）0 错 0 警；纯 WASM 交互改造，无新增后端逻辑。

### ⏳ 批二十三 报表子表口径微调：月度汇总列合并 + 冷轧去标注/吨位小数 + 段落靠左 + 负荷表删延期行（2026-09-10 登记，未打包）

> 用户五处口径：① 质量管理 Tab「不合格品月度汇总」**删除 处置方式汇总/责任部门汇总/责任类别汇总 三列，只留一列「合计」=该处置方式全年（12 月）合计量**；② 同表 **责任类别/责任部门为空直接显「-」**（原「未填写」文字）；③ 生产执行 Tab「冷轧拔近日排程」**删除「矛盾标注」列**，且**涉及吨位数值保留 1 位小数**（可流转量/计划流转量/后流转 t 值）；④ 生产执行 Tab「段落流转分析」**数据值均靠左**（待在产重量/计划重点批流转量/特急批重量 三数值列表头/单元格/页脚靠左，判定 Chip 列保持居中）；⑤ 报表 Tab2「现订单负荷总量」（WorkOrderLoadOverview 共享组件）**删除「订单交期负荷」组三行（订单延期-原料/在产/成检）**，另**序号 4-0「整体完工预计」行整行底色调浅黄 #FFF8E1**（颜色可再调）。
> - 月度汇总口径说明：三汇总列中「处置方式汇总」=单处置方式行全年合计，现并入唯一「合计」列（每行一个，即每个处置方式全年合计量），部门/类别两个跨行小计不再展示；「未填写」显示改「-」仅针对 责任类别/责任部门 两身份列，处置方式空值仍显「未填写」（用户只点名前两者）。
> - 冷轧卡吨位经 `TonsText`（原 G29 去零）→ F1（0 值仍显 "0"），该 helper 仅冷轧建议卡使用，无旁路影响。
> - 负荷表删除在**组件层过滤**（`OverviewRows` 剔除 Category=订单交期负荷），不改后端数据/不改日期桶列（其余行日期桶仍有值）；同一组件亦被 计划排程 PlanOverview 页面使用，两处显示一致。
> - **性质：纯 WASM 前端**（无 EF 迁移、无数据变更、后端零改动），随下次 `03-publish-local.ps1` 发布一次勾除。

| 文件 | 改动 |
|---|---|
| `MES.Blazor/Pages/Reports/ReportOverview.razor` | ①/② 不合格品月度汇总：thead 三汇总 th 删、改单「合计」th（浅黄 #fff8e1 加粗）；tbody 删 处置方式汇总 + 部门/类别 rowspan 小计三 td，加每行 `合计` td（背景 #fffde7 加粗，=r.TotalQuantity/TotalWeight）；责任类别/责任部门单元格 值=="未填写" 时显 "-"；注记文本同步。③ 冷轧建议卡删「矛盾标注」th/td（连带 `hasConflict` 局部） ；④ 段落流转 数值列 待重/计划重点批流转量/特急批重量 表头去 `mud-table-cell--right`、单元格去 `text-center`、页脚去 `text-center`（判定 Chip 列不变） |
| `MES.Blazor/Pages/Reports/ReportOverview.razor.cs` | `TonsText` G29→**F1**（保留 1 位小数，0→"0"）；删 `_ncrDeptTotals`/`_ncrCategoryTotals` 字段及 `ComputeNcrMonthlyRowspans` 内相应求和，仅保留 rowspan 计算 |
| `MES.Blazor/Pages/Scheduling/WorkOrderLoadOverview.razor.cs` | +`OverviewRows` 过滤属性（剔除 Category=「订单交期负荷」行）；`CellClass` 对 序号 4-0（CategoryNo=4 & RowNo=0）追加 `row-overall` |
| `MES.Blazor/Pages/Scheduling/WorkOrderLoadOverview.razor` | `Items` 绑 `OverviewRows`；`<style>` +`.row-overall { background:#FFF8E1; }` |

验证 `dotnet build MES.Blazor`（含 MES.Api）0 错 0 警；纯 WASM 展示口径调整，无新增后端逻辑、无迁移。

### ⏳ 批二十四 菜单整序：改名「订单在库成品」+ 报表系统拍平「报表总览」置首页下 + 数据工具移至参数表后（2026-09-10 登记，未打包）

> 用户四项决策：① **菜单名** 订单管理组「订单成品(在库)」→「订单在库成品」；② **报表系统（单叶组）拍平为一级「报表总览」**、置首页正下方（组节点删、`ReportView` 门控移至叶子，与 数据工具/用户管理 单项一级形态统一）；③ **数据工具**自 生产标准/扫码管理 之间移到 **参数表之后、用户管理之前**（扫码管理顺延前移一格）；④ 一级顺序其余**维持现状不动**（物料管理/扫码管理/设备管理等分析建议暂不采纳）。因菜单为**单源树**（`AppMenu.Root`，桌面/手机共用），均为该文件搬移/改名 + 测试断言 + 文档，**纯 WASM 前端**（无 EF 迁移、无数据变更、后端零改动；角色域文案「报表系统」——Roles 注释/UserRoleDisplayHelper/角色三档——**不随菜单改**），随批十~批二十三之后下次 `03-publish-local.ps1` 发布一次勾除。定稿一级序：`首页, 报表总览, 订单管理, 工单管理, 计划排程, 批次管理, 质量管理, 物料管理, 仓库管理, 设备管理, 生产标准, 扫码管理, 工资结算, 参数表, 数据工具, 用户管理`。

| 文件 | 改动 |
|---|---|
| `MES.Blazor/Shared/AppMenu.cs` | ① 订单管理组叶子 Label 「订单成品(在库)」→「订单在库成品」（Href `/orders/pending-delivery` 不变）；② **删「报表系统」整组块（Children 仅 报表总览）→ 拍平为一级单项** `报表总览` `/reports/overview`（`Policy=ReportView`，紧跟首页第 2 位；原位于 生产标准/数据工具 之间的同组块此前已移除）；③ **「数据工具」单项块移至 参数表块之后、用户管理块之前** |
| `MES.Tests/Components/AppMenuTests.cs` | `根级顺序` 断言更新为定稿序（报表系统→报表总览 第 2 位；数据工具于 参数表/用户管理 之间） |
| `docs/03_安全设计.md` | §3.5 代码节选注释「订单成品(在库)」→「订单在库成品」 |

验证 `dotnet build MES.Blazor` 0 错 0 警；定向 `AppMenuTests` 9 例全绿。菜单权限/路由/页面名（页面正式名仍「订单成品(实时库存)」；角色名仍「报表系统」）均不变。
