# 批次上下文接口设计（V5.3）

**版本**：V5.3
**最后更新**：2026-08-09

---

## 一、概述

本文档定义批次上下文的接口设计，包括DTO、枚举、API端点。

**实现状态**：✅ 核心接口已实现

---

## 二、枚举定义

### 2.1 BatchStatus（批次状态）

**文件：** `MES.Core/Enums/BatchStatus.cs`

| 值 | 名称 | 说明 |
|----|------|------|
| 0 | None | 未产 |
| 1 | InProgress | 在产 |
| 2 | Completed | 完成 |
| 3 | Suspended | 挂起（人工暂停） |
| 5 | InFinalInspection | 成检（已完成生产，处于成品检验阶段） |

数据库存储为字符串（`HasConversion<string>()`）。

### 2.2 ProductionType（生产类型）

**文件：** `MES.Core/Enums/ProductionType.cs`

| 值 | 说明 |
|----|------|
| RoughTube | 荒管生产 |
| InProcess | 在制生产 |
| Inventory | 库存 |
| OutsourcedPurchased | 外购 |
| Rework | 返整 |
| Subcontract | 委外生产 |
| ExternalProcessing | 对外加工 |

数据库存储为字符串（nvarchar(20)）。

### 2.3 PicklingStatus（酸洗入缸状态）

**文件：** `MES.Core/Enums/PicklingStatus.cs`

| 值 | 名称 | 说明 |
|----|------|------|
| 0 | Soaking | 浸泡中（已入缸，尚未出缸） |
| 1 | Completed | 已完工（出缸+冲洗完成） |

数据库存储为字符串（nvarchar(20)）。

---

## 三、DTO定义

### 3.1 列表DTO：ProductionBatchListDto

**文件：** `MES.Core/DTOs/Batch/ProductionBatchListDto.cs`

| 字段 | 类型 | 说明 |
|------|------|------|
| Id | int | 批次ID |
| BatchNo | string | 生产编号 |
| TagNo | string? | 挂牌号 |
| CreatedTime | DateTimeOffset | 创建时间 |
| WorkOrderNo | string | 关联工单号 |
| SalesOrderNo | string | 源订单号 |
| ProductionMainNo | string | 主号 |
| ProductionSubNo | string? | 次号 |
| ProductionType | string | 生产类型（必填） |
| ManufacturingItem | string | 制造物品（MaterialType 枚举名，如 Finished/OrderFinished/Surplus 等） |
| Status | string | 批次状态（字符串） |
| IsForceCompleted | bool | 是否强制完成（强制完成批次不重算成切跟踪） |
| CurrentExecDate | DateTime? | 截止执行日 |
| CurrentGroupName | string? | 当前工序 |
| CurrentSectionName | string? | 当前工段 |
| CurrentEquipmentName | string? | 当前设备名称 |
| CurrentOutsource | string? | 当前委外 |
| NextSectionName | string? | 下一工段 |
| NextProcess | string? | 下一工序（基于下一工段所在工序组的工序名称；尚无记录时为首个工序组的工序名称） |
| CreatedBy | string | 创建人 |
| UpdatedTime | DateTimeOffset | 更新时间 |
| CurrentSpec | string? | 当前规格 |
| CorrespondingSpec | string? | 下一工段对应规格 |
| CurrentValidQty | int? | 现有效原料支数 |
| CurrentValidWeight | int? | 现有效原料重量(kg) |
| CurrentSectionCompleted | bool? | 当前工段是否完工 |
| RemainingWorkDays | int | 剩余工量(天) |
| TotalWorkDays | int | 全工量(天) |
| ProductionRatio | int | 制几率 |
| HasInputChange | bool? | 有效投料是否发生变化（true=是/false=否/null=无法计算，DB持久化，每次刷新跟踪时计算，不可排序不可模糊搜索） |
| ManufacturingStatus | DeliveryState? | 制造状态（枚举，与交货状态同类型） |
| ManufacturingStatusDisplay | string? | 制造状态中文显示（只读计算） |
| ProductUnitWeight | decimal? | 产品单支量(kg/支) = 总重量/总支数，仅"定尺"批次有效，非定尺为空；保留1位小数 |
| **过程检字段** | | |
| ProcessInspectionQualifiedQty | int? | 过程检合格支：批次当前执行工序组全部过程检验的合格支数聚合；无检验/不在产 → null |
| ProcessInspectionQualifiedWeight | decimal? | 过程检合格量(kg)：过程检验合格重量聚合；无检验/不在产 → null |
| ProcessInspectionTheoreticalQty | int? | 过程检理论成品支：Round(合格量 ÷ 合格支 ÷ 成品的理论单支重, AwayFromZero) × 合格支（重量口径折算） |
| ProcessInspectionNeedAdjust | bool? | 需调整：成检/完成时固定 null（-）不判定；其余状态 过程检理论成品支 与 当前理论成品支 偏差 > 3% → true（是）；否则/无数据 → null |
| ProcessInspectionReworkWeight | int | 缺陷-返整量(kg)：过程检验 理论返整重 全量聚合（返整会另开批次，不延续本批） |
| ProcessInspectionScrapWeight | int | 缺陷-纯次品量(kg)：过程检验 理论报废重+理论入库重 全量聚合（彻底退出正常流） |
| **成检附加** | | |
| InspectionStage | string? | 成检附加：仅"成检"状态有效——PreInspection=预检 / FormalInspection=终检；其余状态/无到料为 null |
| InspectionStageDisplay | string? | 预检/终检/空（只读计算） |
| **成切跟踪** | | |
| CutRequirement | bool | 成切需求（成品工序组内是否有「断切」工段） |
| CutRequirementDisplay | string | 是/否（只读计算） |
| CutExecution | bool? | 成切执行（需求=否→null；成品工序组内已有断切生产记录→true；否则→false） |
| CutExecutionDisplay | string? | 是/否/略（只读计算） |
| CutQuantity | int? | 成切支数（断切生产记录 PostCutQuantity 切后支数汇总；无→null） |
| CutQuantityDisplay | string? | 支数/略（只读计算） |
| CutDoubt | CutDoubtType? | 成切存疑（枚举：QuantityMismatch=疑问-数量 / MissingRecords=疑问-缺少 / Normal=正常；null=略） |
| CutDoubtDisplay | string? | 疑问-数量/疑问-缺少/正常/略（只读计算） |
| TheoreticalOutputQty | int? | 理论成品支数（理论计算字段） |
| TheoreticalOutputWeight | int? | 理论成品重量（理论计算字段） |
| TheoreticalUnitWeight | decimal? | 理论单支重（理论计算字段） |

### 3.2 详情DTO：ProductionBatchDetailDto

**文件：** `MES.Core/DTOs/Batch/ProductionBatchDetailDto.cs`

| 分组 | 字段 | 类型 |
|------|------|------|
| 批次自身 | Id, BatchNo, Status, TagNo, ProductionType, ManufacturingItem, ProductionRatio, IsForceCompleted, QualityRemark, SolutionParams, CurrentExecDate, CurrentGroupName, CurrentSectionName, CurrentEquipmentName, CurrentOutsource, CurrentSpec, NextSectionName, NextProcess, CorrespondingSpec, CurrentSectionCompleted, HasInputChange, RemainingWorkDays, TotalWorkDays, Remark, TheoreticalOutputQty, TheoreticalOutputWeight, TheoreticalUnitWeight | 同上 |
| 成检附加 | InspectionStage, InspectionStageDisplay | 同上 |
| 成切跟踪 | CutRequirement, CutRequirementDisplay, CutExecution, CutExecutionDisplay, CutQuantity, CutQuantityDisplay, CutDoubt, CutDoubtDisplay | 同上 |
| 工单冗余 | WorkOrderNo, SalesOrderNo, ProductionMainNo, ProductionSubNo, OrderItemIds, SignDate, Salesman, EndCustomer, DeliveryDate, DelayPenalty, MaterialName, SettlementMethod, StandardCode, DeliveryState, ManufacturingStatus, PlantGrade, Specification, OuterDiameterNegative, OuterDiameterPositive, WallThicknessNegative, WallThicknessPositive, LengthStatus, MinLength, MaxLength, TotalQuantity, TotalMeters, TotalWeight, ProductUnitWeight, TotalItemCount, ItemDetails, TechnicalRequirements（共31个） | 详见实体 |
| 仓库冗余 | SourceBatchNo, SourceMaterialType, SourceName, SourceHeatNo, SourcePlantGrade, SourceSpecification, SourceLengthStatus, SourceUnitWeight, InputQuantity, InputWeight, InputType, SourceRemark, SourceProductionNo, CurrentValidQty, CurrentValidWeight | 详见实体 |
| 审计 | CreatedTime, CreatedBy, UpdatedTime, UpdatedBy | 审计信息 |
| 乐观锁 | RowVersion (byte[]) | |
| 子集 | ProcessGroups (List\<ProcessGroupDto\>) | 工序组列表 |
| 合并投料 | SourceItems (List\<SourceBatchItemDto\>) | 来源库存批次列表（支持多库存批次合并投料） |

### 3.3 工序组DTO：ProcessGroupDto

**文件：** `MES.Core/DTOs/Batch/ProcessGroupDto.cs`

| 字段 | 类型 | 说明 |
|------|------|------|
| Id | int | 工序组ID |
| ProductionBatchId | int | 关联批次ID |
| SequenceNumber | int | 序号 |
| ProcessName | string | 工序名称 |
| ManufacturingSpec | string? | 制造规格 |
| OuterDiameterTolerance | string? | 外径公差 |
| WallThicknessTolerance | string? | 壁厚公差 |
| ManufacturingLength | string? | 制造长度 |
| CuttingTreatment | string? | 断切处理 |
| Remark | string? | 备注 |
| ColdRollDraw / OilPipeCut / Degrease / EmulsionWash / UltrasonicWash / ClothPolish / BrightAnnealing / Solution / Straighten / Cut / ThicknessMeasure / Pickle / OuterPolish / InnerPolish / InnerGrinding / OuterSpotGrinding / SandBlasting / ShotBlasting / Inspection / WeldingHead / Welding / Lubrication / Packing / Warehouse / Extra1 / Extra2 | int? | 26个工段执行顺序（原15个，2026-08-05 扩展；Extra1/Extra2 为槽位列默认隐藏） |
| CreatedTime | DateTimeOffset | 创建时间 |
| CreatedBy | string | 创建人 |

### 3.4 可用批次DTO：AvailableBatchDto

**文件：** `MES.Core/DTOs/Batch/AvailableBatchDto.cs`

| 字段 | 类型 | 说明 |
|------|------|------|
| BatchNo | string | 库存批次号 |
| OutboundRecordId | long | 出库记录ID |
| WarehouseId | int | 仓库ID |
| MaterialType | string? | 原料类型 |
| InboundSource | string? | 入库来源 |
| SourceName | string? | 来料单位 |
| InboundDate | DateTime? | 入库日期 |
| HeatNo | string? | 炉号 |
| PlantGrade | string? | 工厂牌号 |
| Specification | string? | 名义规格 |
| LengthStatus | string? | 长度状态 |
| UnitWeight | decimal? | 单支重 |
| OutboundQuantity | int? | 出库支数 |
| OutboundWeight | decimal? | 出库重量 |
| WorkOrderNo | string? | 关联工单号 |
| WarehouseName | string? | 仓库名称 |

### 3.5 投料来源批次DTO：SourceBatchItemDto

**文件：** `MES.Core/DTOs/Batch/SourceBatchItemDto.cs`

| 字段 | 类型 | 说明 |
|------|------|------|
| InventoryBatchId | int | 关联库存批次ID |
| OutboundRecordId | long? | 关联出库记录ID |
| BatchNo | string | 库存批次号 |
| HeatNo | string? | 炉号 |
| PlantGrade | string? | 工厂牌号 |
| Specification | string? | 规格 |
| MaterialType | string? | 原料类型 |
| SourceName | string? | 来料单位 |
| WarehouseName | string? | 仓库名称 |
| InputQuantity | int | 领料支数 |
| InputWeight | decimal | 领料重量(kg) |

### 3.6 投料来源批次请求：SourceBatchItemRequest

**文件：** `MES.Core/DTOs/Batch/SourceBatchItemRequest.cs`

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| InventoryBatchId | int | ✅ | 关联库存批次ID |
| OutboundRecordId | long? | ❌ | 关联出库记录ID（按出库记录粒度跟踪消耗；可空=向后兼容） |
| InputQuantity | int | ✅ | 领料支数（>0） |
| InputWeight | decimal | ✅ | 领料重量(kg)（>0） |

### 3.7 请求DTO

#### CreateProductionBatchRequest

**文件：** `MES.Core/DTOs/Batch/CreateProductionBatchRequest.cs`

| 分组 | 字段 | 必须 | 说明 |
|------|------|------|------|
| 基础 | WorkOrderNo, TagNo, ProductionType, ManufacturingItem, ProductionRatio, QualityRemark, SolutionParams, Remark | ProductionType/ManufacturingItem 必填（非空） | 基础信息（ManufacturingItem 必填） |
| 仓库来源 | SourceBatchNo, SourceMaterialType, SourceName, SourceHeatNo, SourcePlantGrade, SourceSpecification, SourceLengthStatus, SourceUnitWeight, InputQuantity, InputWeight, InputType, SourceRemark, SourceProductionNo, CurrentValidQty, CurrentValidWeight | | 来源库存信息 |
| 工单字段 | 29个工单字段（SalesOrderNo ~ TechnicalRequirements + ManufacturingStatus） | | 可覆盖，不传则从工单复制 |
| 工序组 | ProcessGroups (List\<CreateProcessGroupRequest\>?) | | 可选，创建时可带或不带 |
| 合并投料 | SourceItems (List\<SourceBatchItemRequest\>?) | | 来源库存批次列表，支持多库存批次合并投料 |

#### UpdateProductionBatchRequest

**文件：** `MES.Core/DTOs/Batch/UpdateProductionBatchRequest.cs`

| 分组 | 可更新字段 | 说明 |
|------|----------|------|
| 批次信息 | TagNo, ProductionType, ManufacturingItem, ProductionRatio(int?), QualityRemark, SolutionParams, Remark, IsForceCompleted(bool?) | 基础信息。ProductionType/ManufacturingItem 必填（非空） |
| 执行信息 | CurrentExecDate, CurrentGroupName, CurrentSectionName, CurrentEquipmentName, CurrentOutsource, NextSectionName | 执行跟踪 |
| 仓库来源 | SourceBatchNo, SourceMaterialType, SourceName, SourceHeatNo, SourcePlantGrade, SourceSpecification, SourceLengthStatus, SourceUnitWeight, InputQuantity, InputWeight, InputType, SourceRemark, SourceProductionNo, CurrentValidQty, CurrentValidWeight | 仓库信息 |
| 工单冗余 | WorkOrderNo, SalesOrderNo, ProductionMainNo, ProductionSubNo, OrderItemIds, SignDate, Salesman, EndCustomer, DeliveryDate, DelayPenalty, MaterialName, SettlementMethod, StandardCode, DeliveryState, ManufacturingStatus, PlantGrade, Specification, OuterDiameterNegative, OuterDiameterPositive, WallThicknessNegative, WallThicknessPositive, LengthStatus, MinLength, MaxLength, TotalQuantity, TotalMeters, TotalWeight, TotalItemCount, ItemDetails, TechnicalRequirements（共30个，全部可空，有值时更新） | 工单字段快照 |
| 合并投料 | SourceItems (List\<SourceBatchItemRequest\>?) | | 来源库存批次列表，支持多库存批次合并投料 |

必填：RowVersion（乐观锁）；ProductionType、ManufacturingItem 非空。

#### SaveBatchRequest（批量保存）

**文件：** `MES.Core/DTOs/Batch/SaveBatchRequest.cs`

| 分组 | 字段 | 说明 |
|------|------|------|
| 批次头 | TagNo, ProductionType, ManufacturingItem, ProductionRatio, IsForceCompleted(bool?), QualityRemark, SolutionParams, Remark | 基础信息。ProductionType/ManufacturingItem 必填（非空），后端接口拦截空值抛出 BusinessException |
| 仓库来源 | SourceBatchNo, SourceMaterialType, SourceName, SourceHeatNo, SourcePlantGrade, SourceSpecification, SourceLengthStatus, SourceUnitWeight, InputQuantity, InputWeight, InputType, SourceRemark, SourceProductionNo, CurrentValidQty, CurrentValidWeight | 仓库信息 |
| 工单冗余 | WorkOrderNo, SalesOrderNo, ProductionMainNo, ProductionSubNo, OrderItemIds, SignDate, Salesman, EndCustomer, DeliveryDate, DelayPenalty, MaterialName, SettlementMethod, StandardCode, DeliveryState, ManufacturingStatus, PlantGrade, Specification, OuterDiameterNegative, OuterDiameterPositive, WallThicknessNegative, WallThicknessPositive, LengthStatus, MinLength, MaxLength, TotalQuantity, TotalMeters, TotalWeight, TotalItemCount, ItemDetails, TechnicalRequirements（共30个） | 工单字段快照 |
| 合并投料 | SourceItems (List\<SourceBatchItemRequest\>?) | 来源库存批次列表，支持多库存批次合并投料（null=不更新） |
| 状态更新 | Status(BatchStatus?) | null=不更新状态 |
| 乐观锁 | RowVersion(byte[]) | 必填 |
| 工序组 | ProcessGroups(List\<CreateProcessGroupRequest\>) | 全量替换，删除旧工序组后重新插入 |

#### UpdateBatchStatusRequest

| 字段 | 类型 | 说明 |
|------|------|------|
| Status | string | 新状态（None/InProgress/Completed/Suspended/Cancelled） |
| RowVersion | byte[] | 乐观锁 |

#### CreateProcessGroupRequest

| 字段 | 类型 | 必须 | 说明 |
|------|------|------|------|
| ProcessName | string | ✅ | 工序名称 |
| ManufacturingSpec | string? | | 制造规格 |
| OuterDiameterTolerance | string? | | 外径公差 |
| WallThicknessTolerance | string? | | 壁厚公差 |
| ManufacturingLength | string? | | 制造长度 |
| CuttingTreatment | string? | | 断切处理 |
| Remark | string? | | 备注 |
| ColdRollDraw ~ Extra2（26个工段） | int? | | 26个工段执行顺序（Extra1/Extra2 为槽位列默认隐藏） |

#### SourceBatchItemDto（合并投料来源显示）

**文件：** `MES.Core/DTOs/Batch/SourceBatchItemDto.cs`

| 字段 | 类型 | 说明 |
|------|------|------|
| InventoryBatchId | int | 关联库存批次ID |
| OutboundRecordId | long? | 关联出库记录ID |
| BatchNo | string | 库存批次号 |
| HeatNo | string? | 炉号 |
| PlantGrade | string? | 工厂牌号 |
| Specification | string? | 规格 |
| MaterialType | string? | 原料类型 |
| SourceName | string? | 来料单位 |
| WarehouseName | string? | 仓库名称 |
| InputQuantity | int | 领料支数 |
| InputWeight | decimal | 领料重量(kg) |

#### SourceBatchItemRequest（合并投料请求项）

**文件：** `MES.Core/DTOs/Batch/SourceBatchItemRequest.cs`

| 字段 | 类型 | 必须 | 说明 |
|------|------|------|------|
| InventoryBatchId | int | ✅ | 关联库存批次ID |
| OutboundRecordId | long? | | 关联出库记录ID |
| InputQuantity | int | ✅ | 领料支数（>0） |
| InputWeight | decimal | ✅ | 领料重量(kg)（>0） |

#### ProcessCardPrintRequest

**文件：** `MES.Core/DTOs/Batch/ProcessCardPrintRequest.cs`

| 字段 | 类型 | 说明 |
|------|------|------|
| Ids | int[] | 选中批次ID数组（空数组=打印全部） |
| Columns | List\<ProcessCardColumnDef\> | 列选择定义列表 |

#### ProcessCardColumnDef

| 字段 | 类型 | 说明 |
|------|------|------|
| BlockKey | string | 区块标识（BatchInfo/Quality/Warehouse/WorkOrder/ProcessGroup） |
| Key | string | 字段键名 |
| Label | string | 显示标签 |
| Visible | bool | 是否可见（默认true） |

### 3.8 生产记录DTO：ProductionRecordDto

**文件：** `MES.Core/DTOs/Batch/ProductionRecordDto.cs`

| 分组 | 字段 | 类型 | 说明 |
|------|------|------|------|
| 关联 | Id | int | 主键 |
| | ProductionBatchId | int | 关联批次ID |
| | ProcessGroupId | int | 关联工序组ID |
| | **BatchNo** | **string?** | **批次号（跨批次列表冗余）** |
| 工序冗余 | ProcessName | string | 工序名称 |
| | ManufacturingSpec | string? | 制造规格 |
| 执行信息 | SectionName | string | 工段名称 |
| | SequenceNumber | int | 执行序号 |
| | **ExecDate** | **DateTime** | **执行日期（列表页首列）** |
| | EquipmentName | string? | 设备名称 |
| | Operator | string? | 操作人 |
| | Shift | ShiftType? | 班次（白班/中班/夜班） |
| 数量/重量 | Quantity | int? | 加工支数 |
| | Weight | decimal? | 加工重量(kg) |
| | SolutionTemperature | decimal? | 固溶温度(℃)，仅固溶工段使用 |
| | SoakTime | int? | 保温时间(min)，仅固溶工段使用 |
| | CuttingMultiple | decimal? | 断切倍数 |
| | FinishedCutLength | decimal? | 成品断切长度(mm) |
| | PostCutQuantity | int? | 切后支数 |
| | FaceCutCount | int? | 平头数，仅荒管切割：1=一端，2=两端 |
| 切割标识 | **IsPreCut** | **bool?** | **预成切（虽是成品切割，但不是正式成品切割；不计入成品切割统计支数）** |
| | **LengthStatus** | **string?** | **长度状态（定尺/范围尺/非定尺），断切成品时自动填充** |
| | **CutLengthMatchType** | **string?** | **定尺切割长度匹配标识：FullMatch=完全匹配 / MainNoMatch=主号匹配 / null=不适用；仅「成品切割+定尺+非预成切+有成品长度」时计算** |
| | **CutLengthMatchTypeDisplay** | **string?** | **完全匹配/主号匹配/空串（只读计算，CutLengthMatchHelper.GetText）** |
| 批次冗余 | TagNo | string? | 挂牌号 |
| | PlantGrade | string? | 工厂牌号 |
| | Remark | string? | 备注 |
| **产类** | **ProductStatus** | **string?** | **产类（荒管/在制/成品），由系统根据工序组和批次信息自动计算** |
| 系统 | DataSource | string? | 数据来源（SCAN=扫码报工 / MANUAL=手动录入） |
| | **CreatedTime** | **DateTimeOffset** | **创建时间** |
| | **UpdatedTime** | **DateTimeOffset** | **更新时间** |
| 批次导航 | WorkOrderNo | string? | 工单号（从批次导航属性投影） |
| | SalesOrderNo | string? | 订单号（从批次导航属性投影） |
| | ProductionMainNo | string? | 主号（从批次导航属性投影） |

### 3.9 工段委外DTO：SectionOutsourceDto

**文件：** `MES.Core/DTOs/Batch/SectionOutsourceDtos.cs`

| 分组 | 字段 | 类型 | 说明 |
|------|------|------|------|
| 关联 | Id | int | 主键 |
| | ProductionBatchId | int | 关联批次ID |
| | ProcessGroupId | int | 关联工序组ID |
| | **BatchNo** | **string?** | **批次号（跨批次列表冗余）** |
| 工序冗余 | ProcessName | string | 工序名称 |
| | ManufacturingSpec | string? | 制造规格 |
| 委外信息 | SectionName | string | 委外工段名称 |
| | SequenceNumber | int | 执行序号 |
| | OutsourceVendor | string | 委外单位 |
| | **SendOutDate** | **DateTime** | **发出日期（列表页首列）** |
| | SendQuantity | int? | 发出支数 |
| | SendWeight | decimal? | 发出重量(kg) |
| | Status | string | 状态（待回收/已回收） |
| | OutsourceSpec | string? | 委外规格 |
| | ExpectedReturnDate | DateTime? | 要求收回日期 |
| | IsUrgent | bool | 是否紧急 |
| 冗余 | TagNo | string? | 挂牌号 |
| | PlantGrade | string? | 工厂牌号 |
| | Remark | string? | 备注 |
| 计算 | TotalRecoveredQuantity | int? | 累计回收数量 |
| | TotalRecoveredWeight | decimal? | 正常回收总重量 |
| | TotalUnprocessedQuantity | int? | 非正常回收总支数 |
| | TotalUnprocessedWeight | decimal? | 非正常回收总重量 |
| | ActualRecoveryDate | DateTime? | 实际回收日期 |
| **产类** | **ProductStatus** | **string?** | **产类（荒管/在制/成品），由系统根据工序组和批次信息自动计算** |
| 系统 | **CreatedTime** | **DateTimeOffset** | **创建时间** |
| | **UpdatedTime** | **DateTimeOffset** | **更新时间** |

### 3.10 委外回收DTO：OutsourceRecoveryDto

**文件：** `MES.Core/DTOs/Batch/SectionOutsourceDtos.cs`

| 分组 | 字段 | 类型 | 说明 |
|------|------|------|------|
| 关联 | Id | int | 回收ID |
| | SectionOutsourceId | int | 关联委外记录ID |
| | **BatchNo** | **string?** | **批次号（跨批次列表冗余）** |
| 回收信息 | **RecoveryDate** | **DateTime** | **回收日期（列表页首列）** |
| | RecoveryQuantity | int? | 回收支数 |
| | RecoveryWeight | decimal? | 回收重量(kg) |
| | UnprocessedQuantity | int? | 未加工支数 |
| | UnprocessedWeight | decimal? | 未加工重量(kg) |
| 冗余 | OutsourceVendor | string? | 委外单位（从SectionOutsource冗余） |
| | ProcessName | string? | 工序名称（从SectionOutsource冗余） |
| | SectionName | string? | 工段名称（从SectionOutsource冗余） |
| | Remark | string? | 备注 |
| 系统 | **CreatedTime** | **DateTimeOffset** | **创建时间** |
| | **UpdatedTime** | **DateTimeOffset** | **更新时间** |
| 冗余 | OutsourceSpec | string? | 委外规格 |
| | ManufacturingSpec | string? | 制造规格 |
| | SendQuantity | int? | 发出支数 |
| | SendWeight | decimal? | 发出重量 |
| | TagNo | string? | 挂牌号 |
| | PlantGrade | string? | 工厂牌号 |

### 3.11 成检到料DTO：MaterialReceiveCheckDto

> **注：** 成检到料模块已迁移至质量管理上下文，DTO 文件位于 `MES.Core/DTOs/Quality/MaterialReceiveCheckDto.cs`。
> 详见 `docs/接口设计/质量管理上下文接口设计.md §3.26`。


### 3.12 跟踪可视化DTO

**文件：** `MES.Core/DTOs/Batch/BatchTrackingVisualDto.cs`

#### BatchTrackingVisualDto

| 字段 | 类型 | 说明 |
|------|------|------|
| BatchId | int | 批次ID |
| BatchNo | string | 批次号 |
| TotalSectionCount | int | 工段总数（排除"入库"工段，入库不计入进度统计） |
| CompletedSectionCount | int | 已完成工段数（排除"入库"工段） |
| ProgressPercent | double | 进度百分比（保留1位小数，只读计算，基于排除入库后的工段数计算） |
| CurrentGroupName | string? | 当前工序名称 |
| CurrentSectionName | string? | 当前工段名称 |
| CurrentEquipmentName | string? | 当前设备名称 |
| CurrentOutsource | string? | 当前委外 |
| CurrentSpec | string? | 当前规格 |
| NextSectionName | string? | 下一工段 |
| NextProcess | string? | 下一工序（基于下一工段所在工序组的工序名称） |
| InputQuantity | int? | 投料支数 = 现有效原料支数(CurrentValidQty) |
| InputWeight | int? | 投料重量 = 现有效原料重量(CurrentValidWeight) |
| TargetQuantity | int? | 目标支数 = 投料支数 × 制几率 |
| TargetWeight | int? | 目标重量 = 投料重量 × 工序组折扣系数 |
| ProcessGroups | List\<ProcessGroupVisualDto\> | 工序组列表（含工段级数据） |

#### ProcessGroupVisualDto

| 字段 | 类型 | 说明 |
|------|------|------|
| Id | int | 工序组ID |
| SequenceNumber | int | 序号 |
| ProcessName | string | 工序名称 |
| ManufacturingSpec | string? | 制造规格 |
| TotalSections | int | 组内工段总数 |
| CompletedSections | int | 组内已完成工段数 |
| Sections | List\<SectionVisualDto\> | 工段列表（按执行序号排序） |

#### SectionVisualDto

| 字段 | 类型 | 说明 |
|------|------|------|
| SectionName | string | 工段名称 |
| SequenceNumber | int | 执行序号 |
| ProcessGroupId | int | 工序组ID |
| Status | string | 状态：Completed/InProgress/Outsource/Next/Pending |
| ExecDate | DateTime? | 执行日期（五源优先级：生产记录→过程检验→检验到料[检验工段匹配]→工段委外→检验到料[末段回退]） |
| EquipmentName | string? | 设备名称 |
| Quantity | int? | 加工支数 |
| Weight | decimal? | 加工重量(kg) |
| Operator | string? | 操作人 |
| OutsourceVendor | string? | 委外单位 |
| OutsourceProgress | decimal? | 委外进度 |

### 3.13 请求DTO

#### CreateProductionRecordRequest

| 字段 | 类型 | 必须 | 说明 |
|------|------|------|------|
| **BatchNo** | **string** | **✅** | **批次号（替代ProductionBatchId，文本输入，服务端自动解析）** |
| ProductionBatchId | int | | 生产批次ID（内部使用，由BatchNo自动解析） |
| **ProcessGroupId** | **int?** | | **工序组ID（可空，服务端自动根据批次号+工序名称+制造规格解析）** |
| ProcessName | string | ✅ | 工序名称 |
| ManufacturingSpec | string? | | 制造规格（参与解析ProcessGroupId） |
| SectionName | string | ✅ | 工段名称（参与解析SequenceNumber） |
| SequenceNumber | int | | 执行序号（传0则由服务端通过ProcessGroupId+SectionName自动计算） |
| **ExecDate** | **DateTime** | **✅** | **执行日期（创建页首列）** |
| EquipmentName | string? | | 设备名称 |
| Operator | string? | | 操作人 |
| Shift | string? | | 班次 |
| Quantity | int? | | 加工支数 |
| Weight | decimal? | | 加工重量(kg) |
| SolutionTemperature | decimal? | | 固溶温度(℃)，仅固溶工段使用 |
| SoakTime | int? | | 保温时间(min)，仅固溶工段使用 |
| IsPreCut | bool? | | 预成切（虽是成品切割，但不是正式成品切割；不计入成品切割统计支数） |
| CuttingMultiple | decimal? | | 断切倍数 |
| FinishedCutLength | decimal? | | 成品断切长度(mm) |
| PostCutQuantity | int? | | 切后支数 |
| FaceCutCount | int? | | 平头数，仅荒管切割：1=一端，2=两端 |
| TagNo | string? | | 挂牌号 |
| PlantGrade | string? | | 工厂牌号 |
| Remark | string? | | 备注 |
| DataSource | string? | | 数据来源（SCAN=扫码报工 / MANUAL=手动录入） |

#### UpdateProductionRecordRequest（内联编辑用）

| 字段 | 类型 | 必须 | 说明 |
|------|------|------|------|
| ExecDate | DateTime | ✅ | 执行日期 |
| EquipmentName | string? | | 设备名称 |
| Operator | string? | | 操作人 |
| Shift | string? | | 班次 |
| Quantity | int? | | 加工支数 |
| Weight | decimal? | | 加工重量(kg) |
| SolutionTemperature | decimal? | | 固溶温度(℃)，仅固溶工段使用 |
| SoakTime | int? | | 保温时间(min)，仅固溶工段使用 |
| IsPreCut | bool? | | 预成切（虽是成品切割，但不是正式成品切割；不计入成品切割统计支数） |
| CuttingMultiple | decimal? | | 断切倍数 |
| FinishedCutLength | decimal? | | 成品断切长度(mm) |
| PostCutQuantity | int? | | 切后支数 |
| FaceCutCount | int? | | 平头数，仅荒管切割：1=一端，2=两端 |
| TagNo | string? | | 挂牌号 |
| PlantGrade | string? | | 工厂牌号 |
| Remark | string? | | 备注 |

#### CreateSectionOutsourceRequest

| 字段 | 类型 | 必须 | 说明 |
|------|------|------|------|
| **BatchNo** | **string** | **✅** | **批次号（替代ProductionBatchId，文本输入，服务端自动解析）** |
| **ProcessGroupId** | **int?** | | **工序组ID（可空，服务端自动根据批次号+工序名称解析）** |
| ProcessName | string | ✅ | 工序名称 |
| ManufacturingSpec | string? | | 制造规格 |
| SectionName | string | ✅ | 委外工段名称 |
| SequenceNumber | int | | 执行序号 |
| OutsourceVendor | string | ✅ | 委外单位 |
| SendOutDate | DateTime | ✅ | 发出日期 |
| SendQuantity | int? | | 发出支数 |
| SendWeight | decimal? | | 发出重量(kg) |
| OutsourceSpec | string? | | 委外规格 |
| ExpectedReturnDate | DateTime? | | 要求收回日期 |
| IsUrgent | bool | | 是否紧急 |
| TagNo | string? | | 挂牌号 |
| PlantGrade | string? | | 工厂牌号 |
| Remark | string? | | 备注 |

#### CreateOutsourceRecoveryRequest

| 字段 | 类型 | 必须 | 说明 |
|------|------|------|------|
| **SectionOutsourceId** | **int** | **✅** | **关联委外记录ID（前端通过 BatchNo+ProcessName+SectionName 解析）** |
| RecoveryDate | DateTime | ✅ | 回收日期 |
| RecoveryQuantity | int? | | 回收支数 |
| RecoveryWeight | decimal? | | 回收重量(kg) |
| UnprocessedQuantity | int? | | 未加工支数 |
| UnprocessedWeight | decimal? | | 未加工重量(kg) |
| Remark | string? | | 备注 |

#### CreateMaterialReceiveCheckRequest

> **注：** 成检到料模块已迁移至质量管理上下文，创建请求已移至 `MES.Core/DTOs/Quality/MaterialReceiveCheckDto.cs`，详见 `docs/接口设计/质量管理上下文接口设计.md`。

| 字段 | 类型 | 必须 | 说明 |
|------|------|------|------|
| **BatchNo** | **string** | **✅** | **批次号（替代ProductionBatchId，文本输入，服务端自动解析）** |
| ProductionBatchId | int | | 生产批次ID（内部使用，由BatchNo自动解析） |
| **ReceiveDate** | **DateTime** | **✅** | **到料日期（创建页首列）** |
| Shift | ShiftType? | | 班次 |
| Checker | string? | | 确认人 |
| Remark | string? | | 备注 |
| DataSource | string? | | 数据来源（SCAN/MANUAL） |
| ProcessGroupId | int | | 所属工序组ID（不传则服务端按规格匹配自动查找） |
| ProcessName | string? | | 工序名称（不传则服务端从ProcessGroup获取） |
| SequenceNumber | int? | | 执行序号（不传则服务端从ProcessGroup获取） |

#### 打印请求DTO

**ProductionRecordPrintBatchRequest：**

| 字段 | 类型 | 说明 |
|------|------|------|
| Ids | int[] | 选中记录ID数组 |
| Columns | List\<PrintColumnDef\> | 列选择定义列表 |

**ProductionRecordPrintAllRequest：**

| 字段 | 类型 | 说明 |
|------|------|------|
| Keyword | string? | 关键词 |
| SortBy | string? | 排序列 |
| IsDescending | bool | 是否降序 |
| ExecDateFrom | DateTime? | 执行日期范围（开始） |
| ExecDateTo | DateTime? | 执行日期范围（结束） |
| Columns | List\<PrintColumnDef\> | 列选择定义列表 |

**MaterialCheckPrintBatchRequest / MaterialCheckPrintAllRequest：** 同上结构，日期字段替换为 ReceiveDateFrom/ReceiveDateTo。|

### 3.14 入缸记录DTO：PicklingInRecordDto

**文件：** `MES.Core/DTOs/Batch/PicklingDtos.cs`

| 分组 | 字段 | 类型 | 说明 |
|------|------|------|------|
| 关联 | Id | int | 入缸记录ID |
| | ProductionBatchId | int | 关联批次ID |
| | ProcessGroupId | int | 关联工序组ID |
| | **BatchNo** | **string** | **批次号（列表页首列）** |
| 工序冗余 | ProcessName | string | 工序名称 |
| | ManufacturingSpec | string? | 制造规格 |
| 入缸信息 | **InDate** | **DateTime** | **入缸日期（列表页首列）** |
| | SectionName | string | 工段名称（去油/酸洗） |
| | SequenceNumber | int | 执行序号 |
| | Status | string | 状态（Soaking=浸泡中, Completed=已完工） |
| 执行信息 | EquipmentName | string? | 设备名称 |
| | Operator | string? | 操作人 |
| | Shift | string? | 班次 |
| 数量/重量 | Quantity | int? | 加工支数 |
| | Weight | decimal? | 加工重量(kg) |
| | ProductStatus | string? | 产类（荒管/在制/成品），由系统根据工序组和批次信息自动计算 |
| 冗余 | TagNo | string? | 挂牌号 |
| | PlantGrade | string? | 工厂牌号 |
| | Remark | string? | 备注 |
| **产类** | **ProductStatus** | **string?** | **产类（荒管/在制/成品），由系统根据工序组和批次信息自动计算** |
| 系统 | DataSource | string? | 数据来源 |
| | **CreatedTime** | **DateTimeOffset** | **创建时间** |
| | **UpdatedTime** | **DateTimeOffset** | **更新时间** |
| 完工汇总 | PicklingOutRecordId | int? | 关联完工记录ID（为空则未完工） |
| | CompleteDate | DateTime? | 完工日期 |

#### 创建入缸记录请求：CreatePicklingInRecordRequest

| 字段 | 类型 | 必须 | 说明 |
|------|------|------|------|
| **BatchNo** | **string** | **✅** | **批次号（替代ProductionBatchId，文本输入，服务端自动解析）** |
| ProcessGroupId | int? | | 工序组ID（可空，服务端自动根据批次号+工序名称+制造规格解析） |
| ProcessName | string | ✅ | 工序名称 |
| ManufacturingSpec | string | ✅ | 制造规格（必填，参与解析ProcessGroupId） |
| SectionName | string | ✅ | 工段名称（"去油"或"酸洗"） |
| SequenceNumber | int | | 执行序号（传0则由服务端自动通过ProcessGroupId+SectionName计算） |
| **InDate** | **DateTime** | **✅** | **入缸日期（创建页首列）** |
| EquipmentName | string? | | 设备名称 |
| Operator | string? | | 操作人 |
| Shift | string? | | 班次 |
| Quantity | int? | | 加工支数 |
| Weight | decimal? | | 加工重量(kg) |
| TagNo | string? | | 挂牌号 |
| PlantGrade | string? | | 工厂牌号 |
| Remark | string? | | 备注 |
| DataSource | string? | | 数据来源 |

#### 更新入缸记录请求：UpdatePicklingInRecordRequest（内联编辑用）

| 字段 | 类型 | 说明 |
|------|------|------|
| InDate | DateTime? | 入缸日期（null=不更新） |
| EquipmentName | string? | 设备名称（null=不更新） |
| Operator | string? | 操作人（null=不更新） |
| Shift | string? | 班次（null=不更新） |
| Quantity | int? | 加工支数（null=不更新） |
| Weight | decimal? | 加工重量（null=不更新） |
| Remark | string? | 备注（null=不更新） |

### 3.15 完工记录DTO：PicklingOutRecordDto

**文件：** `MES.Core/DTOs/Batch/PicklingDtos.cs`

| 分组 | 字段 | 类型 | 说明 |
|------|------|------|------|
| 关联 | Id | int | 完工记录ID |
| | PicklingInRecordId | int | 关联入缸记录ID |
| 完工信息 | **CompleteDate** | **DateTime** | **完工日期（列表页首列）** |
| | Remark | string? | 备注 |
| 系统 | DataSource | string? | 数据来源 |
| | **CreatedTime** | **DateTimeOffset** | **创建时间** |
| 冗余 | BatchNo | string? | 批次号（从PicklingInRecord→ProductionBatch冗余） |
| | ProcessName | string? | 工序名称 |
| | SectionName | string? | 工段名称 |
| | ManufacturingSpec | string? | 制造规格 |
| | TagNo | string? | 挂牌号 |
| | PlantGrade | string? | 工厂牌号 |

#### 创建完工记录请求：CreatePicklingOutRecordRequest

| 字段 | 类型 | 必须 | 说明 |
|------|------|------|------|
| **PicklingInRecordId** | **int** | **✅** | **关联入缸记录ID（前端通过入缸列表完工按钮传递）** |
| **CompleteDate** | **DateTime** | **✅** | **完工日期** |
| Remark | string? | | 备注 |
| DataSource | string? | | 数据来源 |

#### 更新完工记录请求：UpdatePicklingOutRecordRequest

| 字段 | 类型 | 说明 |
|------|------|------|
| CompleteDate | DateTime? | 完工日期（null=不更新） |
| Remark | string? | 备注（null=不更新） |

---

## 四、API端点

**基础路径：** `api/batch`
**控制器：** `BatchController`
**文件：** `MES.Api/Controllers/Batch/BatchController.cs`
**授权：** 所有端点需 `[Authorize]`

### 4.1 批次CRUD

| 方法 | 端点 | 说明 | 权限 |
|------|------|------|------|
| GET | `api/batch/list` | 分页查询批次列表 | BatchStaff/BatchDirector/Admin |
| GET | `api/batch/filter-contexts` | 获取各筛选列 DISTINCT 值（供 ExcelFilter 下拉使用） | BatchStaff/BatchDirector/Admin |
| GET | `api/batch/{id}` | 获取批次详情 | BatchStaff/BatchDirector/Admin |
| POST | `api/batch` | 创建批次（含工序组保存） | BatchDirector/Admin |
| PUT | `api/batch/{id}` | 更新批次信息 | BatchDirector/Admin |
| PUT | `api/batch/{id}/status` | 更新批次状态 | BatchStaff/BatchDirector/Admin |
| DELETE | `api/batch/{id}` | 删除批次（级联删除工序组） | Admin |
| POST | `api/batch/{id}/save-all` | 批量保存批次信息、状态、工序组全量替换 | BatchDirector/Admin |

### 4.2 工序组管理

| 方法 | 端点 | 说明 | 权限 |
|------|------|------|------|
| GET | `api/batch/{batchId}/records` | 获取批次的工序组列表 | BatchStaff/BatchDirector/Admin |
| POST | `api/batch/{batchId}/records` | 添加工序组 | BatchStaff/BatchDirector/Admin |
| DELETE | `api/batch/records/{recordId}` | 删除工序组 | BatchDirector/Admin |

### 4.3 查询

| 方法 | 端点 | 说明 | 权限 |
|------|------|------|------|
| GET | `api/batch/available-batches` | 获取可用库存批次列表 | BatchStaff/BatchDirector/Admin |
| GET | `api/batch/next-batch-no` | 获取下一个生产编号 | BatchStaff/BatchDirector/Admin |
| GET | `api/batch/last-process-groups` | 获取上批次工序组（复制用） | BatchStaff/BatchDirector/Admin |
| GET | `api/batch/by-work-order/{workOrderNo}` | 按工单号查询批次 | BatchStaff/BatchDirector/Admin |
| GET | `api/batch/verify-workorders` | 验证工单号是否存在 | BatchStaff/BatchDirector/Admin |
| GET | `api/batch/{id}/tracking` | 获取批次跟踪可视化数据（8阶段进度） | BatchStaff/BatchDirector/Admin |
| GET | `api/batch/{id}/adjacent` | 获取相邻批次导航（上一条/下一条ID+批次号） | BatchStaff/BatchDirector/Admin |
| GET | `api/batch/{batchNo}/process-groups` | 按批次号调取工序组列表 | BatchStaff/BatchDirector/Admin |
| GET | `api/batch/{id}/operation-logs` | 获取批次操作日志列表 | BatchStaff/BatchDirector/Admin |
| GET | `api/batch/all-list` | 获取全量批次列表 | BatchStaff/BatchDirector/Admin |
| GET | `api/batch/by-batch-no/{batchNo}` | 按批次号查询详情 | BatchStaff/BatchDirector/Admin |

### 4.4 打印

| 方法 | 端点 | 说明 | 权限 |
|------|------|------|------|
| GET | `api/batch/{id}/print` | 打印批次详情PDF | BatchStaff/BatchDirector/Admin |
| POST | `api/batch/print-all` | 打印全部批次PDF（含列选择） | BatchStaff/BatchDirector/Admin |
| POST | `api/batch/print-selected` | 打印选中批次PDF（支持多选） | BatchStaff/BatchDirector/Admin |
| POST | `api/batch/print-process-card` | 工艺流转卡打印PDF（列选择+A4横向） | BatchStaff/BatchDirector/Admin |
| POST | `api/batch/print-batch-file` | 打印批次详情PDF（直接返回 PDF 文件） | BatchStaff/BatchDirector/Admin |
| POST | `api/batch/print-all-file` | 打印全部批次PDF（直接返回 PDF 文件） | BatchStaff/BatchDirector/Admin |
| POST | `api/batch/print-selected-file` | 打印选中批次PDF（直接返回 PDF 文件） | BatchStaff/BatchDirector/Admin |
| POST | `api/batch/print-process-card-file` | 工艺流转卡打印PDF（直接返回 PDF 文件） | BatchStaff/BatchDirector/Admin |

### 4.5 列表查询参数（批次）

**分页参数（BatchQueryParams）：**

| 参数 | 类型 | 说明 |
|------|------|------|
| PageIndex | int | 页码（默认1） |
| PageSize | int | 每页条数（默认20，最大5000） |
| Keyword | string? | 关键字（匹配BatchNo/WorkOrderNo/TagNo） |
| SortBy | string? | 排序列（默认CreatedTime） |
| IsDescending | bool | 是否降序（默认true） |
| WorkOrderNo | string? | 工单号筛选 |
| Status | string? | 状态筛选（后端使用 `Enum.TryParse<BatchStatus>()` 直接比对枚举值，避免 `.ToString()` 导致 EF Core 翻译异常） |
| TagNo | string? | 挂牌号筛选 |
| BatchNo | string? | 批次号筛选 |
| SalesOrderNo | string? | 订单号筛选（通过 filters JSON 传参，非直接 Query 参数）（2026-05-12 新增） |
| ProductionMainNo | string? | 主号筛选（通过 filters JSON 传参，非直接 Query 参数）（2026-05-12 新增） |
| ProductionSubNo | string? | 次号筛选（通过 filters JSON 传参，非直接 Query 参数）（2026-05-12 新增） |
| StartDateFrom | DateTime? | 创建日期范围（开始） |
| StartDateTo | DateTime? | 创建日期范围（结束） |
| HasInputChange | string? | 有效投料变化筛选（是/否，后端查询 DB 字段过滤） |

### 4.6 生产记录API

**基础路径：** `api/production-record`
**控制器：** `ProductionRecordController`
**文件：** `MES.Api/Controllers/Batch/ProductionRecordController.cs`
**授权：** 所有端点需 `[Authorize]`

#### 内部生产记录

| 方法 | 端点 | 说明 | 权限 |
|------|------|------|------|
| GET | `api/production-record/{batchId}/records` | 获取批次的生产记录列表（分页） | BatchStaff/BatchDirector/Admin |
| POST | `api/production-record/record` | 创建内部生产记录（BatchNo替代ProductionBatchId） | BatchDirector/Admin |
| **PUT** | **`api/production-record/record/{id}`** | **更新内部生产记录（内联编辑）** | **BatchDirector/Admin** |
| POST | `api/production-record/records/batch` | 批量创建内部生产记录 | BatchDirector/Admin |
| DELETE | `api/production-record/record/{id}` | 删除内部生产记录 | BatchDirector/Admin |
| GET | `api/production-record/all/records` | 跨批次查询所有生产记录（分页+关键词+ExecDate范围+可排序列） | BatchStaff/BatchDirector/Admin |
| **POST** | **`api/production-record/print-batch`** | **打印选中生产记录（列选择）** | **BatchStaff/BatchDirector/Admin** |
| **POST** | **`api/production-record/print-all`** | **打印全部生产记录（列选择+筛选）** | **BatchStaff/BatchDirector/Admin** |

**跨批次查询参数（4.5 QueryParams 之外的生产记录专用参数）：**

| 参数 | 类型 | 说明 |
|------|------|------|
| ExecDateFrom | DateTime? | 执行日期范围（开始） |
| ExecDateTo | DateTime? | 执行日期范围（结束） |

支持排序列：execdate, batchno, processname, sectionname, sequencenumber, equipmentname, operator, quantity, weight, cuttingmultiple, finishedcutlength, postcutquantity, createdtime, updatedtime

#### 工段委外

| 方法 | 端点 | 说明 | 权限 |
|------|------|------|------|
| GET | `api/production-record/{batchId}/outsources` | 获取批次的工段委外列表（分页） | BatchStaff/BatchDirector/Admin |
| POST | `api/production-record/outsource` | 创建工段委外（BatchNo替代ProductionBatchId） | BatchDirector/Admin |
| POST | `api/production-record/outsources/batch` | 批量创建工段委外 | BatchDirector/Admin |
| **PUT** | **`api/section-outsource/{id}`** | **更新工段委外（内联编辑）** | **BatchDirector/Admin** |
| DELETE | `api/production-record/outsource/{id}` | 删除工段委外 | BatchDirector/Admin |
| GET | `api/production-record/all/outsources` | 跨批次查询所有工段委外（分页+关键词+SendOutDate范围+可排序列） | BatchStaff/BatchDirector/Admin |
| **POST** | **`api/production-record/outsource/print-batch`** | **打印选中工段委外（列选择）** | **BatchStaff/BatchDirector/Admin** |
| **POST** | **`api/production-record/outsource/print-all`** | **打印全部工段委外（列选择+筛选）** | **BatchStaff/BatchDirector/Admin** |

#### 委外回收

| 方法 | 端点 | 说明 | 权限 |
|------|------|------|------|
| GET | `api/production-record/outsource/{outsourceId}/recoveries` | 获取委外记录的回收列表 | BatchStaff/BatchDirector/Admin |
| POST | `api/production-record/recovery` | 创建委外回收（BatchNo+ProcessName+SectionName替代SectionOutsourceId） | BatchDirector/Admin |
| POST | `api/production-record/recoveries/batch` | 批量创建委外回收 | BatchDirector/Admin |
| DELETE | `api/production-record/recovery/{id}` | 删除委外回收 | BatchDirector/Admin |
| GET | `api/production-record/all/recoveries` | 跨批次查询所有委外回收（分页+关键词+RecoveryDate范围+可排序列） | BatchStaff/BatchDirector/Admin |
| **POST** | **`api/production-record/recovery/print-batch`** | **打印选中委外回收（列选择）** | **BatchStaff/BatchDirector/Admin** |
| **POST** | **`api/production-record/recovery/print-all`** | **打印全部委外回收（列选择+筛选）** | **BatchStaff/BatchDirector/Admin** |

#### 成检到料

> **注：** 成检到料模块已迁移至质量管理上下文，API 由 `MaterialReceiveCheckController` 管理。
> 详见 `docs/接口设计/质量管理上下文接口设计.md §4.10`。本处不再重复列出。

#### 跟踪刷新

| 方法 | 端点 | 说明 | 权限 |
|------|------|------|------|
| POST | `api/production-record/{batchId}/refresh-tracking` | 刷新批次跟踪字段 | BatchStaff/BatchDirector/Admin |
| POST | `api/production-record/refresh-all-tracking` | 刷新所有非强制完成批次的跟踪字段（批量更新当前工序/工段/设备/委外/下一工序/完工判定等） | BatchDirector/Admin |
| POST | `api/production-record/backfill-theoretical-output` | 回填所有批次理论计算字段（理论成品支/重/单支重，含强制完成批次） | BatchDirector/Admin |
| POST | `api/production-record/refresh-all-cut-length-match` | 刷新所有生产记录的定尺切割长度匹配标识 CutLengthMatchType | BatchDirector/Admin |

#### 独立 SectionOutsourceController

**基础路径：** `api/section-outsource`
**控制器：** `SectionOutsourceController`
**文件：** `MES.Api/Controllers/Batch/SectionOutsourceController.cs`
**授权：** 所有端点需 `[Authorize]`

提供跨批次工段委外管理，包括列表查询、创建、更新、删除、批量创建、回收管理及打印等 10+ 个端点。

### 4.7 去油/酸洗 API

**基础路径：** `api/pickling`
**常量：** `ApiEndpoints.Pickling = "api/pickling"`
**控制器：** `PicklingController`
**文件：** `MES.Api/Controllers/Batch/PicklingController.cs`
**授权：** 所有端点需 `[Authorize]`

#### 入缸记录

| 方法 | 端点 | 说明 | 权限 |
|------|------|------|------|
| GET | `api/pickling/list` | 跨批次分页查询入缸记录（关键词+InDate范围+CompleteDate范围+排序+筛选） | BatchStaff/BatchDirector/Admin |
| POST | `api/pickling` | 创建入缸记录（BatchNo替代ProductionBatchId） | BatchDirector/Admin |
| POST | `api/pickling/batch` | 批量创建入缸记录 | BatchDirector/Admin |
| PUT | `api/pickling/{id}` | 更新入缸记录（内联编辑） | BatchDirector/Admin |
| DELETE | `api/pickling/{id}` | 删除入缸记录（已有完工时禁止删除） | Admin |
| GET | `api/pickling/filter-contexts` | 入缸记录筛选上下文 | BatchStaff/BatchDirector/Admin |
| POST | `api/pickling/print-selected` | 批量打印选中入缸记录 | BatchStaff/BatchDirector/Admin |
| POST | `api/pickling/print-all` | 按筛选条件打印全部入缸记录 | BatchStaff/BatchDirector/Admin |

#### 完工记录

| 方法 | 端点 | 说明 | 权限 |
|------|------|------|------|
| GET | `api/pickling/{picklingInRecordId}/out-record` | 获取指定入缸的完工记录 | BatchStaff/BatchDirector/Admin |
| GET | `api/pickling/out-records/list` | 跨批次分页查询完工记录（关键词+CompleteDate范围+排序+筛选） | BatchStaff/BatchDirector/Admin |
| POST | `api/pickling/out-record` | 创建完工记录（自动更新入缸状态为Completed） | BatchDirector/Admin |
| PUT | `api/pickling/out-record/{id}` | 更新完工记录 | BatchDirector/Admin |
| DELETE | `api/pickling/out-record/{id}` | 删除完工记录（自动恢复入缸状态为Soaking） | Admin |
| GET | `api/pickling/out-records/filter-contexts` | 完工记录筛选上下文 | BatchStaff/BatchDirector/Admin |

#### 入缸记录查询参数（QueryParams 专用参数）

| 参数 | 类型 | 说明 |
|------|------|------|
| InDateFrom | DateTime? | 入缸日期范围（开始） |
| InDateTo | DateTime? | 入缸日期范围（结束） |
| CompleteDateFrom | DateTime? | 完工日期范围（开始，通过关联完工记录筛选） |
| CompleteDateTo | DateTime? | 完工日期范围（结束） |

支持排序列（24列）：batchno, processname, sectionname, manufacturingspec, indate, status, sequencenumber, tagno, plantgrade, remark, updatedtime, equipmentname, operator, shift, quantity, weight, isfinished, completedate, createdtime

#### 完工记录查询参数

| 参数 | 类型 | 说明 |
|------|------|------|
| CompleteDateFrom | DateTime? | 完工日期范围（开始） |
| CompleteDateTo | DateTime? | 完工日期范围（结束） |

支持排序列：completedate, createdtime

### 版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| **V5.3** | **2026-08-09** | **成切跟踪/产类/26工段/级联重算同步**：§3.1 列表DTO新增 IsForceCompleted/ProductUnitWeight/InspectionStage（成检附加）/6个过程检字段；§3.2 详情DTO批次自身补 HasInputChange/理论字段/InspectionStage/成切跟踪，工单冗余补 ProductUnitWeight（30→31），仓库冗余删 WarehouseId/InboundSource/InboundDate；§3.3/§3.7 工序组 15工段→26工段；§3.7 三请求DTO删 InboundSource/InboundDate 残留、仓库来源组补 InputType/SourceRemark/SourceProductionNo；§3.8 ProductionRecordDto 补 IsPreCut/LengthStatus/CutLengthMatchType+Display/批次导航三字段，ProductStatus 改称「产类」；§3.9/§3.13/§3.14 同步「产类」、请求DTO 移除非输入的 ProductStatus；§4.6 补 backfill-theoretical-output/refresh-all-cut-length-match 端点 |
| **V4.4** | **2026-07-08** | **生产记录/工艺卡打印列分组规范化（仅前端变化，DTO无变化）**：ProductionRecords UI 列拆分为4组（G1执行信息/G2产出数据/G3工艺参数/G4追溯信息）；ProcessCardPrint 分组对齐批次首页（Status/ProductionType/ManufacturingItem 从 G3 移回 G1）|
| **V4.5** | **2026-07-16** | **文档审查修复**：§4.4 补充 print-batch-file/print-all-file/print-selected-file/print-process-card-file 共4个直接返回 PDF 的打印端点 |
| **V5.0** | **2026-07-26** | **ManufacturingStatus/合并投料支持**：§3.1/§3.2/§3.7 ProductionBatchListDto/DetailDto/CreateRequest/UpdateRequest/SaveBatchRequest 均新增 ManufacturingStatus 字段；§3.7 新增 SourceBatchItemDto/SourceBatchItemRequest DTO 文档；CreateRequest 补充 SourceItems 合并投料字段（字段计数 28→29）；UpdateRequest/SaveBatchRequest 补充 ProductionRatio/SourceItems（字段计数 29→30）；DetailDto 补充 SourceItems 列表 |
| **V4.9** | **2026-07-26** | **CreateMaterialReceiveCheckRequest 同步质量管理上下文最新字段**：移除 ReceivedQuantity/ReceivedWeight 已废弃字段；Shift 类型修正为 ShiftType?；新增 DataSource/ProcessGroupId/ProcessName/SequenceNumber 工序关联字段；补充迁移说明 |
| **V4.8** | **2026-07-26** | **文档同步更新**：Version bump，与各模块文档版本对齐 |
| **V4.7** | **2026-07-20** | **操作日志统一管理**：操作日志已由基础设施统一管理，返回类型改为 `OperationLogDto`（`MES.Core.DTOs.Infrastructure`），通过 `IOperationLogService.GetLogsAsync("Batch", entityId)` 查询 |
| **V4.6** | **2026-07-20** | **PicklingOutRecordDto 冗余字段确认**：BatchNo/ProcessName/TagNo 已在 §3.13 中覆盖，无新增 DTO 变更 |
| V1.0 | 2026-05-08 | 初始版本 |
| V1.1 | 2026-05-09 | 补充生产记录完整子DTO（ProductionRecord/SectionOutsource/OutsourceRecovery/MaterialReceiveCheck）和对应请求DTO及19个API端点 |
| V1.2 | 2026-05-09 | **DTO字段同步**：ProductionBatchListDto 去除 ProductionRatio，新增 SalesOrderNo/ProductionMainNo/ProductionSubNo/CurrentEquipmentName/CurrentOutsource/NextSectionName；ProductionBatchDetailDto 仓库冗余新增 SourcePlantGrade/SourceSpecification/SourceLengthStatus/SourceUnitWeight；UpdateProductionBatchRequest 扩展为完整可更新字段（含 IsForceCompleted + 29个工单冗余字段） |
| **V1.3** | **2026-05-11** | **全面更新生产记录/检验到料DTO**：ProductionRecordDto 新增 BatchNo/CreatedTime/UpdatedTime（22字段）；CreateProductionRecordRequest 用 BatchNo 替代 ProductionBatchId、ProcessGroupId 改为可空；新增 UpdateProductionRecordRequest；新增打印请求DTO；新增 PUT/print 端点；CreateMaterialReceiveCheckRequest 用 BatchNo 替代 ProductionBatchId；MaterialReceiveCheckDto 新增 CreatedTime/UpdatedTime；SectionOutsource/OutsourceRecovery DTO 标记为待重建（新增 BatchNo/CreatedTime/UpdatedTime） |
| **V1.4** | **2026-05-11** | **SequenceNumber 改为可选/自动计算**：CreateProductionRecordRequest.SequenceNumber 去掉 Required，传0时服务端自动通过 ProcessGroupId+SectionName 解析；ProcessGroupId 解析补充说明含 ManufacturingSpec（三个条件联合）|
| **V1.5** | **2026-05-12** | **移除所有待重建标记**：工段委外和委外回收 DTO/Request/API 端点文档已全部实现，移除"待重建"标记 |
| **V1.7** | **2026-05-13** | **BatchStatus 新增 Suspended=3**（挂起）；**新增4个API端点**：跟踪可视化 GET `{id}/tracking`、相邻批次 GET `{id}/adjacent`、按批次号调取工序组 GET `{batchNo}/process-groups`、操作日志 GET `{id}/operation-logs`；**新增3个DTO**：AdjacentBatchDto、BatchTrackingVisualDto、OperationLogDto（原 BatchOperationLogDto，后统一管理） |
| **V1.8** | **2026-05-14** | **BatchStatus 新增 Cancelled=4**（作废）；**DTO 新增字段**：ProductionBatchListDto/DetailDto/CreateRequest/UpdateRequest 均增加 CurrentValidQty、CurrentValidWeight
| **V1.9** | **2026-05-14** | **过程检验独立**：生产记录禁止"检验"工段（由过程检验模块管理）；检验到料菜单移至质量管理；过程检验 API 详见 `质量管理上下文接口设计.md` |
| **V2.0** | **2026-05-14** | **删除 DefectQuantity/DefectWeight**：ProductionRecordDto/CreateProductionRecordRequest/UpdateProductionRecordRequest 均移除不合格支数和不合格重量字段；移除对应排序列 |
| **V2.1** | **2026-05-17** | **新增 ManufacturingItem（制造物品）字段**：列表 DTO §3.1 新增 ManufacturingItem 字段；创建/更新/详情 DTO 均已同步新增 ManufacturingItem
| **V2.2** | **2026-05-17** | **新增 SaveBatchRequest 文档 + 字段扩展**：UpdateProductionBatchRequest 追加 ProductionType/InboundSource/InboundDate；新增 SaveBatchRequest DTO 完整字段说明 |
| **V2.3** | **2026-05-17** | **ProductionType/ManufacturingItem 改为必填**：DTO 字段标记改为 string(非空)；Create/Update/SaveAll 接口文档补充必填约束；后端 UpdateAsync 补写赋值（之前缺失）并增加非空验证 |
| **V2.4** | **2026-05-18** | **新增 §3.10 跟踪可视化DTO**：补充 BatchTrackingVisualDto/ProcessGroupVisualDto/SectionVisualDto 完整字段说明，包括 ExecDate 四源优先级说明 |
| **V2.5** | **2026-05-18** | **DTO字段补充 + ValidInputQuestion**：ProductionBatchListDto 新增 ProductionRatio、CurrentSpec、CorrespondingSpec、ValidInputQuestion（初始为内存计算，V2.6 改为 DB 持久化）；ProcessGroupDto 新增 ManufacturingMultiple（制成倍数）；SaveBatchRequest 补充 ProductionRatio；BatchQueryParams 新增 ValidInputQuestion 筛选参数 |
| **V2.6** | **2026-05-23** | **ValidInputQuestion 改为 bool? 持久化**：ProductionBatchListDto.ValidInputQuestion 从 string? 改为 bool?；WarehouseName 从 ProductionBatchDetailDto 移除（不再需要）；ValidInputQuestion 从内存计算改为 ProductionRecordService 刷新跟踪时写库持久化；BatchQueryParams.ValidInputQuestion 说明同步更新 |
| **V2.7** | **2026-06-02** | **新增 NextProcess（下一工序）字段 + refresh-all-tracking 端点**：ProductionBatchListDto/DetailDto/BatchTrackingVisualDto 均新增 NextProcess（string?）；§4.6 跟踪刷新新增 `POST /api/production-record/refresh-all-tracking` 批量刷新端点 |
| **V2.8** | **2026-06-02** | **跟踪可视化 DTO 更新**：SectionVisualDto.ExecDate 优先级从"四源"更新为"五源"（新增检验到料在过程检验和工段委外之间的中间层优先级）；BatchTrackingVisualDto TotalSectionCount/CompletedSectionCount/ProgressPercent 说明更新（排除入库工段不计入进度统计） |
| **V2.9** | **2026-06-02** | **NextProcess 计算逻辑变更**：从"基于 CurrentGroupName 按 SequenceNumber 跳跃到下一工序组"改为"基于下一工段 NextSectionName 所在工序组的工序名称"，与 NextSectionName 保持一致 |
| **V3.0** | **2026-06-13** | **新增去油/酸洗模块接口设计**：新增 PicklingStatus 枚举（§2.3）；新增 PicklingInRecordDto（§3.12）及 Create/Update 请求 DTO；新增 PicklingOutRecordDto（§3.13）及 Create/Update 请求 DTO；新增 §4.7 去油/酸洗 API 端点（16个端点，含入缸记录+完工记录+打印+筛选上下文） |
| **V3.1** | **2026-06-15** | **ProductionRecord 新增 3 个工段专用字段**：ProductionRecordDto/CreateProductionRecordRequest/UpdateProductionRecordRequest 均新增 SolutionTemperature（decimal?，固溶温度℃）、SoakTime（int?，保温时间min）、FaceCutCount（int?，平头数1一端/2两端）|
| **V3.2** | **2026-07-11** | **新增 ProductStatus 制造状态字段**：ProductionRecordDto/SectionOutsourceDto/PicklingInRecordDto 新增 ProductStatus（string?）字段 |
| **V5.2** | **2026-08-02** | **CutDoubt 枚举化**：列表/详情 DTO 的 CutDoubt 从 bool? 改为 CutDoubtType?（枚举字符串），CutDoubtDisplay 四态（疑问-数量/疑问-缺少/正常/略）；新增"疑问-缺少"判定（需求=是、执行=否、批次已到成检/完成且非强制完成 → 缺失成品切割记录） |
| **V5.1** | **2026-08-01** | **批次同步更新**：§3.1 列表 DTO 新增成切跟踪四字段（CutRequirement/CutExecution/CutQuantity/CutDoubt + Display）与理论计算字段（TheoreticalOutputQty/Weight/UnitWeight）；ValidInputQuestion 全文档改为 HasInputChange（2026-07-30 重构）；CurrentValidWeight 类型 decimal?→int?；ProcessGroupDto 移除 ManufacturingMultiple（2026-08-01 已删除，可改制换算改用批次级 ProductionRatio）；版本历史顺序修正（V2.6 按日期重排） |
