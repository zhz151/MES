# 全系统跨上下文联动分析

> 版本：v1.5（2026-09-16）
> 覆盖范围：8 个上下文，累计 25 处遗漏（首轮 14 + 二轮 8 + 三审 3）
> 状态：跨上下文数据联动审计完成并已收敛到当前代码。4 类联动模式（全量聚合重算/派生态重算/增量计算/通知模式/精准消除/主动查询）+ 3 层分工（必须一致/应该知道/可以核查）；8 个刷新目标（WES/QPT/BatchTrk/SourceOrd/MatPlan/OrderList/CutLen/FixedLengthWO），触发矩阵覆盖 14 类写入入口；批处理派生态重算 CutLen（LengthStatus/工单号/订单号/主号任一变更级联 `RecomputeCutLengthMatchByBatchAsync`）；读模型依赖链双路径对齐（WES 全量刷新末尾连带刷新 OrderList）；Hangfire 定时任务兜底全量重建（`FullProjectDataRefreshJob`，每日 11:55 / 23:55）。
>
> **本次变更（v1.5，2026-09-16）**：**补录首轮之后的审计与兜底机制（原文档停留 2026-09-04 首轮状态）** —— 新增「二轮追加修复（2026-08-26，8 项）」「三审追加修复（2026-08-31）」「定时兜底任务（Hangfire）」三节；§自包含上下文的「配置」行由误述「纯查询，无增删改入口」更正为「有写入入口但属主数据维护、无需跨上下文刷新，例外 = 日产量预估」。

## 联动模式

系统中有 4 种跨上下文数据联动模式：

| 模式 | 说明 | 适用场景 | 示例 |
|------|------|---------|------|
| **全量聚合重算** | SaveChanges 后重新 SUM/MAX 源表，Upsert 目标表 | **必须一致** — 不重算就是错的数值聚合 | WES、QPT、SourceOrder 同步、BatchTracking、OrderList 读模型、FixedLengthWorkOrder 重建 |
| **派生态重算** | SaveChanges 后按上游字段重算下游派生列（单批次级联） | **必须一致** — 不重算派生字段过期 | CutLengthMatchType（批次编辑级联 RecomputeCutLengthMatchByBatchAsync） |
| **增量计算** | SaveChanges 后直接 +-= 目标字段 | **必须一致** — 简单的数值加减 | 仓库出库 RemainingQuantity -= OutboundQuantity |
| **通知模式** | SaveChanges 后创建或消除 Notification 记录 | **应该知道** — 可能有影响，需要人判断 | OrderDeleted、OrderChanged（工单页 MudAlert 展示） |
| **精准消除** | SaveChanges 后按 WorkOrderNo 消除指定通知记录 | **必须精准** — 按条件消除，避免误伤其他关联单据的通知 | G8/G9 Type A：批次创建时只消除当前分工单的在产计划通知，保留其他分工单的通知 |
| **主动查询** | 前端页面加载时主动调用校验接口 | **可以核查** — 结果是二元的，实时扫描一步到位 | Batches.razor 的 VerifyWorkOrderNosAsync |

### 三层分工原则

跨上下文数据联动按照"影响的确定性"分为三层：

```
必须一致  →  全量聚合重算 / 增量计算（自动执行，无需人工）
应该知道  →  通知模式（记录事件，用户查看后消除，消除 ≠ 已修复）
可以核查  →  主动查询（页面加载时实时扫描，兜底发现异常）
```

以工单号为核心的具体映射：

| 场景 | 模式 | 原因 | 状态 |
|------|------|------|------|
| 工单**被删除**，批次/入库引用已不存在的工单号 | **主动查询** | 存在/不存在是二元的，无需事件记录，实时扫描即可 | ✅ 已实现（Batches + InboundHistory） |
| 工单**内容变更**（ManufacturingItem/PlantGrade 等变，工单号不变） | **通知模式** | 需要人判断是否要同步到批次，不是所有变更都需要修正 | ✅ 已实现（WorkOrderService → Batches 页 MudAlert） |
| WES/QPT/BatchTrk 聚合值过期 | **全量聚合重算** | 数值计算必须实时准确，不重算就是错的 | ✅ 已实现（触发矩阵覆盖 14 类入口） |

## 8 个刷新目标

| 缩写 | 目标 | 刷新方法 | 实现方式 |
|------|------|---------|---------|
| **WES** | 工单执行摘要 | `IWorkOrderExecutionService.RefreshByWorkOrderNosAsync()` | 全量聚合 |
| **QPT** | 质量过程跟踪 | `IQualityProcessTrackingService.RefreshByMrCheckIdAsync / RefreshByProductionBatchIdAsync / RefreshByBatchNoAsync` | 全量聚合 |
| **BatchTrk** | 批次追踪字段 | `IProductionRecordService.RefreshBatchTrackingFieldsAsync()` | 全量聚合 |
| **SourceOrd** | 采购/委外收货同步 | `IInventorySyncService.SyncSourceOrdersAsync()` | 全量聚合 |
| **MatPlan** | 改制计划周期 | `IMaterialPlanService.RecalculateStandardCycleForBatchAsync()` | 单点重算 |
| **OrderList** | 订单读模型（订单执行聚合三字段 + 成品库存聚合四字段：入库/出库/库存/业务完结） | `IOrderService.RefreshByOrderIdAsync()` | 全量聚合（WES 刷新路径末尾连带，双路径已对齐） |
| **CutLen** | 定尺切割长度匹配标识 | `RecomputeCutLengthMatchByBatchAsync`（生产记录+成检两 Service，批次编辑级联） | 派生态重算（批次编辑级联；无独立全量 refresh-all 端点） |
| **FixedLengthWO** | 定尺工单联通视图 | `BuildFixedLengthWorkOrders`（随工单 CRUD 重建） | 全量重建 |

## 触发矩阵

```
入口 \ 目标            WES   QPT  BatchTrk SourceOrd MatPlan OrderList CutLen FixedLengthWO
────────────────────────────────────────────────────────────────────────────────────────────
仓库-入库/更新/删除     ✅    ✅     ✅       ✅       —       —      —       —
批次-Create/Update     ✅    —     ✅       —       —       —      ⚠️      —
批次-Delete/Status     ✅    —     —       —       —       —      —       —
批次-SaveAll           ✅    —     ✅       —       ✅      —      ⚠️      —
批次-ProcessGroup      —     —     ✅       —       —       —      —       —
生产记录 CRUD           ✅    ✅    ✅       —       —       —      —       —
委外 CRUD              —     —     ✅       —       —       —      —       —
酸洗 CRUD              —     —     ✅       —       —       —      —       —
工序检验 CRUD          —     —     ✅       —       —       —      —       —
成检 CRUD              ✅    ✅    —       —       —       —      —       —
到料 CRUD              ✅    ✅    ✅       —       —       —      —       —
采购单 CRUD            ✅    —     —       —       —       —      —       —
委外单 CRUD            ✅    —     —       —       —       —      —       —
批次库存 CRUD          ✅    —     ✅       —       —       —      —       —
工单-Create/Update      —     —     —       —       —       —      ⚠️      ✅
工单-Delete             —     —     —       —       —       —      ⚠️      ✅
工单-摘要刷新(全量/增量) —     —     —       —       —       ✅      —       —
```

> **⚠️ CutLen 触发明细**：批次编辑（UpdateAsync/SaveAllAsync）仅在 **LengthStatus/工单号/订单号/主号 任一实际变更** 时级联 `RecomputeCutLengthMatchByBatchAsync`（生产记录按自身冗余 LengthStatus 重算、成检按批次 LengthStatus 重算）。工单 CRUD 重建 FixedLengthWorkOrder 后 **不级联** CutLen（遗留：需手动 refresh-all 回填）。成品入库（InventoryBatch）的 CutLengthMatchType 走仓库入库路径的 refresh-all，不在批次编辑级联范围。

## 已修复的 14 处遗漏

| # | 文件 | 方法 | 缺少 | 修复内容 |
|---|------|------|------|---------|
| 1 | `InventoryBatchWriteService.cs` | `InboundAsync` | WES | 新增 `TryRefreshExecutionSummaryAsync(entity.WorkOrderNo)` |
| 2 | `InventoryBatchWriteService.cs` | `BatchInboundAsync` | QPT + WES | 新增去重刷新 QPT + WES |
| 3 | `PurchaseOrderService.cs` | `SyncAllAsync` | WES | 新增去重刷新关联工单 WES |
| 4 | `InventorySyncService.cs` | `SyncSourceOrdersAsync` | WES(PO) | 新增刷新采购单关联的 WES |
| 5 | `PicklingService.cs` | 7 个 CRUD | BatchTrk | 注入 `IProductionRecordService`，全部加刷新 |
| 6 | `MaterialReceiveCheckService.cs` | 3 个 CRUD | WES | 注入 `IWorkOrderExecutionService`，3个方法加刷新 |
| 7 | `WorkOrderService.cs` | `GenerateWorkOrdersAsync` | WorkOrderChanged 通知 | 工单内容变更时按工单聚合通知引用批次页（24h 去重） |
| 8 | `MaterialReceiveCheckService.cs` | `CreateMaterialReceiveCheckAsync` / `BatchCreateMaterialReceiveChecksAsync` | BatchTrk | 创建成检到料时补充 `RefreshBatchTrackingFieldsAsync` 调用（此前仅删除时刷新，创建时遗漏） |
| 9 | `InventoryBatchWriteService.cs` | `TryCompleteProductionBatchAsync` / `HardDeleteInventoryBatchAsync` | BatchTrk | 入库完成推进后补充 BatchTrk 刷新；HardDelete 最后一条入库时回退批次状态 |
| 10 | `BatchService.cs` / `InventoryBatchWriteService.cs` | ProductionBatchInventory CRUD | WES + BatchTrk | 批次库存关联增删改时同步刷新 WES（工单执行摘要）和 BatchTrk（批次追踪字段），确保合并投料场景的聚合数据一致 |
| 11 | `MaterialPlanService.cs` | `DismissInMainWorkOrderPlansByBatchAsync` | 通知消除 | G9 Type A 场景：`DismissInMainWorkOrderPlansByBatchAsync` 批量消除源批次 ALL 在产计划，误伤其他分工单的待处理计划。新增 `DismissInMainWorkOrderPlanByBatchAndWorkOrderAsync` 按 `WorkOrderNo` 精准过滤，JOIN WorkOrders 表后再消除 |
| 12 | `MaterialPlanService.cs` | `DismissInProcessReworkPlansByBatchAsync` | 通知消除 | G8 Type A 场景：与 G9 相同问题。新增 `DismissInProcessReworkPlanByBatchAndWorkOrderAsync`，按 `WorkOrderNo` 精准消除；同时修复 `GetPendingInProcessReworkPlansAsync` 缺少 `PlanStatus == Planned` 筛选导致已完成计划不消失的问题 |
| 13 | `WorkOrderExecutionService.cs` | `RefreshAllAsync` | OrderList | 全量刷新工单摘要路径末尾**只 SaveChanges 不刷新订单读模型**（增量路径 `RefreshByWorkOrderNosAsync` 有）→ 全量刷新后订单页"订单执行"聚合停留旧值。修复：`RefreshAllAsync` 末尾复用已加载的 `salesOrders` 逐个 `RefreshByOrderIdAsync`，双路径对齐 |
| 14 | `BatchService.cs` | `UpdateAsync` / `SaveAllAsync` | CutLen | 批次编辑变更 **LengthStatus/工单号/订单号/主号** 时未重算下游派生列 `CutLengthMatchType` → 生产记录/成检记录该列过期（行内矛盾）。修复：新增 `RecomputeCutLengthMatchByBatchAsync`，两编辑入口在任一上游字段实际变化时级联调用 |

## 二轮追加修复（2026-08-26，8 项）

首轮 14 处之外的补充审计，**已全部落地**：

| # | 文件 | 方法 | 缺什么 | 修复内容 |
|---|------|------|--------|---------|
| 15 | `OrderService.cs` | `UpdateAsync` | WES + OrderList + 待发货缓存 | **订单头变更**未连带刷新：注入 `IServiceScopeFactory` 经 scope **懒解析** `IWorkOrderExecutionService`（规避 DI 环）刷新关联工单 WES，订单读模型按**新旧订单号双刷**，并调用 `IPendingDeliveryQueryService.InvalidateCachesAsync()` 失效待发货客户快照 |
| 16 | `DailyOutputEstimateService.cs` | `SaveAsync` / `DeleteAsync` | WES + 用料总览 | **日产量预估配置 CRUD** 未触发读模型：构造加可选 `IServiceScopeFactory`，保存/删除后 scope 懒解析刷新 `RefreshAllAsync` + 按全部 `WorkOrders.SalesOrderNo` 刷新用料总览（⚠️ 不可直接注入：与 WES 成环） |
| 17 | `PendingDeliveryQueryService.cs` | 新增 `InvalidateCachesAsync()` | 缓存失效入口分散 | 统一清理 **C0/C1/C2** 三层缓存（C2 动态键由静态 `ConcurrentDictionary` 跟踪）；`InventoryService` 原 8 处 `_cache.Remove(CacheKey)` 全部改调该接口 |
| 18 | `MaterialPlanProcessGroupService.cs` | `SaveAsync` → `RefreshReadModelsAsync` | WES + 用料总览 | **工序组编辑**事务提交后未刷新：按 planType 解析 `WorkOrderId` 后刷新执行读模型 + 用料总览 |
| 19 | `BatchService.cs` | 多条写路径 | WorkOrderListSummary | 补 `TryRefreshListSummaryAsync`（**8 处调用点**，含改工单号时新旧 `SalesOrderNo` 双刷；另 `AddProcessGroupAsync` 追加有效工序组改变有效产出折扣，亦需刷产能工量） |
| 20 | `InventoryBatchWriteService.cs` | `TryCompleteProductionBatchAsync` | WES | 入库完成推进目标刷新原按**入库批次**的 `WorkOrderNo` 取数，该字段缺失时漏刷 → 改为按**批次自身** WO/`SalesOrderNo` 刷新 |
| 21 | `PurchaseOrderService.cs` / `SubcontractOrderService.cs` | `UpdateAsync` | WES（改源工单） | **采购/委外改源工单号**只刷新工单 → 记录旧 `SourceWorkOrderNo`，经 `TryRefreshExecutionSummaryBothAsync` **新旧并集双刷** |
| 22 | `MaterialReceiveCheckService.cs` | `Delete` | QPT 整批重推导 | 成检到料删除时 `RefreshByProductionBatchIdAsync` **只重推导剩余到料**，已删到料的物化行成孤儿 → 先显式 `Remove` 孤儿 QPT 行再重推导整批 |

## 三审追加修复（2026-08-31）

| # | 位置 | 缺什么 | 修复内容 |
|---|------|--------|---------|
| 23 | `DataImportService.ImportAsync` | 全部读模型 | **DataExchange Excel 导入直写 DbContext、全程无刷新**（长期已知漏洞）→ 构造加可选 `IServiceScopeFactory`，提交成功后 `TryRefreshReadModelsAfterImportAsync()` 全量刷 4 个读模型（懒解析规避 DI 环） |
| 24 | `BatchService.AddProcessGroupAsync` | WorkOrderListSummary | 原仅 `BatchUpdateBatchTrackingAsync`（exec 路），漏用料总览；已完成批次追加有效工序组会改变有效产出折扣（产能工量）→ 补 `TryRefreshListSummaryAsync(batch.SalesOrderNo)` |
| 25 | `DataFixService.FixWorkOrderSummariesAsync` | 用料总览 | 原仅 `exec.RefreshAllAsync`（级联 OrderListSummary 但**不**级联用料总览）→ 构造加可选 `IWorkOrderListSummaryRefreshService`，补 `RefreshAllAsync()` |

## 定时兜底任务（Hangfire，2026-08-31 上线）

增量刷新是「写入即刷新」的第一道保障，定时任务为**兜底重建**（修复漏网、异常中断、导入直写等造成的不一致）。注册于 `MES.Api/Program.cs:427-449`，实现在 `MES.Api/Services/HangfireJobService.cs`：

| Job ID | 方法 | Cron（本地时区） | 作用 |
|--------|------|-----------------|------|
| `cleanup-old-notifications` | `CleanupOldNotificationsJob` | `0 2 * * *`（每日 02:00） | 清理 30 天前通知 |
| `refresh-quality-process-tracking` | `RefreshQualityProcessTrackingJob` | `7 * * * *`（每小时第 7 分） | QPT 物化表**增量**兜底：刷最近 1 小时有变更的 MRCheck + 从未刷过的记录 |
| `full-project-data-refresh-noon` | `FullProjectDataRefreshJob` | `55 11 * * *` | 全项目读模型**全量重建** + 派生缓存失效 |
| `full-project-data-refresh-midnight` | `FullProjectDataRefreshJob` | `55 23 * * *` | 同上 |

`FullProjectDataRefreshJob` 的**步骤顺序关键**（前一步是后一步的数据源），且**每步独立 `IServiceScope`**（隔离 DbContext 跟踪状态）、**单步失败不中断后续**：

1. `IWorkOrderExecutionService.RefreshAllAsync()` —— WES（数据源最广）
2. `IOrderService.RefreshAllAsync()` —— 订单列表读模型（从 WES 聚合 ScheduleStage/UrgencyLevel）
3. `IWorkOrderListSummaryRefreshService.RefreshAllAsync()` —— 用料计划总览（依赖批次有效产出 + WES 排程档位）
4. `IQualityProcessTrackingService.RefreshAllAsync()` —— QPT 物化表（独立）
5. 派生缓存失效 —— `IPendingDeliveryQueryService.InvalidateCachesAsync()`（C0/C1/C2）+ 冷轧排程三键（`ColdRollPlanService.MachineEstimateCacheKey` / `ScheduleSuggestionCacheKey` / `MachineGroupCacheKey`）

> ⚠️ `IWorkOrderListSummaryRefreshService.RefreshAllAsync()` 是为此任务新增的全量入口：遍历 `WorkOrders` 全部 `SalesOrderNo` **∪ 读模型残留行**（清理孤儿）后逐单 `RefreshBySalesOrderAsync`。

## 自包含上下文（无需跨上下文联动）

| 上下文 | 原因 |
|--------|------|
| **排程** | 计划数据，仅做只读消费，不参与其他上下文的聚合计算 |
| **设备** | 设备状态是内部聚合（EquipmentStatusCalculator），不通知外部 |
| **配置** | 有写入入口（参数表 `save`/`delete`、打印配置 `save-all` 等），但配置项属**主数据维护**，写入后不被其它上下文的聚合读模型直接引用（各上下文运行时按需读取），故无需跨上下文刷新。⚠️ 例外：`DailyOutputEstimateService` 的日产量预估虽在配置控制器下，但其数据被 WES 读模型消费 → 已接刷新（见二轮 #16） |
| **NCR/理化检验/质保书** | 纯数据录入，数据不被其他上下文的聚合引用 |

## 后续维护原则

1. 新增写入方法时，遵循"提交-通知"模式：先 `SaveChangesAsync`，再调用外部刷新
2. 异常用 try-catch 包裹，仅记 Warning 日志，不阻断主流程
3. 批量操作注意去重（`Distinct()`）避免重复刷新
4. 外部写入 Batch.Status 时必须同步刷新 WES
5. 仓库入库/出库涉及 RemainingQuantity 变化的，需同步 SourceOrder + QPT + WES
6. 仓库入库操作同时触发 WES + QPT + BatchTrk 三路刷新（入库完成推进和状态回退均需 BatchTrk 重算）
7. 成检到料 CRUD 需触发 WES + QPT + BatchTrk 三路刷新（不仅 QPT 和 WES）
8. 批次库存关联（ProductionBatchInventory）CRUD 需触发 WES + BatchTrk 两路刷新（合并投料场景）
9. 在产计划通知消除按场景区分：Type A（新建 SplitFromNumber 批次）使用 **精准消除**（按 WorkOrderNo + ProductionBatchId），Type B（工单号变更）使用 **批量消除**（仅按 ProductionBatchId）
10. 新增写入方法时，若消除通知，需确认是否有多个关联方共享同一源记录，防止误消除
11. **读模型依赖链**：凡全量重算路径必须连带刷新所有依赖它的读模型——如 WES 全量刷新末尾须连带刷新 OrderList（订单读模型），双路径（全量/增量）要互审
12. **派生态重算**：批次编辑变更上游字段（LengthStatus/工单号/订单号/主号）必须级联重算下游派生列（CutLengthMatchType），口径与批次编辑级联一致；派生列依赖的上游集合（如 FixedLengthWorkOrder 长度表）被重建后，需评估是否手动 RecomputeCutLengthMatchByBatchAsync 回填

## 已知问题与遗留事项

| # | 问题 | 影响 | 状态 |
|---|------|------|------|
| 1 | 工单更新模式（工单号不变，ManufacturingItem/PlantGrade 等字段变）后，ProductionBatch 冗余字段不会自动同步 | 批次筛选/质检联动可能落到错误分类，但用户已能收到变更通知，自行决定是否修正 | ⏳ 通知已实现，同步待定，等真实触发后再修 |
| 2 | `WorkOrderDeleted` 类型通知已不再写入（写入端已删除），现有存量通知数据待自然过期或被清理 | 遗留数据库中的旧通知记录不再增加 | ✅ 已处理（Service 代码已删除） |
