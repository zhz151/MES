# 质量管理模块详细设计（V7.9）

**版本**：V7.8
**最后更新**：2026-08-01

---

## 一、概述

本文档定义质量管理模块的详细设计，包括页面设计、业务规则、角色权限。

**核心功能**：
- **炉号登记**：来料炉号信息登记，含化学成分录入 + 工厂牌号化分验证 → 与批次关联
- **工厂牌号化学成分**：工厂牌号标准化学成分参考数据维护（⚠️ 已迁移至生产标准上下文）
- **工厂牌号化分验证规则**：工厂牌号各化学元素上下限验证规则配置，含公式"X×C%"（⚠️ 已迁移至生产标准上下文）
- **过程检验**：工序过程中的质量检验记录（跨批次列表+批量创建+内联编辑+列选择+打印）
- **成检到料**：所有工序完成后料到成品检验处的到料确认（独立质量上下文 Service + MaterialReceiveCheck 实体）
- **成品检验**：成品质量检验记录（9类检验项目+批量创建+批次自动填充）
- **质量过程跟踪**：成检到料→成品检验→成品入库整合读模型，按批次聚合展示全过程质量状态（新增 V4.0）
- **理化检测**：理化性能检测记录，9个独立模块，每模块独立表、独立列表页（V7.0）
  - **化学分析（ChemicalAnalysis）**：光谱分析仪检测数据，含15个化学元素含量
  - **硬度检验（HardnessTest）**：硬度检测记录（硬度模式/硬度值）
  - **晶粒度检验（GrainSizeTest）**：晶粒度评级记录（晶粒度等级/测定方法/观察倍数）
  - **扩口试验（FlaringTest）**：扩口性能试验（芯棒锥度/扩口后外径/扩口率/观察结果）
  - **压扁试验（FlatteningTest）**：压扁性能试验（压扁间隙/观察结果）
  - **晶间腐蚀试验（IntergranularCorrosionTest）**：晶间腐蚀性能试验（敏化温度/敏化时间/腐蚀溶液/弯曲角度/观察倍数/观察结果）
  - **金相检验（MetallographicTest）**：金相组织检验（侵蚀方法/电解电压/电解时间/观察倍数/铁素体含量）
  - **点腐蚀试验（PittingCorrosionTest）**：点腐蚀性能试验（抛光级别/原始重量/腐蚀溶液/腐蚀温度/腐蚀时间/最终重量/腐蚀速率/最大点蚀深度）
  - **室温拉伸试验（TensileTest）**：室温拉伸力学性能（原始标距/断后标距/抗拉强度/规定塑性延伸强度Rp0.2/Rp1.0/断后伸长率）
- **质量证明书**：质量证明书管理（列表+创建+编辑/查看，三步创建向导+检验数据自动填充）
- **不合格品报告（NCR）**：不合格品反馈→处置→原因分析→追责→纠正预防措施验证闭环管理（新增 V6.0）

**设计原则**：
- 质量模块独立实体表（FurnaceRegistration/ProcessInspection）
- 炉号登记支持批量创建 + 化学成分自动验证（PREN = Cr + 3.3×Mo + 16×N）
- 工厂牌号化分验证规则支持公式表达式（如 NbMin = "10×C%"）
- 过程检验完全仿照生产记录模式（跨批次独立列表、批量创建、内联编辑、列选择、方向键导航、打印）
- 过程检验实体独立于生产记录（ProcessInspection 表），非 ProductionRecord 子集
- 成检到料独立 Service（MaterialReceiveCheckService）在质量上下文，独立实体表（MaterialReceiveCheck）
- 物理删除策略
- 过程检验创建/更新/删除时自动触发批次跟踪刷新（纳入批次当前执行状态计算）
- 不合格品报告（NCR）单表5组字段设计（G1反馈→G2处置→G3分析→G4责任人→G5纠正措施），2状态机（处理中→已关闭），自动关闭条件（处置完结+责任人处理完结+验证通过/不适用）
- **批次工单号变更联动同步**：FinalInspection（成品检验）、Ncr（不合格品报告）两张表存储了从 ProductionBatch 冗余的 WorkOrderNo 等字段。当批次工单号变更时，自动同步更新。MaterialReceiveCheck 不再冗余存储这些字段（改为通过 ProductionBatch 导航属性实时投影），无需同步

---

## 二、页面设计

### 2.1 炉号登记列表页（/quality/furnace）

| 项目 | 说明 |
|------|------|
| 路由 | `@page "/quality/furnace"` |
| 授权 | QualityStaff/QualityDirector/Admin |
| 状态 | ✅ 已完成 |
| 表格 | MudTable 服务端分页 + auto-table，**内联编辑** |
| 搜索 | MudTextField Immediate + DebounceInterval 500ms，模糊搜索（炉号/原料单位/牌号） |
| 列选择器 | ColumnDisplaySelect + 重置按钮，列偏好通过 ColumnPrefsService 持久化（key: `furnace-registration`） |
| 排序列头 | 带排序箭头（▲▼），`ToggleSort(col.SortKey)` 切换升降序，默认按炉号升序；支持 incomingdate/rawmaterialunit/rawmaterialtype/registeredgrade/relatedplantgrade/furnacenumber/specification/quantity/weight 及15个化学元素(carbon…pren)/remark/createdtime/updatedtime 共 **25列排序** |
| 内联编辑 | _editingIds/_editCache 模式，支持所有字段内联修改 |
| 保存防重提 | _isSaving 标志，try/finally 包裹 |
| 方向键导航 | enableTableArrowNav + _isArrowNavSetup 防重入 |
| 操作列 | 编辑/保存/取消 + 详情查看 + 删除（ConfirmDialog确认） |
| 删除策略 | 物理删除，ConfirmDialog 确认后执行 |
| 打印 | "打印选中"+"打印全部"按钮：选中打印通过 `printTable` 基于DOM打印（自动隐藏复选框列+操作列）；全部打印通过 `printRawHtml` 调用API获取全量数据（pageSize=5000）在新窗口构建完整表格 |
| 新建按钮 | 右上角"新建" → `/quality/furnace/create` |

**字段顺序（28列）：**
来料日期 → 原料单位 → 原料类型 → 登记牌号 → 关联工厂牌号 → 炉号 → 规格 → 支数 → 重量 → C → Si → Mn → P → S → Ni → Cr → Mo → Cu → N → Nb → Ti → Fe → Al → W → PREN腐蚀当量 → 备注 → 创建日期 → 更新日期

### 2.2 炉号登记创建页（/quality/furnace/create）

| 项目 | 说明 |
|------|------|
| 路由 | `@page "/quality/furnace/create"` |
| 授权 | QualityDirector/Admin |
| 入口 | 炉号登记列表页"新建"按钮 |
| 布局 | 批量表格输入，每行对应一条炉号登记记录 |
| 列选择器 | ColumnDisplaySelect + 重置按钮，列偏好持久化（key: `furnace-registration-create`） |
| 方向键导航 | enableTableArrowNav + _isArrowNavSetup 防重入，id 容器 `#furnace-registration-create-table` |
| 添加行 | 工具栏"添加行"按钮 + 表尾 + 按钮 |
| 删除行 | 每行删除按钮（至少保留1行） |
| **关联工厂牌号自动填充** | 输入登记牌号（RegisteredGrade）后 onChange 自动查询 StandardGradeMapping，填充关联工厂牌号 |
| 提交验证 | 后端逐行验证化学成分（ValidateChemicalCompositionAsync），Snackbar 汇总错误 |
| **化学成分验证** | 关联工厂牌号有值时自动按 ChemicalValidationRule 校验 15 个元素（C/Si/Mn/P/S/Ni/Cr/Mo/Cu/N/Nb/Ti/Fe/Al/W）及 PREN 腐蚀当量 |
| **PREN 自动计算** | Cr + 3.3×Mo + 16×N，计算值自动回填并验证是否≥PRENMin |
| **公式支持** | 规则 Min/Max 支持 "10×C%" 格式公式，可计算 Nb/Ti 等元素基于 C 含量的动态阈值 |
| 返回按钮 | 右上角"返回" → 炉号登记列表页 |

**字段顺序（27列）：**
来料日期 * → 原料单位 * → 原料类型 * → 登记牌号 * → 关联工厂牌号 → 炉号 * → 规格 → 支数 → 重量 → C → Si → Mn → P → S → Ni → Cr → Mo → Cu → N → Nb → Ti → Fe → Al → W → PREN腐蚀当量 → 备注

### 2.3 炉号登记详情/编辑页（/quality/furnace/{id}）

| 项目 | 说明 |
|------|------|
| 路由 | `@page "/quality/furnace/{id}"` |
| 授权 | QualityDirector/Admin（编辑），QualityStaff/QualityDirector/Admin（查看） |
| 布局 | 查看/编辑同一页面，URL 参数区分 |
| 查看模式 | 表格 ReadOnly="true"，纯文本显示 |
| 编辑模式 | 切换 _isEditMode，使用独立 _editItem 编辑模型 |
| 提交验证 | 同创建页化学成分验证逻辑 |
| 返回按钮 | "返回" → 炉号登记列表页 |

### 2.4 工厂牌号化学成分列表页（/quality/chemical-composition）

> ⚠️ **此模块已迁移至生产标准上下文**。页面路由已同步更新。

| 项目 | 说明 |
|------|------|
| 路由 | `@page "/chemical-composition"` |
| 授权 | StandardRead/StandardWrite |
| 状态 | ✅ 已完成（已迁移至生产标准上下文） |
| 功能 | 维护工厂牌号的标准化学成分参考值 |
| 表格 | MudTable 服务端分页 + auto-table，**内联编辑** |
| 搜索 | MudTextField Immediate + DebounceInterval 500ms，模糊搜索（工厂牌号） |
| 列选择器 | ColumnDisplaySelect + 重置按钮，列偏好持久化（key: `chemical-composition`） |
| 排序列头 | 带排序箭头（▲▼），`ToggleSort(col.SortKey)` 切换升降序，默认按牌号升序；支持 plantgrade 及 15 个化学元素(carbon…pren) + createdtime/updatedtime 共 **18列排序** |
| 内联编辑 | _editingIds/_editCache 模式，支持17个字段（PlantGrade + 15元素 + PREN） |
| 方向键导航 | enableTableArrowNav + _isArrowNavSetup 防重入 |
| 打印 | "打印选中"+"打印全部"按钮（同炉号登记模式） |
| 新建按钮 | 右上角"新建" → `/chemical-composition/create` |

**字段顺序（20列）：**
工厂牌号 → C → Si → Mn → P → S → Ni → Cr → Mo → Cu → N → Nb → Ti → Fe → Al → W → PREN腐蚀当量 → 创建日期 → 更新日期

### 2.5 工厂牌号化分验证规则列表页（/quality/chemical-validate）

> ⚠️ **此模块已迁移至生产标准上下文**。页面路由已同步更新。

| 项目 | 说明 |
|------|------|
| 路由 | `@page "/chemical-validate"` |
| 授权 | StandardRead/StandardWrite |
| 状态 | ✅ 已完成（已迁移至生产标准上下文） |
| 功能 | 配置工厂牌号各化学元素的上下限验证规则，支持公式表达式 |
| 表格 | MudTable 服务端分页 + auto-table，**内联编辑** |
| 搜索 | MudTextField Immediate + DebounceInterval 500ms，模糊搜索（工厂牌号） |
| 列选择器 | ColumnDisplaySelect + 重置按钮，列偏好持久化（key: `chemical-validate`） |
| 排序列头 | 带排序箭头（▲▼），`ToggleSort(col.SortKey)` 切换升降序，默认按牌号升序；支持 plantgrade 及全部 31 个 Min/Max 字段(cmin…prenmin) + createdtime/updatedtime 共 **34列排序** |
| 内联编辑 | _editingIds/_editCache 模式，**32个规则字段**（15元素×Min/Max + PRENMin + PlantGrade） |
| 方向键导航 | enableTableArrowNav + _isArrowNavSetup 防重入 |
| 打印 | "打印选中"+"打印全部"按钮（同炉号登记模式） |
| 新建按钮 | 右上角"新建" → `/chemical-validate/create` |

**字段顺序（35列）：**
工厂牌号 → C-/C+ → Si-/Si+ → Mn-/Mn+ → P-/P+ → S-/S+ → Ni-/Ni+ → Cr-/Cr+ → Mo-/Mo+ → Cu-/Cu+ → N-/N+ → Nb-/Nb+ → Ti-/Ti+ → Fe-/Fe+ → Al-/Al+ → W-/W+ → PREN腐蚀当量- → 创建日期 → 更新日期

### 2.6 过程检验列表页（/quality/process-inspection）

| 项目 | 说明 |
|------|------|
| 路由 | `@page "/quality/process-inspection"` |
| 授权 | QualityStaff/QualityDirector/Admin |
| 入口 | ①顶部导航"质量管理→过程检验"按钮 |
| 表格 | MudTable 服务端分页 + auto-table，**内联编辑** |
| 搜索 | MudTextField Immediate + DebounceInterval 500ms，模糊搜索（批次号/工序/工段） |
| 日期筛选 | 检验日期范围：两个 MudTextField（_inspectionDateFrom/_inspectionDateTo），格式 yyyy-MM-dd，变化时自动刷新（OnDateFromChanged/OnDateToChanged → Search） |
| 列选择器 | ColumnDisplaySelect + 重置按钮，列偏好通过 ColumnPrefsService 持久化到 localStorage（key: `process-inspection`） |
| 排序列头 | 带排序箭头（▲▼），`ToggleSort(col.SortKey)` 切换升降序，默认按创建时间降序；支持  共 **24列排序** |
| 多选 | 每行 MudCheckBox + 表头全选；**编辑时隐藏多选框** |
| 内联编辑 | _editingIds/_editCache 模式，支持 17 个可编辑字段 |
| 保存防重提 | _isSaving 标志，保存按钮 Disabled="@_isSaving"，SaveEdit 用 try/finally 包裹 |
| 方向键导航 | enableTableArrowNav + _isArrowNavSetup 防重入 |
| 操作列 | 编辑/保存/取消 + 删除（ConfirmDialog确认） |
| 删除策略 | 物理删除（DELETE FROM），ConfirmDialog 确认后执行，成功后 Snackbar 提示 |
| 打印 | "打印选中"+"打印全部"按钮：选中打印通过 `printTable` 基于DOM打印（自动隐藏复选框列+操作列）；全部打印通过 `printRawHtml` 调用API获取全量数据（pageSize=5000）在新窗口构建完整表格 |
| 批次号链接 | BatchNo 列为 MudLink，点击跳转到批次详情页 |
| 新建按钮 | 右上角"新建" → `/quality/process-inspection/create` |

**字段顺序（27列）：**
检验日期 → 批次号（link）→ 工序名称 → 制造规格 → 工段名称 → 执行序号 → 设备名称 → 检验员 → 班次 → 检验支数 → 检验重量(kg) → 检验项目 → 合格支数 → 合格重量(kg) → 返整支数 → 入库支数 → 报废支数 → 理论返整重 → 理论入库重 → 理论报废重 → 不合格情况描述 → 来料单位 → 挂牌号 → 工厂牌号 → 备注 → 创建日期 → 更新日期

### 2.7 过程检验新建页（/quality/process-inspection/create）

| 项目 | 说明 |
|------|------|
| 路由 | `@page "/quality/process-inspection/create"` |
| 授权 | QualityDirector/Admin |
| 入口 | 过程检验列表页"新建"按钮 |
| 布局 | 批量表格输入，每行对应一条检验记录 |
| 列选择器 | ColumnDisplaySelect + 重置按钮，列偏好持久化（key: `process-inspection-create`） |
| 方向键导航 | enableTableArrowNav + _isArrowNavSetup 防重入 |
| 添加行 | 工具栏"添加行"按钮 + 表尾 + 按钮 |
| 删除行 | 每行删除按钮（至少保留1行） |
| 提交验证 | Snackbar 汇总错误（批次号/工序名称/工段名称/检验日期/制造规格/检验支数平衡），不实时校验 |
| 检验支数验证 | 检验支数 = 合格支数 + 返整支数 + 入库支数 + 报废支数（Quantity 有值时校验通过） |
| 提交流程 | 逐行验证 → 批量提交 → 成功跳转列表页 |
| 自动填充 | 输入 BatchNo 后自动填充挂牌号(TagNo)/工厂牌号(PlantGrade)/来料单位(SourceUnit) |
| 自动填充 | 输入 ManufacturingSpec 后自动匹配工序名称(ProcessName) |
| 工段名称默认 | 默认值为"检验" |

**字段顺序（24列）：**
检验日期 * → 批次号 * → 工序名称 * → 制造规格 → 工段名称 * → 设备名称 → 检验员 → 班次 → 检验支数 → 检验重量 → 检验项目 → 合格支数 → 合格重量 → 返整支数 → 入库支数 → 报废支数 → 理论返整重 → 理论入库重 → 理论报废重 → 不合格情况描述 → 来料单位 → 挂牌号 → 工厂牌号 → 备注

> 执行序号（SequenceNumber）已从页面移除，由服务端根据 ProcessGroupId + SectionName 自动计算。

### 2.8 成检到料列表页（/quality/material-receive-checks）

| 项目 | 说明 |
|------|------|
| 路由 | `@page "/quality/material-receive-checks"`（新建：`/quality/material-receive-checks/create`） |
| 授权 | QualityStaff/QualityDirector/Admin |
| 入口 | ①顶部导航"质量管理→成检到料"按钮 ②批次详情页底部的"成检到料"Tab |
| 归属说明 | 菜单归属质量管理，后端独立实现在质量上下文（MaterialReceiveCheckService + MaterialReceiveCheck 实体） |
| 功能 | 跨批次列表+批量创建+内联编辑+列选择+打印 |
| 去重 | 每个（批次+工序组）最多一条到料记录（UK_MaterialReceiveCheck_ProcessGroup），支持多工序组成检到料 |
| 字段投影 | Salesman/DeliveryState/LengthStatus 等批次派生字段在查询时通过 `m.ProductionBatch.xxx` 导航属性实时投影，不存储在 MaterialReceiveCheck 表中 |
| 工序关联 | ProcessGroupId（所属工序组ID）由服务端根据 BatchNo+ProcessName+ManufacturingSpec 自动匹配；ProcessName（工序名称）从该工序组冗余，创建时自动填充，也可手动选择 |
| SequenceNumber | 由服务端根据匹配工序组的 Inspection 值（`pg.Inspection!.Value`）自动计算，非页面填写（修正：V7.4 发现误用了 ProcessGroup.SequenceNumber，已修复） |
| 状态变更 | 创建成检到料不再直接设批次为 Completed，改为 `BatchStatus.InFinalInspection`，由入库完成推进为 Completed |

### 2.9 成品检验列表页（/quality/final-inspection）

| 项目 | 说明 |
|------|------|
| 路由 | `@page "/quality/final-inspection"` |
| 授权 | QualityStaff/QualityDirector/Admin |
| 入口 | 顶部导航"质量管理→成品检验"按钮 |
| 状态 | ✅ 已完成 |
| 表格 | MudTable 服务端分页 + auto-table，**内联编辑** |
| 搜索 | MudTextField Immediate + DebounceInterval 500ms，模糊搜索（生产编号/挂牌号/制造物品） |
| 日期筛选 | 检验日期范围：两个 MudTextField，格式 yyyy-MM-dd，变化时自动刷新 |
| 列选择器 | ColumnDisplaySelect + 重置按钮，列偏好通过 ColumnPrefsService 持久化（key: `final-inspection`） |
| 排序列头 | 带排序箭头（▲▼），`ToggleSort(col.SortKey)` 切换升降序，默认按检验日期降序；支持  共 **63列排序** |
| 多选 | 每行 MudCheckBox + 表头全选；**编辑时隐藏多选框** |
| 内联编辑 | _editingIds/_editCache 模式，支持 40 个可编辑字段（检验日期/成检类型/设备名称/班次/操作员/长度信息/数量重量/检验结果/不合格处理/尺寸水压探伤专有字段/备注，含 InspectionType 枚举下拉） |
| 保存防重提 | _isSaving 标志，保存按钮 Disabled="@_isSaving"，SaveEdit 用 try/finally 包裹 |
| 方向键导航 | enableTableArrowNav + _isArrowNavSetup 防重入 |
| 操作列 | 编辑/保存/取消 + 删除（ConfirmDialog 确认） |
| 删除策略 | 物理删除（DELETE FROM），ConfirmDialog 确认后执行 |
| 打印 | "打印选中"+"打印全部"按钮：选中打印通过 `printTable` 基于DOM打印（自动隐藏复选框列+操作列）；全部打印通过 `printRawHtml` 调用API获取全量数据在新窗口构建完整表格 |
| 生产编号链接 | BatchNo 列为 MudLink，点击跳转到批次详情页 |
| 新建按钮 | 右上角"新建" → `/quality/final-inspection/create` |

**字段顺序（63列）：**
**G1（检验执行）：** 检验项目 → 检验日期 → 设备名称 → 班次 → 操作员 → 成检类型 → 是否交付态 → 资格等级 → 生产编号
**G2（生产批次）：** 挂牌号 → 生产类型 → 制造物品 → 制造状态 → 交货状态 → 工单号 → 订单号 → 主号 → 业务员 → 最终用户 → 来料单位 → 炉号 → 工厂牌号 → 规格 → 长度状态 → 生产支数 → 生产重量(kg)
**G3（检验结果）：** 定尺长度 → 符合工单长度 → 非定尺长度范围 → 检验支数 → 理论检验重量 → 合格支数 → 理论合格重量 → 含让步放行支 → 让步说明
**G4（不合格处理）：** 次品返整支 → 次品入库支 → 次品报废支 → 次品返整重 → 次品入库重 → 次品报废重 → 次品情况描述
**G5（尺寸值）：** 外径范围 → 壁厚范围 → 长度余量范围
**G6（压力值）：** 压力Mpa → 保压时间s
**G7（涡流/超声波探伤）：** 检验标准 → 检验等级 → 仪器型号 → 检验方式 → 标样尺寸 → 标样缺陷 → 探头类型 → 耦合剂 → 校准频率 → 检测频率 → 检测灵敏度 → 检测相位 → 检测速度
**G8（辅助信息）：** 检验备注 → 数据来源 → 更新日期

### 2.10 成品检验新建页（/quality/final-inspection/create）

| 项目 | 说明 |
|------|------|
| 路由 | `@page "/quality/final-inspection/create"` |
| 授权 | QualityDirector/Admin |
| 入口 | 成品检验列表页"新建"按钮 |
| 状态 | ✅ 已完成 |
| 布局 | 批量表格输入，每行对应一条成品检验记录 |
| 列选择器 | ColumnDisplaySelect + 重置按钮，列偏好持久化（key: `final-inspection-create`） |
| 方向键导航 | enableTableArrowNav + _isArrowNavSetup 防重入，id 容器 `#final-inspection-create-table` |
| 添加行 | 工具栏"添加行"按钮 + 表尾 + 按钮 |
| 删除行 | 每行删除按钮（至少保留1行） |
| 提交验证 | Snackbar 汇总错误（生产编号/检验日期/检验项目/检验支数平衡），不实时校验 |
| 检验支数验证 | 检验支数 = 合格支数 + 返整支数 + 入库支数 + 报废支数（Quantity 有值时校验通过） |
| 检验项目联动 | 切换检验项目时自动清空尺寸专有字段（外径/壁厚/余量）或水压专有字段（压力/保压时间） |
| 自动填充 | 输入 BatchNo 后自动查询批次关联信息（物料名称/挂牌号/工单号/订单号/来料单位/炉号/牌号/规格/定尺长度） |
| 提交流程 | 逐行验证 → 批量提交 → 成功跳转列表页 |
| 返回按钮 | 右上角"返回" → 成品检验列表页 |

**字段顺序（54列）：**
**G1（检验执行）：** 检验项目* → 检验日期* → 生产编号* → 设备名称 → 班次 → 操作员 → 成检类型 → 资格等级
**G2（生产批次）：** 挂牌号 → 生产类型 → 制造物品 → 业务员 → 交货状态 → 关联工单号 → 关联订单号 → 来料单位 → 炉号 → 工厂牌号 → 规格 → 长度状态
**G3（检验结果）：** 定尺长度 → 非定尺长度范围 → 检验支数 → 理论检验重量 → 合格支数 → 理论合格重量 → 含让步放行支 → 让步说明
**G4（次品处理）：** 次品返整支 → 次品入库支 → 次品报废支 → 次品返整重 → 次品入库重 → 次品报废重 → 次品情况描述
**G5（尺寸值）：** 外径范围 → 壁厚范围 → 长度余量范围
**G6（压力值）：** 压力Mpa → 保压时间s
**G7（涡流/超声波探伤）：** 检验标准 → 检验等级 → 仪器型号 → 检验方式 → 标样尺寸 → 标样缺陷 → 探头类型 → 耦合剂 → 校准频率 → 检测频率 → 检测灵敏度 → 检测相位 → 检测速度
**G8（辅助信息）：** 检验备注

### 2.11 化学分析列表页（/quality/chemical-analysis）

| 项目 | 说明 |
|------|------|
| 路由 | `@page "/quality/chemical-analysis"` |
| 授权 | QualityStaff/QualityDirector/Admin |
| 状态 | ✅ 已完成（V7.0） |
| 表格 | MudTable 服务端分页 + auto-table，**内联编辑** |
| 搜索 | MudTextField Immediate + DebounceInterval 500ms，模糊搜索（分析员/炉号/牌号/分析标准/C/Si/Mn/P/S/Ni/Cr/Mo/Cu/N/Nb/Ti/Fe/Al/W） |
| 日期筛选 | 分析日期范围：两个 MudTextField，格式 yyyy-MM-dd，变化时自动刷新 |
| 列选择器 | ColumnDisplaySelect + 重置按钮，列偏好持久化（key: `chemical-analysis`） |
| 排序列头 | 带排序箭头（▲▼），ToggleSort 切换升降序，默认按分析日期降序 |
| 内联编辑 | _editingIds/_editCache 模式，支持所有字段内联修改 |
| 操作列 | 编辑/保存/取消 + 删除（ConfirmDialog确认） |
| 删除策略 | 物理删除 |
| 新建按钮 | 右上角"新建" → `/quality/chemical-analysis/create` |

**字段顺序（22列）：**
分析日期 → 分析员 → 炉号 → 牌号 → 分析次数 → 分析标准 → C → Si → Mn → P → S → Ni → Cr → Mo → Cu → N → Nb → Ti → Fe → Al → W → 创建日期 → 更新日期

### 2.12 化学分析创建页（/quality/chemical-analysis/create）

| 项目 | 说明 |
|------|------|
| 路由 | `@page "/quality/chemical-analysis/create"` |
| 授权 | QualityDirector/Admin |
| 入口 | 列表页"新建"按钮 |
| 布局 | 表单页，填写一条化学分析记录，含分析信息 + 15个化学元素输入框 |
| 返回按钮 | 右上角"返回" → 列表页 |

### 2.13 硬度检验列表页（/quality/hardness-test）

| 项目 | 说明 |
|------|------|
| 路由 | `@page "/quality/hardness-test"` |
| 授权 | QualityStaff/QualityDirector/Admin |
| 状态 | ✅ 已完成（V7.0） |
| 表格 | MudTable 服务端分页 + auto-table，**内联编辑** |
| 搜索 | MudTextField Immediate + DebounceInterval 500ms，模糊搜索（检验员/生产编号/牌号/规格/检验标准/硬度模式/硬度值） |
| 日期筛选 | 检验日期范围：两个 MudTextField，格式 yyyy-MM-dd |
| 列选择器 | ColumnDisplaySelect + 重置按钮，列偏好持久化（key: `hardness-test`） |
| 排序列头 | 带排序箭头（▲▼），ToggleSort 切换升降序，默认按检验日期降序 |
| 内联编辑 | _editingIds/_editCache 模式，支持所有字段内联修改 |
| 操作列 | 编辑/保存/取消 + 删除（ConfirmDialog确认） |
| 删除策略 | 物理删除 |
| 新建按钮 | 右上角"新建" → 创建页 |

**字段顺序（12列）：**
检验日期 → 检验员 → 生产编号 → 牌号 → 规格 → 试样编号 → 试样尺寸 → 检验标准 → 硬度模式 → 硬度值 → 判定 → 创建日期 → 更新日期

### 2.14 晶粒度检验列表页（/quality/grain-size-test）

| 项目 | 说明 |
|------|------|
| 路由 | `@page "/quality/grain-size-test"` |
| 授权 | QualityStaff/QualityDirector/Admin |
| 状态 | ✅ 已完成（V7.0） |
| 表格 | MudTable 服务端分页 + auto-table，**内联编辑** |
| 搜索 | MudTextField Immediate + DebounceInterval 500ms，模糊搜索（检验员/生产编号/牌号/规格/检验标准/晶粒度等级/测定方法/观察倍数） |
| 日期筛选 | 检验日期范围：两个 MudTextField |
| 列选择器 | ColumnDisplaySelect + 重置按钮，列偏好持久化（key: `grain-size-test`） |
| 排序列头 | 带排序箭头（▲▼），ToggleSort 切换升降序，默认按检验日期降序 |
| 内联编辑 | _editingIds/_editCache 模式 |
| 操作列 | 编辑/保存/取消 + 删除（ConfirmDialog确认） |
| 删除策略 | 物理删除 |
| 新建按钮 | 右上角"新建" → 创建页 |

**字段顺序（13列）：**
检验日期 → 检验员 → 生产编号 → 牌号 → 规格 → 试样编号 → 试样尺寸 → 检验标准 → 晶粒度等级 → 晶粒度测定方法 → 观察倍数 → 判定 → 创建日期 → 更新日期

### 2.15 扩口试验列表页（/quality/flaring-test）

| 项目 | 说明 |
|------|------|
| 路由 | `@page "/quality/flaring-test"` |
| 授权 | QualityStaff/QualityDirector/Admin |
| 状态 | ✅ 已完成（V7.0） |
| 表格 | MudTable 服务端分页 + auto-table，**内联编辑** |
| 搜索 | MudTextField Immediate + DebounceInterval 500ms，模糊搜索（检验员/生产编号/牌号/规格/检验标准/芯棒锥度/观察结果） |
| 日期筛选 | 检验日期范围：两个 MudTextField |
| 列选择器 | ColumnDisplaySelect + 重置按钮，列偏好持久化（key: `flaring-test`） |
| 排序列头 | 带排序箭头（▲▼），ToggleSort 切换升降序，默认按检验日期降序 |
| 内联编辑 | _editingIds/_editCache 模式，decimal? 字段使用 RenderDecimal |
| 操作列 | 编辑/保存/取消 + 删除（ConfirmDialog确认） |
| 删除策略 | 物理删除 |
| 新建按钮 | 右上角"新建" → 创建页 |

**字段顺序（14列）：**
检验日期 → 检验员 → 生产编号 → 牌号 → 规格 → 试样编号 → 试样尺寸 → 检验标准 → 芯棒锥度 → 扩口后外径 → 扩口率(%) → 观察结果 → 判定 → 创建日期 → 更新日期

### 2.16 压扁试验列表页（/quality/flattening-test）

| 项目 | 说明 |
|------|------|
| 路由 | `@page "/quality/flattening-test"` |
| 授权 | QualityStaff/QualityDirector/Admin |
| 状态 | ✅ 已完成（V7.0） |
| 表格 | MudTable 服务端分页 + auto-table，**内联编辑** |
| 搜索 | MudTextField Immediate + DebounceInterval 500ms，模糊搜索（检验员/生产编号/牌号/规格/检验标准/观察结果） |
| 日期筛选 | 检验日期范围：两个 MudTextField |
| 列选择器 | ColumnDisplaySelect + 重置按钮，列偏好持久化（key: `flattening-test`） |
| 排序列头 | 带排序箭头（▲▼），ToggleSort 切换升降序，默认按检验日期降序 |
| 内联编辑 | _editingIds/_editCache 模式，decimal? 字段使用 RenderDecimal |
| 操作列 | 编辑/保存/取消 + 删除（ConfirmDialog确认） |
| 删除策略 | 物理删除 |
| 新建按钮 | 右上角"新建" → 创建页 |

**字段顺序（12列）：**
检验日期 → 检验员 → 生产编号 → 牌号 → 规格 → 试样编号 → 试样尺寸 → 检验标准 → 压扁间隙 → 观察结果 → 判定 → 创建日期 → 更新日期

### 2.17 晶间腐蚀试验列表页（/quality/intergranular-corrosion-test）

| 项目 | 说明 |
|------|------|
| 路由 | `@page "/quality/intergranular-corrosion-test"` |
| 授权 | QualityStaff/QualityDirector/Admin |
| 状态 | ✅ 已完成（V7.0） |
| 表格 | MudTable 服务端分页 + auto-table，**内联编辑** |
| 搜索 | MudTextField Immediate + DebounceInterval 500ms，模糊搜索（检验员/生产编号/牌号/规格/检验标准/敏化温度/腐蚀溶液/观察结果） |
| 日期筛选 | 检验日期范围：两个 MudTextField |
| 列选择器 | ColumnDisplaySelect + 重置按钮，列偏好持久化（key: `intergranular-corrosion-test`） |
| 排序列头 | 带排序箭头（▲▼），ToggleSort 切换升降序，默认按检验日期降序 |
| 内联编辑 | _editingIds/_editCache 模式 |
| 操作列 | 编辑/保存/取消 + 删除（ConfirmDialog确认） |
| 删除策略 | 物理删除 |
| 新建按钮 | 右上角"新建" → 创建页 |

**字段顺序（17列）：**
检验日期 → 检验员 → 生产编号 → 牌号 → 规格 → 试样编号 → 试样尺寸 → 检验标准 → 敏化温度 → 敏化时间 → 腐蚀溶液 → 腐蚀时间 → 弯曲角度 → 观察倍数 → 观察结果 → 判定 → 创建日期 → 更新日期

### 2.18 金相检验列表页（/quality/metallographic-test）

| 项目 | 说明 |
|------|------|
| 路由 | `@page "/quality/metallographic-test"` |
| 授权 | QualityStaff/QualityDirector/Admin |
| 状态 | ✅ 已完成（V7.0） |
| 表格 | MudTable 服务端分页 + auto-table，**内联编辑** |
| 搜索 | MudTextField Immediate + DebounceInterval 500ms，模糊搜索（检验员/生产编号/牌号/规格/检验标准/侵蚀方法/观察倍数） |
| 日期筛选 | 检验日期范围：两个 MudTextField |
| 列选择器 | ColumnDisplaySelect + 重置按钮，列偏好持久化（key: `metallographic-test`） |
| 排序列头 | 带排序箭头（▲▼），ToggleSort 切换升降序，默认按检验日期降序 |
| 内联编辑 | _editingIds/_editCache 模式，decimal? 字段使用 RenderDecimal |
| 操作列 | 编辑/保存/取消 + 删除（ConfirmDialog确认） |
| 删除策略 | 物理删除 |
| 新建按钮 | 右上角"新建" → 创建页 |

**字段顺序（15列）：**
检验日期 → 检验员 → 生产编号 → 牌号 → 规格 → 试样编号 → 试样尺寸 → 检验标准 → 侵蚀方法 → 电解电压 → 电解时间 → 观察倍数 → 铁素体含量(%) → 判定 → 创建日期 → 更新日期

### 2.19 点腐蚀试验列表页（/quality/pitting-corrosion-test）

| 项目 | 说明 |
|------|------|
| 路由 | `@page "/quality/pitting-corrosion-test"` |
| 授权 | QualityStaff/QualityDirector/Admin |
| 状态 | ✅ 已完成（V7.0） |
| 表格 | MudTable 服务端分页 + auto-table，**内联编辑** |
| 搜索 | MudTextField Immediate + DebounceInterval 500ms，模糊搜索（检验员/生产编号/牌号/规格/检验标准/抛光级别/腐蚀溶液/判定） |
| 日期筛选 | 检验日期范围：两个 MudTextField |
| 列选择器 | ColumnDisplaySelect + 重置按钮，列偏好持久化（key: `pitting-corrosion-test`） |
| 排序列头 | 带排序箭头（▲▼），ToggleSort 切换升降序，默认按检验日期降序 |
| 内联编辑 | _editingIds/_editCache 模式，3个 decimal? 字段使用 RenderDecimal |
| 操作列 | 编辑/保存/取消 + 删除（ConfirmDialog确认） |
| 删除策略 | 物理删除 |
| 新建按钮 | 右上角"新建" → 创建页 |

**字段顺序（18列）：**
检验日期 → 检验员 → 生产编号 → 牌号 → 规格 → 试样编号 → 试样尺寸 → 检验标准 → 抛光级别 → 原始重量(g) → 腐蚀溶液 → 腐蚀温度(℃) → 腐蚀时间(min) → 最终重量(g) → 腐蚀速率(g/h) → 最大点蚀深度(mm) → 判定 → 创建日期 → 更新日期

### 2.20 室温拉伸试验列表页（/quality/tensile-test）

| 项目 | 说明 |
|------|------|
| 路由 | `@page "/quality/tensile-test"` |
| 授权 | QualityStaff/QualityDirector/Admin |
| 状态 | ✅ 已完成（V7.0） |
| 表格 | MudTable 服务端分页 + auto-table，**内联编辑** |
| 搜索 | MudTextField Immediate + DebounceInterval 500ms，模糊搜索（检验员/生产编号/牌号/规格/检验标准/判定） |
| 日期筛选 | 检验日期范围：两个 MudTextField |
| 列选择器 | ColumnDisplaySelect + 重置按钮，列偏好持久化（key: `tensile-test`） |
| 排序列头 | 带排序箭头（▲▼），ToggleSort 切换升降序，默认按检验日期降序 |
| 内联编辑 | _editingIds/_editCache 模式，6个 decimal? 字段使用 RenderDecimal |
| 操作列 | 编辑/保存/取消 + 删除（ConfirmDialog确认） |
| 删除策略 | 物理删除 |
| 新建按钮 | 右上角"新建" → 创建页 |

**字段顺序（16列）：**
检验日期 → 检验员 → 生产编号 → 牌号 → 规格 → 试样编号 → 试样尺寸 → 检验标准 → 原始标距(mm) → 断后标距(mm) → 抗拉强度(Mpa) → 规定塑性延伸强度Rp0.2(Mpa) → 规定塑性延伸强度Rp1.0(Mpa) → 断后伸长率(%) → 判定 → 创建日期 → 更新日期

### 2.21 硬度检验创建页（/quality/hardness-test/create）

| 项目 | 说明 |
|------|------|
| 路由 | `@page "/quality/hardness-test/create"` |
| 授权 | QualityDirector/Admin |
| 入口 | 硬度检验列表页"新建"按钮 |
| 布局 | 批量表格输入，每行对应一条硬度检验记录 |
| 方向键导航 | enableTableArrowNav，id 容器 `#hardness-test-create-table` |
| 添加行 | 工具栏"添加行"按钮 + 表尾 + 按钮 |
| 删除行 | 每行删除按钮（至少保留1行） |
| 提交验证 | Snackbar 汇总错误（生产编号/检验员/牌号/检验日期 必填），不实时校验 |
| 默认行数 | 初始化 3 行空白记录 |
| 返回按钮 | 右上角"返回" → 硬度检验列表页 |

**字段顺序（11列）：**
检验日期 * → 检验员 * → 生产编号 * → 牌号 * → 规格 → 试样编号 → 试样尺寸 → 检验标准 → 硬度模式 → 硬度值 → 判定

### 2.22 晶粒度检验创建页（/quality/grain-size-test/create）

| 项目 | 说明 |
|------|------|
| 路由 | `@page "/quality/grain-size-test/create"` |
| 授权 | QualityDirector/Admin |
| 入口 | 晶粒度检验列表页"新建"按钮 |
| 布局 | 批量表格输入，每行对应一条晶粒度检验记录 |
| 方向键导航 | enableTableArrowNav，id 容器 `#grain-size-test-create-table` |
| 添加行 | 工具栏"添加行"按钮 + 表尾 + 按钮 |
| 删除行 | 每行删除按钮（至少保留1行） |
| 提交验证 | Snackbar 汇总错误（生产编号/检验员/牌号/检验日期 必填），不实时校验 |
| 默认行数 | 初始化 3 行空白记录 |
| 返回按钮 | 右上角"返回" → 晶粒度检验列表页 |

**字段顺序（12列）：**
检验日期 * → 检验员 * → 生产编号 * → 牌号 * → 规格 → 试样编号 → 试样尺寸 → 检验标准 → 晶粒度等级 → 晶粒度测定方法 → 观察倍数 → 判定

### 2.23 扩口试验创建页（/quality/flaring-test/create）

| 项目 | 说明 |
|------|------|
| 路由 | `@page "/quality/flaring-test/create"` |
| 授权 | QualityDirector/Admin |
| 入口 | 扩口试验列表页"新建"按钮 |
| 布局 | 批量表格输入，每行对应一条扩口试验记录 |
| 方向键导航 | enableTableArrowNav，id 容器 `#flaring-test-create-table` |
| 添加行 | 工具栏"添加行"按钮 + 表尾 + 按钮 |
| 删除行 | 每行删除按钮（至少保留1行） |
| 提交验证 | Snackbar 汇总错误（生产编号/检验员/牌号/检验日期 必填），不实时校验 |
| 默认行数 | 初始化 3 行空白记录 |
| 返回按钮 | 右上角"返回" → 扩口试验列表页 |

**字段顺序（13列）：**
检验日期 * → 检验员 * → 生产编号 * → 牌号 * → 规格 → 试样编号 → 试样尺寸 → 检验标准 → 芯棒锥度 → 扩口后外径 → 扩口率(%) → 观察结果 → 判定

### 2.24 压扁试验创建页（/quality/flattening-test/create）

| 项目 | 说明 |
|------|------|
| 路由 | `@page "/quality/flattening-test/create"` |
| 授权 | QualityDirector/Admin |
| 入口 | 压扁试验列表页"新建"按钮 |
| 布局 | 批量表格输入，每行对应一条压扁试验记录 |
| 方向键导航 | enableTableArrowNav，id 容器 `#flattening-test-create-table` |
| 添加行 | 工具栏"添加行"按钮 + 表尾 + 按钮 |
| 删除行 | 每行删除按钮（至少保留1行） |
| 提交验证 | Snackbar 汇总错误（生产编号/检验员/牌号/检验日期 必填），不实时校验 |
| 默认行数 | 初始化 3 行空白记录 |
| 返回按钮 | 右上角"返回" → 压扁试验列表页 |

**字段顺序（11列）：**
检验日期 * → 检验员 * → 生产编号 * → 牌号 * → 规格 → 试样编号 → 试样尺寸 → 检验标准 → 压扁间隙 → 观察结果 → 判定

### 2.25 晶间腐蚀试验创建页（/quality/intergranular-corrosion-test/create）

| 项目 | 说明 |
|------|------|
| 路由 | `@page "/quality/intergranular-corrosion-test/create"` |
| 授权 | QualityDirector/Admin |
| 入口 | 晶间腐蚀试验列表页"新建"按钮 |
| 布局 | 批量表格输入，每行对应一条晶间腐蚀试验记录 |
| 方向键导航 | enableTableArrowNav，id 容器 `#intergranular-corrosion-test-create-table` |
| 添加行 | 工具栏"添加行"按钮 + 表尾 + 按钮 |
| 删除行 | 每行删除按钮（至少保留1行） |
| 提交验证 | Snackbar 汇总错误（生产编号/检验员/牌号/检验日期 必填），不实时校验 |
| 默认行数 | 初始化 3 行空白记录 |
| 返回按钮 | 右上角"返回" → 晶间腐蚀试验列表页 |

**字段顺序（16列）：**
检验日期 * → 检验员 * → 生产编号 * → 牌号 * → 规格 → 试样编号 → 试样尺寸 → 检验标准 → 敏化温度 → 敏化时间 → 腐蚀溶液 → 腐蚀时间 → 弯曲角度 → 观察倍数 → 观察结果 → 判定

### 2.26 金相检验创建页（/quality/metallographic-test/create）

| 项目 | 说明 |
|------|------|
| 路由 | `@page "/quality/metallographic-test/create"` |
| 授权 | QualityDirector/Admin |
| 入口 | 金相检验列表页"新建"按钮 |
| 布局 | 批量表格输入，每行对应一条金相检验记录 |
| 方向键导航 | enableTableArrowNav，id 容器 `#metallographic-test-create-table` |
| 添加行 | 工具栏"添加行"按钮 + 表尾 + 按钮 |
| 删除行 | 每行删除按钮（至少保留1行） |
| 提交验证 | Snackbar 汇总错误（生产编号/检验员/牌号/检验日期 必填），不实时校验 |
| 默认行数 | 初始化 3 行空白记录 |
| 返回按钮 | 右上角"返回" → 金相检验列表页 |

**字段顺序（14列）：**
检验日期 * → 检验员 * → 生产编号 * → 牌号 * → 规格 → 试样编号 → 试样尺寸 → 检验标准 → 侵蚀方法 → 电解电压 → 电解时间 → 观察倍数 → 铁素体含量(%) → 判定

### 2.27 点腐蚀试验创建页（/quality/pitting-corrosion-test/create）

| 项目 | 说明 |
|------|------|
| 路由 | `@page "/quality/pitting-corrosion-test/create"` |
| 授权 | QualityDirector/Admin |
| 入口 | 点腐蚀试验列表页"新建"按钮 |
| 布局 | 批量表格输入，每行对应一条点腐蚀试验记录 |
| 方向键导航 | enableTableArrowNav，id 容器 `#pitting-corrosion-test-create-table` |
| 添加行 | 工具栏"添加行"按钮 + 表尾 + 按钮 |
| 删除行 | 每行删除按钮（至少保留1行） |
| 提交验证 | Snackbar 汇总错误（生产编号/检验员/牌号/检验日期 必填），不实时校验 |
| 默认行数 | 初始化 3 行空白记录 |
| 返回按钮 | 右上角"返回" → 点腐蚀试验列表页 |

**字段顺序（17列）：**
检验日期 * → 检验员 * → 生产编号 * → 牌号 * → 规格 → 试样编号 → 试样尺寸 → 检验标准 → 抛光级别 → 原始重量(g) → 腐蚀溶液 → 腐蚀温度(℃) → 腐蚀时间(min) → 最终重量(g) → 腐蚀速率(g/h) → 最大点蚀深度(mm) → 判定

### 2.28 室温拉伸试验创建页（/quality/tensile-test/create）

| 项目 | 说明 |
|------|------|
| 路由 | `@page "/quality/tensile-test/create"` |
| 授权 | QualityDirector/Admin |
| 入口 | 室温拉伸试验列表页"新建"按钮 |
| 布局 | 批量表格输入，每行对应一条室温拉伸试验记录 |
| 方向键导航 | enableTableArrowNav，id 容器 `#tensile-test-create-table` |
| 添加行 | 工具栏"添加行"按钮 + 表尾 + 按钮 |
| 删除行 | 每行删除按钮（至少保留1行） |
| 提交验证 | Snackbar 汇总错误（生产编号/检验员/牌号/检验日期 必填），不实时校验 |
| 默认行数 | 初始化 3 行空白记录 |
| 返回按钮 | 右上角"返回" → 室温拉伸试验列表页 |

**字段顺序（15列）：**
检验日期 * → 检验员 * → 生产编号 * → 牌号 * → 规格 → 试样编号 → 试样尺寸 → 检验标准 → 原始标距(mm) → 断后标距(mm) → 抗拉强度(Mpa) → 规定塑性延伸强度Rp0.2(Mpa) → 规定塑性延伸强度Rp1.0(Mpa) → 断后伸长率(%) → 判定

### 2.29 质量证明书列表页（/quality/certificates）

| 项目 | 说明 |
|------|------|
| 路由 | `@page "/quality/certificates"`（新建：`/quality/certificates/create`，详情：`/quality/certificates/{Id:int}`） |
| 授权 | QualityStaff/QualityDirector/Admin（列表）；QualityDirector/Admin（新建/编辑/删除） |
| 状态 | ✅ 已完成（V7.4） |
| 功能 | 质量证明书（质保书）管理，支持证书头选择 → 库存批次选择 → 检验数据自动填充的三步创建向导 |
| 表格 | MudTable 服务端分页 + auto-table，标准列表页 |
| 搜索 | MudTextField Immediate + DebounceInterval 500ms，模糊搜索（质保书编号/客户名称/产品标准/产品名称/交货状态） |
| 列选择器 | ColumnDisplaySelect + 重置按钮，列偏好持久化（key: `quality_certificates`） |
| 排序列头 | 带排序箭头（▲▼），ToggleSort 切换升降序，默认按签发日期降序 |
| 操作列 | 编辑按钮（导航到详情页）+ 删除（ConfirmDialog确认） |
| 删除策略 | 物理删除（级联删除子项），ConfirmDialog 确认后执行 |
| 新建按钮 | 右上角"新建" → `/quality/certificates/create` |
| 方向键导航 | enableTableArrowNav，id 容器 `#certificates-table` |
| 关联模块 | 依赖于待发货查询（PendingDeliveryQueryService）获取证书头选项和库存批次 |

**字段顺序（6列）：**
质保书编号（link）→ 签发日期 → 客户名称 → 产品标准 → 产品名称 → 交货状态

### 2.30 质量证明书创建页（/quality/certificates/create）

| 项目 | 说明 |
|------|------|
| 路由 | `@page "/quality/certificates/create"` |
| 授权 | QualityDirector/Admin |
| 入口 | 列表页"新建"按钮 |
| 布局 | 三步创建向导：Step 1 选择证书头 → Step 2 选择库存批次 → Step 3 编辑子项明细 |
| **Step 1 选择证书头** | 从 PendingDeliveryService.GetHeaderOptionsAsync() 加载不重复的头4字段组合（订单号/客户名称/产品标准/交货状态）；带搜索功能；选中后自动解析产品名称（通过 StandardRegister 三级容错匹配）；点击"确认"进入 Step 2 |
| **Step 2 选择库存批次** | 从 PendingDeliveryService.GetAllAsync() 加载待发货批次；按头4字段过滤；支持关键词搜索 + 入库日期范围筛选 + 列排序 + 分页；多选复选框 + 全选；"填充选中到子表" 按钮将选中批次转换为子项（含长度描述构建、去重） |
| **Step 3 子项明细** | 4 组可编辑子表：仓库信息（10列）+ 化学成分（16列）+ 成品检验（9列）+ 理化检测（18列）；"生成（自动填充检验数据）"按钮调用后端 AutoFillInspectionDataAsync 填充 FurnaceRegistration/FinalInspection/TensileTest 数据；"提交保存"构建 CertificateCreateRequest 调用 CreateAsync |
| 数据模型 | Certificate（主表：编号/签发日期/客户/标准/产品名称/交货状态/备注）→ CertificateItem（子表：4组字段） |
| 编号规则 | `{订单号}-{序号:00}`，如 `SO20240714001-02`，服务端自动生成 |
| 验证 | 订单号和子表至少一项为必填 |
| 返回按钮 | 右上角"返回" → 列表页 |

### 2.31 质量证明书详情/编辑页（/quality/certificates/{Id:int}）

| 项目 | 说明 |
|------|------|
| 路由 | `@page "/quality/certificates/{Id:int}"` |
| 授权 | QualityDirector/Admin（编辑），QualityStaff/QualityDirector/Admin（查看） |
| 入口 | 列表页点击质保书编号或编辑按钮 |
| 布局 | 查看/编辑同一页面 |
| 查看模式 | 只读文本显示头字段 + 4 个子表（仓库信息/化学成分/成品检验/理化检测） |
| 编辑模式 | 切换 _isEditMode，头字段 MudTextField + 4 个子表全部可编辑 |
| 编辑流程 | EnterEditMode() 备份头+子项 → CancelEdit() 恢复 → SaveAll() 保存 |
| 保存调用 | CertificateUpdateRequest（可更新头字段 + 子项增/删/改） |
| 子表列分组 | col-g1-cell（仓库信息）、col-g2-cell（化学成分）、col-g3-cell（成品检验）、col-g4-cell（理化检测） |

### 2.32 质量过程跟踪（/quality/process-tracking）

| 项目 | 说明 |
|------|------|
| 路由 | `@page "/quality/process-tracking"` |
| 授权 | QualityStaff/QualityDirector/Admin |
| 状态 | ✅ 已完成（V4.0新增，V5.0增强） |
| 功能 | 整合 成检到料（MaterialReceiveCheck）→ 成品检验（FinalInspection）→ 成品入库（InventoryPlan）三表，另含 ProductionBatch（CurrentValidWeight）和 ProductionRecord（断切支数），按批次聚合展示全过程质量状态 |
| 归属说明 | 仅读模型，不涉及写操作。数据来自批次上下文（MaterialReceiveCheck/ProductionBatch/ProductionRecord）+ 质量上下文（FinalInspection）+ 仓库上下文（InventoryPlan） |
| 表格 | MudTable 服务端分页 + auto-table，**只读**（无编辑/删除操作） |
| 搜索 | MudTextField Immediate + DebounceInterval 500ms，模糊搜索（批次号/制造物品/挂牌号/牌号/规格/炉号/确认人/业务员） |
| 列选择器 | ColumnDisplaySelect + 重置按钮，列偏好持久化（key: `quality-process-tracking`） |
| 排序列头 | 带排序箭头（▲▼），`ToggleSort(col.SortKey)` 切换升降序；支持 G1含新字段共 **36列排序** |
| 多选 | 无（只读列表，无需操作） |
| 操作列 | 无（只读列表） |
| 状态标签 | QualityStatus 列使用 MudChip 彩色标签显示；IsForceCompleted=true 时显示"异常完成"(Color.Info 蓝色)，IsForceCompleted=false 时按原逻辑：入库存疑=Color.Error(红色)、待检验=默认、检验中=Color.Warning(警告)、完成检验=Color.Success(成功) |
| 新建按钮 | 无 |
| 列分组 | G1批次信息(15列) → G2检验来料(4列) → G3各项检验的日期(10列) → G4检验的数量信息(5列) → G5入库的信息(3列) → G6执行状态(2列: QualityStatus + IsForceCompleted) |
| B33分页汇总 | 支持10个汇总列（检测项数/生产支数/生产重量/检验支数/合格支数/返整支数/不合格入库/报废支数/入库支数/入库重量） |
| 分组标题栏 | col-g1~col-g6 CSS 彩色标识，`initGroupHeaders` JS 动态对齐列宽 |
| 筛选增强 | G6 QualityStatus 筛选含"异常完成"和"入库存疑"选项（FilterType="enum" + EnumOptions），IsForceCompleted 新增布尔筛选（true="是"/false="否"） |

**字段顺序（36列）：**
**G1（批次信息）：** 生产编号 → 制造物品 → 工厂牌号 → 规格 → 长度状态 → 挂牌号(隐藏) → 工单号(隐藏) → 订单号(隐藏) → 炉号(隐藏) → 来料单位(隐藏) → 生产类型(隐藏) → 业务员 → 交货状态 → 生产重量(kg) → 生产支数
**G2（检验来料）：** 到料日期 → 班次 → 确认人 → 强制完成
**G3（各项检验的日期）：** 检测项数 → PMI检验 → 表检 → 尺寸 → 内窥 → 水压 → 水下气压 → 涡流 → 超声波 → 端口着色
**G4（检验的数量信息）：** 检验支数 → 合格支数 → 返整支数 → 不合格入库 → 报废支数
**G5（入库的信息）：** 入库日期 → 入库支数 → 入库重量
**G6（执行状态）：** 执行状态

### 2.33 不合格品报告列表页（/quality/ncr）

| 项目 | 说明 |
|------|------|
| 路由 | `@page "/quality/ncr"` |
| 授权 | QualityStaff/QualityDirector/Admin |
| 入口 | ①顶部导航"质量管理→不合格报告"按钮 |
| 状态 | ✅ 已完成（V6.0 新增） |
| 表格 | MudTable 服务端分页 + auto-table，标准列表页 |
| 搜索 | MudTextField Immediate + DebounceInterval 500ms，模糊搜索（生产编号/反馈人/工单号/牌号/规格） |
| 列选择器 | ColumnDisplaySelect + 重置按钮，列偏好持久化（key: `ncr`） |
| 排序列头 | 带排序箭头（▲▼），`ToggleSort(col.SortKey)` 切换升降序，默认按反馈日期降序 |
| 操作列 | 编辑按钮（跳转表单页）+ 删除（ConfirmDialog） |
| 删除策略 | 物理删除，ConfirmDialog 确认后执行 |
| 状态标签 | Status 列使用 MudChip 彩色标签显示（Processing=警告色/Closed=成功色） |
| 严重程度标签 | Severity 列使用 MudChip 显示（Critical=错误色/General=默认色） |
| 内联编辑 | 无（复杂表单，跳转编辑页） |
| 新建按钮 | 右上角"新建" → `/quality/ncr/create` |
| **待处理卡片** | 页面顶部 MudExpansionPanel（默认展开），显示待处理不合格卡片。数据来源：过程检验和成品检验中不合格数据（返整≥5支且≥5%/入库≥5支且≥5%/报废≥3支且≥5%，阈值由配置 NcrThreshold 控制，默认一致）聚合为卡片，去重规则：(BatchNo, InspectionItem, DisposalMethod) 三字段组合 |

**字段顺序（34列）：**
**G1（问题反馈）：** 反馈日期 → 反馈部门 → 反馈人 → 钢管类别 → 生产编号 → 工单号 → 牌号 → 规格 → 不合格支数 → 问题描述
**G2（不合格品处置）：** 处置方式 → 处置完结 → 处置完结日期 → 处置备注
**G3（原因分析）：** 严重程度 → 原因分析 → 分析确认人 → 确认日期
**G4（责任人及处理）：** 责任类别 → 责任部门 → 责任人 → 操作日期 → 责任人处理 → 追责完结日期 → 追责完结
**G5（纠正预防措施及结果验证）：** 纠正预防措施 → 计划人 → 计划日期 → 验证人 → 验证日期 → 验证结论 → 结果判定
**G6（状态）：** 状态
**审计：** 更新日期

### 2.34 不合格报告新建/编辑页（/quality/ncr/create, /quality/ncr/{id}）

| 项目 | 说明 |
|------|------|
| 路由 | `@page "/quality/ncr/create"`（新建），`@page "/quality/ncr/{id}"`（编辑），`@page "/quality/ncr/print/{Id:int}"`（打印预览） |
| 授权 | QualityDirector/Admin |
| 入口 | 列表页"新建"或"编辑"按钮 |
| 状态 | ✅ 已完成（V6.0 新增） |
| 布局 | MudContainer + 5个分组（G1-G5），每组一个 MudCard 分组显示 |
| 待处理卡片 | 创建页顶部 MudExpansionPanel（默认展开），数据同列表页卡片，每张卡片显示：来源类型/检验项目/批次号/缺陷描述/处置方式/不合格支数/占比 |
| 卡片填充 | 点击卡片上的 AddCircle 按钮直接填充表单（FillFromCard），无需刷新页面 |
| 批量自动填充 | 输入 BatchNo 时自动调取 LookupBatchAsync 填充 WorkOrderNo/PlantGrade/Specification |
| 枚举选择 | PipeCategory(MaterialType)/DisposalMethod/SeverityLevel/ResponsibilityCategory/VerifyResult 使用 MudSelect 下拉选择 |
| 日期字段 | 全部使用 MudTextField T="string" + Placeholder="yyyy-MM-dd"，禁止 MudDatePicker |
| 状态显示 | 编辑模式顶部显示当前状态（MudChip 标签） |
| 保存逻辑 | 新建/编辑均在后端自动检测关闭条件（DisposalIsCompleted ∧ PersonIsCompleted ∧ (VerifyResult=Passed ∨ NotApplicable)），满足则自动将状态从 Processing 变更为 Closed |
| 状态回退 | 已关闭的 NCR 编辑保存时自动检测，若关闭条件不再满足则自动回退为 Processing（即不禁止回退） |
| 提交验证 | form.Validate() + Snackbar 汇总提示，不实时校验 |
| 返回按钮 | 右上角"返回" → 列表页 |

## 三、业务规则

### 3.1 炉号登记规则

- **关联工厂牌号自动填充**：输入登记牌号（RegisteredGrade）后，前端自动查询 StandardGradeMapping（标准牌号 → 工厂牌号），填充到关联工厂牌号（RelatedPlantGrade）
- **化学成分验证**：仅当 RelatedPlantGrade 有值且 ChemicalValidationRule 中存在该工厂牌号的规则时执行
- **15个元素验证**：C/Si/Mn/P/S/Ni/Cr/Mo/Cu/N/Nb/Ti/Fe/Al/W，每个元素有 Min 下限和 Max 上限
- **PREN 自动计算**：PREN = Cr + 3.3×Mo + 16×N，自动计算后回填保存
- **PREN 验证**：计算值必须 ≥ PRENMin（若规则设置了 PRENMin）
- **公式支持**：NbMin/TiMin 等字段支持 "10×C%" 格式，实际阈值 = 系数 × C 含量值
- **验证阶段**：创建页批量提交时后端逐行验证；详情页编辑时单行验证
- **仅验证有值的元素**：元素值为 null 时跳过该元素验证
- **炉号可重复**：允许同一个炉号多次登记（唯一索引已取消）

### 3.2 过程检验规则

- 执行序号 SequenceNumber 由服务端自动解析：ProcessGroupId + SectionName → SequenceNumber（来自工序组中该工段的顺序值），传0时自动计算
- **工段名称默认"检验"**：新建页面工段名称默认值设置为"检验"
- **检验支数平衡验证**：Quantity = QualifiedQuantity + DefectReworkQuantity + DefectWarehouseQuantity + DefectScrapQuantity（仅 Quantity 有值时校验）
- **让步放行验证**：QualifiedConcessionQuantity ≤ QualifiedQuantity（仅两者均有值时校验）
- **检验重量验证**：Weight 不能大于批次现有效原料重量（CurrentValidWeight），超出时返回 BusinessException
- **执行序号范围验证**：新建记录时，该批次的 SequenceNumber 不得超过"该批次最近有检验记录的日期的最大 SeqNumber + 7"。即取该批次所有 InspectionDate < 当前记录日期(.Date) 的记录中，InspectionDate 最大的一天对应的最大 SequenceNumber + 7 作为上限（上限由配置 SequenceJump.MaxJump 控制，默认7）
- **重复记录校验**：同批次+同工序组+同工段已存在则拒绝
- **来料单位自动填充**：输入 BatchNo 后从批次的 SourceName 自动填充
- **批次跟踪联动**：过程检验创建/更新/删除后自动刷新对应批次的跟踪字段（CurrentExecDate/CurrentGroupName/CurrentSectionName 等）
- **生产记录互斥**："检验"工段已在生产记录中禁用，仅通过过程检验模块管理

### 3.3 成检到料规则

- 详见 批次模块详细设计.md §4.9

### 3.4 成品检验规则

- **检验支数平衡验证**：Quantity = QualifiedQuantity + DefectReworkQuantity + DefectWarehouseQuantity + DefectScrapQuantity（仅 Quantity 有值时校验）
- **让步放行验证**：QualifiedConcessionQuantity ≤ QualifiedQuantity（仅两者均有值时校验）
- **检验重量验证**：Weight 不能大于批次现有效原料重量（CurrentValidWeight），超出时返回 BusinessException
- **重复记录校验**：同检验日期+同批次(ProductionBatchId)+同检验项目(InspectionItem)+同操作人(Operator)已存在则拒绝（FinalInspectionService.cs:924）
- **检验项目联动**：切换检验项目为非尺寸（Dimension）时自动清空 OuterDiameterRange/WallThicknessRange/LengthAllowanceRange；切换为非水压（HydrostaticPressure）和非水下气压（UnderwaterPneumatic）时自动清空 Pressure/HoldTime
- **批次信息自动填充**：输入 BatchNo 后通过 `LookupBatchAsync` 查询 ProductionBatch 自动填充 9 个冗余字段（MaterialName/TagNo/WorkOrderNo/SalesOrderNo/SourceUnit/FurnaceNo/PlantGrade/Specification/FixedLength/ProductionType）
- **成检类型判定**：InspectionType（PreInspection=预成检/FormalInspection=正式成检）——创建请求不传时服务端按「优先正式成检」自动判定（到料含正式检验→FormalInspection，否则预成检→PreInspection，兜底正式成检）；传了则以传入值为准，但必须校验「指定的成检类型必须在批次成检到料类型集合内」，防止创建即制造不符（与健康通知口径一致）。内联编辑同样校验，不传保留原值
- **重量自动回填**：DefectReworkWeight/DefectWarehouseWeight/DefectScrapWeight 前端按单支重×支数（AutoFillWeight）自动回填，仅当重量为空时不覆盖
- **定尺长度校验**：批次 LengthStatus=定尺时 FixedLength 必填；非定尺时 FixedLength 必须为空，改填 NonFixedLengthRange
- **定尺长度归属校验**：按「订单号+主号」维度调用 FixedLengthWorkOrder 定尺长度集合校验（仅正式成检要求，预成检无需）
- **定尺切割长度匹配标识（CutLengthMatchType）**：仅正式成检（InspectionType=FormalInspection）+ 批次 LengthStatus=Fixed + FixedLength 可解析时计算——长度 ∈ 本工单号定尺长度集合→`FullMatch`（完全匹配），否则长度 ∈ 订单+主号定尺长度集合→`MainNoMatch`（主号匹配），其余→null（不适用）；创建/更新时由 FinalInspectionService 计算，另提供 `POST api/final-inspection/refresh-all-cut-length-match` 回填端点，批次编辑（LengthStatus/工单号/订单号/主号变更）经 BatchService 级联调用 RecomputeCutLengthMatchByBatchAsync 重算；显示走 CutLengthMatchHelper（完全匹配/主号匹配/空白），MudChip 绿/橙/默认
- **物理删除策略**
- **InspectionItem 枚举存储**：数据库存储为 nvarchar(20)，页面通过 DisplayHelper.GetInspectionItemText() 显示中文

### 3.5 不合格品报告（NCR）规则

- **2 状态机**：仅 Processing（处理中）和 Closed（已关闭）两个状态，登记即 Processing
- **自动关闭条件**：保存时自动检测 — DisposalIsCompleted=true ∧ PersonIsCompleted=true ∧ (VerifyResult=Passed ∨ VerifyResult=NotApplicable)，满足则自动变更为 Closed
- **自动回退**：编辑保存时若关闭条件不再满足，自动从 Closed 回退为 Processing（不禁止状态回退）
- **卡片排重**：待处理卡片使用三字段组合 (BatchNo, SourceInspectionItem, DisposalMethod) 去重，已创建 NCR 的记录不重复显示
- **卡片数据源**：过程检验（ProcessInspection）按 ProductionBatchId GROUP BY 聚合不合格数据；成品检验（FinalInspection）按 (ProductionBatchId, InspectionItem) GROUP BY 聚合。每个聚合组生成 3 种处置方式卡片（返整≥5支且≥5%/入库≥5支且≥5%/报废≥3支且≥5%，阈值由配置 NcrThreshold 控制，默认一致）
- **批次信息冗余**：WorkOrderNo/PlantGrade/Specification 从 ProductionBatch 冗余，创建时通过 LookupBatchAsync 自动填充
- **SourceInspectionItem**：存储来源检验项目名称，用于卡片排重和自动填充
- **物理删除策略**

---

## 四、角色权限

| 角色 | 创建 | 编辑 | 删除 | 查看 | 打印 |
|------|------|------|------|------|------|
| Admin | ✅ | ✅ | ✅ | ✅ | ✅ |
| QualityDirector | ✅ | ✅ | ❌ | ✅ | ✅ |
| QualityStaff | ❌ | ❌ | ❌ | ✅ | ✅ |

---

## 五、与其它上下文的集成

### 5.1 批次上下文

- 过程检验（ProcessInspection）关联 ProductionBatch + ProcessGroup，批次号/工序名称/制造规格从批次冗余
- 过程检验创建/更新/删除时通过 `IProductionRecordService.RefreshBatchTrackingFieldsAsync()` 触发批次跟踪刷新
- 批次跟踪计算纳入过程检验记录的最大 SequenceNumber（三路比较：生产记录/工段委外/过程检验）

### 5.2 物料/标准上下文

- 炉号登记的 RegisteredGrade → RelatedPlantGrade 查询 StandardGradeMapping（标准牌号对照）
- 化学成分验证规则（ChemicalValidationRule）以工厂牌号 PlantGrade 为唯一标识
- 工厂牌号化学成分（ChemicalComposition）以工厂牌号 PlantGrade 为唯一标识

### 5.3 成检到料

- 后端独立实现在质量上下文（MaterialReceiveCheck 实体 + MaterialReceiveCheckService）
- 通过 IMaterialReceiveCheckService 接口在质量上下文内管理

---

## 六、API 控制器

> ⚠️ **跨上下文标注**：`(→订单)` 表示读取牌号对照数据（StandardGradeMapping），`(→批次)` 表示读取批次信息（ProductionBatch），数据所有权在对应上下文。

### 6.1 炉号登记 API

| Controller | 路由前缀 | 文件 |
|-----------|---------|------|
| FurnaceRegistrationController | `api/furnace-registration` | `MES.Api/Controllers/Quality/FurnaceRegistrationController.cs` |

| 方法 | 路由 | 说明 |
|------|------|------|
| GET | `api/furnace-registration/{id}` | 获取炉号登记详情 |
| GET | `api/furnace-registration/all-list` | 获取所有炉号登记（无分页） |
| GET | `api/furnace-registration/all` | 分页查询炉号登记 |
| POST | `api/furnace-registration/batch` | 批量创建炉号登记 |
| PUT | `api/furnace-registration/{id}` | 更新炉号登记 |
| DELETE | `api/furnace-registration/{id}` | 删除炉号登记 |
| GET | `api/furnace-registration/filter-contexts` | 筛选上下文 |
| GET | `api/furnace-registration/lookup-grade` | 根据登记牌号查询关联工厂牌号 (→订单) |

### 6.2 工厂牌号化学成分 API（⚠️ 已迁移至生产标准上下文）

> 此模块已迁移至生产标准上下文。API 路径、方法保持不变。控制器文件路径：`MES.Api/Controllers/StandardRegister/ChemicalCompositionController.cs`

| Controller | 路由前缀 | 文件 |
|-----------|---------|------|
| ChemicalCompositionController | `api/chemical-composition` | ~~`MES.Api/Controllers/Quality/ChemicalCompositionController.cs`~~ → `MES.Api/Controllers/StandardRegister/ChemicalCompositionController.cs` |

| 方法 | 路由 | 说明 |
|------|------|------|
| GET | `api/chemical-composition/all` | 分页查询工厂牌号化学成分 |
| GET | `api/chemical-composition/all-list` | 获取所有工厂牌号化学成分（无分页） |
| POST | `api/chemical-composition/batch` | 批量创建工厂牌号化学成分 |
| PUT | `api/chemical-composition/{id}` | 更新工厂牌号化学成分 |
| DELETE | `api/chemical-composition/{id}` | 删除工厂牌号化学成分 |
| GET | `api/chemical-composition/filter-contexts` | 筛选上下文 |
| GET | `api/chemical-composition/template` | 下载Excel导入模板 |
| POST | `api/chemical-composition/preview` | 预览Excel导入结果 |
| POST | `api/chemical-composition/import` | 上传Excel导入工厂牌号化学成分 |

### 6.3 工厂牌号化分验证规则 API（⚠️ 已迁移至生产标准上下文）

> 此模块已迁移至生产标准上下文。API 路径、方法保持不变。控制器文件路径：`MES.Api/Controllers/StandardRegister/ChemicalValidationRuleController.cs`

| Controller | 路由前缀 | 文件 |
|-----------|---------|------|
| ChemicalValidationRuleController | `api/chemical-validation-rule` | ~~`MES.Api/Controllers/Quality/ChemicalValidationRuleController.cs`~~ → `MES.Api/Controllers/StandardRegister/ChemicalValidationRuleController.cs` |

| 方法 | 路由 | 说明 |
|------|------|------|
| GET | `api/chemical-validation-rule/{id}` | 获取规则详情 |
| GET | `api/chemical-validation-rule/all` | 分页查询规则 |
| GET | `api/chemical-validation-rule/all-list` | 获取所有规则（无分页） |
| GET | `api/chemical-validation-rule/filter-contexts` | 筛选上下文 |
| GET | `api/chemical-validation-rule/by-plant-grade` | 根据工厂牌号获取规则 |
| POST | `api/chemical-validation-rule/batch` | 批量创建规则 |
| PUT | `api/chemical-validation-rule/{id}` | 更新规则 |
| DELETE | `api/chemical-validation-rule/{id}` | 删除规则 |

### 6.4 过程检验 API

| Controller | 路由前缀 | 文件 |
|-----------|---------|------|
| ProcessInspectionController | `api/process-inspection` | `MES.Api/Controllers/Quality/ProcessInspectionController.cs` |

| 方法 | 路由 | 说明 |
|------|------|------|
| GET | `api/process-inspection/all` | 跨批次分页查询过程检验 |
| GET | `api/process-inspection/all-list` | 获取所有过程检验（无分页） |
| POST | `api/process-inspection/batch` | 批量创建过程检验 |
| PUT | `api/process-inspection/{id}` | 更新过程检验 |
| DELETE | `api/process-inspection/{id}` | 删除过程检验 |
| GET | `api/process-inspection/filter-contexts` | 筛选上下文 |

### 6.5 成品检验 API

| Controller | 路由前缀 | 文件 |
|-----------|---------|------|
| FinalInspectionsController | `api/final-inspection` | `MES.Api/Controllers/Quality/FinalInspectionsController.cs` |

| 方法 | 路由 | 说明 |
|------|------|------|
| GET | `api/final-inspection/{id}` | 获取成品检验详情 |
| GET | `api/final-inspection/all` | 分页查询成品检验 |
| GET | `api/final-inspection/all-list` | 获取所有成品检验（无分页） |
| POST | `api/final-inspection` | 创建成品检验 |
| PUT | `api/final-inspection/{id}` | 更新成品检验 |
| DELETE | `api/final-inspection/{id}` | 删除成品检验 |
| POST | `api/final-inspection/batch` | 批量创建成品检验 |
| GET | `api/final-inspection/filter-contexts` | 筛选上下文 |
| GET | `api/final-inspection/lookup-batch` | 根据生产编号调取批次信息 (→批次) |
| GET | `api/final-inspection/health-summary` | 成品检验健康汇总（按当前筛选条件统计「成检类型与成检到料不符/无成检到料」的生产编号，前端实时健康校验通知条） |
| POST | `api/final-inspection/refresh-all-cut-length-match` | 回填全部成品检验记录的定尺切割长度匹配标识（CutLengthMatchType） |

### 6.6 质量过程跟踪 API

| Controller | 路由前缀 | 文件 |
|-----------|---------|------|
| QualityProcessTrackingController | `api/quality-process-tracking` | `MES.Api/Controllers/Quality/QualityProcessTrackingController.cs` |

| 方法 | 路由 | 说明 |
|------|------|------|
| GET | `api/quality-process-tracking/list` | 分页查询质量过程跟踪数据（只读） |
| GET | `api/quality-process-tracking/filter-contexts` | 筛选上下文 |

### 6.7 不合格品报告 API

| Controller | 路由前缀 | 文件 |
|-----------|---------|------|
| NcrController | `api/ncr` | `MES.Api/Controllers/Quality/NcrController.cs` |

| 方法 | 路由 | 说明 |
|------|------|------|
| GET | `api/ncr/all` | 分页查询 NCR（keyword/sortBy/filters/pageSize/pageIndex） |
| GET | `api/ncr/{id}` | 获取 NCR 详情 |
| POST | `api/ncr` | 创建 NCR（自动检测关闭条件） |
| PUT | `api/ncr/{id}` | 更新 NCR（自动检测关闭条件，支持状态回退） |
| DELETE | `api/ncr/{id}` | 删除 NCR |
| PUT | `api/ncr/{id}/status` | 状态流转（手动切换处理中/已关闭，无禁止回退限制） |
| GET | `api/ncr/pending-checks` | 获取待处理卡片数据（聚合过程检验和成品检验的不合格数据） |
| GET | `api/ncr/lookup-batch` | 根据生产编号调取批次信息（→批次） |
| GET | `api/ncr/filter-contexts` | 筛选上下文 |

### 6.8 质量证明书 API

| Controller | 路由前缀 | 文件 |
|-----------|---------|------|
| CertificateController | `api/certificate` | `MES.Api/Controllers/Quality/CertificateController.cs` |

| 方法 | 路由 | 说明 |
|------|------|------|
| GET | `api/certificate/all` | 分页查询质保书列表 |
| GET | `api/certificate/{id}` | 获取质保书详情 |
| POST | `api/certificate` | 创建质保书 |
| PUT | `api/certificate/{id}` | 更新质保书 |
| DELETE | `api/certificate/{id}` | 删除质保书 |
| POST | `api/certificate/auto-fill` | 自动填充检验数据（按炉号+生产批号查询化学分析/成品检验/拉伸检验） |
| GET | `api/certificate/filter-contexts` | 筛选上下文 |
| GET | `api/certificate/next-no` | 获取下一个质保书编号 |

### 6.9 成检到料 API

| Controller | 路由前缀 | 文件 |
|-----------|---------|------|
| MaterialReceiveCheckController | `api/material-receive-check` | `MES.Api/Controllers/Quality/MaterialReceiveCheckController.cs` |

| 方法 | 路由 | 说明 |
|------|------|------|
| GET | `api/material-receive-check/{batchId}` | 获取批次的成检到料记录 |
| GET | `api/material-receive-check/all` | 跨批次分页查询成检到料记录 |
| GET | `api/material-receive-check/all-list` | 获取所有成检到料记录（无分页） |
| GET | `api/material-receive-check/pending` | 获取待检验到料批次（成品检验阶段且未创建记录） |
| POST | `api/material-receive-check` | 创建成检到料 |
| POST | `api/material-receive-check/batch` | 批量创建成检到料 |
| PUT | `api/material-receive-check/{id}` | 更新成检到料 |
| DELETE | `api/material-receive-check/{id}` | 删除成检到料 |
| GET | `api/material-receive-check/filter-contexts` | 筛选上下文 |
| POST | `api/material-receive-check/print-batch` | 批量打印成检到料（返回 base64） |
| POST | `api/material-receive-check/print-all` | 按筛选条件打印全部成检到料（返回 base64） |
| POST | `api/material-receive-check/print-batch-file` | 批量打印成检到料（直接返回 PDF 文件） |
| POST | `api/material-receive-check/print-all-file` | 按筛选条件打印全部成检到料（直接返回 PDF 文件） |

### 6.10 理化检测 API（V7.0 新增）

理化检测共 9 个模块，均遵循相同的 CRUD 模式：

| Controller | 路由前缀 | 控制器文件 |
|-----------|---------|-----------|
| ChemicalAnalysisController | `api/chemical-analysis` | `MES.Api/Controllers/Quality/ChemicalAnalysisController.cs` |
| HardnessTestController | `api/hardness-test` | `MES.Api/Controllers/Quality/HardnessTestController.cs` |
| GrainSizeTestController | `api/grain-size-test` | `MES.Api/Controllers/Quality/GrainSizeTestController.cs` |
| FlaringTestController | `api/flaring-test` | `MES.Api/Controllers/Quality/FlaringTestController.cs` |
| FlatteningTestController | `api/flattening-test` | `MES.Api/Controllers/Quality/FlatteningTestController.cs` |
| IntergranularCorrosionTestController | `api/intergranular-corrosion-test` | `MES.Api/Controllers/Quality/IntergranularCorrosionTestController.cs` |
| MetallographicTestController | `api/metallographic-test` | `MES.Api/Controllers/Quality/MetallographicTestController.cs` |
| PittingCorrosionTestController | `api/pitting-corrosion-test` | `MES.Api/Controllers/Quality/PittingCorrosionTestController.cs` |
| TensileTestController | `api/tensile-test` | `MES.Api/Controllers/Quality/TensileTestController.cs` |

每个模块提供的标准端点：

| 方法 | 路由 | 说明 | 权限 |
|------|------|------|------|
| GET | `{base-path}/all` | 分页查询（QueryParams + InspectionDateRange） | QualityStaff/QualityDirector/Admin |
| GET | `{base-path}/{id}` | 查询单条 | QualityStaff/QualityDirector/Admin |
| POST | `{base-path}` | 创建 | QualityDirector/Admin |
| PUT | `{base-path}/{id}` | 更新 | QualityDirector/Admin |
| DELETE | `{base-path}/{id}` | 删除 | QualityDirector/Admin |
| POST | `{base-path}/batch` | 批量创建（DataExchange导入） | QualityDirector/Admin |
| GET | `{base-path}/filter-contexts` | 筛选上下文 | QualityStaff/QualityDirector/Admin |

---

## 版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| **V7.9** | **2026-08-09** | **定尺切割长度匹配 + 成检字段同步**：§2.9 成检列表列数更新为 63 列（G2 补主号/最终用户/生产支数/生产重量，G3 补符合工单长度=CutLengthMatchType），排序更新为 63 列，内联编辑字段更新为 40 个（含成检类型下拉）；§3.4 新增定尺长度归属校验、定尺切割长度匹配（CutLengthMatchType）计算规则，成检类型判定更新为「可传入+校验到料类型集合」；§6.5 补充 health-summary/refresh-all-cut-length-match 2 个端点 |
| **V7.8** | **2026-08-01** | **成检/过程检验新增字段 + 阈值配置化**：§2.9 成检列表列数更新为 58 列（新增成检类型/非定尺长度范围/次品返整重/次品入库重/次品报废重/是否交付态等），排序更新为 58 列；§2.10 成检新建页补充成检类型/非定尺长度范围/次品重量字段；§2.6/§2.7 过程检验补充理论返整重/理论入库重/理论报废重；§3.2 执行序号上限注明由 SequenceJump.MaxJump 控制；§2.33/§3.5 NCR 卡片阈值注明由 NcrThreshold 控制；§3.4 重复记录校验改为同检验日期+批次+检验项目+操作人 |
| **V7.7** | **2026-07-26** | **§2.8 成检到料同步多工序组支持**：去重说明更新为 UK_MaterialReceiveCheck_ProcessGroup 按工序组唯一；新增"工序关联"行描述 ProcessGroupId/ProcessName/SequenceNumber 三字段 |
| **V7.6** | **2026-07-26** | **文档同步更新**：Version bump，与各模块文档版本对齐 |
| **V7.5** | **2026-07-24** | **质量状态新增"入库存疑" + NCR 列数修正**：§2.32 QPT 新增"入库存疑"状态（5档：异常完成→入库存疑→待检验→检验中→完成检验），颜色定义（异常完成=Info蓝/入库存疑=Error红），IsForceCompleted 新增布尔筛选；§2.33 NCR 列数 19→34 与实际代码对齐，补充 G2/G4/G5 遗漏的 15 列定义 |
| **V7.4** | **2026-07-15** | **补充 8 个理化检测创建页 + 质量证明书状态更新**：新增 §2.21-§2.28 硬度/晶粒度/扩口/压扁/晶间腐蚀/金相/点腐蚀/室温拉伸创建页；§2.29 质量证明书由"预留"更新为"已完成"（含列表/创建/详情页面）；修正 §2.13-§2.20 中"炉号"→"生产编号"；修正后续编号 §2.12-§2.15 → §2.29-§2.34 |
| **V1.0** | **2026-05-14** | **初稿**：过程检验完整实现（列表+新建+内联编辑+打印+跟踪联动）+ 检验到料菜单迁移 + 预留4个子模块 |
| **V2.0** | **2026-05-14** | **补充**：炉号登记（FurnaceRegistration）完整实现（列表+批量创建+详情/编辑+化学成分验证+PREN自动计算）+ 工厂牌号化学成分（ChemicalComposition）列表页 + 工厂牌号化分验证规则（ChemicalValidationRule）列表页和创建页 + 炉号登记/工厂牌号化学成分/工厂牌号化分验证 DataExchange 导入导出支持 |
| **V2.1** | **2026-05-14** | **排序+打印增强**：4个列表页全部列支持排序（后端ApplySorting全覆盖）+ 打印改为"打印选中"+"打印全部"双按钮模式，全部打印通过API获取全量数据构建HTML |
| **V3.0** | **2026-05-15** | **补充**：成品检验（FinalInspection）完整实现（列表页+新建页+内联编辑+列选择+方向键导航+打印+批次自动填充+检验项目联动+检验支数平衡验证）|
| **V4.0** | **2026-05-27** | **补充**：质量过程跟踪（QualityProcessTracking）只读读模型列表页 + 成品检验新增 ProductionType 批次冗余字段 |
| **V4.1** | **2026-05-28** | **修正**：移除标准工艺生产周期（StandardProcessCycle），消除跨模块设计重叠 |
| **V5.0** | **2026-06-07** | **补充**：成检到料新增 Salesman/DeliveryState 字段 + MaterialReceiveCheck 创建时自动从 ProductionBatch 复制；质量过程跟踪重构成6组（G1批次信息→G6执行状态）+ 新增 LengthStatus/ProductionWeight/IsForceCompleted/Salesman/DeliveryState 字段 + IsForceCompleted=true 显示"异常完成" + B33分页汇总 + 分组标题栏对齐；**页面改名**："检验到料"→"成检到料"，"成检到料及执行"→"成检追踪" |
| **V7.3** | **2026-07-09** | **成检到料迁移为独立质量上下文 Service**：MaterialReceiveCheck 从 ProductionRecordService 拆分为独立 MaterialReceiveCheckService，归属质量上下文；文档 §2.8 路由同步为 /quality/material-receive-checks；§2.13 路由同步为 /quality/process-tracking；ChemicalComposition/ValidationRule 路由同步为 /chemical-composition /chemical-validate |
| **V7.2** | **2026-07-08** | **上下文迁移**：工厂牌号化学成分（ChemicalComposition）和工厂牌号化分验证规则（ChemicalValidationRule）迁移至生产标准上下文；文档 §2.4/§2.5/§6.2/§6.3 增加迁移标注 |
| **V7.1** | **2026-06-28** | **新增**：批次工单号变更时自动同步 MaterialReceiveCheck/FinalInspection/Ncr 三张关联表的冗余字段（由 BatchService.SaveAllAsync/UpdateAsync 触发，ExecuteUpdateAsync 直接 SQL），确保成检到料/成品检验/不合格品报告中的批次快照字段与批次保持一致 |
| **V7.0** | **2026-06-22** | **新增**：理化检测9个模块（化学分析/硬度检验/晶粒度检验/扩口试验/压扁试验/晶间腐蚀试验/金相检验/点腐蚀试验/室温拉伸试验），每模块独立表+独立列表页+内联编辑+列选择+搜索+日期筛选+DataExchange导入导出 |
| **V6.0** | **2026-06-12** | **新增**：不合格品报告（NCR）完整实现（列表页 §2.14 + 新建/编辑页 §2.15 + 业务规则 §3.5 + API §6.7），单表5组35+字段设计，2状态机（Processing→Closed），自动关闭条件（处置完结+责任人处理完结+验证通过/不适用），卡片排重（三字段组合），待处理卡片数据源（过程检验/成品检验不合格聚合） |
