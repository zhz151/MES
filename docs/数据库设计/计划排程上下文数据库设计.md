# 计划排程上下文数据库设计

**版本：V5.33**
**最后更新：2026-08-20**

---

## 一、概述

本文档定义计划排程上下文（Scheduling Context）的数据库设计。计划排程上下文包含两个独立实体、一个薄表和多个纯查询聚合：工单需求调整（OrderDemandAdjustment，API/UI 已迁移至工单上下文，数据库实体保留在此）、原锁计划（LEFT JOIN 实时查询模式，使用 RawMaterialLockPreExecution 独立表）、工单排程（WorkOrderSchedule，LEFT JOIN 实时查询模式，使用 WorkOrderPlan 薄表）、订单总况（ProductionOverview，无独立表）和成检计划（FinalInspectionPlan，无独立表）。所有实体均不通过外键关联到其他表（WorkOrderId 为逻辑关联）。

**当前范围（V5.3）：**
- 订单总况（ProductionOverview）：纯查询聚合，无独立实体/表
- 工单需求调整（OrderDemandAdjustment）：手工填写调整信息的独立实体
- 原锁计划：LEFT JOIN 实时查询模式（WorkOrderExecutionSummary + RawMaterialLockPreExecution），G13 直接从实体读取，无物化表
- **工单排程（WorkOrderSchedule）：LEFT JOIN 实时查询 + WorkOrderPlan 薄表，WorkOrderSchedules 物化表已删除（V4.5）**
- **段落分类设置（SectionFlowCategorySetting + CombinationGroups 组合归类表）：流转量分析预设参数表（V1.2 新增；V5.31 重构为三维归属映射，SectionFlowCategoryItem 已删除）**
- **批次计划（BatchPlan）：LEFT JOIN 实时查询视图（ProductionBatch + WorkOrderExecutionSummary + WorkOrderPlan + BatchPlanSchedule 薄表），G4（工单计划）在 Service 投影时 COALESCE（Plan优先），G10（工单需求调整）直接从 WorkOrderExecutionSummary 实体读取**
- **冷轧计划（ColdRollPlan）：LEFT JOIN 实时查询视图（ProductionBatch + WorkOrderExecutionSummary），无独立表**
- **冷轧排程（ColdRollSpecSchedule）：独立实体表，存储排程员在冷轧计划上作出的按规格的滚动排程决策**
- **批次计划薄表（BatchPlanSchedule）：独立实体表，存储排程员在批次计划上的批次级流转计划决策（8 个自动计算字段 + 抢单/备注），通过"计划安排"按钮自动快照，支持手工内联编辑**
- **成检计划（FinalInspectionPlan）：纯查询聚合，基于 ProductionBatch + MaterialReceiveCheck + FinalInspection 实时查询，无独立实体/表**

**设计原则：**
- 五个独立实体（OrderDemandAdjustment、RawMaterialLockPreExecution、ColdRollSpecSchedule、WorkOrderPlan、BatchPlanSchedule）均为独立实体，非读模型——包含需要持久化的手工覆盖字段
- 原锁计划、批次计划、冷轧计划、工单排程使用 LEFT JOIN 实时查询，不再存储物化表
- G15 预执行标记存储在 RawMaterialLockPreExecution 独立表（仅含 IsPreInput/BudgetInputDate 两个业务字段；IsMainNoMaterialComplete/IsBudgetComplete 已删除，见 §4.5）
- G15 工单计划覆盖值存储在 WorkOrderPlan 薄表（仅含 WorkOrderId + 4 个可 null 覆盖字段）
- 订单总况为纯查询聚合，数据来自 WorkOrderExecutionSummary + ProductionBatch/ProcessGroup，不涉及数据写入
- WorkOrderId 均为唯一索引（每个工单一条记录）
- 工单排程为 LEFT JOIN 实时查询（WorkOrderExecutionSummary LEFT JOIN WorkOrderPlan），筛选条件：ProductionFlowProperty != "略"；WorkOrderSchedules 物化表已删除
- 批次计划为 LEFT JOIN 实时查询视图（ProductionBatch LEFT JOIN WorkOrderExecutionSummary + WorkOrderPlan + OrderDemandAdjustment），筛选条件：Status=None 或 InProgress
- 冷轧计划为 LEFT JOIN 实时查询视图（ProductionBatch LEFT JOIN WorkOrderExecutionSummary），按规格维度聚合冷轧时间桶分布，筛选条件：Status=None 或 InProgress
- 冷轧排程（ColdRollSpecSchedule）为独立实体表，是冷轧计划只读聚合的写入目标，使排程员能保存滚动排程决策，全量同步模式（增/改 + 删除不在列表中的旧记录）
- 段落分类设置表（SectionFlowCategorySetting + CombinationGroups 组合归类表 + SectionParagraphConfigs 段落日产配置）为 AdminOnly 预设参数，前端管理页在"参数表"菜单下

---

## 二、实体清单

| 序号 | 实体 | 表名 | 说明 |
|------|------|------|------|
| 1 | OrderDemandAdjustment | OrderDemandAdjustment | 工单需求调整（手工填写） |
| 2 | RawMaterialLockPreExecution | RawMaterialLockPreExecution | 原锁预执行（G15 独立表，通过 LEFT JOIN 实时加载） |
| 3 | WorkOrderPlan | WorkOrderPlan | 工单计划薄表（G15 覆盖值，仅含 WorkOrderId + 4 个可 null 覆盖字段，通过 LEFT JOIN 实时加载） |
| 4 | SectionFlowCategorySetting | SectionFlowCategorySettings | 段落分类设置（预设参数，含 DisplayOrder） |
| 5 | CombinationGroup | CombinationGroups | 组合归类表（(工序组,工段,产类) 三维唯一归属映射，含归属流转类别 + 归属生产段落） |
| 6 | SectionParagraphConfig | SectionParagraphConfigs | 段落日产配置（生产段落，ParagraphName 唯一） |
| 7 | BatchPlan | — | 批次计划（LEFT JOIN 实时查询视图，无独立表，数据源：ProductionBatch + WorkOrderExecutionSummary + WorkOrderPlan + OrderDemandAdjustment） |
| 8 | ColdRollPlan | — | 冷轧计划（LEFT JOIN 实时查询视图，无独立表，数据源：ProductionBatch + WorkOrderExecutionSummary，按规格维度聚合冷轧时间桶分布） |
| 9 | ColdRollSpecSchedule | ColdRollSpecSchedule | 冷轧排程（独立实体，按 (ProcessType, BilletSpec, RollingSpec, IsFinished) 唯一约束，全量同步模式，存储排程员滚动排程决策） |
| 10 | BatchPlanSchedule | BatchPlanSchedules | 批次计划薄表（独立实体，批次级计划安排决策，8 个自动计算字段 + 抢单/备注） |
| 11 | ColdRollCapacity | ColdRollCapacity | 冷轧产能配置（排程保存自动反哺的产能档案，按排程小表四维粒度，DailyOutput 单机单日量方式B，SampleCount/LastConfirmedAt 样本置信度预留） |
| 12 | ColdRollMachineConfig | ColdRollMachineConfig | 冷轧机台数配置（按冷轧类型机台数参数，ProcessType 唯一，排程建议引擎产能平衡输入） |

---

## 三、OrderDemandAdjustment（工单需求调整）

### 3.1 表名

`OrderDemandAdjustment`

### 3.2 字段定义

| 字段 | 类型 | 必填 | 默认值 | 说明 | 生成方式 |
|------|------|------|--------|------|----------|
| Id | int | ✅ | 自增 | 主键 | 🔄 |
| WorkOrderId | int | ✅ | - | 工单ID（唯一约束） | ⚙️ |
| IsUrging | bit | ✅ | 0 | 催单 | 👤 |
| IsBatchDelivery | bit | ✅ | 0 | 分批交货 | 👤 |
| IsPaused | bit | ✅ | 0 | 主号暂停（联动连带：同主号下未入库完结工单一并写入） | 👤 |
| AdjustmentRemark | nvarchar(500) | ❌ | - | 调整备注 | 👤 |
| CreatedTime | datetimeoffset | ✅ | - | 创建时间 | 🔄 |
| CreatedBy | nvarchar(50) | ✅ | - | 创建人 | 🔄 |
| UpdatedTime | datetimeoffset | ✅ | - | 更新时间 | 🔄 |
| UpdatedBy | nvarchar(50) | ✅ | - | 更新人 | 🔄 |

### 3.3 索引

| 索引名 | 类型 | 列 | 说明 |
|--------|------|----|------|
| UK_SU_WorkOrderId | UNIQUE | WorkOrderId | 每工单一条 |

---

## 四、RawMaterialLockPreExecution（原锁预执行 — G15 独立表）

### 4.1 表名

`RawMaterialLockPreExecution`

### 4.2 概述

原锁计划页面**不再使用物化表**，改为 LEFT JOIN 实时查询模式。`RawMaterialLockPreExecution` 是 G15 预执行数据的独立存储表，仅包含工单 ID 和用户编辑字段。

G1~G12 字段从 `WorkOrderExecutionSummary` 实时读取，G13 直接从 WorkOrderExecutionSummary 实体读取（由 RefreshAllAsync 从 OrderDemandAdjustment 同步），G15 从本表 LEFT JOIN 实时读取。

### 4.3 字段定义

| 字段 | 类型 | 必填 | 默认值 | 说明 | 生成方式 |
|------|------|------|--------|------|----------|
| Id | int | ✅ | 自增 | 主键 | 🔄 |
| WorkOrderId | int | ✅ | - | 工单ID（唯一约束） | ⚙️ |
| IsPreInput | bit | ✅ | 0 | 执行（近几日会投料） | 👤 |
| BudgetInputDate | date | ❌ | - | 预算投料日 | 👤 |
| CreatedTime | datetimeoffset | ✅ | - | 创建时间 | 🔄 |
| CreatedBy | nvarchar(50) | ✅ | - | 创建人 | 🔄 |
| UpdatedTime | datetimeoffset | ✅ | - | 更新时间 | 🔄 |
| UpdatedBy | nvarchar(50) | ✅ | - | 更新人 | 🔄 |

### 4.4 索引

| 索引名 | 类型 | 列 | 说明 |
|--------|------|----|------|
| PK_RawMaterialLockPreExecution | PRIMARY KEY | Id | 主键 |
| UK_RMLPE_WorkOrderId | UNIQUE | WorkOrderId | 每工单一条 |

### 4.5 业务说明

- `BudgetInputDate` 为用户手工输入的日期（MudTextField，失焦保存），仅当 IsPreInput=true 时显示输入框
- `ExecutionError` 为 DTO 计算字段（不存储）：`IsPreInput && (!BudgetInputDate.HasValue || BudgetInputDate.Value.Date < DateTime.Today)`
- `IsMainNoMaterialComplete`/`IsBudgetComplete` 原「就绪判定/级联完成」字段已删除（2026-08-11 迁移 `20260811125308_DropRawMaterialLockPreExecutionCompleteFields`），其 A/B/C/D 就绪判定规则（含引用 G5 物料执行待回量）随之废弃

---

## 五、SectionFlowCategorySetting（段落分类设置）

### 5.1 表名

`SectionFlowCategorySettings`

### 5.2 字段定义

| # | 字段名 | 类型 | 必填 | 默认值 | 说明 |
|---|--------|------|------|--------|------|
| 1 | Id | int | ✅ | 自增 | 主键 |
| 2 | CategoryName | nvarchar(50) | ✅ | - | 段落类别名称 |
| 3 | DisplayOrder | int | ✅ | 0 | 展示序号（汇总表显示顺序） |
| 4 | DailyProductionTarget | decimal(18,2) | ❌ | NULL | 变异量预算日产 |
| 5 | LowerLimitDays | decimal(18,2) | ❌ | NULL | 偏少下限天数 |
| 6 | UpperLimitDays | decimal(18,2) | ❌ | NULL | 过多上限天数 |
| 7 | Remark | nvarchar(200) | ❌ | NULL | 备注 |
| — | 审计字段（继承 BaseEntity） |
| 8 | CreatedTime | datetime2 | ✅ | - | 创建时间 |
| 9 | CreatedBy | nvarchar(50) | ✅ | - | 创建人 |
| 10 | UpdatedTime | datetime2 | ✅ | - | 更新时间 |
| 11 | UpdatedBy | nvarchar(50) | ✅ | - | 更新人 |

> 类别编码（CategoryCode）已于 V5.31 删除；类别包含的 (工序组, 工段, 产类) 组合由组合归类表 CombinationGroups 承载。

### 5.3 索引

| 索引名 | 类型 | 字段 | 说明 |
|--------|------|------|------|
| PK_SectionFlowCategorySettings | 主键 | Id | |

---

## 六、CombinationGroup（组合归类表）

> 原 SectionFlowCategoryItem（段落分类明细项，含系数 Coefficient）已于 V5.31 删除，替换为本表（(工序组, 工段, 产类) 三维唯一归属映射）。

### 6.1 表名

`CombinationGroups`

### 6.2 字段定义

| # | 字段名 | 类型 | 必填 | 默认值 | 说明 |
|---|--------|------|------|--------|------|
| 1 | Id | int | ✅ | 自增 | 主键 |
| 2 | ProcessGroupName | nvarchar(100) | ✅ | - | 工序组（英文 Key，"全部"=通配任意工序组） |
| 3 | SectionName | nvarchar(50) | ✅ | - | 工段（英文 Key） |
| 4 | ProductStatus | nvarchar(20) | ✅ | - | 产类（ProductStatuses 三态，或 AllStatus 哨兵=不限定） |
| 5 | FlowCategoryId | int | ❌ | NULL | 归属流转类别外键 → SectionFlowCategorySettings.Id（空=未归属） |
| 6 | ParagraphName | nvarchar(50) | ❌ | NULL | 归属生产段落（中文段落名，SectionParagraphConfig.ParagraphName；空=未归属段落） |
| — | 审计字段（继承 BaseEntity） |
| 7 | CreatedTime | datetime2 | ✅ | - | 创建时间 |
| 8 | CreatedBy | nvarchar(50) | ✅ | - | 创建人 |
| 9 | UpdatedTime | datetime2 | ✅ | - | 更新时间 |
| 10 | UpdatedBy | nvarchar(50) | ✅ | - | 更新人 |

### 6.3 索引

| 索引名 | 类型 | 字段 | 说明 |
|--------|------|------|------|
| PK_CombinationGroups | 主键 | Id | |
| UK_CG_ProcessGroupName_SectionName_ProductStatus | 唯一 | ProcessGroupName, SectionName, ProductStatus | 三维唯一归属映射 |
| IX_CombinationGroups_FlowCategoryId | 非聚集 | FlowCategoryId | 按类别查询加速 |
| FK_CG_FlowCategoryId | 外键 | FlowCategoryId → SectionFlowCategorySettings.Id | ON DELETE SET NULL |

---

## 六之一、SectionParagraphConfig（段落日产配置）

### 6.1 表名

`SectionParagraphConfigs`

### 6.2 字段定义

| # | 字段名 | 类型 | 必填 | 默认值 | 说明 |
|---|--------|------|------|--------|------|
| 1 | Id | int | ✅ | 自增 | 主键 |
| 2 | ParagraphName | nvarchar(50) | ✅ | - | 段落名称（唯一） |
| 3 | DisplayOrder | int | ✅ | 0 | 展示序号 |
| 4 | DailyFlowTarget | decimal(18,2) | ❌ | NULL | 日流转设定（吨/天） |
| 5 | LowerLimitDays | decimal(18,2) | ❌ | NULL | 偏少天数值 |
| 6 | UpperLimitDays | decimal(18,2) | ❌ | NULL | 过多天数值 |
| 7 | Remark | nvarchar(200) | ❌ | NULL | 备注 |
| — | 审计字段（继承 BaseEntity） |
| 8 | CreatedTime | datetime2 | ✅ | - | 创建时间 |
| 9 | CreatedBy | nvarchar(50) | ✅ | - | 创建人 |
| 10 | UpdatedTime | datetime2 | ✅ | - | 更新时间 |
| 11 | UpdatedBy | nvarchar(50) | ✅ | - | 更新人 |

### 6.3 索引

| 索引名 | 类型 | 字段 | 说明 |
|--------|------|------|------|
| PK_SectionParagraphConfigs | 主键 | Id | |
| UK_SPC_ParagraphName | 唯一 | ParagraphName | 段落名称唯一 |

> 段落包含的 (工序组, 工段, 产类) 组合由组合归类表 CombinationGroups 的「归属段落」承载。

---

## 七、WorkOrderPlan（工单计划薄表 — G15 覆盖值）

### 7.1 表名

`WorkOrderPlan`

### 7.2 概述

工单计划薄表存储用户在工单排程页面对 G15 分组字段的手工覆盖值。WorkOrderSchedules 物化表已于 V4.5 删除，工单排程改为 LEFT JOIN 实时查询模式。WorkOrderPlan 是唯一的持久化覆盖表。

**覆盖展示模式（COALESCE）：** `DisplayXxx = PlanXxx ?? SystemXxx`
- PlanXxx 有值（非 null）→ 使用用户覆盖值
- PlanXxx 为 null → 使用 WorkOrderExecutionSummary 中的系统值

### 7.3 字段定义

| # | 字段名 | 类型 | 必填 | 默认值 | 说明 |
|---|--------|------|------|--------|------|
| 1 | Id | int | ✅ | 自增 | 主键 |
| 2 | WorkOrderId | int | ✅ | — | 工单ID（唯一约束） |
| 3 | ScheduleStage | int | ❌ | NULL | 工单状态覆盖值（0=主号完成 1=原料锁定 2=生产执行 3=成品检验；null=使用系统值） |
| 4 | UrgencyLevel | nvarchar(50) | ❌ | NULL | 紧急性覆盖值（A+急/A急/B顺/C缓/D缓；null=使用系统值） |
| 5 | ProductionAttentionProcess | nvarchar(200) | ❌ | NULL | 生产关注工序覆盖值（null=使用系统值） |
| 6 | ProductionFlowProperty | nvarchar(50) | ❌ | NULL | 生产流转性覆盖值（正常/暂停/待料/疑问/略；null=使用系统值） |
| — | 审计字段（继承 BaseEntity） |
| 7 | CreatedTime | datetime2 | ✅ | — | 创建时间 |
| 8 | CreatedBy | nvarchar(50) | ✅ | — | 创建人 |
| 9 | UpdatedTime | datetime2 | ✅ | — | 更新时间 |
| 10 | UpdatedBy | nvarchar(50) | ✅ | — | 更新人 |

**字段数：** 5 业务字段 + 4 审计字段 = 10 字段

### 7.4 索引

| 索引名 | 类型 | 列 | 说明 |
|--------|------|----|------|
| PK_WorkOrderPlans | PRIMARY KEY | Id | 主键 |
| UK_WOP_WorkOrderId | UNIQUE | WorkOrderId | 每工单一条 |

### 7.5 业务说明

- WorkOrderPlan 为薄表，仅存储用户手工覆盖值，主数据来自 WorkOrderExecutionSummary
- 4 个字段全部可 null：null = 使用系统值
- 当用户将 4 个覆盖字段全部清空（全部为 null）时，后端自动删除该行记录（无需保留无意义的空行）
- 保存 `SavePlanAsync` 实现 Upsert 语义：存在则更新，不存在则插入，全部 null 则删除
- "计划安排"按钮：批量将匹配查询的工单 Plan 覆盖值设为系统值（等于重置为用户未覆盖状态），并删除不匹配查询的 Plan 行（清理僵尸数据）

---

## 八、BatchPlanSchedule（批次计划薄表）

### 8.1 表名

`BatchPlanSchedules`

### 8.2 概述

批次计划薄表存储排程员在批次计划上对批次级流转计划的决策结果。通过"计划安排"按钮自动计算 8 个字段的快照写入本表，同时支持排程员在页面上手工内联编辑。批次计划查询时通过 LEFT JOIN 加载本表数据，映射到 BatchPlanDto 的 `Plan*` 属性。本表独立于实时计算的批次流转组（IsFlow），是批次计划组的数据源。

### 8.3 字段定义

| # | 字段名 | 类型 | 必填 | 默认值 | 说明 |
|---|--------|------|------|--------|------|
| 1 | Id | int | ✅ | 自增 | 主键 |
| 2 | BatchId | int | ✅ | — | 批次ID（唯一约束，一个批次一条记录） |
| 3 | IsFlow | bit | ✅ | 0 | 流转 |
| 4 | FlowLevel | int | ✅ | 3 | 等级（V5.28 五档：1=急+, 2=急, 3=急-, 4=一般, 5=略；与实时排程档位映射：急+→急+/急→急/急-→急-/顺·带→一般/略→略；原 4 档特急A/B 手工档已删除） |
| 5 | FlowTarget | nvarchar(50) | ❌ | NULL | 流转位（六档：成检/完工冷轧/冷轧/荒管检/在制检/成品检验；规则1 排程命中取实时 FlowTarget，规则2 重点批次按冷轧类型补充档位） |
| 6 | FlowCRType | nvarchar(100) | ❌ | NULL | 目标工序（如 20冷轧/30冷轧/50冷轧 或 "-"） |
| 7 | PlanOuterDiameterSpan | nvarchar(50) | ❌ | NULL | 外径跨度（坯料外径-轧制外径，如"110-89"） |
| 8 | FlowExecSpec | nvarchar(200) | ❌ | NULL | 执行规格 |
| 9 | TargetSequence | int | ❌ | NULL | 目标序（计划安排的快照值） |
| 10 | ExecutionSequence | int | ❌ | NULL | 执行序（计划安排的快照值，始终取当前工段在当前工序组中的序号） |
| 11 | IsGrabOrder | bit | ✅ | 0 | 抢单 |
| 12 | PlanRemark | nvarchar(500) | ❌ | NULL | 计划备注 |
| — | 审计字段（继承 BaseEntity） |
| 13 | CreatedTime | datetime2 | ✅ | — | 创建时间 |
| 14 | CreatedBy | nvarchar(50) | ✅ | — | 创建人 |
| 15 | UpdatedTime | datetime2 | ✅ | — | 更新时间 |
| 16 | UpdatedBy | nvarchar(50) | ✅ | — | 更新人 |

**字段数：** 11 业务字段 + 4 审计字段 = 15 字段（不含 BatchId 重复计数）

### 8.4 索引

| 索引名 | 类型 | 列 | 说明 |
|--------|------|----|------|
| PK_BatchPlanSchedules | PRIMARY KEY | Id | 主键 |
| UK_BPS_BatchId | UNIQUE | BatchId | 每批次一条记录 |

### 8.5 业务说明

- 8 个字段（IsFlow/FlowLevel/FlowTarget/FlowCRType/PlanOuterDiameterSpan/FlowExecSpec/TargetSequence/ExecutionSequence）由 `PlanAllAsync()` 按三规则自动计算（优先级 规则1 > 规则2 > 规则3）：
  - **规则1 冷轧排程命中**：FlowTarget=实时 FlowTarget（成检/完工冷轧/冷轧），FlowCRType=命中排程行 ProcessType
  - **规则2 重点生产批次（IsKeyBatch）兜底**：FlowTarget=`MapFlowTargetByCRType(MainNoAttentionProcess)`（荒管处理→荒管检 / 在制修检→在制检 / 生产收尾→成品检验 / 空→null / 剩余冷轧类→冷轧），FlowCRType=MainNoAttentionProcess
  - **规则3 降级（非流转）**：FlowTarget/FlowCRType/外径跨度/FlowExecSpec 全 null
  - ExecutionSequence 三规则均快照批次现执行序（未产视为 0）
- IsGrabOrder（抢单）和 PlanRemark（计划备注）为纯手工字段，计划安排时不覆盖

---

## 九、BatchPlanTarget（批次计划产量目标）【⚠️ 已删除】

> 2026-08-24 整套删除：实体/EF 配置/DbSet/后端 Service+接口/Controller/DTO/前端 Service/两处 DI 注册全删，迁移 `20260823194559_DropBatchPlanTargetTable` DROP TABLE `BatchPlanTargets`。

## 十、迁移信息

| 迁移文件 | 说明 |
|----------|------|
| 20260529041541_AddSchedulingContext | 创建 OrderDemandAdjustment 和 RawMaterialLockPlanAndExecution 两张表 |
| 20260602184642_AddSectionFlowAnalysisTables | 创建 SectionFlowCategorySettings 和 SectionFlowCategoryItems 两张表 |
| 20260603203851_RemoveG14Fields | 删除 RawMaterialLockPlanAndExecution 的 G14 字段（CurrentScheduleStage, CurrentRawMaterialLockRemark, IsExecuted） |
| 20260602213055_AddIsPreInputToRawMaterialLockPlanAndExecution | RawMaterialLockPlanAndExecution 表新增 IsPreInput 字段（G15） |
| 20260602214624_AddIsMainNoMaterialCompleteToRawMaterialLockPlanAndExecution | RawMaterialLockPlanAndExecution 表新增 IsMainNoMaterialComplete 字段（G15） |
| 20260603224128_AddCapacityWorkDaysToWorkOrderSchedule | WorkOrderSchedules 表新增 CapacityWorkDays(int?) 字段（产能工量） |
| 20260604032838_RefactorToLeftJoinMode | **重构为 LEFT JOIN 模式**：DROP RawMaterialLockPlanAndExecution 物化表 + CREATE RawMaterialLockPreExecution（G15 独立表，4 字段 + BaseEntity） |
| 20260604041505_RemoveUnusedFieldsFromSalesUrging | 删除 OrderDemandAdjustment 表中已废弃的 EstimatedArrivalDate、IsMainNoMaterialComplete、IsLockConfirmed 三个字段 |
| 20260602210321_AddWorkOrderScheduleTable | **创建 WorkOrderSchedules 物化表**：G1+G7+G12+G13 分组字段，继承 BaseEntity，WorkOrderId 唯一 |
| 20260605030206_AddG14FieldsToWorkOrderSchedule | **WorkOrderSchedules 新增 G14 字段**：PendingSection(8) + DeformedProcessCompleted + ProductionAttentionProcess |
| 20260605030700_AddBatchDeliveryToWorkOrderSchedule | **WorkOrderSchedules 新增 IsBatchDelivery 和 ProductionAttentionProcess MaxLength** |
| 20260605051230_RenameIsDemandAdjustmentToIsUrging | **WorkOrderSchedules 列重命名**：IsDemandAdjustment→IsUrging，新增 IsPaused 列 |
| 20260607195742_AddColdRollSpecSchedule | **创建 ColdRollSpecSchedules 表**：首次提交冷轧排程表，含 ScheduleDate/ProcessType/BilletSpec/RollingSpec/IsFinished/MachineNo/CompletionType/RollType/RollOrder/MergeDisplay/Remark 共 11 业务字段 + BaseEntity |
| 20260607210528_RemoveDailyTonsFromColdRollSpecSchedule | **ColdRollSpecSchedules 删除 DailyTons 字段**：DailyTons 字段设计时考虑但实际排程场景不需要存储，从冷轧计划实时计算 |
| 20260607213604_AddCompletionTypeToColdRollSpecSchedule | **ColdRollSpecSchedules 新增 CompletionType 字段**：完工要求（在轧要求维度），None=无计划/All=全量完工/Urgent=急单完工/Partial=部分完工 |
| 20260607235216_RemoveScheduleDateFromColdRollSpecSchedule | **ColdRollSpecSchedules 删除 ScheduleDate 字段，唯一索引降为四维**：ScheduleDate 无业务意义已删除，唯一索引改为 (ProcessType, BilletSpec, RollingSpec, IsFinished) 四维度，前端改为全量同步模式（SaveAllAsync） |
| 20260609030217_AddWorkOrderExecutionG13Fields | **WorkOrderExecutionSummary 新增 G13 字段**：IsUrging/IsBatchDelivery/IsPaused/AdjustmentRemark 四字段直接存入读模型实体，不再 LEFT JOIN OrderDemandAdjustment |
| 20260609040439_AddWorkOrderPlan | **创建 WorkOrderPlans 薄表**：存储 G15 手工覆盖值（WorkOrderId + ScheduleStage/UrgencyLevel/ProductionAttentionProcess/ProductionFlowProperty 4 个可 null 字段 + BaseEntity） |
| 20260609042931_DropWorkOrderScheduleTable | **删除 WorkOrderSchedules 物化表**：工单排程改为 LEFT JOIN 实时查询 + WorkOrderPlan 薄表模式，物化表不再使用 |
| 20260609213351_AddBatchPlanSchedule | **创建 BatchPlanSchedules 表**：批次计划薄表，存储批次级流转计划决策（IsFlow/FlowLevel/FlowTarget/FlowCRType/FlowExecSpec/TargetSequence/ExecutionSequence + 抢单/备注） |
| 20260610025914_AddMaxBatchRemainingWorkDaysAndMainNoAttentionProcess | **BatchPlanSchedule 新增字段**（补充字段） |
| 20260610061116_DropRollOrderFromColdRollSpecSchedule | **ColdRollSpecSchedules 删除 RollOrder 字段** |
| 20260610191704_UpdateSectionFlowCategories | **工段流转类别重构**：K 过程检→K荒管检(仅荒管处理/检验)、L 成品待检→L在制检(在制修检/全部)、新增 M 成品待检(在制修检/全部)；支持工段="全部"通配 |
| 20260611223846_AddBatchPlanTarget | **创建 BatchPlanTargets 表**：批次计划产量目标（SectionName + DailyTarget），唯一索引 UK_BPT_SectionName |
| 20260626023959_AddIsBudgetComplete | **RawMaterialLockPreExecution 新增 IsBudgetComplete 字段**：预算主号齐全，MudSwitch 手工切换，级联影响 IsMainNoMaterialComplete |
| 20260814115408_BatchPlanScheduleAddIsPaused | **BatchPlanSchedules 新增 IsPaused（bit）字段** |
| 20260814165659_AddCombinationGroupAndDisplayOrder | **SectionFlowCategorySettings 新增 DisplayOrder（存量按 CategoryCode 回填）+ 创建 CombinationGroups 表**（二维 ProcessGroupName+SectionName 唯一） |
| 20260814182415_DropSectionFlowCategoryCodeAddCategoryKind | **SectionFlowCategorySettings 删 CategoryCode + 新增 CategoryKind**（过渡字段，后续迁移删除） |
| 20260814192306_SectionFlowCategoryThreeDimensionProductStatus | **CombinationGroups 新增 ProductStatus → 三维唯一索引**；删 SectionFlowCategorySettings.CategoryKind；**DROP SectionFlowCategoryItems 表** |
| 20260814202319_CombinationGroupDropCoefficientRebuild | **CombinationGroups 删 Coefficient 重建**（删除变异量系数概念） |
| 20260815052422_AddSectionParagraphConfigAndParagraphName | **CombinationGroups 新增 ParagraphName（归属生产段落）+ 创建 SectionParagraphConfigs 表**（ParagraphName 唯一索引 UK_SPC_ParagraphName） |
| 20260819174709_AddColdRollSpecScheduleDailyOutput | **ColdRollSpecSchedules 新增 DailyOutput 字段**：单机单日量(kg/天，decimal(18,3) NULL)，用于冷轧排程排机估算 |
| 20260822065324_AddColdRollCapacity | **创建 ColdRollCapacity 表**：冷轧产能配置（排程保存自动反哺的产能档案，四维 (ProcessType, BilletSpec, RollingSpec, IsFinished) 唯一索引 UK_CRC_Dimensions，含 MachineNo/DailyOutput/SampleCount/LastConfirmedAt） |
| 20260822084902_AddColdRollMachineConfig | **创建 ColdRollMachineConfig 表**：冷轧机台数配置（按冷轧类型 ProcessType 唯一索引 UK_CRMC_ProcessType，含 OwnedCount/MinMachines/MaxMachines/EstimatedDailyOutput/Remark） |
| 20260823194559_DropBatchPlanTargetTable | **删除 BatchPlanTargets 表**：批次计划产量目标模块整套删除（实体/EF/DbSet/后端 Service+接口/Controller/DTO/前端 Service/两处 DI 注册全删），DROP TABLE |
| 20260823194848_DropWorkOrderExecutionSummaryObsoleteMaterialFields | **WorkOrderExecutionSummary 删除 G5 旧物料字段 6 个**：PendingRoughTubeQty/PendingRoughTubeWeight/PendingOutsourceFinishQty/PendingOutsourceFinishWeight/TheoreticalFinishQty/TheoreticalFinishWeight |

---

## 十二、版本历史

| 版本 | 日期 | 变更说明 |
|------|------|----------|
| **V5.35** | **2026-08-24** | **BatchPlanTarget 整套删除 + WorkOrderExecutionSummary 旧物料字段删除**：① BatchPlanTarget 模块整套删除（迁移 `20260823194559_DropBatchPlanTargetTable` DROP TABLE `BatchPlanTargets`），§九 标注「已删除」；② WorkOrderExecutionSummary 删除 G5 旧物料字段 6 个（迁移 `20260823194848_DropWorkOrderExecutionSummaryObsoleteMaterialFields`）；③ V5.28 历史条目「BatchPlanTarget 表标注弃用（表/实体保留未删）」→「已删除，见 V5.35」；④ 迁移信息表补 2 条记录 |
| **V5.34** | **2026-08-22** | **新增冷轧产能配置与机台数配置两表**：① `ColdRollCapacity`（冷轧产能档案，排程保存自动反哺，四维粒度同排程小表，DailyOutput 单机单日量方式B，SampleCount/LastConfirmedAt 样本置信度预留）；迁移 `20260822065324_AddColdRollCapacity`；② `ColdRollMachineConfig`（冷轧机台数配置，按冷轧类型 ProcessType 唯一，OwnedCount/MinMachines/MaxMachines/EstimatedDailyOutput 方式A兜底，排程建议引擎产能平衡输入）；迁移 `20260822084902_AddColdRollMachineConfig`；§二 实体清单补 12/13 + 文档末尾新增 §十四/§十五 |
| **V5.33** | **2026-08-20** | **ColdRollSpecSchedules 新增单机单日量 DailyOutput**：decimal(18,3) NULL，单台设备每日可轧制重量，供排机估算（GET /api/cold-roll-plan/machine-estimate）使用；迁移 `20260819174709_AddColdRollSpecScheduleDailyOutput`；§13.3 字段表同步（原 #7-#14 → #8-#15） |
| **V5.31** | **2026-08-15** | **组合归类表重构 + 新增段落日产配置**：① SectionFlowCategorySettings 删 CategoryCode、新增 DisplayOrder；② SectionFlowCategoryItems 已删除，替换为 CombinationGroups（(工序组,工段,产类) 三维唯一归属映射，含 FlowCategoryId + 归属段落 ParagraphName，无 Coefficient）；③ 新增 SectionParagraphConfigs 表（段落日产配置，ParagraphName 唯一）；④ §一/§二/§五/§六 同步更新；⑤ 迁移信息补 6 个新迁移（BatchPlanScheduleAddIsPaused / AddCombinationGroupAndDisplayOrder / DropSectionFlowCategoryCodeAddCategoryKind / SectionFlowCategoryThreeDimensionProductStatus / CombinationGroupDropCoefficientRebuild / AddSectionParagraphConfigAndParagraphName） |
| **V5.29** | **2026-08-14** | **删除"非全量·保留调整工序"功能（无表结构变更）**：删除 `POST /plan-keep-attention` 端点及后端 `PlanScheduleKeepAttentionAsync`、前端按钮/事件/服务方法、接口方法及 2 个测试。该功能将 summary 5 档 ScheduleStage 直接写入 WorkOrderPlan 4 档字段（缺 `MapSummaryStageToPlanStage` 映射），导致 G15 工单状态错位；WorkOrderPlan 表结构无变更，仅删除相关行为路径 |
| **V5.28** | **2026-08-14** | **批次计划页列重组 + 流转目标六档 + 薄表规则细化**：① 无表结构变更（仅说明更新）；BatchPlanSchedule.FlowLevel 说明改 V5.28 五档（1=急+/2=急/3=急-/4=一般/5=略，特急A/B 手工档删除）；FlowTarget 说明扩六档（成检/完工冷轧/冷轧/荒管检/在制检/成品检验）；② 8.5 业务说明补三规则（规则1 排程命中/规则2 重点批次 MapFlowTargetByCRType 档位补充/规则3 降级，ExecutionSequence 三规则均快照现执行序）；③ BatchPlanTarget 表标注弃用（批次计划页移除产量目标输入行+成品检验 Tab，表/实体保留未删）——**⚠️ 2026-08-24 已整套删除，见 V5.35** |
| **V5.11** | **2026-08-13** | **BatchPlanSchedule FlowLevel 等级 5 档改 4 档**：字段表 FlowLevel 说明更新为 1=特急/2=急/3=一般/4=略（原 3=B顺 与 4=普通 合并为 3=一般、原 5=不流转 归为 4=略；特急按 PlanFlowCRType 与主号关注工序匹配拆 A/B，PlanFlowLevelDisplay 显示特急A/特急B）；迁移 `20260813042907_UpdateBatchPlanScheduleFlowLevelFourTiers` 存量 UPDATE（5→4、4→3） |
| **V5.10** | **2026-08-09** | **文档与代码现状核对**：排程上下文表结构无新增/变更（WarehouseThreshold 入库阈值、WorkOrderExecutionSummary 到料实投五态/五档主号关注均属工单上下文读模型，不在本上下文落表）；WorkOrderPlan.ScheduleStage 仍为排程 4 档覆盖（0=工单完成/1=原料锁定/2=生产执行/3=成品检验），不受 summary 五档升级影响 |
| V5.4 | 2026-06-26 | RawMaterialLockPreExecution 新增 IsBudgetComplete 字段（预算主号齐全），更新字段定义与业务说明 |
| **V5.5** | **2026-06-30** | **ColdRollSpecSchedule CompletionType/RollType 枚举值更新**：从技术名（Urgent/Partial/Subsequent）改为 4 档业务名（特急单=Urgent/急单=Partial2/含B顺=Partial3/全量=All），删除 Partial1/Subsequent 内部值，字段存储值不变仅说明更新 |
| **V5.6** | **2026-07-03** | **BatchPlanSchedule 新增 PlanOuterDiameterSpan 字段 + 等级1拆分为1A/1B**：字段表新增外径跨度字段，FlowLevel 说明更新为1A/1B/2/3/4/5 六档，业务说明从7字段改为8字段 |
| **V5.7** | **2026-07-10** | **订单需求调整迁移至工单上下文**：OrderDemandAdjustment 说明中"API/UI 已迁移至订单上下文"改为"已迁移至工单上下文" |
| **V5.8** | **2026-07-15** | **ColdRollSpecSchedule 移除 RollOrder 字段**：实体已删除 RollOrder（迁移 20260610061116），同步更新 §13.3 字段表及编号 |
| **V5.9** | **2026-08-01** | CategoryCode 编码说明更新为 A~N 14 类 |

## 十三、ColdRollSpecSchedule（冷轧排程）

### 13.1 表名

`ColdRollSpecSchedule`

### 13.2 概述

冷轧排程是冷轧计划的写入目标实体，存储排程员在冷轧计划简化视图上所作出的滚动排程决策（3-4 天短期排程）。按 `(ProcessType, BilletSpec, RollingSpec, IsFinished)` 四字段唯一约束，全量同步模式（前端全量加载 + 全量保存，增/改 + 删除不在列表中的旧记录）。

冷轧计划为只读 LEFT JOIN 实时聚合视图，排程员在 UI 上编辑排程计划后保存到本表，系统将排程数据反推应用到 `ProductionBatch` 进行工单锁定。

### 13.3 字段定义

| # | 字段名 | 类型 | 必填 | 默认值 | 说明 |
|---|--------|------|------|--------|------|
| 1 | Id | int | ✅ | 自增 | 主键 |
| 2 | ProcessType | nvarchar(50) | ✅ | — | 冷轧类型（60冷轧/50冷轧/30冷轧/20冷轧/三辊冷轧/冷拔） |
| 3 | BilletSpec | nvarchar(100) | ✅ | — | 轧坯规格（前一工序组制造规格） |
| 4 | RollingSpec | nvarchar(100) | ✅ | — | 轧制规格（当前冷轧工序组制造规格） |
| 5 | IsFinished | bit | ✅ | 0 | 是否成品（是否最后工序） |
| 6 | MachineNo | nvarchar(200) | ❌ | NULL | 轧机设备号（多台用分号分隔） |
| 7 | DailyOutput | decimal(18,3) | ❌ | NULL | 单机单日量(kg/天)：单台设备每日可轧制重量，用于排机估算 |
| 8 | CompletionType | nvarchar(20) | ✅ | "None" | 完工要求（在轧要求维度）：None=无计划/All=全量/Urgent=特急单/Partial2=急单/Partial3=含B顺 |
| 9 | RollType | nvarchar(20) | ✅ | "None" | 排程类型（待轧要求维度）：None=无计划/All=全量/Urgent=特急单/Partial2=急单/Partial3=含B顺 |
| 10 | MergeDisplay | nvarchar(300) | ❌ | NULL | 合并显示文本（冗余，方便查询匹配） |
| 11 | Remark | nvarchar(500) | ❌ | NULL | 备注 |
| — | 审计字段（继承 BaseEntity） |
| 12 | CreatedTime | datetime2 | ✅ | — | 创建时间 |
| 13 | CreatedBy | nvarchar(50) | ✅ | — | 创建人 |
| 14 | UpdatedTime | datetime2 | ✅ | — | 更新时间 |
| 15 | UpdatedBy | nvarchar(50) | ✅ | — | 更新人 |

### 13.4 索引

| 索引名 | 类型 | 列 | 说明 |
|--------|------|----|------|
| PK_ColdRollSpecSchedules | PRIMARY KEY | Id | 主键 |
| UK_CRSS_Dimensions | UNIQUE | ProcessType, BilletSpec, RollingSpec, IsFinished | 四维度唯一约束，同规格组合仅一条 |

### 13.5 业务说明

- 排程数据由冷轧计划简化视图的"编辑排程"模式生成，通过工具栏按钮进入/退出编辑模式
- 简化视图保存时展开到明细级别（`_rawAllItems` 匹配明细行），确保每个明细行均保存排程数据
- 前端通过 `ColdRollSpecScheduleService.GetAllAsync()` 加载全部排程数据，通过 `SaveAllAsync(list)` 全量同步保存（增/改 + 删除不在列表中的旧记录）

---

## 十四、ColdRollCapacity（冷轧产能配置）

### 14.1 表名

`ColdRollCapacity`

### 14.2 概述

冷轧排程保存时自动反哺的产能档案（机台×规格 单机单日量）。粒度与冷轧排程小表 ColdRollSpecSchedule 四维一一对应 `(ProcessType, BilletSpec, RollingSpec, IsFinished)`，DailyOutput 单位 kg/天/单机，与排程小表 DailyOutput 口径一致。排程建议引擎流转保底以本表为方式B（优先）数据源。SampleCount/LastConfirmedAt 本期仅记录不消费，作为样本置信度权重为后续产能聚合/排程建议铺路。

### 14.3 字段定义

| # | 字段名 | 类型 | 必填 | 默认值 | 说明 |
|---|--------|------|------|--------|------|
| 1 | Id | int | ✅ | 自增 | 主键 |
| 2 | ProcessType | nvarchar(50) | ✅ | — | 冷轧类型（ProcessKeys 英文 Key，如 ColdRoll60） |
| 3 | BilletSpec | nvarchar(100) | ✅ | — | 轧坯规格（前一工序组制造规格，如 "219*8"） |
| 4 | RollingSpec | nvarchar(100) | ✅ | — | 轧制规格（当前冷轧工序组制造规格） |
| 5 | IsFinished | bit | ✅ | 0 | 是否成品（最后工序组） |
| 6 | MachineNo | nvarchar(200) | ❌ | NULL | 常用机台设备号（多台分号分隔） |
| 7 | DailyOutput | decimal(18,3) | ❌ | NULL | 单机单日量（kg/天） |
| 8 | SampleCount | int | ✅ | 0 | 该维度以有产能信息被反哺的次数 |
| 9 | LastConfirmedAt | datetimeoffset | ❌ | NULL | 最近一次反哺时间 |
| — | 审计字段（继承 BaseEntity） |
| 10 | CreatedTime | datetime2 | ✅ | — | 创建时间 |
| 11 | CreatedBy | nvarchar(50) | ✅ | — | 创建人 |
| 12 | UpdatedTime | datetime2 | ✅ | — | 更新时间 |
| 13 | UpdatedBy | nvarchar(50) | ✅ | — | 更新人 |

### 14.4 索引

| 索引名 | 类型 | 列 | 说明 |
|--------|------|----|------|
| PK_ColdRollCapacities | PRIMARY KEY | Id | 主键 |
| UK_CRC_Dimensions | UNIQUE | ProcessType, BilletSpec, RollingSpec, IsFinished | 四维度唯一约束，与排程小表同粒度 |

### 14.5 业务说明

- 数据来源：排程保存（ColdRollSpecSchedule.SaveAllAsync）自动反哺（ReverseFillCapacityAsync）——四维维度已存在则更新 DailyOutput/MachineNo 并 SampleCount++/LastConfirmedAt=now，不存在则新增；产能档案累积，不随排程小表僵尸清理删除
- 手工调整：配置页 `/cold-roll-capacities` + `POST /api/cold-roll-capacity/save`（调整后反向同步排程小表已存在维度）

---

## 十五、ColdRollMachineConfig（冷轧机台数配置）

### 15.1 表名

`ColdRollMachineConfig`

### 15.2 概述

按单冷轧类型的机台数参数（排程建议引擎产能平衡输入）。覆盖关系：60 冷轧可干 50 冷轧的活、30 冷轧可覆盖 20 冷轧的活（机台需求按机台类型组聚合）。

### 15.3 字段定义

| # | 字段名 | 类型 | 必填 | 默认值 | 说明 |
|---|--------|------|------|--------|------|
| 1 | Id | int | ✅ | 自增 | 主键 |
| 2 | ProcessType | nvarchar(50) | ✅ | — | 机型（ProcessKeys 英文 Key：ColdRoll60/50/30/20/ThreeRollColdRoll/ColdDraw，唯一） |
| 3 | OwnedCount | int | ✅ | 0 | 本厂数量（该机型实有机台数） |
| 4 | MinMachines | int | ✅ | 0 | 最小机台数（排程机台需求下限） |
| 5 | MaxMachines | int | ✅ | 0 | 最大机台数（排程机台需求上限） |
| 6 | EstimatedDailyOutput | decimal(18,3) | ❌ | NULL | 估算单机日产（kg/天，平衡流转方式A兜底） |
| 7 | Remark | nvarchar(200) | ❌ | NULL | 备注 |
| — | 审计字段（继承 BaseEntity） |
| 8 | CreatedTime | datetime2 | ✅ | — | 创建时间 |
| 9 | CreatedBy | nvarchar(50) | ✅ | — | 创建人 |
| 10 | UpdatedTime | datetime2 | ✅ | — | 更新时间 |
| 11 | UpdatedBy | nvarchar(50) | ✅ | — | 更新人 |

### 15.4 索引

| 索引名 | 类型 | 列 | 说明 |
|--------|------|----|------|
| PK_ColdRollMachineConfigs | PRIMARY KEY | Id | 主键 |
| UK_CRMC_ProcessType | UNIQUE | ProcessType | 机型唯一，一机型一条 |

### 15.5 业务说明

- 全栈 CRUD：配置页 `/cold-roll-machine-configs`（GET /all、GET /list、POST /save、POST /delete/{id}）
- 排程建议引擎产能平衡输入：组级 MinMachines/MaxMachines = 组内机型配置之和；EstimatedDailyOutput 为流转方式A（无机台数配置优先于方式B）兜底
