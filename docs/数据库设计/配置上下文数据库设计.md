# 配置上下文数据库设计

**版本：V1.12**
**最后更新：2026-08-19**

---

## 一、概述

本文档定义配置上下文（Configuration Context）的数据库设计。配置上下文包含九张系统参数表：标准工量天数（StandardWorkDays）、交货状态附加天数（StandardWorkDayDeliveryStates）、业务参数配置（ConfigParameters）、日产估算（DailyOutputEstimates）、重点工段日产（DailyProductionCapacities）、员工管理（Employees）、枚举显示配置（EnumDisplayDefinitions）、字典值配置（DictValueDefinitions）和工序组定义（ProcessDefinitions），用于替代原有硬编码逻辑，支持管理员页面动态配置。此外，配置上下文的管理页面还包括流转类别日产配置（SectionFlowCategorySettings）和工位管理（Workstations，扫码报工基础配置）。

**当前范围（V1.9）：**
- StandardWorkDays：各工段标准天数配置，支持牌号前缀覆盖；V1.9 起含 SectionKey/DisplayOrder/IsEnabled 展示层动态化字段
- StandardWorkDayDeliveryStates：各交货状态附加天数配置，含默认值
- ConfigParameters：通用数值型业务参数配置（EAV 模式），替代约 50+ 处硬编码业务数值常量
- DailyOutputEstimates：日产估算配置，按成品外径区间设定日产能（吨/天），用于产能工量计算
- DailyProductionCapacities：重点工段日产配置，各工段日产能力（吨/天），用于工单总览产能负荷计算
- Employees：员工基础信息（工号/姓名/部门/岗位/工资结算模式），为扫码报工和工资结算提供基础数据
- EnumDisplayDefinitions（V1.9 新增）：枚举显示配置（EnumKey/Value/DisplayName/DisplayOrder/Remark），管理枚举值的中文显示名与排序（不可加值）
- DictValueDefinitions（V1.9 新增）：字典值配置（DictKey/Value/DisplayName/DisplayOrder/IsEnabled/Remark），管理字典值的显示名/排序/启停（可隐藏加值）
- ProcessDefinitions（V1.9 新增）：工序组定义（ProcessKey/ProcessName/DisplayOrder/IsEnabled/IsColdRoll/IsColdDraw/DefaultSections/Remark），管理工序组中文名/启停/默认工段
- Workstations（扫码报工基础配置）：工位管理，绑定工位编码 → 工段 + 设备 + 报工模板类型
- SectionFlowCategorySetting：流转类别日产配置预设表，管理 UI 在配置上下文参数表菜单下（类别包含的 (工序组,工段,产类) 组合由 CombinationGroups 表承载）

**设计原则：**
- 各参数表均为独立参数表，不通过外键关联任何业务表
- 业务模块通过 Service 层提供的 `GetStandardDaysMapAsync` / `GetDeliveryStateExtraDaysMapAsync` / `GetConfigMapAsync` 方法读取参数
- CRUD 统一通过内联编辑页面，AdminOnly 权限

---

## 二、实体清单

| 序号 | 实体 | 表名 | 说明 |
|------|------|------|------|
| 1 | StandardWorkDay | StandardWorkDays | 标准工量天数 |
| 2 | StandardWorkDayDeliveryState | StandardWorkDayDeliveryStates | 交货状态附加天数 |
| 3 | ConfigParameter | ConfigParameters | 业务参数配置（通用 EAV 键值对） |
| 4 | DailyOutputEstimate | DailyOutputEstimates | 日产估算配置 |
| 5 | DailyProductionCapacity | DailyProductionCapacities | 重点工段日产配置 |
| 6 | Employee | Employees | 员工基础信息（扫码报工和工资结算基础） |
| 7 | Workstation | Workstations | 工位管理（扫码报工基础配置，绑定工位编码 → 工段 + 设备 + 报工模板类型） |
| 8 | EnumDisplayDefinition | EnumDisplayDefinitions | 枚举显示配置（EnumKey/Value/DisplayName/DisplayOrder/Remark） |
| 9 | DictValueDefinition | DictValueDefinitions | 字典值配置（DictKey/Value/DisplayName/DisplayOrder/IsEnabled/Remark） |
| 10 | ProcessDefinition | ProcessDefinitions | 工序组定义（ProcessKey/ProcessName/DisplayOrder/IsEnabled/IsColdRoll/IsColdDraw/DefaultSections/Remark） |

---

## 三、StandardWorkDays（标准工量天数）

### 3.1 表名

`StandardWorkDays`

### 3.2 字段定义

| # | 字段名 | 类型 | 必填 | 默认值 | 说明 |
|---|--------|------|------|--------|------|
| 1 | Id | int | ✅ | 自增 | 主键 |
| 2 | SectionName | nvarchar(50) | ✅ | - | 工段名称，对应 SectionDefs 常量（显示中文，可改名） |
| 3 | SectionKey | nvarchar(50) | ❌ | NULL | 稳定 Key（对应 SectionDefs 常量名，如 ColdRollDraw），程序识别工段用，不受改名影响；null 表示历史数据尚未回填 |
| 4 | DisplayOrder | int | ✅ | 0 | 显示顺序（升序排列，1 排最前） |
| 5 | IsEnabled | bit | ✅ | true | 是否启用（false 表示隐藏，界面不显示该工段） |
| 6 | PlantGradePrefix | nvarchar(50) | ❌ | NULL | 牌号前缀（如 "3"），null 表示所有牌号通用 |
| 7 | StandardDays | float | ✅ | - | 标准天数 |
| 8 | Remark | nvarchar(200) | ❌ | NULL | 说明 |
| — | 审计字段（继承 BaseEntity） |
| 9 | CreatedTime | datetime2 | ✅ | - | 创建时间 |
| 10 | CreatedBy | nvarchar(50) | ✅ | - | 创建人 |
| 11 | UpdatedTime | datetime2 | ✅ | - | 更新时间 |
| 12 | UpdatedBy | nvarchar(50) | ✅ | - | 更新人 |

### 3.3 索引

| 索引名 | 类型 | 字段 | 说明 |
|--------|------|------|------|
| PK_StandardWorkDays | 主键 | Id | |
| UK_SWD_SectionName_PlantGradePrefix | 唯一 | SectionName + PlantGradePrefix | 同一工段+牌号前缀组合唯一 |

### 3.4 Seed 数据

```sql
-- 通用配置（PlantGradePrefix = null，26 工段预置超集，含 SectionKey/DisplayOrder/IsEnabled）
INSERT INTO StandardWorkDays (SectionName, SectionKey, DisplayOrder, IsEnabled, PlantGradePrefix, StandardDays, Remark) VALUES
('冷轧拔',    'ColdRollDraw',      1,  1, NULL, 2,   '冷轧/冷拔'),
('油管断',    'OilPipeCut',        2,  1, NULL, 1,   NULL),
('去油',      'Degrease',          3,  1, NULL, 1,   NULL),
('乳液浸洗',  'EmulsionWash',      4,  1, NULL, 0.5, NULL),
('超声波洗',  'UltrasonicWash',    5,  1, NULL, 0.5, NULL),
('布轮抛光',  'ClothPolish',       6,  1, NULL, 1,   NULL),
('光亮退火',  'BrightAnnealing',   7,  1, NULL, 0.5, NULL),
('固溶',      'Solution',          8,  1, NULL, 1,   NULL),
('矫直',      'Straighten',        9,  1, NULL, 0.5, NULL),
('断切',      'Cut',               10, 1, NULL, 0.5, NULL),
('测壁厚',    'ThicknessMeasure',  11, 1, NULL, 1,   NULL),
('酸洗',      'Pickle',            12, 1, NULL, 2,   '非3系牌号'),
('外抛光',    'OuterPolish',       13, 1, NULL, 0.5, NULL),
('内抛光',    'InnerPolish',       14, 1, NULL, 1,   NULL),
('内修磨',    'InnerGrinding',     15, 1, NULL, 0.5, NULL),
('外点磨',    'OuterSpotGrinding', 16, 1, NULL, 0.5, NULL),
('喷砂',      'SandBlasting',      17, 1, NULL, 1,   NULL),
('喷丸',      'ShotBlasting',      18, 1, NULL, 0.5, NULL),
('检验',      'Inspection',        19, 1, NULL, 1,   NULL),
('焊头',      'WeldingHead',       20, 1, NULL, 0.5, NULL),
('焊接',      'Welding',           21, 1, NULL, 0.5, NULL),
('润滑',      'Lubrication',       22, 1, NULL, 1,   NULL),
('包装',      'Packing',           23, 1, NULL, 0.5, NULL),
('入库',      'Warehouse',         24, 1, NULL, 2,   NULL),
('备用1',     'Extra1',            25, 0, NULL, 0.5, NULL),
('备用2',     'Extra2',            26, 0, NULL, 0.5, NULL);

-- 牌号前缀覆盖：3系牌号酸洗只需1天
INSERT INTO StandardWorkDays (SectionName, SectionKey, DisplayOrder, IsEnabled, PlantGradePrefix, StandardDays, Remark) VALUES
('酸洗', 'Pickle', 12, 1, '3', 1, '3系牌号（304/316/321等）');
```

> **注：** 上述为 26 工段预置超集（共 27 行，含酸洗 3 系覆盖行）。存量库升级时由迁移 `20260805052930_AddSevenProcessGroupSections`（新增 7 工段）与 `20260805055822_AddStandardWorkDayDictionaryFields`（新增 SectionKey/EnglishName/DisplayOrder/IsEnabled）处理，DbInitializer 对 `SectionKey == null` 的历史行按 SectionName 反查回填。

---

## 四、StandardWorkDayDeliveryStates（交货状态附加天数）

### 4.1 表名

`StandardWorkDayDeliveryStates`

### 4.2 字段定义

| # | 字段名 | 类型 | 必填 | 默认值 | 说明 |
|---|--------|------|------|--------|------|
| 1 | Id | int | ✅ | 自增 | 主键 |
| 2 | DeliveryState | nvarchar(100) | ✅ | - | 交货状态枚举名，空字符串表示默认值 |
| 3 | ExtraDays | float | ✅ | - | 附加天数 |
| 4 | PlantGradePrefix | nvarchar(50) | ❌ | NULL | 牌号前缀覆盖（预留） |
| 5 | Remark | nvarchar(200) | ❌ | NULL | 备注 |
| — | 审计字段（继承 BaseEntity） |
| 6 | CreatedTime | datetime2 | ✅ | - | 创建时间 |
| 7 | CreatedBy | nvarchar(50) | ✅ | - | 创建人 |
| 8 | UpdatedTime | datetime2 | ✅ | - | 更新时间 |
| 9 | UpdatedBy | nvarchar(50) | ✅ | - | 更新人 |

### 4.3 索引

| 索引名 | 类型 | 字段 | 说明 |
|--------|------|------|------|
| PK_StandardWorkDayDeliveryStates | 主键 | Id | |
| UK_SWDDS_DeliveryState_PlantGradePrefix | 唯一 | DeliveryState + PlantGradePrefix | 同一交货状态+牌号前缀组合唯一 |

### 4.4 Seed 数据

```sql
-- 默认：非固溶酸洗/非硬态 +4 天
INSERT INTO StandardWorkDayDeliveryStates (DeliveryState, ExtraDays, Remark) VALUES
('',                            4, '默认附加天数（非固溶酸洗/非硬态）'),
-- 固溶酸洗及其变体 → +0 天（不额外加）
('SolutionAnnealedAndPickled',  0, '固溶酸洗'),
('SolutionAnnealedAndPickledAndSkinPassed', 0, '固溶酸洗+平整'),
('SolutionAnnealedAndPickledAndColdDrawn',  0, '固溶酸洗+冷拔'),
-- 其它交货状态 → +4 天（使用默认值）
('BrightCoiled',                4, '光亮-盘管'),
-- 硬态 → +0 天（不额外加）
('Hard',                        0, '硬态');
```

---

## 五、ConfigParameters（业务参数配置）

### 5.1 表名

`ConfigParameters`

### 5.2 字段定义

| # | 字段名 | 类型 | 必填 | 默认值 | 说明 |
|---|--------|------|------|--------|------|
| 1 | Id | int | ✅ | 自增 | 主键 |
| 2 | Category | nvarchar(50) | ✅ | - | 参数分类（如 "MaterialPlanStatus"、"DefaultValue"、"DimensionTolerance" 等） |
| 3 | CategoryDisplay | nvarchar(100) | ❌ | NULL | 分类及用途（中文解读，如"仓库-完工阈值"） |
| 4 | Context | nvarchar(50) | ❌ | NULL | 所属上下文（如"采购+仓库"、"工单"） |
| 5 | ParamKey | nvarchar(100) | ✅ | - | 参数键名（如 "FixedPartial"、"PipeLength"） |
| 6 | ParamValue | decimal(18,4) | ✅ | - | 参数数值 |
| 7 | Remark | nvarchar(200) | ❌ | NULL | 说明 |
| — | 审计字段（继承 BaseEntity） |
| 8 | CreatedTime | datetime2 | ✅ | - | 创建时间 |
| 9 | CreatedBy | nvarchar(50) | ✅ | - | 创建人 |
| 10 | UpdatedTime | datetime2 | ✅ | - | 更新时间 |
| 11 | UpdatedBy | nvarchar(50) | ✅ | - | 更新人 |

### 5.3 索引

| 索引名 | 类型 | 字段 | 说明 |
|--------|------|------|------|
| PK_ConfigParameters | 主键 | Id | |
| UK_CP_Category_ParamKey | 唯一 | Category + ParamKey | 同一分类下参数键唯一 |
| IX_CP_Category | 非聚集 | Category | 按分类快速查询 |

### 5.4 设计说明

ConfigParameter 采用 EAV（Entity-Attribute-Value）模式，将散落在10+个 Service 中的硬编码数值常量统一为数据库配置：

- **Category**：按功能模块分组，如 `MaterialPlanStatus`（用料计划状态阈值）、`DefaultValue`（通用默认值）、`DimensionTolerance`（尺寸公差系数）、`WarehouseThreshold`（仓库阈值）、`ReworkRatio`（改制系数）、`SequenceJump`（序号跳步）等
- **ParamKey**：每个 Category 下的唯一键名
- **ParamValue**：decimal 类型，Service 通过 `GetConfigMapAsync(category)` 获取整个分类的字典映射

### 5.5 分类及参数清单

| Category | ParamKey | 默认值 | 说明 | 使用方 |
|----------|----------|--------|------|--------|
| DefaultValue | BatchMaxSequence | 9999 | 批次号最大序号 | BatchService |
| DefaultValue | DefaultProcessCycle | 22 | 工艺周期默认值 | WorkOrderExecutionService |
| DefaultValue | StandardCycle | 3 | 标准生产周期默认天数 | MaterialPlanService 等 |
| DefaultValue | RoughTubeFinishRatio | 0.92 | 荒管转成品系数 | WorkOrderExecutionService |
| MaterialPlanStatus | FixedPartial | 102 | 定尺：主号级部分边界 | WorkOrderService 等（4个Service） |
| MaterialPlanStatus | FixedSatisfied | 110 | 定尺：已满足阈值 | 同上 |
| MaterialPlanStatus | NonFixedPartial | 105 | 非定尺：主号级部分边界 | 同上 |
| MaterialPlanStatus | NonFixedSatisfied | 120 | 非定尺：已满足阈值 | 同上 |
| MaterialPlanRatio | FixedFinishRatio | 1.02 | 定尺成品采购比率 | WorkOrderListSummaryRefreshService |
| MaterialPlanRatio | FixedInventoryRatio | 1.02 | 定尺库存使用比率 | 同上 |
| MaterialPlanRatio | NonFixedFinishRatio | 1.05 | 非定尺成品采购比率 | 同上 |
| MaterialPlanRatio | NonFixedInventoryRatio | 1.05 | 非定尺库存使用比率 | 同上 |
| DimensionTolerance | OdLower | 1.002 | 外径下限系数 | MaterialPlanService |
| DimensionTolerance | OdUpper | 0.998 | 外径上限系数 | 同上 |
| DimensionTolerance | WtLower | 1.02 | 壁厚下限系数 | 同上 |
| DimensionTolerance | WtUpper | 0.98 | 壁厚上限系数 | 同上 |
| LengthDefault | PipeLength | 6000 | 管材默认长度 | 同上 |
| LengthDefault | UnitWeightLength | 4500 | 单重计算长度 | 同上 |
| ReworkRatio | EmptyDrawingOdLower | 1.05 | 空拉改制外径下限 | 同上 |
| ReworkRatio | FewerPassOdLower | 1.1 | 少道次改制外径下限 | 同上 |
| ReworkRatio | OdUpper | 2.0 | 改制外径上限 | 同上 |
| ReworkRatio | EmptyDrawingWtLower | 0.95 | 空拉改制壁厚下限 | 同上 |
| ReworkRatio | FewerPassWtLower | 1.05 | 少道次改制壁厚下限 | 同上 |
| ReworkRatio | EmptyDrawingWtUpper | 1.05 | 空拉改制壁厚上限 | 同上 |
| ReworkRatio | FewerPassWtUpper | 2.0 | 少道次改制壁厚上限 | 同上 |
| ReworkRatio | MinUnitWeightRatio | 1.05 | 最小单支重量比 | 同上 |
| WarehouseThreshold | CompleteRatio | 0.95 | 工单入库完结比率（主号入库重量/需求重量≥此比率） | WorkOrderExecutionService |
| WarehouseThreshold | CompleteDeviation | 100 | 工单入库完结绝对偏差(kg) | 同上 |
| WarehouseThreshold | CompleteOverRatio | 1.05 | 工单入库超额比率（重量口径入库重>需求重×此比率判定入库超额） | 同上 |
| WarehouseThreshold | SubcontractCompleteRatio | 0.95 | 委外完成比例 | InventorySyncService |
| WarehouseThreshold | PurchaseCompleteRatio | 0.965 | 采购完成比例 | PurchaseOrderService、SubcontractOrderService |
| WarehouseThreshold | PurchaseCompleteDeviation | 200 | 采购完成偏差(kg) | 同上 |
| WarehouseThreshold | PurchaseOverRatio | 1.05 | 采购超额比率(实际采购/委外量>计划量×此比率判定超额采购/超额穿孔) | PurchaseOrderService |
| WarehouseThreshold | PurchaseOverDeviation | 100 | 采购超额绝对偏差(kg)(到料重量>计划×超额比率且超出量>此阈值判定超量到货) | PurchaseOrderService |
| WarehouseThreshold | SubcontractOverRatio | 1.05 | 委外-超额比率(回收重量>需求重量×此比率判定超量回收) | SubcontractOrderService、InventorySyncService |
| WarehouseThreshold | SubcontractOverDeviation | 100 | 委外-超额偏差(kg)(回收重量>需求×超额比率且超出量>此阈值判定超量到货) | 同上 |
| WarehouseThreshold | OutsourceRecoveryRatio | 0.99 | 委外回收比例 | SectionOutsourceService、DataFixService |
| ContractWeight | LowerBound | 0.94 | 合同重量下限 | OrderService |
| ContractWeight | UpperBound | 1.06 | 合同重量上限 | 同上 |
| SequenceJump | MaxJump | 7 | 检验序号最大跳步 | ProcessInspectionService |
| ProcessingDiscount | RawMaterialRatio | 1.1 | 原料加工折扣系数 | ProductionOverviewService |
| DateBucket | （5个键） | - | 各工段日期桶 | 同上 |

> **注：** 以上为主要参数，非全量，完整种子见 DbInitializer。
>
> **存量库幂等补行：** DbInitializer 种子仅全新库自动写入；存量库升级时需手动执行以下 SQL 补行（以 PurchaseOverRatio 为例）：

```sql
-- 存量库幂等补行：PurchaseOverRatio（采购超额比率）
IF NOT EXISTS (
    SELECT 1 FROM ConfigParameters
    WHERE Category = 'WarehouseThreshold' AND ParamKey = 'PurchaseOverRatio'
)
INSERT INTO ConfigParameters (Category, CategoryDisplay, Context, ParamKey, ParamValue, Remark, CreatedTime, CreatedBy, UpdatedTime, UpdatedBy)
VALUES ('WarehouseThreshold', '采购-超额比率', '物料', 'PurchaseOverRatio', 1.05, '采购超额比率阈值(实际采购/委外量>计划量×此比率判定超额采购/超额穿孔)', GETDATE(), 'system', GETDATE(), 'system');

-- 存量库幂等补行：SubcontractOverRatio（委外-超额比率）+ SubcontractOverDeviation（委外-超额偏差）
IF NOT EXISTS (
    SELECT 1 FROM ConfigParameters
    WHERE Category = 'WarehouseThreshold' AND ParamKey = 'SubcontractOverRatio'
)
INSERT INTO ConfigParameters (Category, CategoryDisplay, Context, ParamKey, ParamValue, Remark, CreatedTime, CreatedBy, UpdatedTime, UpdatedBy)
VALUES ('WarehouseThreshold', '委外-超额比率', '物料', 'SubcontractOverRatio', 1.05, '委外超额回收比率阈值(回收重量>需求重量×此比率判定超量回收)', GETDATE(), 'system', GETDATE(), 'system');

IF NOT EXISTS (
    SELECT 1 FROM ConfigParameters
    WHERE Category = 'WarehouseThreshold' AND ParamKey = 'SubcontractOverDeviation'
)
INSERT INTO ConfigParameters (Category, CategoryDisplay, Context, ParamKey, ParamValue, Remark, CreatedTime, CreatedBy, UpdatedTime, UpdatedBy)
VALUES ('WarehouseThreshold', '委外-超额偏差', '物料', 'SubcontractOverDeviation', 100, '委外超额回收绝对偏差(kg)：回收重量>需求×超额比率且超出量>此阈值判定超量到货', GETDATE(), 'system', GETDATE(), 'system');
```

### 5.6 缓存策略

Service 层通过 `IConfigParameterService.GetConfigMapAsync(category)` 获取参数映射。由于 Service 注册为 `Scoped` 生命周期（每次 HTTP 请求创建新实例），各 Service 内部的 `_configMaps` 字典仅缓存单次请求内的重复查询，修改参数后下一次请求自动加载新值，无需重启。

---

## 六、DailyOutputEstimates（日产估算配置）

### 6.1 表名

`DailyOutputEstimates`

### 6.2 字段定义

| # | 字段名 | 类型 | 必填 | 默认值 | 说明 |
|---|--------|------|------|--------|------|
| 1 | Id | int | ✅ | 自增 | 主键 |
| 2 | MinOuterDiameter | decimal(18,2) | ✅ | - | 最小外径（mm），匹配规则：≤ 实际外径的最大值 |
| 3 | DailyOutputTons | decimal(18,2) | ✅ | - | 日产能力（吨/天） |
| 4 | Remark | nvarchar(200) | ❌ | NULL | 说明 |
| — | 审计字段（继承 BaseEntity） |
| 5 | CreatedTime | datetime2 | ✅ | - | 创建时间 |
| 6 | CreatedBy | nvarchar(50) | ✅ | - | 创建人 |
| 7 | UpdatedTime | datetime2 | ✅ | - | 更新时间 |
| 8 | UpdatedBy | nvarchar(50) | ✅ | - | 更新人 |

### 6.3 索引

| 索引名 | 类型 | 字段 | 说明 |
|--------|------|------|------|
| PK_DailyOutputEstimates | 主键 | Id | |

---

## 七、DailyProductionCapacities（重点工段日产配置）

### 8.1 表名

`DailyProductionCapacities`

### 8.2 字段定义

| # | 字段名 | 类型 | 必填 | 默认值 | 说明 |
|---|--------|------|------|--------|------|
| 1 | Id | int | ✅ | 自增 | 主键 |
| 2 | ProcessName | nvarchar(50) | ✅ | - | 工序名称（中文，如荒管抛光、50,60轧机） |
| 3 | DailyCapacity | decimal(18,2) | ✅ | - | 日产能力（吨/天） |
| 4 | Remark | nvarchar(200) | ❌ | NULL | 说明 |
| — | 审计字段（继承 BaseEntity） |
| 5 | CreatedTime | datetime2 | ✅ | - | 创建时间 |
| 6 | CreatedBy | nvarchar(50) | ✅ | - | 创建人 |
| 7 | UpdatedTime | datetime2 | ✅ | - | 更新时间 |
| 8 | UpdatedBy | nvarchar(50) | ✅ | - | 更新人 |

### 8.3 索引

| 索引名 | 类型 | 字段 | 说明 |
|--------|------|------|------|
| PK_DailyProductionCapacities | 主键 | Id | |

### 8.4 Seed 数据

```sql
INSERT INTO DailyProductionCapacities (ProcessName, DailyCapacity, Remark) VALUES
('荒管抛光', 15, '荒管抛光日产能(吨)'),
('50,60轧机', 11, '50,60轧机日产能(吨)'),
('20,30轧机', 9,  '20,30轧机日产能(吨)'),
('三辊轧机', 0.5, '三辊轧机日产能(吨)'),
('拉机',     3,   '拉机日产能(吨)');
```

---

## 八、Employees（员工基础信息）

### 8.1 表名

`Employees`

### 8.2 字段定义

| # | 字段名 | 类型 | 必填 | 默认值 | 说明 |
|---|--------|------|------|--------|------|
| 1 | Id | int | ✅ | 自增 | 主键 |
| 2 | Code | nvarchar(50) | ✅ | - | 工号（二维码内容），扫码报工用，唯一 |
| 3 | Name | nvarchar(100) | ✅ | - | 姓名 |
| 4 | Department | nvarchar(100) | ❌ | NULL | 部门 |
| 5 | Position | nvarchar(100) | ❌ | NULL | 岗位 |
| 6 | PositionRemark | nvarchar(200) | ❌ | NULL | 岗位备注 |
| 7 | SalaryMode | nvarchar(50) | ❌ | NULL | 工资结算模式（如计时/计件/月薪等） |
| 8 | SalaryRemark | nvarchar(200) | ❌ | NULL | 工资结算备注 |
| 9 | IsActive | bit | ✅ | 1 | 是否启用 |
| — | 审计字段（继承 BaseEntity） |
| 10 | CreatedTime | datetime2 | ✅ | - | 创建时间 |
| 11 | CreatedBy | nvarchar(50) | ✅ | - | 创建人 |
| 12 | UpdatedTime | datetime2 | ✅ | - | 更新时间 |
| 13 | UpdatedBy | nvarchar(50) | ✅ | - | 更新人 |

### 8.3 索引

| 索引名 | 类型 | 字段 | 说明 |
|--------|------|------|------|
| PK_Employees | 主键 | Id | |
| UK_Employees_Code | 唯一 | Code | 工号唯一 |

---

## 九、Workstations（工位管理）

### 9.1 表名

`Workstations`

### 9.2 字段定义

| # | 字段名 | 类型 | 必填 | 默认值 | 说明 |
|---|--------|------|------|--------|------|
| 1 | Id | int | ✅ | 自增 | 主键 |
| 2 | Code | nvarchar(50) | ✅ | — | 工位编码（二维码内容），唯一 |
| 3 | Name | nvarchar(100) | ❌ | NULL | 工位名称 |
| 4 | EquipmentName | nvarchar(100) | ❌ | NULL | 设备名称（自动填入报工表单） |
| 5 | SectionName | nvarchar(50) | ✅ | — | 工段名（如"外抛光""冷轧拔""检验"） |
| 6 | ReportType | nvarchar(50) | ✅ | — | 报工模板类型（ProductionRecord/PicklingInRecord/等） |
| 7 | IsActive | bit | ✅ | 1 | 是否启用 |
| — | 审计字段（继承 BaseEntity） |
| 8 | CreatedTime | datetime2 | ✅ | — | 创建时间 |
| 9 | CreatedBy | nvarchar(50) | ✅ | — | 创建人 |
| 10 | UpdatedTime | datetime2 | ✅ | — | 更新时间 |
| 11 | UpdatedBy | nvarchar(50) | ✅ | — | 更新人 |

### 9.3 索引

| 索引名 | 类型 | 字段 | 说明 |
|--------|------|------|------|
| PK_Workstations | 主键 | Id | |
| UK_WS_Code | 唯一 | Code | 工位编码唯一 |

---

## 十、EnumDisplayDefinitions（枚举显示配置）

### 10.1 表名

`EnumDisplayDefinitions`

### 10.2 字段定义

| # | 字段名 | 类型 | 必填 | 默认值 | 说明 |
|---|--------|------|------|--------|------|
| 1 | Id | int | ✅ | 自增 | 主键 |
| 2 | EnumKey | nvarchar(50) | ✅ | - | 枚举标识（枚举类型名，如 "BatchStatus"） |
| 3 | Value | nvarchar(50) | ✅ | - | 枚举值名（Enum.ToString()，如 "InProgress"），稳定英文名，不受改名影响 |
| 4 | DisplayName | nvarchar(50) | ✅ | - | 可改名中文显示（须含至少一个汉字） |
| 5 | DisplayOrder | int | ✅ | 0 | 显示顺序（升序，1 排最前） |
| 6 | Remark | nvarchar(200) | ❌ | NULL | 说明 |
| — | 审计字段（继承 BaseEntity） |
| 7 | CreatedTime | datetime2 | ✅ | - | 创建时间 |
| 8 | CreatedBy | nvarchar(50) | ✅ | - | 创建人 |
| 9 | UpdatedTime | datetime2 | ✅ | - | 更新时间 |
| 10 | UpdatedBy | nvarchar(50) | ✅ | - | 更新人 |

### 10.3 索引

| 索引名 | 类型 | 字段 | 说明 |
|--------|------|------|------|
| PK_EnumDisplayDefinitions | 主键 | Id | |
| UK_EnumKey_Value | 唯一 | EnumKey + Value | 同一枚举下值名唯一（不可加值） |

> **注：** 管理 C# 强类型枚举的中文显示名与排序，不改值域（不可加值）。显示层"配置表优先 → EnumHelper 静态字典兜底"；改名后 DataExchange 导出新中文，导入可反向解析。当前种子 41 个枚举。

---

## 十一、DictValueDefinitions（字典值配置）

### 11.1 表名

`DictValueDefinitions`

### 11.2 字段定义

| # | 字段名 | 类型 | 必填 | 默认值 | 说明 |
|---|--------|------|------|--------|------|
| 1 | Id | int | ✅ | 自增 | 主键 |
| 2 | DictKey | nvarchar(50) | ✅ | - | 字典标识（SectionKey/ProcessKey/UrgencyLevelKey/ProductStatus/ProductionFlowKey/FlowTargetKey/ProductionOverviewRowKey/LiabilityTypeKey） |
| 3 | Value | nvarchar(50) | ✅ | - | 英文稳定 Key（如 "ColdRollDraw"），存储层/后端匹配用，不受改名影响 |
| 4 | DisplayName | nvarchar(50) | ✅ | - | 可改名中文显示（须含至少一个汉字） |
| 5 | DisplayOrder | int | ✅ | 0 | 显示顺序（升序，1 排最前） |
| 6 | IsEnabled | bit | ✅ | true | 是否启用（false 表示隐藏，下拉/筛选不出现；存量数据仍可正确显示映射，不丢数据） |
| 7 | Remark | nvarchar(200) | ❌ | NULL | 说明 |
| — | 审计字段（继承 BaseEntity） |
| 8 | CreatedTime | datetime2 | ✅ | - | 创建时间 |
| 9 | CreatedBy | nvarchar(50) | ✅ | - | 创建人 |
| 10 | UpdatedTime | datetime2 | ✅ | - | 更新时间 |
| 11 | UpdatedBy | nvarchar(50) | ✅ | - | 更新人 |

### 11.3 索引

| 索引名 | 类型 | 字段 | 说明 |
|--------|------|------|------|
| PK_DictValueDefinitions | 主键 | Id | |
| UK_DictKey_Value | 唯一 | DictKey + Value | 同一字典下 Key 唯一 |

> **注：** 管理 string 存储字典字段的中文显示名/排序/隐藏/加值。可新增行（新 Value + DisplayName）实现"加值"，后端业务判定用编译期常量，新 Key 可存/可显示/可筛选。当前种子 8 个字典。责任类型（LiabilityType）由迁移 `20260805190330_MergeLiabilityTypeIntoDictValueDefinition` 从旧 LiabilityTypeDefinitions 表并入（DictKey=LiabilityTypeKey），种子不含。

---

## 十二、ProcessDefinitions（工序组定义）

### 12.1 表名

`ProcessDefinitions`

### 12.2 字段定义

| # | 字段名 | 类型 | 必填 | 默认值 | 说明 |
|---|--------|------|------|--------|------|
| 1 | Id | int | ✅ | 自增 | 主键 |
| 2 | ProcessKey | nvarchar(50) | ✅ | - | 稳定 Key（对应 ProcessKeys 常量名，如 ColdRoll60），程序识别工序用，不受改名影响 |
| 3 | ProcessName | nvarchar(50) | ✅ | - | 工序组中文名（可改名，显示用） |
| 4 | DisplayOrder | int | ✅ | 0 | 显示顺序（升序，1 排最前） |
| 5 | IsEnabled | bit | ✅ | true | 是否启用（false 表示隐藏，界面不显示该工序） |
| 6 | IsColdRoll | bit | ✅ | false | 是否冷轧系列（含三辊冷轧） |
| 7 | IsColdDraw | bit | ✅ | false | 是否冷拔工序 |
| 8 | DefaultSections | nvarchar(500) | ❌ | NULL | 默认工段（JSON 数组字符串，存 SectionKey 列表，如 ["Straighten","Cut","Pickle","OuterPolish","OuterSpotGrinding","Inspection"]）；计划页新增该工序行时自动填充；null/空数组表示无默认工段 |
| 9 | Remark | nvarchar(200) | ❌ | NULL | 说明 |
| — | 审计字段（继承 BaseEntity） |
| 10 | CreatedTime | datetime2 | ✅ | - | 创建时间 |
| 11 | CreatedBy | nvarchar(50) | ✅ | - | 创建人 |
| 12 | UpdatedTime | datetime2 | ✅ | - | 更新时间 |
| 13 | UpdatedBy | nvarchar(50) | ✅ | - | 更新人 |

### 12.3 索引

| 索引名 | 类型 | 字段 | 说明 |
|--------|------|------|------|
| PK_ProcessDefinitions | 主键 | Id | |
| UK_ProcessKey | 唯一 | ProcessKey | 工序组稳定 Key 唯一 |

> **注：** 替代 ProcessNames 常量类的硬编码，支持管理员页面动态配置（新增/启停/排序/改名）。冷轧类判定（IsColdRoll/IsColdDraw）配置化，新增冷轧/冷拔工序只需勾选标记。迁移 `20260807171239_AddDefaultSectionsToProcessDefinitions` 新增 DefaultSections 列。

---

## 十三、版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| **V1.13** | **2026-08-19** | **委外超量回收参数**：§5.5 参数清单新增 WarehouseThreshold/SubcontractOverRatio(1.05, 委外-超额比率)+SubcontractOverDeviation(100, 委外-超额偏差kg)——回收重量>需求×超额比率 且 超出量>超额偏差 → 子项超量到货（SubcontractOrderService/InventorySyncService 使用）；§5.5 存量库幂等补行 SQL 补两行；存量库幂等补行见迁移 `20260819100000_SeedSubcontractOrderOverReceived` |
| **V1.12** | **2026-08-19** | **采购超量到货偏差参数**：§5.5 参数清单新增 WarehouseThreshold/PurchaseOverDeviation(100kg)（到料>计划×超额比率且超出量>此阈值判定超量到货，PurchaseOrderService 使用）；存量库幂等补行见迁移 `20260819000000_SeedPurchaseOrderOverReceived` |
| **V1.11** | **2026-08-15** | **过时描述清理**：§3.2 StandardWorkDays 字段表删除 EnglishName（已由迁移 `20260805145413_RemoveEnglishNameColumn` 移除）；概述/当前范围删除 SectionFlowCategoryItem 及"数据表属 Scheduling 上下文"描述（实体已移至 Configuration 上下文，明细已删，组合由 CombinationGroups 承载）；"五张表"计数更正 |
| **V1.10** | **2026-08-09** | **显示层参数化三表新增**：§2 实体清单新增 EnumDisplayDefinitions/DictValueDefinitions/ProcessDefinitions（第 8~10 项）；新增 §十~§十二 三表字段定义（含 DefaultSections JSON 数组、IsEnabled 隐藏语义）；§3 StandardWorkDays 字段表补充 SectionKey/EnglishName/DisplayOrder/IsEnabled 并更新 26 工段预置超集 Seed；§5.5 参数清单新增 WarehouseThreshold/CompleteRatio(0.95)/CompleteDeviation(100)/CompleteOverRatio(1.05)（工单入库完结阈值，WorkOrderExecutionService 使用）；版本历史重编号为 §十三 |
| **V1.9** | **2026-08-08** | §5.5 参数清单新增 WarehouseThreshold/PurchaseOverRatio(1.05)；§5.5 补充存量库幂等补行 SQL 示例 |
| **V1.8** | **2026-08-01** | §5.5 参数清单修正：删除 DefaultValue 重复项、ProcessCycle→DefaultProcessCycle/22、使用方类名更新 |
| **V1.7** | **2026-07-15** | **新增 Workstations 工位管理实体**：§2 实体清单新增第 7 项；§9 定义表结构与索引（Code 唯一） |
| **V1.6** | **2026-06-25** | **新增重点工段日产表（DailyProductionCapacities）**：工序名称+日产能力+说明；§2 实体清单新增；§5.5 分类清单移除 ProductionCapacity；Seed 数据 ProcessName 中文化 |
| **V1.4** | **2026-06-14** | **新增**：Employees 员工基础信息表定义；概述更新为 5 张系统参数表（含 Employee）；新增工位管理引用说明 |
| **V1.3** | **2026-06-04** | **新增**：DailyOutputEstimates 日产估算配置表定义；修正 ConfigParameter.Category 长度 nvarchar(100)→nvarchar(50) |
| **V1.2** | **2026-06-03** | **新增跨上下文引用**：SectionFlowCategorySetting + SectionFlowCategoryItem（数据表属 Scheduling 上下文，管理 UI 在配置上下文） |
| **V1.1** | **2026-06-02** | **新增**：ConfigParameters（业务参数配置）通用键值表定义，替代约 50+ 处硬编码数值常量 |
| **V1.0** | **2026-06-02** | **初始版本**：StandardWorkDays + StandardWorkDayDeliveryStates 两张参数表定义 |
